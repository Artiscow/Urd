# Endringslogg

Alle vesentlige endringer i Urd dokumenteres i denne filen.

Formatet følger [Keep a Changelog](https://keepachangelog.com/no/1.1.0/),
og prosjektet følger [semantisk versjonering](https://semver.org/lang/no/).

## [Ulansert]

Hvert innslag arver nummeret til backlog-punktet arbeidet hører under, pluss
ett siffer for rekkefølge (0.6.6.5 → 0.6.6.5.1, 0.6.6.5.2 …). Arbeid som ikke
hører under noen milepæl (arbeidsflyt, kodegjennomganger, vedlikehold) hører
under fasens stående «Løpende»-punkt i backloggen og nummereres 0.6.0.1,
0.6.0.2 … (neste fase: 0.7.0.x). Flate fasenumre (0.6.34, 0.6.35) deles ikke
lenger ut - de leses feilaktig som milepæl-plassering - men de historiske
består, siden commit-titler i git ikke kan skrives om. Fase-slippet døpes til
det siste nummeret ved gaten. Push-innslag arver commit-spennet siden forrige
push med p-suffiks: én commit gir 0.6.0.4p, flere commits (0.6.7.2 til
0.6.7.5) gir 0.6.7.2-5p. Kortformen brukes kun når numrene deler prefiks; med
blandede serier skrives begge fullt ut (0.6.6.5.11-0.6.0.1p). Spennet er
entydig: alle commit-innslag over forrige p-innslag.

### 0.6.7.7 - Slash-kommando på lerretet, pluss galleri-CSS-fiksen - 9. august 2026
- SLASH-KOMMANDOEN (ELEMENTKART 5a.2): «/» på lerretet åpner + Ny blokk-menyen med søkefeltet fokusert, i den aktive seksjonen (ellers seksjonen under pekeren, sporet via passiv pointermove-lytter); står pekeren i seksjonen, blir pekerpunktet landingsplass som ved dobbeltklikk. Flyten «/», skriv, Enter setter inn FØRSTE treff; Escape lukker (begge stoppes fra lerret-snarveiene). Vakter: aldri på mobil, i Ren visning, med Ctrl/Alt eller fra skrivefelt. Dobbeltklikk-åpningens posisjonering er trukket ut i delt openBlockMenuAt, så de to veiene aldri kan drifte. Slash INNE i tekstblokker står fortsatt som utsatt i backloggen.
- BUGFIKS (eierfunn, to dekke-bugs i seksjonsgalleriet med samme rot): seksjonsbarens chip-stil (.urd-add-section button: translateY(-50%), stiplet 999px-ramme, opacity .75) treffer alle knapper i baren; de nye fane- og malkort-knappene manglet .urd-preset-menu-prefikset og transform-resetene som de gamle galleri-reglene verger seg med. Fanene la seg over «Ny seksjon»-tittelen, og malkortet ble løftet 51px over fanene med chip-rammen skalert til en stiplet ellipse. Selektorene går nå via .urd-preset-menu med eksplisitt transform/opacity-reset og en forklarende kommentar, så neste meny-knapp ikke går i samme felle. Nytt base.css-stempel (27710857) x5, kopiene byte-like.
- VERIFISERT: 312/312 tester (preview-edit og base.css buntes ikke, ingen gjenbygg), røyktest i headless nettleser (slash åpner fokusert, «but» + Enter satte inn knapp, Escape lukker, slash død i Ren visning) og før/etter-skjermbilder av galleri-fiksen.

### 0.6.7.6 - Søk i blokkvelgeren - 9. august 2026
- TREDJE OG SISTE FERDIG NÅR-KRITERIUM LEVERT. Designrunde nummer to (artifact med søk- og bytt oppsett-variantene); eieren valgte VARIANT B begge steder: flat, rangert treffliste ved aktivt søk (bytt oppsett-valget gjelder runde 0.6.7.9). Ny ren modul palette-search.js (bundlet): normalize med NFD-diakritikkstripp («gallerí» matcher galleri), rankLabel (start foran ordstart foran delstreng) og stabil searchItems; søket matcher alltid de SYNLIGE ta-oversatte etikettene, aldri nøkler.
- CANVAS-MENYEN (+ Ny blokk): søkefelt med lupe øverst, fokusert ved åpning (knapp- og dobbeltklikk-veien, forbereder slash-runden). Aktivt søk skjuler menyens vanlige innhold (urd-searching) og viser flat treffliste: kjerneblokker, former, plugin-blokker med variantene flatet ut («Kalender: Måned») og blokkgruppe-maler. Treffene kjører de EKTE knappenes klikk-handlere, så treff og meny aldri divergerer. Tomtreff-linje; tømming gir menyen tilbake.
- BLOKKER-PANELET: samme søkefelt øverst; aktivt søk erstatter gruppene med samme flate treffliste (bilde/galleri beholder filepick-oppførselen i trefflisten), tomtreff som panel-hint.
- To nye nøkler i kjernespråkene (canvas.searchBlocks, canvas.searchEmpty); ny canvas-CSS med nytt base.css-stempel (2feb6fcc) x5, kopiene byte-like; AGENTS-bundlelisten à jour med palette-search.
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 312/312 tester (fire nye i tests/palette-search.test.mjs), alle skjemavalideringene, PLUSS røyktest i headless nettleser (engelsk locale): fokus ved åpning, filtrering med skjult normalinnhold, variant-utflating, treff-klikk setter inn blokk, tomtreff-linjen, panelets flate liste.

### 0.6.7.5 - Blokkgruppe som gjenbrukbar - 8. august 2026
- ANDRE FERDIG NÅR-KRITERIUM LEVERT etter snippets-modellen (ELEMENTKART 1.5: lagrede grupper dukker opp i de EKSISTERENDE innsettingsmenyene, aldri eget komponentpanel; innsatte grupper er alltid uavhengige kopier, aldri synkede symboler): bokmerke-knapp i multiverktøylinjen lagrer hele utvalget som blokkgruppe-mal (urd-save-template kind blocks fra _urdCtx-blokkene, kun ved to eller flere valgt), med samme navnedialog og «finnes ikke»-baseline som seksjons-malene.
- INNSETTING I BEGGE BLOKKMENYER: canvas-menyen (+ Ny blokk) har en «Mine maler»-seksjon som bygges fersk ved HVER åpning (knapp- og dobbeltklikk-veien), så listen aldri er foreldet uten rerender; ved dobbeltklikk lander gruppen med øvre venstre hjørne på klikkpunktet, ellers beholdes lagrede posisjoner (kun groupDelta-klem). Blokker-panelet har tilsvarende gruppe som ruter via ny urd-insert-template-melding til previewens innsettingslogikk (aktiv seksjon).
- INNSETTINGEN DELER HALE MED LIM INN: cloneBlocksForInsert (re-id + anker/klem + minBottom) og urd-add-blocks gir ETT angre-steg, og det innsatte blir det nye utvalget. Publiseringen fra 0.6.7.4 dekker allerede blocks-maler (materializeBlockImages). To nye nøkler i kjernespråkene (canvas.saveGroup, canvas.insertGroup); «Mine maler»-tittelen gjenbrukes.
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 308/308 tester, alle skjemavalideringene, PLUSS ende-til-ende røyktest i headless nettleser: marquee-utvalg (5 blokker), lagring med kind blocks, gruppen i canvas-menyen, innsetting av alle 5 uten id-kollisjon, ETT Ctrl+Z fjerner hele gruppen, panelgruppen i Blokker.

### 0.6.7.4 - Publisering av maler - 8. august 2026
- MALENE UT AV LOCALSTORAGE OG INN I REPOET, alt etter samlinger-forbildet: endrede mal-utkast blir content/maler/<id>.json i commiten, indeks-utkastet blir content/maler.json, og maler fjernet fra indeksen slettes fra repoet med samme create-vern (en id som gjenoppstår i samme publisering slettes aldri). Utkastnøklene ryddes etter vellykket commit; «maler» føres i publiseringsmeldingen. «Upubliserte endringer»-merket teller nå også mal-utkast (bevisst utsatt fra forrige runde til Publiser-knappen faktisk kunne holde løftet).
- BILDE-MATERIALISERINGEN REFAKTORERT til delte hjelpere materializeSection (bakgrunn + blokker) og materializeBlockImages, brukt av både sidepubliseringen (uendret adferd) og malene: base64-bilder i en seksjons- eller blokkgruppe-mal blir deterministiske media/-filer i samme commit, aldri base64 i malfilen.
- RE-BASELINING ETTER PUBLISERING speiler samlinger-mønsteret: materialiseringen speiles inn i minnet, mal-stores og indeks-store gjenskapes mot ny publisert baseline, publishedMaler fylles (riktig angre-baseline etter publisering), previewen får fersk liste.
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 308/308 tester, alle skjemavalideringene. Selve publiseringen krever functions (deployet side) og er testrunde-punkt mot urd-web.

### 0.6.7.3 - Lagre som mal og Mine maler i seksjonsgalleriet - 8. august 2026
- FØRSTE FERDIG NÅR-KRITERIUM LEVERT (editor-lokalt; publisering i neste runde): en egen seksjon kan lagres som mal og settes inn igjen fra + Ny seksjon. Designrunde med tre artifact-utkast (liste-rader, rutenett, segmentfaner); eieren valgte VARIANT C: to segmentfaner i galleriet, «Innebygde» (kjerne- og plugin-presets som før) og «Mine maler» (2-kolonners rutenett med ekte presetThumb-miniatyrer, navn og sletteknapp ved pek). Fanevalget huskes i økta; tomtilstanden forklarer hvordan første mal lagres.
- LAGRE-FLYTEN: bokmerke-knapp (tegnet SVG) i seksjonsverktøylinjen poster et navnløst snapshot (urd-save-template); editoren navngir i ny askPrompt-dialog (bekreftelsesdialogen utvidet med tekstfelt og Enter-innsending), slugger til mal-id og lagrer som utkast med «finnes ikke»-baseline (0.6.7.1-regelen). Duplikat/ugyldig navn gir statusfeil. Sletting går via kortets sletteknapp med askConfirm i editoren (urd-delete-template).
- INNSETTING FØLGER RE-ID-REGELEN (cloneSectionForInsert): samme mal kan settes inn flere ganger uten id-kollisjon. Mal-stores er med i angre-snapshotet (lagring/sletting/innsetting angres; restoreMaler speiler samlinger-mønsteret), og Forkast utkast rydder aldri-publiserte maler helt bort.
- RØRLEGGING: urd-maler/urd-save-template/urd-delete-template dokumentert i bro-headeren, setMaler-eksport i preview-edit rutet fra urd.js, fane-/kort-CSS i base.css (nytt stempel a917ffcf x5, kopiene byte-like), ti nye nøkler i kjernespråkene, AGENTS-bundlelisten à jour med maler-model.
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 308/308 tester, alle skjemavalideringene, PLUSS ende-til-ende røyktest i headless nettleser mot dev-serveren: lagre, navngi (slug royk-test-hero), fane-bytte, miniatyr, innsetting, dobbel innsetting uten id-kollisjon, to Ctrl+Z ruller begge tilbake, sletting tømmer indeks-utkastet. Røyktesten kjørte engelsk locale og beviste dermed kjernespråk-fallbacken.

### 0.6.7.2 - Mal-formatet: skjema, indeks og re-id-modellen - 8. august 2026
- MALFIL-KONTRAKTEN FESTET (tre-steder-regelen i samme commit): nytt schema/mal.schema.json (metahodet mal {name, kind} med kind section|blocks|page; nyttelast-nøkkelen må matche kind og de andre er forbudt; seksjon/blokk gjenbrukes fra page-skjemaets $defs via $id), SKJEMA.md-Maler utvidet med kind-enumen, indeksfilen og re-id-regelen som kontraktstekst, og tom template/content/maler.json-indeks (samlinger-presedensen, ADR-0007: statiske hoster kan ikke liste mapper). Eierskap/vokter dekker alt via content/**.
- NY MOTORMODUL maler-model.js (ubundlet, node-testbar): malId (slug av navnet; slugify i imageTools fikk fallback-parameter så ugyldig navn gir tom streng i stedet for «bilde»), cloneSectionForInsert (dyp klone, ny seksjons-id + nye blokk-id-er, geometri urørt) og cloneBlocksForInsert (nye id-er, anker-flytting med groupDelta-klem innenfor seksjonen, minBottom for seksjonsvekst; frames.mobile urørt som ved lim inn).
- VALIDERINGEN UTVIDET: page-skjemaet i ajv-registeret (mal-skjemaet refererer det), maler-indeksen valideres i løkke som samlinger, og to syntetiske maler bygget fra første preset MED blokker holder kontrakten levende selv med tom indeks (den aller første preseten er «tom» og røk på minItems under utviklingen; find-vernet dokumenterer det).
- VERIFISERT: editor-bygg grønt med gjenbygd bundel (imageTools er bundlet), 308/308 tester (åtte nye i tests/maler.test.mjs: id-regime, re-id-invarianter med disjunkte id-sett ved dobbel innsetting, anker/klem-geometri, mobile-frames), alle skjemavalideringene uten strict-advarsler.

### 0.6.7.1 - Middels-reviewrunden: fem funn verifisert og fikset - 8. august 2026
- Milepælen 0.6.7 åpner med de fem Middels-punktene fra kodegjennomgangen 27. juli (eierbeslutning 8. august: reviewrunden er milepælens første runde); hvert punkt re-verifisert som reelt før fiks. Runde-planen er samtidig ført inn i backloggen (rundene 0.6.7.2-10, eierbeslutningene om side-maler, Urd.maler-fundamentet og artifact-designutkast notert der).
- MIGRERINGSINVARIANTEN TETTET: migrate.lift leste data uten `version` som gjeldende (undefined gjør både nyere-sjekken og migreringsløkken falske), så håndredigert/gammelt format hoppet over migreringene. Manglende/ugyldig version defaulter nå til 1 og løftes stegvis; to nye tester dekker hullet.
- SAMLINGS-DRIFTEN TETTET: en fersk samling fikk baseline lik ferskt innhold, så hasDraft() var usann og indeksen kunne publiseres uten tilhørende fil. «Finnes ikke»-baseline (null) innført tre steder (addSamling, restore-fallbacken ved angret sletting, init-syntesen); init reparerer nå også et allerede driftet deploy (indeks uten fil gir tomt utkast med finnes-ikke-baseline, så filen publiseres og driften heles). Ny draft-store-test; regelen gjenbrukes for mal-stores i 0.6.7.
- MOBIL-AUTOSTABLINGEN: autovoksende blokker fikk fast desktophøyde i mobilflyten, så høyere mobilinnhold rant over neste blokk. Nytt additivt `autoGrow`-flagg på blokkdefinisjonen (faq/galleri/samling + kalender/skjema/kart) som render.js leser i flytmodus; dokumentert i plugin-README for tredjeparter.
- GEOCODE-VAKTEN: /api/geocode var en åpen, uautentisert proxy mot Nominatim (misbruk kunne fått deployment-IP-en bannlyst); har nå samme cookie-vakt som latest.js og gjenbruker feilkoden notLoggedIn (ingen nye ordboknøkler, admin oversetter via taApiError som før).
- HINT-RESTENE (prosa-regelen, siste del av panel-hint-punktet): PANEL_INTROS dekker nå også Blokker, Rutenett (begge avsnittene, satt sammen med linjeskift) og Historikk; autoDark-forklaringen ligger på Automatisk-knappen, lagrekkefølge-forklaringen på lagenes pilknapper (som manglet tooltip), galleribakgrunnens avspillingsforklaring på Sekunder per bilde. Gjenværende panel-hints er kun legitime tomtilstander, statuser og feil.
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 300/300 tester (tre nye), alle skjemavalideringene.

### 0.6.6.6.4 - Sider, Samlinger og Plugins på panel-språket: milepælen 0.6.6.6 fullført - 8. august 2026
- PANEL-INTROENE BOR NÅ PÅ PANEL-TITTELEN: nytt PANEL_INTROS-kart legger intro-forklaringen som tooltip på panelets `<h2>` i stedet for som prosaavsnitt i panelkroppen; Sider (utkast/Ctrl+Z), Samlinger (hva samlinger er) og Plugins (hva plugins er) bruker den, og fremtidige paneler er én linje i kartet. I Sider ligger auto-meny-forklaringen som tooltip på Opprett side-knappen, og i Plugins er «Funnet i repoets plugins/-mappe:» blitt en ekte panel-strong-etikett; tomtilstandene og feil-/varsellinjene står igjen som prosa-regelen tillater. Ingen ordbok-endringer (alle nøklene gjenbrukes).
- INNSTILLINGER-PANELET (site) revidert i samme runde siden milepælens «Ferdig når» teller seks paneler: det var allerede rent (tooltips på alt, ingen prosa, delte klasser). Dermed er MILEPÆLEN 0.6.6.6 oppfylt på alle tre kriteriene: de seks panelene bruker de delte panel-klassene, Egenskaper har det observerbare Innhold/Stil-skillet, og ADR-0016 er skrevet. Restene av panel-hint-punktet (Blokker-, Rutenett- og Historikk-introene) ligger utenfor de seks og består som eget backlog-punkt.
- BUGFIKS (eierfunn): undermenyen fulgte ikke barens tone når nav-en har lag-bakgrunn - den ble stående på standard-sløret (surface 85 %). Ny ren funksjon navLayerVeil i nav-model.js flater lagstakkens FARGELAG til ett slør (color-mix i tegnerekkefølge) som settes som --urd-nav-sub-bg, så nedtrekket, mobilpanelet og pille-designet følger baren; bilde- og gradientlag holdes fortsatt ute (samme prinsipp som subImage). Ny kontraktstest dekker flatingen.
- BUGFIKS (eierfunn): nedtrekket hadde en lys glorie i mørkt tema fordi undermeny-skyggene var farget med --urd-color-text (lys tekst gir lysende «skygge»). Alle fire skyggene i undermeny-familien (kortet, pillene, utfallet, mobilpanelet) er nå svartbaserte med samme dekkevne: praktisk talt identisk i lyst tema, ingen glød i mørkt.
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 297/297 tester (én ny), alle skjemavalideringene, nytt base.css-stempel (0d86fed8) i alle fem skall med slug-kopiene byte-like roten.

### 0.6.6.6.3 - Nav og Footer på panel-språket, pluss uskarpheten bak menyen tilbake - 8. august 2026
- NAV-PANELETS FIRE PROSAAVSNITT håndtert etter prosa-regelen: «Logoen er også Hjem-knappen» ligger nå som tooltip på Logo-gruppens summary og undermeny-forklaringen som tooltip på Meny-punkter-summaryen; intro-avsnittet og tallfelt-forklaringen under logobildet (som bare gjentok feltenes egne tooltips) er fjernet, med de døde nøklene `hint.nav.intro` og `hint.nav.logoFields` ryddet ut av alle fem ordbøkene.
- FOOTER-PANELET gjennomgått og trengte ingen endring: alle gruppene komponerer de delte klassene, hver innstilling har tooltip, ingen prosa. Mal-miniatyrene og listeradene er kontekstklasser med eget innhold, ikke drift. Begge panelene var forbildene for pilotarbeidet; runden bekrefter at de står på kontrakten fra ADR-0016.
- BUGFIKS (eierfunn): «Uskarphet bak menyen» var død i alle nav-varianter siden 0.6.0.5. Årsak: de statiske `view-transition-name`-reglene på `#urd-nav`/`#urd-footer` i base.css gjør elementene til backdrop-roots (spec-oppførsel, ikke motorspesifikk), så navens `backdrop-filter` samplet bare innholdet INNI verten i stedet for siden bak; CSS-kommentaren påsto navnene var inerte utenom overganger, og det stemte ikke.
- FIKSEN er standardbasert og nettleser-nøytral: reglene er fjernet fra base.css, og ny `wireViewTransitionNames` i urd.js setter navnene KUN i selve overgangsvinduet (`pageswap` ved utreise, `pagereveal` ved innreise, fjernes når overgangen er ferdig; `pagereveal` uten overgang rydder eksplisitt, som dekker bfcache). Sideovergangene beholder dermed nav/footer i ro, mens uskarpheten virker i all vanlig visning; motorer uten cross-document-støtte fyrer aldri hendelsene og navigerer som før (ADR-0011-fallbacken). Verifisert empirisk med instrumentert headless-test (sondeside med stripemønster bak nav) i både programvare- og GPU-renderingsbanen, og bekreftet av eieren på dev-serveren; deployede sider får fiksen ved neste push/oppdatering.
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 296/296 tester, alle skjemavalideringene, nytt base.css-stempel (e0c3d114) i alle fem skall med slug-kopiene byte-like roten.

### 0.6.6.6.2 - Egenskaper på panel-språket: Innhold/Stil-fanene - 8. august 2026
- INNHOLD/STIL-FANENE (ADR-0016, mønsteret fra ELEMENTKART 1.2): blokk-egenskapene er delt i to segmentfaner. Innhold: knappetekst/lenkemål, bilde med alt/lenke/lightbox, videolenke og tittel, FAQ-spørsmålene, ikonvalget, samlingskobling/antall/rekkefølge, galleribildene, formtype og plugin-feltene. Stil: justering/tekstboks/kortstil, knappestil, bildefit/radius/utsnitt/zoom/fargejusteringer, ikonstørrelse og -farge, visningsvalgene for samling og galleri, formfarge/tykkelse/fyll, pluss de delte seksjonene animasjon/hover, festet blokk og plassering. Fanene bor i blockPropsUI-snippeten, så Egenskaper-panelet og den flytende blokkmenyen deler dem; fanevalget huskes på tvers av markeringer. Tekstblokkens Innhold-fane viser en tomtilstand (ny nøkkel hint.textInline) som peker til tekstlinjen. To skjønnsvalg: bildeblokkens lightbox står under Innhold fordi den er betinget av lenkefeltet (relevans-regelen), galleriets under Stil blant visningsvalgene; seksjonsvisningen beholder én liste uten faner (nesten bare stil).
- PROSA-HINTENE I EGENSKAPER FLYTTET TIL TOOLTIPS (prosa-regelen; panelets del av backlog-punktet om de 33 avsnittene): minHeight, video, ikonfarge, eget ikonbilde, plassering (på gruppesummaryen), plugin-knappen og galleriet (på miniatyrene). Samling-hintet delt i to målrettede tooltips (tip.samling.source på samlingsvelgeren, tip.samling.limit på maks antall) med oversettelser bevart i alle fem ordbøkene; den døde hint.samling-nøkkelen fjernet overalt. Tomtilstander og place-søkestatus står igjen som panel-hint.
- Nye nøkler props.tabContent/props.tabStyle og hint.textInline i kjernespråkene; ny props-tabs-kontekstklasse på segmentkontrollen (full bredde).
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 296/296 tester, alle skjemavalideringene.

### 0.6.6.6.1 - Panel-språkets fundament: ADR-0016 og de delte klassene - 8. august 2026
- ADR-0016 SKREVET (panel-språket og Innhold/Stil-modellen): byggeklossene festet som delt kontrakt (panel-overskrift, kontroll-rad, sammenleggbare grupper, segmentkontroll, mini-etikett, chip, forhåndsvisningsflate, panel-hint kun for tomtilstand/feil), prinsippet om levende forhåndsvisninger, Innhold/Stil-modellen for Egenskaper (to faner, INGEN tredje «Avansert»-fane; Urd har ikke dybden som begrunner Elementors tredeling), og regelen om at nye kontroller skal komponere de delte klassene: en duplikatklasse med driftede verdier er et review-funn. ADR-listen i AGENTS.md à jour.
- DE DELTE KLASSENE TRUKKET UT av Tema-pilotens kode i en merket seksjon i App.svelte: ctl-row (fra autorow/palhead/update-opt-head/rng-lab, som nå kun bærer marger), mini-label (fra palname/tpv-cap/update-opt-label), chip med accent-variant (fra stdtag/update-tag; Standard-knappene bruker class:accent i stedet for invertert ghost), og sample-flaten (fra typo-sample/form-prev). Verdiene forent; de små pikselavvikene mellom panelene var nettopp driften som skulle bort.
- VERIFISERT: editor-bygg grønt uten unused-selector-advarsler, 296/296 tester, alle skjemavalideringene.

### 0.6.0.13-14p - Push-klargjøring: geokode-kapplop og tre småfunn rettet - 8. august 2026
- VERIFISERT (full runde, diffen har kode-, skjema- og innholdsendringer): editor-bygg grønt og ferskt bygg byte-likt committet bundel, 296/296 tester, alle skjemavalideringene, base.css-stempelet matcher filinnholdet i alle fem skall og slug-kopiene er byte-like roten, ingen tankestrek i diffen.
- UAVHENGIG GJENNOMGANG (frisk agent uten øktens kontekst, hele diffen mot AGENTS.md og ADR-0006/0008/0009/0012/0013/0014, inkludert egen kjøring av testene og byte-sammenlikning av gjenbygd bundel): sikkerhetssjekken av notat-pipelinen var ren (release-body via env uten shell-interpolasjon, notes kappet og rendret som ren tekst, ankret TAG_RE, PAT kun mot malrepoet), $state.snapshot-fiksene, felt-kontraktens additivitet og CSP-sjekkens eksakte vertssammenlikning bekreftet. Fire funn, alle rettet i denne committen:
  - RETTET (middels, kapplop): place-søket i Egenskaper leste selectedBlock PÅ NYTT etter await, så et blokkbytte mens geokodingen var i flukt ville skrevet location/lat/lon inn i den nye blokkens props, uansett blokktype. blockId fanges nå før await og svaret forkastes om markeringen har byttet; placeBusy nullstilles i finally så et forkastet svar ikke låser Søk-knappen.
  - RETTET (lav): kartets tomtilstand (kart.edit.empty) ba brukeren åpne «Innstillinger …»-knappen som felt-kontrakten nettopp fjernet; omskrevet i kjernespråkene til å peke på feltene i Egenskaper. (Gjennomgåerens observasjon: paritetstesten fanger manglende NØKLER, aldri utdaterte VERDIER.)
  - RETTET (lav): det norske CSS-klassenavnet felt-feil byttet til place-error (engelske identifikatorer i kode).
  - RETTET (lav): multiutvalgets dra-håndtak fikk isPrimary-vakt, så en finger nummer to på håndtaket ikke starter et konkurrerende dra med egne lyttere.
- TESTRUNDER-strykingene i spennet (to eldre batcher og fem 0.6.9.x-punkter) er eierens egne etter testing, i tråd med regelen om at kun testeren stryker. Committen med 0.6.0.14-tittelen bærer også 0.6.0.13-innholdet (slått sammen ved commit); begge innslagene under beskriver hver sin runde.

### 0.6.0.14 - Dra-håndtak for multiutvalget - 6. august 2026
- UTVALGSLINJEN («N valgt») HAR FÅTT ET DRA-HÅNDTAK, egen knapp med gripe-ikon rett ved siden av søppelikonet: hele blokkutvalget dras samlet med livevisning, gruppen klemmes av groupDelta så den holder seg innenfor seksjonen, verktøylinjen følger utvalget under draet, og slipp bokføres som ETT angre-steg via samme flyt som juster/fordel-knappene (applySelectionMoves). Avbrudd (pointercancel) stiller alt tilbake. Ny nøkkel canvas.dragSelected i kjernespråkene; cursor grab/grabbing og touch-action i base.css (nytt stempel i alle fem skall).
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 296/296 tester.

### 0.6.0.13 - Oppdatering-panelet i nytt design, kjernespråk-modellen og bug-runden - 6. august 2026
- OPPDATERING-PANELET I NYTT DESIGN (designrunde på eierbestilling: tre interaktive utkast i artifact, eieren valgte «Tre etasjer - med plass for å vise informasjon om oppdateringene»): versjonskort med dempet installert-versjon, tegnet pil og målversjon som badge, pluss oppsummeringslinje ({writes} skrives, {deletes} slettes); motorgruppen som ÉN fold med antall i summary og atomforklaring i tooltip, konflikt-flaggede motorfiler løftet UT av folden som varselrader; valgfrie filer med «Behold min» som kolonneoverskrift én gang, slettes-chip per rad og rene switcher; `_headers`-instruksen i egen fold døpt «Sikkerhetsheadere» (nettsøkt terminologi; filnavnet ute av admin-etiketten, ADR-0006-modellen uendret).
- UTGIVELSESNOTATER I PANELET (ny pipeline for «Om {target}»-folden): release-Action-en publiserer monorepo-releasens notater videre som release på malrepoet (body via env, injeksjonssikkert; hoppes over uten notater), update-GET-en henter dem feiltolerant fra målversjonens tagg (404 er normalt, feltet blir null, maks 4000 tegn), og panelet viser dem som ren tekst i en fold kun når de finnes.
- KJERNESPRÅK-MODELLEN (eierbeslutning 6. august 2026, ADR-0012-addendum): nye UI-nøkler skrives kun i nb, en-GB og tr frem til egne oversettelsesrunder. Paritetstesten krever fullt nøkkelsett i de tre kjernespråkene, logger etterslep for nn/se (som faller til bokmål ved kjøring) og slår fortsatt ned på ukjente nøkler i alle språk. AGENTS-leksen oppdatert etter godkjent utkast.
- BUGFIKS (backlog): plugin-blokker kunne ikke settes inn fra menyene - addPluginBlock kjørte structuredClone på en $state-proxy (defaults fra urd-plugin-blocks-meldingen), som kaster DataCloneError stille, så klikket gjorde ingenting. Rettet med $state.snapshot; samme latente feil rettet i site-utkast-vernet.
- BUGFIKS (backlog): slett utvalget - multimarkeringens verktøylinje har nå slett-knapp (tegnet SVG) og Delete/Backspace sletter alle markerte blokker i ETT angre-steg (felles deleteSelection i preview-edit.js, urd-delete med blockIds). Ny nøkkel canvas.deleteSelected i kjernespråkene; hover-rødt på knappen ga nytt base.css-stempel i alle fem HTML-skall.
- BUGFIKS (backlog): CSP-varselet i Plugins-panelet ropte alltid («Needs CSP exceptions in _headers: frame-src https://www.openstreetmap.org» selv når verten sto i _headers) fordi det bare sammenlignet manifest mot manifest. Panelet leser nå den LEVENDE content-security-policy-svarheaderen (same-origin fetch av /urd.json) og varsler kun om verter som faktisk mangler i frame-src/connect-src; uten header (lokal utvikling) vises ingen varsel.
- FEILMELDINGER UTENFOR ADMIN PÅ ENGELSK (backlog, eierbeslutning): 65 fallback-tekster i tolv functions-filer oversatt (error-felter, Error-meldinger, detail-felter og OAuth-callbackens to rå svar); api-kodene er urørt så admin oversetter som før med taApiError, og ingen tester refererte de norske literalene. AGENTS-leksen («engelsk error-tekst er fallback») oppdatert etter godkjent utkast.
- FELT-KONTRAKTEN (backlog-punktet om kart-innstillingene, løst som additiv kontrakt dokumentert i SKJEMA.md og plugin-README): en plugin-blokk-def kan deklarere `fields` (typene text, number med klemming, toggle, select temastyrt, place) som admin rendrer rett i Egenskaper-panelet; etikettnøklene løses på iframe-siden der plugin-ordboka bor og sendes ferdige i urd-plugin-blocks, som label/variants. place skriver teksten til key og koordinater til lat/lon: «lat, lon» tolkes lokalt, lenker skrives urørt (pluginen tolker dem ved rendring), adresser geokodes via /api/geocode med Søk-knapp, alt i ETT angre-steg. Kartet er referansen (location/zoom/height som felt; config-panelet med tannhjulet fjernet, døde ordboknøkler ryddet i alle fem språk, hint-tekster oppdatert i kjernespråkene, fire nye props.place-nøkler); kalender og skjema beholder «Innstillinger …»-knappen (config-panel-mønsteret for mer enn flate felt-lister).
- VERIFISERT: editor-bygg grønt med gjenbygd bundel, 296/296 tester (paritetstesten omskrevet for kjernespråk-modellen), alle skjemavalideringene.

## [0.6.10] - 2026-08-06

### 0.6.0.12p - Utgivelsen 0.6.10: README-designet ut i malen, og oppdaterens første ekte test - 6. august 2026
- FØRSTE UTGIVELSE ETTER DET NYE RELEASE-RITUALET i AGENTS.md (beskjeden «release» utløser slippritualet + push-klargjøringen): engine 0.6.10, git mv av motormappa, ni skall, fire HTML-skall (base.css-stempelet uendret, filinnholdet er det samme), editor/package.json + låsefil, gjenbygd bundel, og denne utgivelsesoverskriften. check-release grønn i FULL modus før forslag om commit-melding.
- UTGIVELSEN BÆRER innslagene 0.6.9.10p og 0.6.0.11p (engelsk mal-README med readme/-mappe, docs-normaliseringen, designrunden, AGENTS.md-revisjonen) ut til malrepoet, og er samtidig den første EKTE testen av Oppdatering-panelet: urd-web (klonet fra v0.6.9) skal tilby v0.6.10 og gjennomføre motorbyttet med README-omleggingen som valgfrie filer.
- VERIFISERT: bygg grønt med gjenbygd bundel, 296/296 tester (mappenavn-invariantene fulgte omdøpingen automatisk), alle ni valideringene, check-release v0.6.10 grønn i full modus. Diffen er ritualet + de versjonsrefererende testfilene/testrunde-punktene; docs-only-vurdering gjelder ikke (bundel + urd.json), så full runde ble kjørt. Den uavhengige gjennomgangen fra 0.6.9.10p/0.6.0.11p dekker innholdet som skipper; selve ritualet er mekanisk og testvoktet.

### 0.6.0.11p - AGENTS.md-revidert etter 0.6.9: 35 avvik rettet, ni nye regler festet - 5. august 2026
- FULL REVISJON AV AGENTS.MD (planlagt i planmodus med egen revisjonsagent som fant 35 avvik og 20 regelkandidater; planen VAR forhåndsvisningen av regelteksten og ble godkjent av eier): repo-strukturen beskriver nå versjonert motormappe/assets-urd-skall/urd.json-kontrakten/readme-mappen/scripts/workflows/dev-server.py, verifiseringens importliste fikk den manglende `i18n` (hyppigst endrede bundlede modul) med riktige kildestier og kjørbar grep-kommando, CI-beskrivelsen dekker alle fire workflowene, ADR-listen går til 0015 med addenda, plugin-seksjonen nevner språkpakken og manifest-feltene, og lokal server-rådet peker på dev-server.py.
- NYE REGLER FESTET: release-ritualet under Arbeidsflyt (slippritual i tillegg til push-klargjøringen, tagg SIST, opprydding ved feilet release, malrepo-taggen flyttes aldri), check-release som fjerde verifiseringssteg ved slipp, migreringsfritaket før v1 i invariant-punktet (backloggens gamle påstand om at det sto der, er nå sann), _headers/_redirects-presiseringen, eierskaps-klassifiseringsplikten med atomgruppen, reserverte slugs to steder, korrigert språkregel (norsk kanonisk, ikke enerådende; engelsk for utad-vendte navn og topics), fase-slipp døpes til treparts-nummer, og fem nye motor-lekser (plugin-import kun /assets/urd/, api.<kode> x5, locale-paritet + seed-regel, statisk lukning, kopi-oppfriskningsplikten). Bevisst utelatt for å holde filen kort: seks kandidater som alt er dekket av kode/tester/ADR-er.
- SIDEFUNN RETTET: ADR-0008-eksemplets døde sti /assets/engine/hint.js til skallet /assets/urd/hint.js (overlevde 0.6.9.2-oppryddingen), og backlog-notatet om fritaksregelen oppdatert.
- PUSHEN OMFATTER OGSÅ 0.6.9.10p-committen (upushet til nå); dens egen p-fortelling under står uendret. Denne runden er ren docs: mekaniske sjekker (ingen tankestrek, alle relative lenker i AGENTS.md treffer, grep-kommandoen gir nøyaktig de 24 lovede importtreffene), 296/296 tester.

### 0.6.9.10p - Engelsk mal-README, docs-normalisering og designrunde - 5. august 2026
- MALENS STANDARD-README ER ENGELSK (eierbeslutning): template/README.md er den engelske teksten GitHub viser på malrepo-forsiden, bokmål og tyrkisk bor i ny `readme/`-mappe med flagg-lenkelinjer alle veier. Eierskapet skalerer: ownedPaths/guard-speilet bruker mønsteret `readme/**` (ett nytt språk = én fil, ingen kontrakt-endring), og `readme` er reservert slug i BÅDE publiseringsvokteren og editorens slug-vakt, med testdekning begge veier.
- DOCS-NORMALISERT (eierbeslutning etter spørsmål om strukturen): alle oversatte dokumentfamilier følger nå samme modell der alle språk bor i familiemappen, også bokmål. docs/OPPSETT-PUBLISERING.md er git mv-et til languages/setup-publication/SETUP-nb.md og docs/BRUKERVEILEDNING.md til languages/user-guide/GUIDE-nb.md; bokmål er fortsatt kanonisk TEKST ved avvik (AGENTS.md-regelen gjelder innhold, ikke plassering). Alle referanser fulgt: språklinjer, åtte oversettelsers bokmålslenker, README-tabellene i nb/nn/se/tr (som nå peker på HVER SITT språks guide; de utdaterte «(på bokmål)»-forbeholdene fra før guidene fantes er fjernet), VEIKART, backloggens v0.9-punkt, malens README-er og synlige lenke-etiketter.
- DESIGNRUNDE (eierbestilling) over de fire dokumentfamiliene: rot-README x5 fikk badge-linje (Tester-workflowens live-status + release-versjon i Urd-turkis) og hurtiglenke-linje (Kom i gang · Brukerveiledning · Veikart, språkriktig per fil); malens README x3 fikk sentrert Urd-logo (raw-URL mot hovedrepoets brand-mappe, virker i alle kloner) og hurtiglenker; oppsettsguidens Forutsetninger-seksjon flyttet ØVERST i alle fem språk (sto etter steg 0); brukerveiledningen x5 fikk énlinjes innholdsfortegnelse med GitHub-ankre generert fra hver fils egne oversatte overskrifter.
- UAVHENGIG GJENNOMGANG (full runde: diffen rører guard.js/urd.json/tester) fant seks ting, alle håndtert:
  - RETTET (middels): editorens RESERVED_SLUGS manglet `readme`, så en side med den slugen ville passert slug-valideringen og først feilet server-side ved publisering med generisk feil i stedet for den vennlige reservert-navn-meldingen. Lagt til + gjenbygd bundel.
  - DOKUMENTERT (middels, smalt): en side med slug «readme» bygget FØR reservasjonen ville mistet ruten ved neste motoroppdatering (kopien friskes ikke lenger og kan ikke re-publiseres). Akseptert uten migreringsvei fordi ingenting eksternt er bygget pre-v1, ført under kjente begrensninger med eksplisitt regel: slug-reservasjoner ETTER v1 krever oppryddingsvei i oppdatereren.
  - RETTET (middels-lav): to TOC-ankre i den tyrkiske guiden var døde pga. İ-sluggingen: GitHub downcaser İ (U+0130) til i + kombinerende prikk (U+0307), så «İçeriği düzenleme»/«İlk kez» får ankre med U+0307 som lenker med ren i aldri treffer. Verifisert på byte-nivå og rettet til U+0307-formen; de øvrige 53 ankrene på fem språk var riktige.
  - RETTET (lav): åtte oversettelsesnoter navnga de gamle filnavnene i prosa, badge-alt-tekstene i se/tr sto på norsk, og nb-guiden brukte filnavn som lenkeetikett; README-nb-tabellens languages-rad beskrev gammel tilstand.
- VERIFISERT (også gjenkjørt av gjennomgåeren): 296/296 tester (kontrakt-, fullstendighets- og slug-vaktene dekker de nye mønstrene), bygg grønt med gjenbygd bundel etter slug-fiksen, alle ni valideringene, check-release grønn, lenke-/anker-skript over alle 21 rørte filer, oppdaterer-simulering mot ekte planUpdate (readme-filene er valgfrie, aldri atom; gamle README-varianter slettes rent ved neste oppdatering), ingen tankestrek.

## [0.6.9] - 2026-08-05

### 0.6.9.9p - Engangsoppsettet i praksis: PAT-instruksene presisert og topic-konvensjonen ble engelsk - 5. august 2026
- SKREVET UNDER SELVE ENGANGSOPPSETTET, der instruksene ble prøvd mot GitHubs faktiske UI: UTVIKLING-stegene for tokenet er nå den eksakte klikkstien (Fine-grained token med Only select repositories → urd-template og Contents = Read and write; GitHubs nye permissions-UI legger rettigheten til via «Add permissions»-listen FØRST og viser tilgangsnivå-menyen på den tilføyde raden), og secret-steget sier utvetydig at tokenet limes inn i MONOREPOET Urd sine Actions-secrets, ikke i malrepoet, som ikke skal ha noen secrets. Presisert etter at eieren sto i feil repo med god grunn: instruksen sa bare «monorepoets Actions-secrets». Navn-mot-verdi-skillet er også nedfelt (navnet URD_TEMPLATE_PAT står i workflow og docs; strengen skal aldri inn i noen fil).
- TOPIC-KONVENSJONEN ER `urd-template` (eierbeslutning 5. august 2026: engelsk er standarden, konsistent med `urd-plugin`): byttet fra `urd-mal` i UTVIKLING, VEIKART, backloggen, testrunde-punktet, plugin-README og den engelske roadmapen. Historiske CHANGELOG-innslag som nevner det gamle navnet står urørt som historikk; ingen kode leser topicen.
- SPENNET: eieren committet og pushet README-/oppsettsforbedringene fra samme arbeidsøkt selv underveis («Oppdatert readme»); dette innslaget dekker resten av diffen. Docs-only push: mekaniske sjekker (tankestrek, sjekkboks-syntaks, lenkemål, at ingenting er fjernet fra TESTRUNDER) i stedet for uavhengig review, per verifiseringsregelen.

### 0.6.0.9-0.6.9.8p - Push-klargjøring: fasegaten gjennomgått, to oppdaterer-funn rettet - 5. august 2026
- SPENNET er blandet serie og skrives fullt ut: 0.6.0.9, 0.6.0.10 og hele 0.6.9-milepælen (0.6.9.1-0.6.9.8; 0.6.9.1-innholdet ligger i 0.6.9.2-committen, med egne CHANGELOG-innslag).
- VERIFISERT (også gjenkjørt av gjennomgåeren selv): editor-bygg grønt og ferskt bygg byte-likt committet bundel, 296/296 tester, alle ni skjemavalideringene, `check-release v0.6.9` grønn i FULL modus, ingen tankestrek i diffen, lenkemål finnes.
- UAVHENGIG GJENNOMGANG av hele push-diffen (frisk agent uten øktens kontekst, med rename-deteksjon så motorflyttingene ikke druknet det substansielle): sikkerhetskritisk serverkode, workflows, motor- og editorendringene, tester, locales-stikkprøver og docs, mot AGENTS.md og ADR-0003/0005/0006/0013/0014. Funnet RENT: update-endepunktet slipper aldri egne bytes inn (alt hentes fra malrepoet ved validert tagg), skip kan aldri røre atomgruppen, GraphQL-spørringen er injeksjonssikker, race-vernet er dobbelt, token aldri i feilmeldinger, release.yml uten shell-injeksjon (tag via env, sitert overalt), eierskapskartet i synk med urd.json, og ingen emoji/native select/$state-proxy i ny editor-kode.
- FUNN 1, RETTET (middels): kalender-presetens blokk seedet fortsatt `version: 3` mot en definisjon pre-v1-innbakingen satte til 1; `lift()` behandler nyere data enn definisjonen som «newer-than-engine», så «Hva skjer»-seksjonen fra preset-menyen ville rendret kalenderen som DØD PLASSHOLDER. Oppdaget ved grep etter `version: [2-9]` over motor + plugins (eneste treff); skjemavalideringen kan ikke fange det (version er bare «integer >= 1»). Rettet til 1.
- FUNN 2, RETTET (middels): oppdaterer-POST-en la malens egne slug-kopier (kaker/om-oss/kontakt) i tre-innslagene TO ganger ved motorbytte - én gang fra planen (de ligger i maltrærne) og én gang fra kopi-oppfriskningen, verifisert med kjørbar simulering over de ekte funksjonene; duplikate innslag i et git-tre kan avvise hele commiten. Rettet dobbelt: copyPaths filtrerer bort stier som alt er planlagt, OG slug-kopiene er flyttet INN i motor-atomgruppen (`isAtomPath` bruker `isPageIndexCopy`), som samtidig tetter at et «Behold min»-valg på en kopi ville blitt stille overstyrt - en tilbakeholdt kopi ville pekt på slettet motormappe, nøyaktig det ADR-0013 forbyr. Test-case lagt til.
- FUNN 3-4, DOKUMENTERT i stedet for rettet (lav): (a) oppdatereren gjenoppretter/sletter malens eksempelside-ruter i kantene der eier og mal er uenige om en slug - selvhelbredende ved neste publisering, ført under kjente begrensninger; (b) POST-en tillater nedgradering til eldre tagg (GET tilbyr kun nyeste; herding, ikke hull) og en treparts rc-tagg blir synlig som «nyeste» straks den pushes - ført som herding-punkt under v0.7 med rc-rådene.
- UNDERVEIS FRA EIER: docs/image.png (skjermbilde av «+ Ny blokk»-menyen, feilcommittet med 0.6.29, referert ingen steder) er slettet og bug-punktet lukket; alle 0.6.9.x-testrunde-batchene er samlet til ÉN 0.6.9.x-batch, og etter ny beskjed slått sammen fra 40 punkter til 16 der like og samme-tema-punkter er flettet (alt testbart innhold bevart: API- og panel-sjekkene for samme scenario står nå i samme punkt).
- ETTERFUNN FRA EIER (dokumentasjonspresisjon): «Use this template»-knappen er en egenskap ved et REPO merket som mal (Settings → General → «Template repository»), aldri ved en mappe, og urd-template-lenkene gir 404 til engangsoppsettet er gjort - verifisert mot GitHubs egen dokumentasjon. UTVIKLING-engangsoppsettet er skrevet om til den eksakte rekkefølgen (opprett repo MED README så main finnes, secret, første synk, SÅ mal-avhukingen som gir knappen), og alle fem README-ene fikk direkte lenke til oppsettsguiden i steg 1 (den lå før kun i dokument-tabellen).
- GRUNDIG BUGGJAKT (bestilt av eier etter gjennomgangen): tre parallelle gjennomgåere med hver sin linse (oppdaterer-kjeden server-side, motor/kjøretid i nettleser, editor-UI), alle med kjørbare verifiseringer. Sju funn rettet:
  - GAMLE UTKAST-VERNET (viktigst): et localStorage-utkast med HØYERE schemaVersion enn motoren (typisk liggende igjen fra før pre-v1-innbakingen) passerte USETT gjennom liftPageFile (no-op nedover), rendret bakgrunnslag stille borte og kalender som plassholder, og ETT trykk på Publiser ville sementert det i repoet. Nå forkastes slike utkast ved lasting (console.warn, serveren er fasiten), både for sider og site.json.
  - PUBLISERINGS-SPERRE I DEPLOY-VINDUET: publish() skriver slug-kopier fra SERVERT rot-index, som i minuttet etter en motoroppdatering fortsatt peker på slettet motormappe; samme fane sperres nå mens oppdateringen ruller ut (ny nøkkel update.publishBlocked x5). Flerfane-varianten er dokumentert i backloggen (konfliktsjekken fanger den, men uten oppdaterings-kontekst i varselet).
  - 409-grenen i runUpdate avventer nå re-sjekken (panelet sto ellers blankt i et vindu der en ekstra GET kunne slippe forbi busy-vakten).
  - Avkortet git-tre (GitHub trunkerer ved ~100k innslag) stoppes nå trygt i oppdaterings-sjekken i stedet for å feilklassifisere filer utenfor utsnittet som «mangler lokalt» og overskrive dem uten varsel - samme forsiktighet commitFiles alt hadde for slettinger.
  - Formgyldig men ikke-eksisterende måltagg diagnostiseres nå som updateBadTarget i stedet for «malrepoet utilgjengelig»; ugyldig skip-liste bruker updateFailed-teksten med riktig detalj i stedet for commit-endepunktets villedende api.badFiles.
  - `git push --atomic` i release.yml: gren og tagg går sammen eller ikke i det hele tatt, så en manuell commit i malrepoet aldri kan etterlate en tagg uten innhold.
  - speculation-rules ekskluderer nå /api/* (hover kunne prerendre /api/github/login og klobre OAuth-state-cookien midt i en innlogging).
  - DOKUMENTERT i stedet for rettet: tags-pagineringen (kun første 100 tagger leses; ført på herding-punktet), og at en gjennomgåer fant bundelen utilbakestilt etter sin egen verifisering - gjenbygd og bekreftet: ferskt bygg er byte-likt, null 0.6.8-referanser igjen.
  - Sjekket RENT med simuleringer: decodeBlob (ÆØÅ/emoji-rundtur), planUpdate-kantene, rootDir-modusen, GraphQL-escaping, skallenes eksporter (alle ni importert i node), modul-instans-deling, base.css-stempelet mot djb2, demoinnholdet løftet uten plassholdere, dev-serverens ruting, oppsettsveiviserens felt-sletting ved publisering, i18n-nøkkelparitet, og at «Forkast utkast»-gjenoppliving av setup-signalet er riktig adferd (serveren HAR feltet til første publisering; localStorage-vakta hindrer gjentatt visning).

### 0.6.9.8 - Fase-slippet: mellomstemplet omdøpt til 0.6.9 - 5. august 2026
- OMDØPINGEN: urd.json.engine er 0.6.9, motormappa git mv-et til `assets/engine/0.6.9/`, alle ni skallene i assets/urd/ re-eksporterer fra ny versjon, alle fire HTML-skallene (rot + tre slug-kopier, 40 referanser hver) peker på den nye stien, og editor/package.json + låsefilen er rettet fra 0.5.10-driften til 0.6.9. Ett-steds-designet fra 0.6.9.2 gjorde resten automatisk: vite-aliaset, validate.mjs og alle testene leser mappenavnet fra urd.json.
- CHANGELOG-UTGIVELSEN: `## [0.6.9] - 2026-08-05` satt inn over 0.6.9-innslagene (Keep a Changelog); [Ulansert] står igjen med konvensjonsprosaen. Testenes eksempelstier og de to testrunde-punktene som nevnte den versjonerte mappa ved navn, fulgte med til 0.6.9.
- PLUGIN-KRAVENE står bevisst på `>=0.6.8 <1.0.0`: funksjonene de krever kom i 0.6.8-milepælen, og 0.6.9 tilfredsstiller kravet.
- BESLUTNING (eier, 5. august 2026): slippet gjøres FØR ende-til-ende-testingen i stedet for rc-veien - uten eksterne brukere er en utestet førsteutgivelse akseptabel pris for å få malrepoet ut, og oppdaterings-testen trenger uansett to versjoner i malrepoet (testes med en liten rc/patch etterpå).
- VERIFISERT: bygg grønt med gjenbygd bundel, 296/296 tester (modulepreload-testens invarianter fulgte omdøpingen automatisk), alle ni valideringene, `check-release v0.6.9` grønn i FULL modus (engine == mappe == CHANGELOG-overskrift == package.json, porten release-Action-en håndhever), lokal røyk-test 200 på alle 0.6.9-stiene med /urd.json som melder 0.6.9. Eneste gjenværende «0.6.8» i malen er en korrekt historisk kommentar og requiresEngine-kravene.

### 0.6.9.7 - Docs- og ADR-rest: redirects-eierskapet festet, splitten dokumentert ende til ende - 5. august 2026
- ADR-0015 REDIRECTS-EIERSKAP (gate-avklaringen festet): generert modell over host-nøytralt datalag - redirect-data i innholdet, publisering genererer `_redirects` i hostens format som slug-kopiene; filen er publiserings-generert (verken Urd-eid eller håndredigert, så ADR-0006-konflikten oppstår ikke), forblir i guard-DENY til byggingen i v0.7 med SEO-grunnpakken, og en håndlagt fil via git overlever både publisering og oppdatering i mellomtiden.
- ADR-0004-ADDENDUM: splitten gjennomført - release-Action-en, «Use this template»-modellen, urdweb-utskillelsen som dogfooding, og oppdaterings-avsnittets «overskriver kun manifest-stier» presisert til blob-SHA-klassifiseringen i ADR-0014.
- ARKITEKTUR: to nye seksjoner (Motorversjonering/ADR-0013 og Oppdateringsmekanismen/ADR-0014) erstatter det gamle «fremtidig oppdaterer»-avsnittet; det åpne sjekksum-spørsmålet der er nå besvart og fjernet. UTVIKLING: strukturtreet viser versjonert motormappe + assets/urd-skallene, guard-regelen peker på oppdaterens motsatte domene.
- OPPSETT-PUBLISERING PÅ ALLE FEM SPRÅK: ny seksjon «0. Lag nettsidens repo» (Use this template + Oppdatering-panelet), URD_TEMPLATE_REPO som ny valgfri variabel 8 med DEPLOY_HOOK_URL omnummerert til 9 (også i feilsøkingstabellen), speilet inn i en-GB/nn/se/tr.
- README X5: «How it works»-steg 1 peker på urd-template med «Use this template» i stedet for «klon malen», og nytt steg 5 om Oppdatering-panelet, i rot-READMEen og alle fire oversettelsene.
- BACKLOG: 5b.7-notatet i moderniserings-seksjonen sier nå eksplisitt at punktet ble vurdert og UTSATT ved gaten, med den tekniske begrunnelsen (spenningen mot modulepreload-lista og kopi-oppfriskningsplikten, notert i ADR-0013).
- AGENTS.MD-UTKAST lagt frem for godkjenning (regelen krever forhåndsvisning, ikke skrevet inn): versjonert motorsti + assets/urd i repo-strukturen, ADR-listen komplettert med 0012-0015, og ny motor-lekse om at plugins kun importerer /assets/urd/-stier.
- VERIFISERT: ren docs-diff med mekaniske sjekker (ingen tankestrek, lenkemål finnes, variabelnummer konsistente), 296/296 tester fortsatt grønne, ingen editor-gjenbygg (markdown kan ikke påvirke bundelen).

### 0.6.9.6 - Mal-forberedelser: setup-signalet, malens README og støyverifiseringen - 5. august 2026
- SETUP-SIGNALET PÅ PLASS: malens site.json har `"setup": true`, og navnematch-fallbacken («Min forening») er fjernet fra App.svelte, så oppsettsveiviseren utløses KUN av det eksplisitte signalet (som applySetup sletter ved fullføring). FUNN: veiviseren var i praksis DØD på ferske kloner - feltet fantes i skjemaet fra 0.6.2, men malinnholdet satte det aldri, og malens tittel er «Urd», ikke fallback-navnet. Merk: urdweb-demoen viser nå veiviseren for førstegangsbesøkende i admin til repoet skilles ut (eiersteget fjerner feltet der).
- MALENS EGEN README PÅ TRE SPRÅK (eierens valg): template/README.md (bokmål) + README-en.md + README-tr.md med flagg-lenkelinje etter rot-READMEens konvensjon, skrevet for MAL-BRUKEREN (repoet ER siden, «Use this template» + oppsettsguiden, lokal titt med python-server, Oppdatering-panelet, innholdsstrukturen, plugin-topicen), med lenker til hovedrepoets docs og oversettelsene (engelsk/tyrkisk peker på sine egne guide-oversettelser). Alle tre klassifisert Urd-eid (urd.json + guard-speilet; utenfor atomgruppen, så eieren kan svare «Behold min» og skrive sine egne) - fullstendighets-testen fra 0.6.9.4 fanget klassifiseringsplikten automatisk, nøyaktig som designet.
- STØYVERIFISERINGEN (ingen kodeendring, det var konklusjonen): history.js/revert.js deler content-filteret, og i et SPLITTET brukerrepo er det riktig som det står - publiseringer rører alltid content/, utviklingscommits finnes ikke lenger, og oppdaterings-commits (aldri content/) holdes riktig utenfor både historikk-panelet og angre-målene. Monorepo-støyen forsvinner av seg selv med splitten; truncated-håndteringen består som korrekt vern for reelt store repoer. FUNN til kjente begrensninger: angring bytter hele nettside-undertreet, så «angre siste publisering» rett etter en Urd-oppdatering ruller også motoren tilbake (konsistent gammel tilstand, aldri blandet, men kan overraske).
- SIKKERHETSSJEKK: .dev.vars/.wrangler i template/ er gitignorert, så release-Action-ens ferske utsjekk kan aldri lekke dem til malrepoet.
- VERIFISERT: 296/296 tester, bygg grønt med gjenbygd bundel (App.svelte-endringen), alle ni valideringene (site.json med setup validerer), ingen tankestrek.

### 0.6.9.5 - Oppdaterer admin-UI: Oppdatering-panelet med per-fil-valg og deploy-polling - 5. august 2026
- NYTT OPPDATERING-PANEL i verktøy-gruppen ved Historikk (stabil id `update`; panelskinnen er tekstbasert, så planens SVG-ikon-behov bortfalt). Åpning kjører GET-sjekken automatisk og viser: installert motorversjon (vises for FØRSTE gang i admin - feltet fantes kun som intern plugin-sjekk), tilgjengelig versjon, «kjører nyeste»-tilstanden, og endringslisten delt i motorfiler (atomgruppen, med varseltrekant + forklarende tooltip på håndredigerte) og valgfrie filer med «Behold min»-checkbox per fil (kun utenfor atomgruppen; serveren validerer det samme, så UI-et lover aldri mer enn endepunktet holder).
- `_headers`-INSTRUKSEN følger plugin-CSP-mønsteret: når filen avviker fra målversjonen vises en advarsel og oppstrøms-teksten i en markerbar pre-blokk, så eieren kan føre endringene inn for hånd - filen skrives aldri (ADR-0006/0014).
- UTFØR-FLYTEN: askConfirm-dialog med målversjon, antall skrivinger/slettinger og eksplisitt filliste-varsel når håndredigerte atomfiler overskrives; POST med to/expect/skip; 409 gir oversatt updateRace og automatisk ny sjekk; suksess poller /urd.json til engine-feltet melder målversjonen og laster admin på nytt (awaitRevertDeploy-mønsteret, men utkastene BEHOLDES: oppdatereren rører aldri brukereide filer, så de er like gyldige etter omlastingen). Oppdater-knappen krever publiseringstilgang som angre-knappen; lokalt uten functions degraderer panelet pent til utilgjengelig-melding med Prøv igjen.
- I18N: panel.update + 27 update.*/confirm.update.*-nøkler i alle fem admin-ordbøkene med {var}-paritet (paritetstesten vokter); feil vises via taApiError så alle api.update*-kodene fra 0.6.9.4 kommer oversatt.
- VERIFISERT: 296/296 tester, bygg grønt med gjenbygd bundel (editor.js + editor.css), alle ni valideringene, ingen tankestrek. Byggets 43 advarsler er de FORHÅNDSEKSISTERENDE label/felt-koblingene (backloggens v0.7-punkt) pluss to pointerdown-roller; null css-advarsler og ingen gjelder ny markup (ny label omslutter checkboxen sin). Ende-til-ende (rc-til-rc i testklon) står i testrunden etter rc-synken.

### 0.6.9.4 - Oppdaterer server-side: blob-SHA-sjekksum, ren plan og atomisk inline-commit - 5. august 2026
- EIERSKAPSKARTET I GUARD.JS (ADR-0014, ny): OWNED_PATTERNS/USER_PATTERNS speiler urd.json med kontraktstest som hindrer drift, pluss matchesPattern (eksakt sti eller prefiks/**), isOwnedPath, isUserPath og isPageIndexCopy; normaliseringen (skråstreker, absolutte stier, ..) faktorert ut og delt med isAllowedPath. FUNN underveis: speculation-rules.json sto uten eierskap (verken owned eller user); lagt inn i ownedPaths. Ny fullstendighets-test krever at HVER faktiske fil i template/ har nøyaktig én eierskapsklasse, så en fremtidig fil uten klassifisering stopper i CI i stedet for å falle utenfor både publisering og oppdaterer.
- REN PLAN I NY `_lib/update-plan.js`: planUpdate(baseline, target, user) tar tre blob-SHA-kart (malens tre ved baseline-taggen v<engine>, ved målversjonen, og brukerens) og gir endringssettet uten å laste en eneste fil: endret+urørt skrives, endret+håndredigert flagges `edited`, ny+eksisterende flagges `created`, fjernet oppstrøms slettes (redigert sletting flagges `editedDelete`), manglende eide filer gjenopprettes, og filer oppstrøms ikke rørte består i stillhet (Publii-semantikken fra LAERDOMMER). Motorbytte-swappen (gammel versjonert mappe ut, ny inn) faller ut av de generiske reglene uten egen kode. Pluss highestVersionTag, chunkEntries og isAtomPath: motor-atomgruppen (assets/admin/index.html/slug-kopier/urd.json) kan aldri splittes i per-fil-valget - MERK at urd.json ble flyttet INN i gruppen (korreksjon av planutkastet: engine-feltet ER mappenavn-invarianten fra ADR-0013, så å holde den tilbake under et motorbytte ville brutt siden).
- ENDEPUNKTET `api/github/update.js`: GET-sjekk (~8 subrequests) leser engine fra brukerens urd.json I REPOET (aldri fra klienten), klassifiserer alle Urd-eide filer, og leverer `_headers`-avvik med oppstrøms tekst til diff-instruksen (filen skrives ALDRI, jf. ADR-0006). POST-utfør regner planen ut på nytt server-side (klienten får kun velge bort filer utenfor atomgruppen, validert), henter alle måltekster i ETT GraphQL-kall (Blob.text via aliaser), frisker slug-kopiene i SAMME commit (kopi-oppfriskningsplikten; blob-SHA-sammenligning mot malens rot-index avgjør hvilke som faktisk trenger skriving), og committer atomisk med expect-vern via ny commitTree i github.js: inline content i kjedede base_tree-trær i stedet for commitFiles' éne blob-POST per fil, som ville sprengt Cloudflares 50-subrequest-tak på ~90 filer. Binære/avkortede blober tar REST-omveien (base64 rett inn som blob). cfg() fikk templateRepo (URD_TEMPLATE_REPO, default Artiscow/urd-template) og github.js fikk ghGraphql med samme retry-regel som gh().
- FEILKODENE maskinlesbare fra dag én: seks nye api.update*-nøkler (updateBadTarget, updateNoBaseline, updateTemplateUnreachable, updateRace, updateBadSkip, updateFailed) i alle fem admin-ordbøkene; i18n-kontraktstesten fanger dem automatisk.
- ADR-0014 fester modellen med forkastede alternativer: PR-basert oppdatering (Decap) står som mulig v2 etter v1.0, eget sjekksum-manifest er overflødig når blob-SHA-ene alt bor i trærne, og gjenbruk av commit-endepunktet ville åpnet den testvoktede allowlisten for kode.
- VERIFISERT: 296/296 tester (14 nye: eierskapskart, fullstendighet, disjunkthet, hele plan-regeltabellen, chunking, tagg-valg), bygg rent og bundelen UENDRET (kun functions + kjøretids-lastede ordbøker rørt), alle ni valideringene, ingen tankestrek. Ende-til-ende mot ekte malrepo venter på rc-synken (testrunde).

### 0.6.9.3 - Release-Action: template/ synkes squashet til urd-template med baseline-tagg - 5. august 2026
- NY WORKFLOW `.github/workflows/release.yml` etter tests.yml-idiomet (SHA-pinnede actions med versjonskommentar som dependabot-gruppen alt vedlikeholder, norske stegnavn): trigges av `release: published` eller manuell dispatch (tag- + prerelease-input), sjekker ut monorepoet ved taggen, kjører versjonskonsistens-sjekk og testsuiten som røykvern, sjekker ut malrepoet med fine-grained PAT-secreten URD_TEMPLATE_PAT (GITHUB_TOKEN når ikke på tvers av repoer), tømmer alt unntatt .git, kopierer inn template/-innholdet, committer ÉN squashet «Urd vX.Y.Z» og tagger + pusher. Malrepoets tagg er oppdaterens sjekksum-baseline; synk og tagging i samme kjøring kan aldri drive fra hverandre.
- ROBUSTHET: concurrency-gruppe (to samtidige synk ville kappløpt om malrepoets HEAD), idempotent omkjøring (tom diff hopper over commit; eksisterende tagg godtas kun når den alt peker på samme innhold, ellers stopp - baseline-tagger flyttes aldri i stillhet), full historikk-utsjekk av malrepoet (push fra grunn klon er skjørt, og historikken er én commit per utgivelse).
- NYTT SCRIPT `scripts/check-release.mjs`: tagg-format, tagg == urd.json.engine, motormappa `assets/engine/<engine>/` finnes (ADR-0013), og i full modus CHANGELOG-utgivelsesoverskriften + editor/package.json. `--prerelease` (rc-synk for oppdaterer-testing) hopper over de to siste. Verifisert begge veier: prerelease-modus grønn i dag, full modus feiler korrekt på [0.5.10]-overskriften og package.json-driften som fase-slippet (0.6.9.8) skal rette.
- DOCS: UTVIKLING-utgivelsesritualet omskrevet til den automatiserte flyten (motor-bump-steget fra ADR-0013, engangsoppsettet malrepo + PAT, topic-konvensjonen urd-mal/urd-plugin, og rc-prosedyren med merknad om at engine-feltet er streng treparts semver, så rc-er bruker egne treparts numre). Plugin-README fikk «Dele pluginen»-avsnitt med urd-plugin-topicen; OPPSETT-PUBLISERING sin blokkert-liste fikk assets/urd/ (var utdatert etter 0.6.9.2).
- VERIFISERT: YAML parser rent, check-release testet i begge moduser, 282/282 tester, ingen tankestrek. Ingen bundlede filer rørt (ingen editor-gjenbygg). Selve workflowen kan først kjøres når eierens engangssteg er gjort (opprette urd-template, legge inn secreten); det står som testrunde-punkter.

### 0.6.9.2 - Motor-URL-versjonering: immutable motor bak versjonert mappe og stabilt plugin-API - 5. august 2026
- MOTOREN ER VERSJONSADRESSERT (ADR-0013, ny): hele `assets/engine/` er flyttet (git mv, historikken består) til `assets/engine/0.6.8/`, der mappenavnet ALLTID er lik urd.json.engine. Relative ES-importer arver ingen `?v=`-query, så stien selv er eneste måte å adressere hele import-grafen per versjon; semver-navnet ble valgt over en egen teller fordi en glemt teller-bump ville servert stale immutable-moduler i stillhet, mens navn == engine-felt er trivielt testbart. 0.6.8 er fortsatt mellomstemplet; fase-slippet omdøper felt + mappe i ett.
- STABILT PLUGIN-API I NY `assets/urd/`: ni skallmoduler (i18n, hint, dropdown, language-packs og admin-ordbøkene x5) som re-eksporterer fra gjeldende versjonert motor. Plugins importerer KUN disse stiene (alle tre referansepluginene og plugin-README byttet); de dynamiske importene i i18n.js/plugins.js går også via skallene siden samme linjer kjører fra editor-bundelen. Skallene står UTENFOR det immutable treet og revalideres normalt; motor og skall ender på samme modul-URL, så i18n-registrene forblir singletons. Alternativene forkastet: importmap krever inline script (CSP-en har ikke unsafe-inline), skall på engine-roten ville krevd versjonert immutable-regel i den håndredigerbare _headers.
- IMMUTABLE-CACHE via to VERSJONSNØYTRALE regler i _headers (`/assets/engine/*` + base.css, som nå alltid refereres med innholdsstempel `?v=<djb2-hash>` fra skallene): skrives én gang, trenger aldri endres ved motor-bump, avgjørende siden _headers er håndredigerbar (ADR-0006) og permanent unntatt auto-oppdatering. guard-DENY og ownedPaths fikk `assets/urd/`.
- SLUG-KOPIENE REGENERERT som byte-like kopier av rot-index.html, som samtidig rettet en EKSISTERENDE drift utforskingen fant: om-oss/kaker/kontakt manglet theme.css-lenken (temaet var ikke render-blokkerende på undersidene) og theme-init.js (lagret lys/mørk-valg blinket). Ufanget til nå fordi modulepreload-testen kun leste roten.
- ETT-STEDS-OMDØPING: editor-bygget leser motormappa fra urd.json via nytt `$engine`-vite-alias (24 importer i main.js/App.svelte/lib konvertert), validate.mjs interpolerer engine-feltet, og alle 25 testfilene henter motormoduler via ny tests/_engine.mjs (`await engineImport(...)`, siden statisk import ikke kan interpolere stier). Fase-slippets omdøping er dermed feltbytte + git mv; alt annet følger automatisk.
- MODULEPRELOAD-TESTEN VOKTER NÅ SEKS INVARIANTER: mappenavn == engine og nøyaktig ÉN versjonert mappe, preload-lista == boot.js-lukningen, alle motor-referanser i index.html versjonerte, slug-kopier byte-like roten, skallenes re-export-mål inn i gjeldende versjon (egen regex: skall er `export * from`, ikke `import` - første utkast av testen feilet nettopp der og ble rettet), immutable-reglene i _headers og base.css-stempelet mot filinnholdet.
- FIRE MOTOR-LENKER i eldre docs som flyttingen knakk, gjort versjonsnøytrale: SKJEMA/ADR-0010 til ren kodetekst, ADR-0008/0009 peker på de stabile skallene.
- VERIFISERT: 282/282 tester (fire nye vakter netto), editor-bygget rent med gjenbygd bundel (kjøretids-importene i bundelen går via /assets/urd/, de versjonerte stiene i den er kun bundlerens kildekommentarer), alle ni valideringene, og lokal røyk-test mot dev-serveren: rot, versjonert boot.js, skall, admin, stemplet base.css og slug-kopi svarer 200 med 40 versjonerte referanser i servert HTML.

### 0.6.9.1 - Pre-v1-innbaking: utviklingsformatene er versjon 1 igjen - 5. august 2026
- MILEPÆLEN 0.6.9 ER I GANG, og de fem gate-avklaringene ble landet med eier først (loggført i backloggen): pre-v1-innbaking som første steg, sjekksum-modell for oppdatereren (git blob-SHA-er mot baseline-taggen, `_headers` permanent unntatt auto-overskriving), release-Action som squashet én-commit-synk via fine-grained PAT, urdweb som eget repo laget FRA malen, og redirects avgjort kun som eierskap (generert modell med host-nøytralt datalag, bygges i v0.7 med SEO-pakken). Batchplanen 0.6.9.1-0.6.9.8 er fastlagt; dette er første batch.
- INNBAKINGEN (presisert 24. juli 2026: migrerings-overlevelse er uviktig før v1, ingenting eksternt er bygget): sidefilene og site.json er `schemaVersion` 1 igjen (før 3 og 2), gradient-laget, bilde-bakgrunnslaget og kalender-blokken er `version` 1 (før 3, 2 og 3). Dagens props-former ER de nye v1-formene: ingen data endret form, kun versjonstallene og migreringstabellene.
- SLETTET: pageMigrations/siteMigrations-stegene med gridToSquare-hjelperen i migrate.js, gradientens liftStops/sharesFromPositions med hele migrations-objektet, image-lagets parallax-steg og kalenderens source-til-sources/showCategories-steg. Maskineriet (`lift`, `liftPageFile`, `liftSiteFile`) og de tomme `migrations: {}`-kontraktene består urørt som grunnlag for 0.6.9-oppdatereren. App.sveltes gradientProps/mutateGradient ble rene gjennomslipp (lift-løftingen av eldre lag er formålsløs når v1 er nyeste), og den ubrukte lift-importen er fjernet.
- KONSISTENS I STEDET FOR HARDKODING: blankPage i App.svelte og tom-side-fallbacken i urd.js skriver nå `PAGE_SCHEMA_VERSION` i stedet for literal 3, så neste filnivå-bump ikke kan glemme dem. validate.mjs sin syntetiske preset-side fulgte ned til 1.
- DEMOINNHOLDET omskrevet med assert-verifiserte utskiftninger (forventet treff-antall per fil, JSON reparset etterpå): fem version-felt og fem schemaVersion-felt til 1.
- TRE-STEDER-REGELEN fulgt for skjemarelaterte tekster i samme commit: «fra schemaVersion 2»-formuleringene ryddet i page-/site-skjemaets beskrivelser, SKJEMA.md (også versjonseksemplene og migreringsavsnittet, som nå forteller om innbakingen) og render.js/render-testens kommentarer.
- TESTER: page-migration.test.mjs slettet, gradientens fire og image-lagets ene migreringstest fjernet (11 tester bevisst ut); de syntetiske maskineri-testene i migrate.test.mjs og kontraktstestene i animations/galleri består og dekker fortsatt lift-reglene fullt.
- ADR-0005 fikk et addendum om innbakingen, med presiseringen at «minst én reell migrering i testsuiten»-kravet gjelder fra første reelle formatendring etter v1.0 (backloggens v1.0-bevis) og at skjemavalideringen er det aktive vernet frem til da. Merk: backloggens påstand om at migreringsfritaket for upushede endringer «står i AGENTS.md» stemte ikke (kun invarianten står der); addendumet peker på backlog-presiseringen i stedet.
- VERIFISERT: editor-bygget rent og bundelen gjenbygd (migrate/gradient/image buntes), 275/275 tester (286 minus de 11 slettede), alle ni skjemavalideringene, ingen tankestrek i rørte docs.

### 0.6.0.10 - Flerspråk-restene lukket og død CSS ryddet - 5. august 2026
- API-FEILENE PÅ FEM SPRÅK via maskinlesbare koder: alle 37 feilsvar i template/functions/ bærer nå et additivt `code`-felt (engelsk identifikator per datakontrakt-regelen: `loginExpired`, `publishRace`, `calendarHostNotAllowed` ...) pluss strukturerte parametre der teksten interpolerer (`key`, `login`, `path`, `host`, `status`, `detail`). Den norske `error`-teksten består uendret som fallback og for curl-feilsøking, så functions selv trenger aldri locale-infrastruktur: oversettelsen bor i admin-ordbøkene der den alt finnes.
- NY HJELPER `taApiError` i i18n.js slår opp `api.<code>` i admin-ordboka og interpolerer med svarets egne felter; ukjent kode faller til backend-teksten, manglende svar gir null så kalleren `??`-er sin egen fallback. De fem admin-ordbøkene fikk 37 `api.*`-nøkler hver, formulert konsistent med de eksisterende status-tekstene (samisk fortsatt maskinutkast).
- DEN SKJØRE KONTRAKTEN FRA PUSH-GJENNOMGANGEN ER BORTE: App.svelte sammenliknet mot den norske literalen «Ugyldig eller utløpt innlogging» for å velge nøkkel; nå sjekkes `data?.code === 'loginExpired'`. Alle fem rå `?.error`-visningene i App.svelte (historikk, angre, publiserings-401/403/andre) og begge i pluginene (kalender-feed, kart-geokoding) gikk over til `taApiError`.
- TO NYE TESTER i tests/i18n.test.mjs: en kontraktstest som skanner alle functions-filer for `code:` og krever at hver har sin `api.*`-nøkkel i nb-basen (feiler under 30 funn, så regex-drift ikke gir falsk grønt), og en enhetstest av taApiError-fallbackene. Paritetstesten dekker de nye nøklene automatisk i alle fem språk.
- MOTOR-STEMPELET BUMPET (etter eierens beslutning om å ta det før 0.6.9 i stedet for ved fase-slippet): urd.json melder `engine: 0.6.8`, døpt etter milepælen som innførte funksjonene kravene skal uttrykke (firedelte commit-numre finnes ikke i semver, og satisfiesEngine parser kun treparts). Alle fire referansepluginene strammet fra `>=0.5.0 <1.0.0` til `>=0.6.8 <1.0.0` (kalender/skjema/kart bruker locales/names fra 0.6.8, sprak-svensk bruker languages), og eksempel-manifestene i plugins/README.md og SKJEMA.md oppdatert i samme jafs. Merk: 0.6.8 er et mellomstempel; fase-slippet omdøper motoren til det endelige 0.6-nummeret, og først da stemmer git-tagg og CHANGELOG-overskrift med engine-feltet igjen (UTVIKLING-regelen).
- DØD CSS RYDDET: alle 18 ubrukte selektorene fra byggeloggen er fjernet fra App.svelte (netto 78 linjer), inkludert foreldreløse kommentarer og nedtrekk-referanser i kommentarer som ble stående etter ADR-0009. Hvert navn ble sjekket mot markupen først: ingen `<select>` igjen i editor-kilden, ingen dynamiske `type=`-attributter, ingen dynamisk bygde klassenavn - backlog-punktets forbehold om dynamiske klassenavn slo ikke til. editor.css er uendret (Svelte strippet alt dette fra outputen allerede; gevinsten er en byggelogg uten støy som skjuler ekte advarsler).
- VERIFISERT: bygg uten en eneste advarsel, 286/286 tester (to nye), alle ni skjemavalideringene, bundelen gjenbygd (i18n.js buntes av editoren). Engine-utskiftingene ble gjort assert-verifisert (forventet treff-antall per fil før skriving); ingen tester eller annen kode refererte 0.5.10.

### 0.6.0.9 - Actions SHA-pinnet: forsyningskjede-vern for CI-workflowene - 5. august 2026
- BAKGRUNN: codeql-PR-en (#10) ble merget i stedet for lukket, så codeql-action sto pinnet til eksakt versjon mens de tre andre actions fløt på major-tagg. Mergen avgjorde backlog-beslutningen fra 0.6.0.8: pinning ble valgt, og da er ignore-regel-forslaget feil (det ville frosset codeql på 4.37.4 til neste major). I stedet ble backlog-alternativet gjennomført: pinne alle.
- ALLE FIRE actions (sju uses-linjer i tre workflower) er SHA-pinnet med versjonskommentar bak (`actions/checkout@3d3c42e... # v7.0.1`): en tagg kan flyttes av en angriper som kompromitterer action-repoet, en commit-SHA kan ikke. SHA-ene er slått opp direkte mot upstream-repoene med `git ls-remote` (derefererte commit-SHA-er, ikke tag-objekter), pinnet til nyeste innenfor de majorene som alt var i bruk: checkout v7.0.1, setup-node v7.0.0, dependency-review-action v5.0.0 og codeql-action v4.37.6 (to patcher nyere enn PR-ens 4.37.4).
- DEPENDABOT.YML har fått en gruppe som samler alle actions-bumper i én ukentlig PR, så pinningen ikke gir en PR per action-utgivelse. Dependabot leser versjonskommentaren bak SHA-en og holder pinningene ferske, som er nøyaktig auto-oppdateringen de flytende taggene ga, bare med PR-review som port.
- VERIFISERT: alle fire YAML-filene parser rent, ingen tankestrek. Diffen rører kun .github/, så bygg, tester og bundel er upåvirket; selve pinningen kan først observeres ved at workflowene kjører grønt etter push (testrunde-punkt).

### 0.6.0.8p - Push-gjennomgang: avhengighetsrunden - 5. august 2026
- SPENNET er én commit (0.6.0.8), så pushen arver nummeret med p-suffiks.
- VERIFISERT: `npm ci` mot den nye låsefilen ga nøyaktig de låste versjonene (ingen avhengighets-drift, i motsetning til hendelsen 22. juli), `npm run build` grønt, `node --test tests/*.mjs` 284/284, `npm run validate` alle ni, `npm audit` null sårbarheter, og to påfølgende bygg ga bit-identisk bundel (deterministisk).
- BUNDELEN GRANSKET MASKINELT i stedet for en vanlig lesegjennomgang, siden diffen er minifisert output: alle streng-literaler i gammel og ny bundel ble trukket ut og talt opp mot hverandre. 4161 mot 4161, og det ENESTE avviket er én malstreng der minifiserer har døpt om den interpolerte variabelen (`${H(wt)}` til `${V(wt)}`). Hver brukersynlig tekst, hver nøkkel og hver sti er dermed uendret; forskjellen på 15 linjer er symbol-omdøping og litt tettere output. Bundelen ble i tillegg parse-sjekket som gyldig ES-modul.

### 0.6.0.8 - Avhengigheter: vite 8.2.0, fast-uri-advisory lukket, gjenbygd bundel - 5. august 2026
- VITE 8.1.5 → 8.2.0 (Dependabot, utvikling-mindre-gruppa). Bumpen kunne IKKE merges alene på GitHub: editoren bunter byggverktøyets output, og 8.2.0 minifiserer annerledes, så den committede bundelen ble 385,68 kB mot 386,18 kB. Uten gjenbygd bundel i samme commit hadde `git diff --exit-code -- template/admin/assets` i tests.yml feilet på merge-committen. Samme mønster som svelte-bumpen i 0.6.6.5.10.
- FAST-URI 3.1.4 → 3.1.5, en HIGH-advisory (GHSA-7p8r-x3mc-p8w7, vertsforveksling via omvendt skråstrek som authority-innleder) som dukket opp i `npm audit` under arbeidet. Pakken er transitiv under ajv og brukes kun av skjemavalideringen på våre egne filer, så eksponeringen er teoretisk, men fiksen er tre linjer. `npm audit fix` ble FORKASTET fordi den skrev om mer enn nødvendig: den strippet `libc`-feltene fra vite sine plattform-binærer, altså urelatert støy i en låsefil Dependabot eier og som ville kollidert med neste Dependabot-PR. I stedet ble version/resolved/integrity endret målrettet, med integrity-hashen hentet fra registeret, og `npm ci` bekreftet at låsefilen fortsatt installerer rent.
- VURDERING AV DEN ANDRE ÅPNE PR-EN (codeql-action v4 → v4.37.4), levert som anbefaling om å LUKKE den: alle fem actions i repoet flyter i dag på major-tagg (`actions/checkout@v7`, `dependency-review-action@v5`, `setup-node@v7`, `codeql-action@v4`). Å pinne kun codeql til en eksakt patch gjør den til eneste avviker, fjerner den automatiske patch-oppdateringen den har nå, og gir en ny PR for hver utgivelse. Reell pinning krever SHA, og da for alle fem. Rotårsaken er at dependabot.yml grupperer minor+patch for npm, men mangler tilsvarende regel for github-actions; forslaget om en ignore-regel står i backloggen.
- SPENNET er fjorten commits (0.6.0.4, 0.6.0.5, 0.6.0.6, 0.6.6.5.12, 0.6.8.1 til 0.6.8.10 og 0.6.0.7), altså hele 0.6.8-milepælen pluss native/CSS-moderniseringen, gradient-kompositorarbeidet og batch E. Nummereringen skrives fullt ut i begge ender fordi serien er blandet.
- VERIFISERT: `npm run build` grønt, `node --test tests/*.mjs` 284/284, `npm run validate` alle ni grønne (inkludert den nye språkpakkens manifest), og bygg-samsvar bekreftet ved at et ferskt bygg på rent arbeidstre ikke rører den committede bundelen.
- UAVHENGIG GJENNOMGANG av hele push-diffen (~3450 linjer kode; locale-dataene stikkprøvet i stedet for lest linje for linje). Ren i alle kontrollerte kategorier: ingen tankestrek, invarianten holdt (gradient v3 endret kun RENDER, footer-modellens defaults flyttet fra AVLEDET visningsmodell og ikke fra lagrede props, blokkene fikk kun additiv labelKey - ingen migrering var påkrevd), tre-steder-regelen oppfylt for begge skjemaendringene, motor-leksene fulgt (ingen $state-proxy over postMessage, ingen native select, nav-lytteren henger på renderingens AbortController), alle nye URL-regexer ankret, og mekanisk verifisert nøkkeldekning: hver `t()`/`ta()` i motoren, pluginene og editoren slår opp i nb-basen, likeså alle dynamiske nøkkelkilder (fonter, glyfer, ikoner, seksjonstemaer, panel-id-er, 23 presets, 9 blokker, 6 bakgrunnslag). Null manglende.
- FUNN 1, RETTET (den alvorligste): `matchLang` matchet med bar `startsWith`, så en språkpakke med en kode som tilfeldigvis begynte som et innebygd språk («ses», «trv», «nnh») ble stille omdirigert til det innebygde språket og kunne ALDRI lastes - selv om både skjemaet og kjøretidsvalideringen godtok koden. Speilbildet var et hull i vernet mot at en plugin kaprer bokmål: «nob» (ISO 639-3) traff verken `matchLang` eller `SUPPORTED_LANGS` og var dermed ledig som pakkekode. Oppdaget ved at gjennomgåeren kjørte `requestedLang('ses')` og fikk 'se'. Rettet ved å matche på HELE tagen eller tag pluss undertagg (`v === tag || v.startsWith(tag + '-')`) mot en eksplisitt tabell som nå også holder trebokstavskodene (nob/nno/sme/tur/eng/nor), så de er opptatte i stedet for ledige. Sør-/lulesamisk (sma/smj) er fortsatt en dokumentert TILNÆRMING til nordsamisk og derfor også opptatt. Testtabellen utvidet med både treffene og ikke-treffene.
- FUNN 2, RETTET: `initSiteLocale` satte `site.lang` og nullstilte ordboka til bokmålsbasen SYNKRONT, før den ventet på overlay-importen. Et nytt site-utkast som kom i det vinduet så at språket allerede stemte, hoppet over re-initieringen og rendret mot en halvbygd ordbok - et kort norsk glimt i previewen hvis eieren skrev videre rett etter et språkbytte. Rettet ved å bytte ordbok i ETT jafs først når tekstene er i hånden: en render underveis ser forrige språk, aldri et halvbygd.
- FUNN 3, RETTET: to dynamiske plugin-importer manglet `/* @vite-ignore */` og ga et nytt `vite-dynamic-import-vars`-varsel i byggeloggen. Ufarlig i kjøretid, men i strid med kodebasens eget mønster for kjøretids-importer. Varselet er borte. Samme runde fjernet den døde `.contrast-warn`-CSS-en fra kontrast-varselet som ble bygget og forkastet i dette spennet; de 18 gjenværende ubrukte selektorene er eldre (mest `select`-regler foreldreløse etter ADR-0009) og er notert i backloggen som en samlet opprydding i stedet for å blandes inn her.
- FUNN 4, RETTET: en doc-kommentar pekte fortsatt på `normalizeLang`, som ble slettet i samme commit.
- FUNN 5-6, NOTERT I BACKLOGGEN i stedet for rettet (de hører til fasegaten, ikke til denne pushen): (a) `template/functions/` er ikke oversatt, så api-feilene vises rått på norsk i all admin - og App.svelte sammenlikner mot den norske literalen «Ugyldig eller utløpt innlogging» for å velge nøkkel, en skjør kontrakt mot backend-tekst som bør bli en maskinlesbar kode; (b) `urd.json` melder fortsatt engine 0.5.10 gjennom hele v0.6, så `requiresEngine` ikke kan uttrykke krav om 0.6-funksjoner, og referansepluginene må si `>=0.5.0` selv om locales/languages krever nyere.
- ETT RAPPORTERT FUNN VAR IKKE ET FUNN: gjennomgåeren meldte at et testrunde-batch (0.6.34) var slettet i strid med regelen om at ingenting fjernes fra TESTRUNDER. Det var testeren selv som strøk det etter å ha testet punktet, som er nøyaktig det regelen tillater. Tilbakeføringen ble reversert.
- EKSTRA BUGJAKT-RUNDE (bestilt før push) på hele diffen inkludert push-klargjøringens egne rettelser, som til da kun forfatteren hadde vurdert. Den nye matchLang/initSiteLocale-koden ble empirisk diffet mot gammel adferd over en tag-korpus (alle avvik tilsiktede) og grenmatrisen kjørt i node - rent. To nye funn, begge rettet: (1) KAPPLØP i previewens språkbytte: initSiteLocale-fullføringene var ikke serialisert, så et tregt pakkespråk-bytte (nettverkslast) som fullførte ETTER at eieren hadde byttet tilbake, klobret det nye valget og lot previewen stå på feil språk til neste utkast-melding. Rettet med en språk-kø i enablePreview: hver oppgave leser utkastets språk på kjøretidspunktet, og hurtigveien (likt språk, synkron render) tas aldri mens en lasting pågår (langPending-vakten). Fiksen er verifisert med en kjørbar simulering av selve kappløpet: gammel logikk endte på det forkastede språket, ny ender på siste valg. (2) Editorens packLangOptions leste rå manifest-data uten per-innslag-validering: en aktivert plugin med et languages-innslag uten navn ga TypeError i sortLangs midt i markup-evalueringen og felte panelet. Rettet med samme filtrering som motorens lasting (hopp over innslag uten gyldig kode/navn, og innebygde koder), så velgeren aldri lover mer enn det som kan lastes.

### 0.6.8.10 - Språkpakker: et nytt språk er nå en plugin uten kode - 5. august 2026
- KONTRAKTEN er det additive manifest-feltet `languages: [{ code, name, site, admin }]`, som lover `locales/site/<kode>.js` og/eller `locales/admin/<kode>.js` med MOTORENS egne nøkler (ikke pluginens egne tekster, det er `locales` fra 0.6.8.8). `entry` og `provides` er gjort valgfrie når feltet er satt: en ren språkpakke har ingen kode å kjøre, og lasteren returnerer etter ordbok-registreringen i stedet for å importere en entry som ikke finnes. Innebygde koder kan ikke overstyres, håndhevet både i skjemaet (`not: {enum: [...]}`) og i kjøretidsvalideringen - en plugin skal ikke kunne kapre bokmål. Tre-steder-regelen fulgt i samme commit: docs/SKJEMA.md, schema/plugin.schema.json (med betinget `required` for entry/provides) og eksempeldata.
- NY MODUL `engine/language-packs.js` eier oppdagelsen (skanner manifestene til de AKTIVERTE pluginene, maks én gang per side) og lastingen. MODULEPRELOAD-TESTEN FANGET EN REELL FEIL underveis: første utkast lot plugins.js importere modulen statisk, og da havnet den i motorens statiske import-lukning - altså nedlastet av hver eneste besøkende, også på sider uten språkpakker. Rettet ved å flytte de rene språkkode-funksjonene (`LANG_CODE_RE`, `isBuiltinLang`, `validateLanguages`) til i18n.js, som alt er lastet, og la selve pakkemodulen hentes dynamisk kun når noen faktisk ber om et språk kjernen ikke har. Vite lagde deretter egne chunk-filer av den dynamiske importen (`language-packs-*.js`, `i18n-*.js` i admin/assets); løst med samme kjøretids-mønster som admin-ordbøkene bruker fra 0.6.8.3 - absolutt sti + `/* @vite-ignore */` - så bundelen er fortsatt kun editor.js + editor.css.
- `normalizeLang` ERSTATTET AV `requestedLang` (innebygd treff, ellers en gyldig pakkekode som den er, ellers bokmål) i initSiteLocale, initAdminLocale, detectAdminLang og previewens språkbytte-sammenlikning. Den gamle funksjonen sto igjen uten kallsteder og er fjernet i stedet for å bli liggende som død kode; testen dekker nå `matchLang`, som faktisk brukes, og ADR-0012 + SKJEMA.md er rettet der de pekte på den gamle. Previewens urd-plugins-gren re-initierer localen når lista inneholdt en pakke, slik at et utkast-aktivert pakkespråk virker uansett meldingsrekkefølge.
- ADMIN: begge språkvelgerne viser pakkespråk alfabetisk sammen med de innebygde, men fra ULIKE kilder, og det er et bevisst valg - besøkende-velgeren følger plugin-UTKASTET (site.lang publiseres i samme slengen), mens admin-velgeren kun tilbyr PUBLISERTE pakker, siden admin leser den publiserte plugin-lista ved oppstart og et valg fra utkastet ellers ville landet på bokmål ved neste innlasting. Et lagret valg som ikke finnes lenger beholdes som eget alternativ i begge. Plugins-panelet viser «Språkpakke: {list}» (én ny nøkkel x5), historikk-datoenes Intl-konstruktør er vaktet mot koder Intl ikke godtar, og main.js setter `<html lang>` fra det som FAKTISK ble lastet.
- REFERANSEPAKKEN `sprak-svensk`: svensk for besøkende-siden (28 nøkler), deaktivert som standard. Bevisst site-only - den er dermed komplett for det den lover, og demonstrerer samtidig delvis dekning med bokmålsfallback, som er hele poenget med at `site` og `admin` er uavhengige felt.
- Verifisert: bygg grønt med ren bundel, 284 tester grønne (fire nye: manifest-validering, pakkeoppdagelse via stubbet fetch der en DEAKTIVERT pakke ikke skal telle med, filparitet der pakker slipper unna fullt sett men ikke ukjente nøkler, og matchLang-tabellen), skjemavalidering grønn med den nye pakken i validate-lista, og alle fire URL-ene mekanismen bygger svarer 200 på lokal server. requiresEngine satt til `>=0.5.0 <1.0.0` som søsken-pluginene: urd.json melder fortsatt 0.5.10, så `>=0.6.0` ville gjort referansepakken uvirksom i repoet den ligger i. SIDESPOR RETTET: de fire oversatte READMEene manglet Språk-seksjonen som bare den engelske fikk i 0.6.8.9.

### 0.6.8.9 - Dokumentasjonen på fem språk: engelsk default-README, brukerveiledning, oppsettsguide og bidragsguide for oversettere - 4. august 2026
- FEMTEN OVERSATTE DOKUMENTER i tre sett: README (fem språk), brukerveiledningen (fem) og oppsettsguiden (fem), pluss utvikling, veikart og visjon på engelsk (eierens valg: de er utvikler-/planleggingsdokumenter). Filnavnene bærer dokumenttype for å unngå fane-forvirring: `README-se.md`, `GUIDE-tr.md`, `SETUP-nn.md`, `DEVELOPMENT-en-GB.md`.
- ENGELSK ER NÅ DEFAULT-README (eierens valg): repo-rotens README.md er britisk engelsk med lenkelinje til de fire andre; den norske ligger som docs/languages/README/README-nb.md. Rot-READMEen fikk et «Languages»-avsnitt og en UTTALE-FORKLARING for ikke-nordiske lesere: *Urd* er omtrent «oord» ([ʉːɖ]), med den etymologiske kroken at engelsk *weird* kommer fra samme rot (gammelengelsk *wyrd*, skjebne). Samme forklaring på tyrkisk.
- LENKELINJENE bruker flagg der de finnes: `Sámegiella · 🇬🇧 English · 🇳🇴 Bokmål · 🇳🇴 Nynorsk · 🇹🇷 Türkçe`. Rene flagg var umulig: det finnes INGEN samisk flagg-emoji (Unicode har kun landkoder, og Sápmi er ikke et land i ISO-forstand), og bokmål/nynorsk ville delt norsk flagg - derfor navn på samisk og navn ved siden av flagget for de to norske. Linjene eies av ett skript som regner relative stier per fil på tvers av alle seks dokumentsett; en bug underveis (mønsteret krevde skilletegn ETTER språknavnet, som ikke holder når bokmål står sist) ga duplikat-linjer i fire filer og ble rettet ved å gjøre skriptet idempotent (fjern alle, sett inn én). Verifisert med en andre kjøring som endret null filer.
- BRUKERVEILEDNINGEN slår opp de FAKTISKE admin-tekstene per språk fra locale-filene (panel-navn, ui.*, blokktyper, preset-etiketter, plugin-strenger), så den engelske sier «press Publish» og den tyrkiske «Yayımla» i stedet for oversatt norsk. Der ordbøkene er ujevne (nynorsk har «Footer» uoversatt) følger veiledningen ordboka, med den riktige merknaden at slikt rettes i ordboka, ikke i veiledningen.
- CONTRIBUTING fikk «Bidra med språk»: hvor de tre locale-settene ligger, at nøkler aldri endres, at {var}-plassholdere må bli med, tankestrek-forbudet, `node --test tests/i18n.test.mjs` som «language file checker» (Publii-mønsteret), hva et helt nytt språk krever - og at gjennomgang av det samiske maskinutkastet er et like verdifullt bidrag som en ny oversettelse.
- TO SELVMOTSIGELSER SOM MILEPÆLEN SELV SKAPTE, funnet av oversettelsesarbeidet og rettet i begge språk: veikartet listet «flerspråkstøtte» som uprioritert etter v1.0 (nå presisert til flerspråklig INNHOLD; UI og dokumentasjon er flerspråklige fra 0.6.8), og UTVIKLING-regel 7 sa «Norsk i dokumenter og brukerflater» (nå: norsk er KANONISK, ikke enerådende, med ADR-0012-henvisning).
- Verifisert: 224 lenker i de 22 berørte filene, null brutte (maskinell sjekk av markdown-, src- og href-mål); ingen tankestreker; 278 tester grønne. Oversettelsesvalg dokumentert: Cloudflare/GitHub-menyvalg står på engelsk (det brukeren ser i dashbordet), feilsøkingstabellens symptomer beholder Urds faktiske norske feilmeldinger så de gjenkjennes på skjermen, kodeblokker er bit-identiske, og domene-eksempler er lokalisert mens ekte konfigurasjonsverdier står urørt. FUNN utenfor scope: docs/PLAN.md har fem lenker med feil `docs/`-prefiks (fila ligger selv i docs/).

### 0.6.8.8 - Plugin-locales: kalender/skjema/kart på fem språk, manifest-felt og iframe-løste etiketter - 4. august 2026
- LASTEMEKANISMEN i plugins.js: `loadPluginLocale(id, lang)` (nb-basen i bunn, valgt språk oppå, stille fallback per fil) kalles FØR plugin-entry importeres, så t/ta treffer fra første render. Besøkende får site-språket i site-registret; preview legger i tillegg admin-språkets fil i admin-registret. AGENTFUNN underveis (reelt): `initSiteLocale` bygger fersk basekopi ved språkbytte og ville strippet plugin-overlayene i preview - løst med et `localePlugins`-register og eksportert `applyPluginSiteLocales()` som re-appliserer plugin-ordbøkene i språkbytte-løpet i urd.js før rerender.
- ADDITIVE MANIFEST-FELT `locales: true` og `names` (visningsnavn per språk: Kalender/Calendar/Takvim …), med valgfri validateManifest-sjekk. TRE-STEDER-REGELEN fulgt: schema/plugin.schema.json, docs/SKJEMA.md (manifest-avsnittet, som samtidig fikk det lenge manglende `csp`-feltet dokumentert - gammelt housekeeping-punkt) og alle tre plugin.json i samme runde.
- IFRAME-LØSTE ETIKETTER (admin-vinduets register har aldri plugin-nøkler): urd-plugin-blocks-meldingen bærer nå FERDIG oppløste blokk-/variant-etiketter (labelKey-mønsteret fra 0.6.8.6), `fromPlugin` stemples med names på admin-språket, og Plugins-panelet leser names rett fra manifest-json i App. preview-edit fikk variant.labelKey-konsumenten.
- STRENGENE: kalender 54 nøkler (19 besøkende + 35 edit), skjema 48 (7 + 41, inkl. parameterisert '{label} må fylles ut' via ny valgfri messages-parameter i form.js så de rene testene forblir selvbærende), kart 25 - totalt 127 nøkler x 5 språk i 15 nye locale-filer, med gjenbruk av kjerne-nøkler i config-panelene. Skjemaets felt-defaults (Navn/E-post/Melding) og preset-seedene klassifisert som SEED (innsettings-tid); render-fallbackene og mailto følger site-språket.
- KALENDER-DATOENE via motorens Intl: MONTHS_NB/WEEKDAYS_NB/MONTH_NAMES_NB erstattet med `dates()`, nedtellingen via egne nøkler + `tp()` (begrunnet valg: nordsamisk mangler i Intl.RelativeTimeFormat, egne nøkler gir ekte samisk tekst; datolinje-rekkefølgen er nøkkelbasert så hvert språk får sin ordstilling). ics.js sin interne tidssone-Intl urørt. A11y-fiksen fra kartleggingen: ‹/›-månedsknappene har nå aria-labels.
- validateManifest-feilene står bevisst på norsk (utvikler-diagnostikk på linje med console.warn-tekstene). plugins/README.md fikk «Flerspråk (ADR-0012)»-avsnitt. Notat for 0.6.8.10: mekanismen er direkte gjenbrukbar for rene språkpakker; det som gjenstår er å åpne SUPPORTED_LANGS/deteksjonen for nye koder.
- Uavhengig verifisert i hovedøkta: 278 tester grønne (paritetstesten fant de tre nye settene automatisk), alle tre manifestene validerer mot nytt skjema, editor gjenbygd (plugins.js er bundlet) med bundle-renhet (null plugin-locale-verdier), applyPluginSiteLocales-koblingen og aria-fiksene bekreftet i kildene, ingen tankestreker.

### 0.6.0.7 - Topbar-opprydding: Urd-innstillinger nede i railen, zoom-steppere og tydelig hjelpelinje-knapp - 4. august 2026
- URD-INNSTILLINGER I EGEN POPOVER: admin-fargetemaet og admin-språkvelgeren er flyttet ut av topbaren og inn i en popover som åpnes fra et tegnet tannhjul nederst i venstre-railen. Popoveren (178px, temastyrte Dropdowns med etikettene Fargetema/Språk, tooltips beholdt) lukkes ved klikk utenfor, Escape OG klikk i forhåndsvisningen - iframe-klikk når aldri editorens document, så window-blur lukker (samme mønster og av samme grunn som fargevelgeren). Første tannhjul-utkast så ut som en sol og ble byttet til et ekte tannhjul med tenner etter visuell sjekk.
- ZOOM-STEPPERE: 100 %-knappen er fjernet; zoomen er nå [tilpass] [minus] [prosent-avlesning] [pluss], der stepperne går i 10 %-poengs trinn fra gjeldende visning (klemt 10-400 %, ny manuell modus i tillegg til tilpass). Nøklene tip.zoomOut/tip.zoomIn inn og tip.zoomFull ut i alle fem admin-localene (paritet grønn).
- HJELPELINJE-KNAPPEN: ikonet byttet fra det pluss-lignende trådkorset til et tegnet rutenett, og på-tilstanden er nå synlig - rotårsaken var at topbarens aktiv-stiler var container-avgrenset til zoom-/visningsbryterne, så knappens class:active aldri fikk stil; den har nå egen aksentfarget aktiv-stil.
- Språkvelgerne sorterer nå språkene alfabetisk etter eget navn (endonym); «Automatisk» står fortsatt øverst i admin-velgeren.
- Verifisert: alle 272 tester grønne (inkl. paritet etter nøkkelendringene), editor gjenbygd med committet bundel, nøkkeldekning komplett.

### 0.6.8.7 - Seed-innholdet på fem språk: innsettings-oversettelse av presets, blokk-defaults og footer-maler - 4. august 2026
- SEED-REGELEN FRA ADR-0012 IMPLEMENTERT FULLT UT: tekst som skrives inn i brukerdata oversettes ÉN gang i innsettingsøyeblikket med admin-språket; besøkende-rendring oversetter aldri brukerdata, og ta() kalles kun inne i fabrikk-/defaults-kropper (aldri modulnivå i besøkende-lukningen - presets.js og blocks/* bundles og lastes hos besøkende).
- sections/presets.js (115 assert-verifiserte regler): alle 23 preset-defs fikk additive labelKey/groupKey/hintKey (+ itemLabelKey på de 10 utvidbare) med de gamle feltene som plugin-fallback; ~70 seed-kallsteder i fabrikkene går via seed.*-nøkler, med parameteriserte kortmaler ({title}/{price}/{role}/{day}/{month}). Preset-galleriet i preview-edit grupperer fortsatt på group-ID-en (uendret gruppering, verifisert) og oversetter kun visningen; plugin-gruppen samles som før.
- App.svelte (49 regler): BLOCK_DEFAULTS (inkl. FAQ-seedene og addFaqItem-pushen), HELE footer-malbyggeren (~45 strenger: kolonnetitler, taglines, «Personvern», nyhetsbrev-/Stor CTA-feltene, adresse/åpningstider, «Laget med Urd»), «Lenke»/«Kolonne»-innsettingsdefaultene, «Nytt innslag», «Bli medlem». blocks/button.js («Les mer»), blocks/text.js og blocks/faq.js i defaults().
- KLASSIFISERINGSVALG dokumentert: video-title-defaulten «Video» ikke oversatt (teknisk rammetittel, tilnærmet lik på alle fem språk), «Min forening»-sammenligningen er oppsettsveiviser-logikk, «Uten tittel» forblir data, priser nøklet (valutaformat er lokalt), telefonnumre står.
- 184 NYE NØKLER x 5 språk (125 seed.*, 46 preset.*, 10 item.*, 3 presetGroup.*) - 979 nøkler per admin-locale.
- KANTTILFELLET VERIFISERT: npm run validate kjører preset-fabrikkene i node UTEN ordbok (ta() gir nøkkelnavn som innholdsverdier) og validerer likevel grønt - skjemaet stiller formkrav, ikke innholdskrav. Uavhengig verifisert i hovedøkta: 272 tester grønne, bygg OK, alle fem locale-filene eksakt 979 nøkler, ingen tankestreker, bundle-renhet (null norske seed-verdier bundlet), plugin-mappene urørt, og fabrikk-vs-modulnivå-stikkprøver rene.

### 0.6.8.6 - Canvas-chromen på fem språk: milepælens «Ferdig når» er oppfylt - 4. august 2026
- MILEPÆLENS KJERNEMÅL NÅDD: admin-UI-et OG motorens besøkende-tekster kan byttes mellom alle fem språkene, og paritetstesten i CI er grønn (backlogens «Ferdig når» for 0.6.8). Gjenstår: seed (0.6.8.7), plugins (0.6.8.8 + 0.6.8.10), docs (0.6.8.9).
- ARKITEKTUR (hovedøkta): delt språkdeteksjon og ordbok-lasting flyttet inn i i18n.js (`detectAdminLang`/`initAdminLocale` - kjøretids-import fra absolutt sti virker i både admin-bundelen og iframen); main.js forenklet til å bruke dem; preview-booten i urd.js laster admin-ordboka FØR preview-edit.js importeres, så canvas-chromen følger admin-språket uten ny postMessage.
- TIMING-REGELEN håndhevet: i besøkende-lukningen er ta() kun lov inne i funksjonskropper (modulnivå ville frosset nøkkelnavn før ordboka finnes); preview-edit/image-editor/color-picker evalueres etter ordboka og kan ha modulnivå-tabeller. AGENTENS EGET FUNN: første render fester hjelpechips FØR preview-grenen har lastet ordboka - et frys-vindu instruksene ikke dekket. Løst additivt med `adminLocaleReady`-promiset i i18n.js (løses av initAdminLocale, aldri hos besøkende); blokkene venter på det sammen med hint-importen, korrekt preview-gatet (verifisert: besøkende awaiter det aldri).
- CANVAS-STRENGENE (delegert agent, samme metode som 0.6.8.5): preview-edit.js ~120 strenger (blokkpaletten, «Former ▾», parameterisert «Fra pluginen {plugin}», hele tekst-verktøylinjens ~45 titler med språkriktige F/K/U/S-bokstaver, seksjons-/blokk-verktøylinjene, multimarkeringslinjen med «{n} valgt», preset-galleri-chromen), image-editor (18+2), engine-fargevelgeren (11), hint.js-chromen («Slik virker {title}»), og hjelpekort-innholdet i faq/galleri/samling (15). 140 nye nøkler x5 - 795 nøkler per admin-locale.
- DELTE ETIKETTKART nøkkelbaserte med konsument-innpakking (fonts.js font.*, theme.js sectionTheme.*, icons.js iconCat.*, glyphs.js glyphCat.*; ICON_LIBRARY-merkenavnene urørt - de er besøkende-aria-labels), og additivt `labelKey` med label-fallback på def-objektene (blokker, bakgrunnslag, animasjoner) - samme kontrakt plugin-batchen skal bruke. FONT_STACKS-konsumentene verifisert til kun å matche på CSS-stacken, aldri displaynavnet.
- Uavhengig verifisert i hovedøkta: 272 tester grønne, bygg + validering OK, ingen tankestreker, seed og plugin-mapper urørt, bundle-renhet holdt (proben ga null ordbok-verdier; eneste treff var endonymet «Davvisámegiella» i språkvelgeren, som skal bundles). validateManifest-feilene står bevisst til 0.6.8.8.

### 0.6.8.5 - Admin-strenger B: alle tooltips, placeholders og markup-tekster på fem språk (447 nye nøkler) - 4. august 2026
- DEN STORE MEKANISKE SKIVEN, utført av en delegert agent etter den etablerte metoden og deretter uavhengig verifisert i hovedøkta: 407 assert-verifiserte erstatninger i App.svelte (152 statiske title-tooltips + 6 betingede/interpolerte, 29 placeholder-forekomster, ~35 panel-hint-avsnitt, 16 gruppetitler, ~140 tekstnoder/knapper, 7 script-litteraler) pluss lib-komponentene: ColorPicker (12, inkl. parameterisert «koblet til temafargen»-tittel), IconEditor (12, gjenbruker delte lbl.*/common.*-nøkler), GlyphPicker (5). Dropdown hadde ingen litteraler.
- 447 NYE NØKLER x 5 SPRÅK (~2200 oversettelser): nb-basen ordrett dagens tekst, fulle oversettelser til nynorsk, britisk engelsk, nordsamisk (maskinutkast, flagget) og tyrkisk. Hver admin-locale har nå 655 nøkler. Fet/kursiv-knappene ble språkriktige (F/K norsk, B/I engelsk, K/I tyrkisk: Kalın/İtalik).
- KLASSIFISERINGSGRENSEN CHROME/DATA håndhevet: seed-innhold urørt til 0.6.8.7 (BLOCK_DEFAULTS, FAQ-seeds, footer-malenes innholdsstrenger), og commit-meldingen i publish() («Oppdater … via Urd-admin») pluss `'Uten tittel'`-fallbacken klassifisert som DATA (skrives i delt git-historikk/samlingsdata på tvers av redaktørers språk) - bevisst ikke oversatt. Merkevaren «Urd», px-enheten, R/G/B og endonymene forblir universelle.
- Uavhengig verifisert i hovedøkta (agentens rapport ble ikke tatt på ordet): alle 272 tester grønne, bygget reproduserer bundelen, skjemavalidering OK, null tankestreker i alle ti endrede filer, nøkkeldekning komplett (hver ta()-nøkkel finnes i nb-basen), seed-markørene intakte, og eneste gjenværende title-litteral er `title="Urd"`. Stikkprøver i diffen (ColorPicker-importsti, interpolerte titler, ui.publish på tyrkisk) holdt mål.

### 0.6.8.4 - Admin-strenger A: statusmeldinger, dialoger, etikettkart og alle nedtrekksvalg på fem språk - 3. august 2026
- 92 KALLSTEDER TA()-IFISERT i App.svelte med eksakt-match-verifiserte masseerstatninger (hver erstatning asserter antall treff før skriving): alle 38 setStatus-meldingene (inkl. parameteriserte som «Bildet er stort ({kb} kB)» og «{n} av bildene kunne ikke leses»), alle tre askConfirm-dialogene (konflikt, ukjent grunnlag, angre publisering) pluss standardknappene OK/Avbryt, pageSlugError- og historikk-feilene, alle script-side etikettkartene (ADMIN_THEMES, BLOCK_LABELS, SHAPE_KINDS, COLOR_TOKENS, PALETTE_KEYS, HOVER_COLOR_LABELS, FOOTER_TEMPLATES, SAMLING_KINDS, GRAD_ANIMATIONS, THEME_PRESETS-navn og -noter), og alle 32 inline Dropdown-option-tuplene i markupen.
- «{label}-blokk»-KONKATINERINGEN er nå parameterisert nøkkel (`blocks.suffix`): engelsk får «Text block» i riktig ordstilling i stedet for et påklistret suffiks - i18n-fella fra kartleggingen er lukket.
- ~174 NYE NØKLER i alle fem admin-localene: nb-basen speiler de gamle tekstene ordrett, og nynorsk, britisk engelsk, nordsamisk og tyrkisk er fullt oversatt (samisk fortsatt flagget for morsmålsgjennomgang). Gjenbrukbare fellesnøkler (`common.none/left/center/right/standard/inherit/choose`) holder settet nede; tema- og admin-temanavnene («Brønn»/«Well»/«Gáivo»/«Kuyu») er oversatt som beskrivende navn.
- Nøkkeldekningen er verifisert mekanisk: hver ta()-nøkkel brukt i App.svelte finnes i nb-basen (panel.*-settet slås opp via template literal og er dekket via PANEL_GROUPS).
- Verifisert: alle 272 tester grønne (paritetstesten dekker de fullvoksne admin-settene automatisk: nøkkelsett, tomme verdier, tankestrek-forbud, {var}-token-paritet), editor gjenbygd med committet bundel (ordbøkene selv bundles fortsatt aldri).

### 0.6.8.3 - Språkvelgerne: admin-locales med auto-deteksjon fra enhetsspråket, og site.lang-velger med WYSIWYG-preview - 3. august 2026
- ADMIN-LOCALES (`engine/locales/admin/{nb,nn,en-GB,se,tr}.js`) med første skive (16 nøkler: panelnavn og velger-tekster). KJØRETIDS-LASTET av editorens main.js FØR mount - aldri bundlet (verifisert: null treff på oversatte strenger i bundelen), så oversettelses-PR-er krever aldri gjenbygg. nb-basen lastes alltid i bunn (manglende nøkler faller til bokmål); feiler lastingen helt (vite dev uten template-server) vises nøklene i stedet for krasj. PANEL_LABELS bygges fra `ta('panel.*')`.
- AUTO-DETEKSJON (brukerens beslutning: standard huskes per nettleser og velges automatisk fra enhetens språk): eksplisitt valg i localStorage `urd-admin-lang` vinner; ellers matches `navigator.languages` STRENGT mot de støttede via ny `matchLang` (null ved ikke-treff, så NESTE tag prøves - `normalizeLang` gjenbruker den med nb-fallback); ingen treff gir engelsk. Samme rekkefølge som i18next-browser-languagedetector, håndrullet uten avhengighet. main.js setter også `document.documentElement.lang` for admin-dokumentet.
- SPRÅKVELGEREN i topbaren (temastyrt Dropdown ved admin-tema-valget): «Automatisk (følg enheten)» øverst, deretter de fem språkene med sine EGNE navn (endonymer - Norsk bokmål, Davvisámegiella, Türkçe - aldri oversatt, aldri flagg). Bytte skriver/sletter nøkkelen og laster admin på nytt (Publii-modellen; iframen følger med).
- SITE.LANG-VELGER i Nettsted-panelet (under Beskrivelse): historisk `no` vises som bokmål, en håndredigert verdi utenfor lista bevares som eget alternativ øverst så panelåpning aldri ødelegger noe. Endringen går som vanlig site-utkast, og previewen er WYSIWYG: `urd-site`-grenen i motoren re-initierer besøkende-localen og re-rendrer når utkastets språk avviker fra det lastede (booten leste den publiserte site.json).
- Historikk-panelets datoer formateres med admin-språkets Intl-tag (var hardkodet nb-NO). SKJEMA.md dokumenterer at `site.lang` nå faktisk er i bruk: anbefalte koder, normaliseringen, og at eierens innhold aldri oversettes (chrome følger innholdsspråket).
- Verifisert: 272 kontraktstester grønne (paritetstesten fant admin-settet automatisk og dekker det uten endring), skjemavalidering OK, editor gjenbygd med committet bundel.

### 0.6.8.2 - activePanel-refaktoren: stabile engelske panel-id-er, visning via PANEL_LABELS - 3. august 2026
- Panelnavnene var identifikatorer: 'Sider'/'Blokker'/'Egenskaper'/... ble brukt som knappetekst, overskrift OG sammenligningsverdi i en 11-greins {#if}-kjede. Alle 16 literalene er byttet til stabile engelske id-er (`pages`, `blocks`, `properties`, `grid`, `site`, `theme`, `nav`, `footer`, `collections`, `plugins`, `history`), og visningen eies av et PANEL_LABELS-oppslag - forutsetningen for at panelnavn kan oversettes (0.6.8.3). Null atferdsendring; grep bekrefter at ingen norske panelnavn står igjen i logikk.
- Verifisert: bygg + alle 270 tester grønne, bundelen gjenbygd.

### 0.6.8.1 - Flerspråk-rammeverkets kjerne: i18n.js, fem komplette besøkende-locales, native Intl-datoer, paritetstest i CI og ADR-0012 - 3. august 2026
- MILEPÆL 0.6.8 PÅBEGYNT (foran 0.6.7, brukervalg) med revidert språksett: bokmål, nynorsk, britisk engelsk, nordsamisk og tyrkisk komplette nå (nordsamisk er maskinskrevet førsteutkast, merket «trenger morsmålsgjennomgang» i filen og ADR-en); de øvrige språkene fra den gamle listen forblir påfyllbare senere. Arkitekturen ble planlagt mot research på Shopify/Squarespace/Publii/Ghost/WordPress og moderne systemer (Paraglide, i18next-detektoren, native Intl) - hele batch-planen (0.6.8.1-0.6.8.9) står i backloggen.
- NY KJERNE `engine/i18n.js` (ADR-0012): `t(key, params)` med `{var}`-interpolasjon for besøkende-tekster som følger site.lang; nb er alltid statisk base (forhåndslastet), andre språk dynamisk-importeres i boot() oppå en FERSK basekopi (en tidlig utgave muterte basen permanent, så språkbytte ville arvet forrige språk - fanget av egen test); manglende nøkkel gir bokmålsteksten, ukjent nøkkel gir nøkkelen selv. `normalizeLang` ('no'/nb-varianter til nb, sør-/lulesamisk til nordsamisk, ukjent til nb), `tp()` med Intl.PluralRules (nordsamisk har totallsform - håndskrevne en/mange-par ville vært grammatisk feil), datonavn fra Intl.DateTimeFormat med nb-tabell-fallback, `relativeDays` via Intl.RelativeTimeFormat, og `addSiteDict`/`addAdminDict` klare for plugin- og admin-batchene. ICU-støtten for alle fem språkene ble verifisert (inkl. «njukčamánnu»/«vuossárga»).
- FEM KOMPLETTE SITE-LOCALES (`engine/locales/site/{nb,nn,en-GB,se,tr}.js`): 28 nøkler med engelske punktum-navn (datakontrakt-regelen). Rene ES-moduler som aldri bundles - en oversettelses-PR er en ren filendring uten bygg.
- MOTOREN T()-IFISERT: nav (logo-tittel, temabryter-labels, burger, undermeny-aria), til-toppen-pilen, lysboksen, footer/nyhetsbrev (feltetiketter, valideringsmeldinger, mailto-emne/-kropp i footer-cta), galleri (pil-labels, prikkenes «Bilde {n}»), video-tomtilstandene, manglende-plugin-plassholderen og samlings-datobadgen (Intl-måneder). `boot()` laster site-språket FØR første render og setter `document.documentElement.lang` fra site.lang - feltet var påkrevd i skjemaet, men ble aldri lest (latent bug fra kartleggingen). Skallene beholder lang="no" kun som pre-JS-standard; to preload-linjer (i18n.js + nb-basen) lagt i alle fire HTML-skall.
- SPRÅKFRI FOOTER-MODELL: CTA-defaultene («Meld på»/«Les mer»/«Takk, du er påmeldt!») er flyttet fra footer-model.js til render-tidspunktet i footer.js, så modellaget aldri bærer norske tekster; footer-testene speiler den språkfrie kontrakten. To a11y-fikser på kjøpet: sosiallenkene annonserer ikonets visningsnavn i stedet for rå ikon-id, og galleri-prikkene fikk parameterisert etikett.
- PARITETSTEST `tests/i18n.test.mjs`: finner alle locale-sett automatisk (site + fremtidige admin- og plugin-mapper) og krever identisk nøkkelsett mot nb-basen begge veier, ikke-tomme verdier, ingen tankestrek og samme `{var}`-tokens per nøkkel; tester også normalizeLang-kontrakten, fallback-kjeden og datotabellene. Fungerer som Urds «language file checker» for bidragsytere (Publii-mønsteret): `node --test tests/i18n.test.mjs`.
- ADR-0012 «Flerspråk via rene ES-modul-locales, to registre og native Intl»: fester nøkkelformatet, base+overlay-modellen, aldri-bundlede admin-locales, auto-deteksjon kun for admin (besøkende-chromen følger alltid site.lang, konsistent med innholdet), seed-regelen (innsettings-tid, aldri render av brukerdata) og forkastede alternativer (Paraglide-kompilering, i18next/gettext, kildestreng-som-nøkkel).
- Verifisert: alle 270 kontraktstester grønne (8 nye i18n-tester), skjemavalidering OK, editor-bygget bekrefter at bundelen er uberørt (ingen av de endrede modulene bundles - som designet). Visuell sjekk av de fem språkene står i testrunde-batchen.

### 0.6.6.5.12 - Batch E fullført: lenker til seksjon/anker i nav og footer, og nav scroll-adferd (krymp/skjul) - 3. august 2026
- LENKER TIL SEKSJON/ANKER (footer-resten fra 0.6.6.5): `resolveItem` godtar nå site-interne mål via den delte `isSafeHref`-vokteren fra 0.6.35 - `#seksjons-id` på samme side, `/sti#seksjons-id` fra en annen side. Interne mål markeres aldri som eksterne (ingen noopener/ny fane), og alt utrygt (javascript:, data:, protokoll-relative //) avvises fortsatt til `#`. Alle lenkeflater går gjennom samme funksjon og fikk dette samtidig: menypunkter, undermenypunkter, footer-kolonner, bunnlinje- og doormat-lenker, og footer-CTA-en.
- SEKSJONER ER ANKERMÅL: seksjonselementene rendres nå med DOM-id (= seksjonens id) i render.js, så ankrene treffer nativt og ruller mykt (scroll-behavior fra 0.6.0.5). Oppdagbarhet i admin: seksjonens Egenskaper viser en «Anker»-rad med `#id-en` og kopier-knapp (nytt tegnet kopier-ikon i ICONS), og alle fem lenkemål-UI-ene heter nå «Lenke (URL/anker)» med plassholder og tooltip som forklarer ankermønsteret.
- NAV SCROLL-ADFERD (siste E-punkt): nytt additivt felt `nav.scroll` - `shrink` krymper menyen etter et stykke scrolling (paddingen halveres via faktor-varen `--urd-nav-shrink`, som komponerer med både størrelsesklassene og pillens egen 0.67-skalering), `hide` glir menyen ut ved scrolling ned og inn ved scrolling opp (translate på kompositoren, i tråd med transform/opacity-regelen fra 0.6.0.6). Ren tilstandslogikk i ny `navScrollState` (nav-model.js) med toppsone (alltid synlig øverst) og dirr-vern mot småbevegelser; DOM-delen er en rAF-throttlet passiv lytter i renderNav på samme AbortController som øvrige lyttere. Gating som sticky blokker: kun klistret topplinje (ikke sidestilt, ikke sticky av), inaktiv under redigering (aktiv i Ren visning og hos besøkende), alltid av mens mobilmenyen er åpen. Redusert bevegelse gir klipp i stedet for overgang. Panel-valget «Ved scrolling» i Nav-panelet vises kun når det er relevant (klistret, ikke sidestilt).
- SKJEMA TRE STEDER: `nav.scroll`-enum + utvidede href-beskrivelser i site.schema.json, nytt href-avsnitt («href i nav-/footer-lenker») og nav.scroll-dokumentasjon i SKJEMA.md, og demoinnholdet demonstrerer begge: nav har `"scroll": "shrink"`, og footer-kolonnen Utforsk fikk lenken «Kakevideoen» til `/kaker#sec-4c908354` (videoseksjonen på Kaker-siden).
- Verifisert: 262 kontraktstester grønne (6 nye: site-interne href-mål i resolveItem, og navScrollState-kontraktene for toppsone/retning/dirr-vern), skjemavalidering OK inkludert endret demoinnhold, editor gjenbygd med committet bundel. Visuell sjekk av krymp/skjul og anker-scrolling står i testrunde-batchen.

### 0.6.0.6 - Gradient pan/orbit på kompositoren (transform-løpere), og person-attribusjoner ut av docs og kodekommentarer - 3. august 2026
- TRANSFORM I STEDET FOR BACKGROUND-POSITION (siste motor-rad i ELEMENTKARTs «gammel måte»-tabell): pan- og orbit-animasjonene på gradientlaget animerte `background-position`, som repainter laget hver frame. gradientRender returnerer nå en løper-oppskrift for begge: gradienten males på en 200 %-løper som forskyves med `transform` på kompositor-tråden, samme modell som pan-loop alt brukte (verten klipper via `urd-bg-loop-host`). Matematisk ekvivalent: translate-% regnes av løperen (200 %), så pan-reisen `translate(-50%, -50%)` = gamle `background-position 0→100%`, og orbit-utslaget ±2 % av løperen = ±4 % av flaten; orbit-løperen forankres med inline left/top (-x %, -y %) så gradientsentrum står nøyaktig der det står uanimert. Adferdsforbedring på kjøpet: orbit under `prefers-reduced-motion` viser nå det korrekte forankrede sentrumet (før falt background-position til 0,0 og viste feil hjørne av 200 %-lerretet). Ingen props- eller skjemaendring (animasjonsverdiene `pan`/`orbit` uendret, versjon 3 består), så ingen migrering (ADR-0005 urørt). Konsumenter sjekket: admin-panelet bruker kun lagets defaults/label (render skjer i preview-iframen med base.css); ingen etterlatte referanser til de gamle klassene eller `--urd-bg-px/py`-variablene.
- PERSON-ATTRIBUSJONER FJERNET (avklart 3. august 2026, samme opprydding som backloggen fikk i 0.6.6.5.6-perioden): «eiers regel/ønske/valg/testfunn/krav»-formuleringer omskrevet til upersonlige, daterte former («valgt 23. juli 2026», «testfunn 23. juli 2026») i TESTRUNDER (tittel + strykeregel, nå «Punkter strykes kun av den som tester»), fire ADR-er (0007-0010) og kommentarer i nav-model.js, nav.js, theme.js, preview-edit.js, gradient.js, App.svelte og to testfiler. Avklart og bevisst beholdt: fagtermen «eieren»/«sideeieren» (produktrollen som eier nettsiden, i motsetning til besøkende - tillitsmodellen i text.js, SKJEMA.md, ADR-0006), dokument-eierskapsverbet («BACKLOG.md eier oppgavene», «Urd-eid»), og historiske CHANGELOG-innslag (omskriving av leveransefortellinger er en egen beslutning).
- Verifisert: 256 kontraktstester (orbit-kontraktstesten omskrevet til løper-oppskriften, ny pan-test) og skjemavalidering grønne; editoren gjenbygd (gradient.js er editor-bundlet) med committet bundel - kommentar-endringene i App.svelte/motorfilene endret ikke bundelen (kommentarer strippes i bygget). Visuell pan/orbit-sjekk står i testrunde-batchen (maskinen mangler headless Chromium).

### 0.6.0.5 - Moderniser til native/CSS runde 2: cross-document View Transitions, scrollås via :has(dialog:modal), og myk ankerscroll - 3. august 2026
- SIDE-OVERGANGER (ELEMENTKART 5b.8): `@view-transition { navigation: auto }` i base.css gir myk krysstoning mellom sidene på det publiserte nettstedet, i ren CSS uten JS. At-regelen er sin egen gate (motorer uten støtte ignorerer den og navigerer som før, jf. ADR-0011s sluttilstand-fallback). Nav- og footer-hostene får egne `view-transition-name` (urd-nav/urd-footer) så side-chromen står i ro mens innholdet toner; navnene er inerte utenom en aktiv overgang. Ved `prefers-reduced-motion` beholdes overgangen, men all animasjon i den slås av (rent klipp). Editor-previewen berøres aldri: sidebytter der skjer via postMessage, ikke navigasjon, så regelen er død i preview.
- SCROLLÅS VIA `body:has(dialog:modal)` (5b.9-mønsteret fra ELEMENTKART 2.2): lysboksens JS-bokføring av `body.style.overflow` (prevOverflow-variabelen med lagring/gjenoppretting i tre kodeveier) er slettet fra lightbox.js; CSS-en i base.css låser nå bakgrunnsscrollen for enhver modal dialog automatisk, også framtidige showModal-flater. `:has()` og `:modal` er universell baseline («bruk direkte» i støtte-tieren); uten støtte mangler kun låsen, dialogen virker som før.
- MYK ANKERSCROLL: `scroll-behavior: smooth` på html (bak `prefers-reduced-motion: no-preference`), så blokk-lenker til `#anker` (åpnet av isSafeHref i 0.6.35) og «Til toppen»-pilen ruller nativt. Forutsetningen ble ryddet først: dra-kompensasjonen i preview-edit.js brukte to-args `scrollTo` og ville arvet mykheten (drivende scroll under seksjonsdrag ovenfra); den sier nå `behavior: 'instant'` eksplisitt. Alle andre scroll-kall i motoren hadde allerede eksplisitt behavior.
- REST I MODERNISERINGS-PUNKTET (notert i backlog): transform/opacity-revisjonen fant to reelle syndere (`urd-bg-pan` og `urd-bg-orbit` animerer `background-position`, repaint-mønsteret fra ELEMENTKART del 6) - konvertering krever runner-elementer à la `urd-bg-loop`, rører gradient.js (editor-bundlet) og tas som egen runde med visuell sjekk. Minimal-wrapper/betinget lasting (5b.7) ses med 0.6.9; container queries/subgrid (5b.9) trenger konkrete brukssteder.
- Motorfiler (base.css, lightbox.js, preview-edit.js) - ingen skjema- eller innholdsendring, ingen migrering (ren CSS/adferd, ingen props-form rørt). Verifisert: 255 kontraktstester og skjemavalidering grønne; editor-bygget bekreftet at bundelen er uberørt (ingen av de rørte filene er i editor-importene).

### 0.6.0.4 - Samtaleregler, løpende docs ved «commit»-beskjed, stående 0.6.0 Løpende-punkt, og CHANGELOG-opprydding etter nummerfeil - 3. august 2026
- NY NUMMERERINGSKONVENSJON for arbeid utenfor milepælene: den flate 0.6.x-serien (0.6.34, 0.6.35) delte tallrom med milepælene 0.6.1-0.6.9, så «0.6.34» leses som noe i nærheten av milepæl 0.6.3, FØR 0.6.6-arbeidet, selv om det kom etter - nummeret lyver om rekkefølgen. Nytt stående backlog-punkt «0.6.0 Løpende» (arbeidsflyt, kodegjennomganger, vedlikehold) eier nå slike innslag (0.6.0.x, neste fase 0.7.0.x); flate fasenumre deles ikke lenger ut, de historiske består. Dokumentert i innledningen over og i AGENTS.md.
- CHANGELOG-OPPRYDDING etter nummerfeil: tre pushede commits gjenbrukte numre - 96c5f49 gjenbrukte «0.6.35», og 2865625/70d6ba2 brukte begge «0.6.35p», som attpåtil er push-notasjon og ikke et commit-nummer. Rotårsak: agenten behandlet hver ny regelrunde som «justering av den ventende pushen» i stedet for som en ny nummerert enhet, selv om rundene ble committet fortløpende. Arbeidet deres var flettet inn i 0.6.35-/0.6.35p-innslagene; det er nå skilt ut som 0.6.0.1-0.6.0.3 under, med commit-hash siden git-titlene bærer feil nummer og ikke kan skrives om (alt er pushet).
- SAMTALEREGLER i AGENTS.md (Arbeidsflyt): en brukermelding som slutter med et spørsmål er ren diskusjon (svar i tekst, ingen endringer før et rent direktiv), og ny/endret regeltekst forhåndsvises i chatten for godkjenning før den skrives inn.
- BESKJED-FLYTEN presisert i AGENTS.md: «commit» avslutter en arbeidsrunde og skriver docs LØPENDE (commit-innslag, backlog, TESTRUNDER ved leveranse, pluss nummerert meldingsforslag), «push» utløser klargjøringen (full verifisering, review, p-innslag). Spennregelen fikk kortform-begrensning (kun ved delt prefiks) og entydig grense (alle commit-innslag over forrige p-innslag).

### 0.6.0.3 - Spennarv for push-nummer og innhold/prosess-grensen mellom commit- og p-innslag - 3. august 2026 (commit 70d6ba2, feilnummerert «0.6.35p» i git)
- Pushen arver commit-spennet siden forrige push med p-suffiks i stedet for bare siste commit-nummer, så backlog-nummereringen aldri berøres av push-navnet. Grense mot drift: commit-innslag beskriver hva som ble bygget, hendelsesforløpet fra gjennomgangen står kun i p-innslaget (review-gjenfortellingene i 0.6.35-innslaget ble kortet til «se 0.6.35p»).

### 0.6.0.2 - Commit/push-modellen: p-innslag som prosessrekord, og SKJEMA.md-unntak i docs-only-snarveien - 3. august 2026 (commit 2865625, feilnummerert «0.6.35p» i git)
- Commit = kodebygging, push = testleveranse med eget p-innslag i CHANGELOG som eier prosessfortellingen: hva som ble verifisert, og ved rettelser hva som var galt, hvordan det ble oppdaget og hvordan det ble rettet. Docs-only-snarveien fikk unntak: SKJEMA.md er aldri docs-only (tre-steder-regelen gjør en ren SKJEMA.md-diff til en halvgjort skjemaendring). Første versjon av «commit»/«push»-beskjed-skillet i Arbeidsflyt; 0.6.35p-innslaget under ble skrevet som første prosessrekord.

### 0.6.0.1 - Diff-styrt verifiseringsomfang i AGENTS.md - 3. august 2026 (commit 96c5f49, feilnummerert «0.6.35» i git)
- Verifiseringens omfang følger diffen: mellomcommits trenger kun testene som dekker det som røres, docs-only-pusher trenger ikke editor-gjenbygg og kan bruke mekaniske sjekker i stedet for full uavhengig review (presedens 0.6.34), kode-/skjemaendringer får full runde. Bakgrunn: hver push kjørte hele ritualet uansett diffens innhold, også for rene markdown-endringer, og selve testsuiten (sekunder) var aldri flaskehalsen - det var gjenbygg og review.

### 0.6.35p - Push-gjennomgang av 0.6.35 - 3. august 2026
- p-innslaget dokumenterer hva som ble gjennomgått og rettet før pushen av Høy-fiksene (modellen som innførte p-innslag kom i samme periode, se 0.6.0.2).
- VERIFISERT (full runde, kodeendringer): editor-bygg mot låsefilens Svelte, 255 kontraktstester grønne, skjemavalidering OK, deterministisk bygg bekreftet (identisk sjekksum ved gjenbygg), og uavhengig review av hele diffen ved en frisk agent uten øktens kontekst. Browser-røyken kunne ikke kjøres lokalt (maskinen mangler headless Chromium); punktene ligger i TESTRUNDER-batch 0.6.35.
- FANGET FØR BYGG: avhengighets-drift i node_modules (Svelte 5.56.5 der låsefilen pinner 5.56.8, AGENTS.md-fallgruven fra 22. juli). Oppdaget ved rutinesjekken av at `node_modules/svelte` matcher låsefilen; rettet med `rm -rf node_modules && npm ci` + gjenbygg, så den committede bundelen er bygget mot riktig runtime.
- REVIEW-FUNN 1 (reelt, ville knust publiserte sider): første versjon av blokk-lenke-vernet brukte `isSafeUrl` (kun https?/mailto/tel), men samlings-skjemaet dokumenterer entry.href som «intern sti eller ekstern URL» og malens eget innhold bruker `#` på en knapp - interne lenker og ankere ville stille mistet lenken ved neste motoroppdatering, i strid med aldri-knus-invarianten. Oppdaget av revieweren ved å lese diffen MOT skjemakontraktene (schema/collection.schema.json + docs/SKJEMA.md), ikke bare mot koden. Rettet med ny delt `isSafeHref` (eksterne skjemaer pluss ankret regex for `/`-stier og `#`-anker) i alle fire blokkene.
- ETTERKONTROLL-FUNN (reviewerens verifisering av rettelsen): den nye regexen utelukket `//evil.no` men slapp `/\evil.no` gjennom, og nettlesere normaliserer backslash til skråstrek i http(s)-URL-er, så den ble protokoll-relativ likevel. Tettet ved å avvise backslash i sti-grenen, med nye testcaser. Lærdom: la revieweren etterkontrollere RETTELSEN, ikke bare funnet.
- REVIEW-FUNN 2-4 (mindre, alle rettet): (2) init-kappløp der et angre-snapshot tatt mens `initSamlinger` fetchet asynkront hadde indeks men manglet samling-stores, så en senere angring til det snapshotet ville slettet reelle utkast - tettet med `samlingerReady`-flagg som gater samling-delen av snapshotet; (3) CHANGELOG-påstand uten dekning («dupliser» fantes ikke; riktig er tittel-oppdatering av en annen sides utkast); (4) fire nye kommentarer brutt midt i setninger, slått sammen til hele setninger per linje.

### 0.6.35 - Kodegjennomgangens Høy-punkter: endelse-allowlist i publiseringsvokteren, delt isSafeHref på blokk-lenker, kvotevarsel i utkastlagringen, og angre som dekker samlinger/plugins - 3. august 2026
- BAKGRUNN: de fire Høy-funnene (sikkerhet + data-tap) fra kodegjennomgangen 27. juli, som backloggen satte foran 0.6.7. Hvert funn ble re-verifisert mot dagens kode før fiks; alle fire stemte. Flat nummerering (0.6.35) siden runden ikke hører under noe 0.6.6.x-punkt.
- ENDELSE-ALLOWLIST I PUBLISERINGSVOKTEREN (`functions/_lib/guard.js`): `media/` slapp gjennom ALLE endelser, så en kapret publisher-økt kunne skrive `media/x.js` (kjører under `script-src 'self'`) eller `media/x.html` - i strid med guardens egen invariant «aldri kode». Allow-prefiksene har nå endelse-allowlist per prefiks: `media/` = webp/svg/png/jpg/jpeg/gif/avif/ico (editoren skriver kun webp+svg via `mediaExtension`; rasterformatene er romslighet for manuelt git-lagt media), `content/` = json/css (publiseringen skriver kun det; UTEN denne var media-fiksen omgåelig via `content/evil.js`). Endelsen sammenlignes lowercase (`media/X.JS` avvises), fil uten endelse avvises. Slug-rutingskopiene (`<slug>/index.html`) består uendret (eksakt filnavn, reserverte slugs avvist fra før); publiseringens slette-stier (`content/**/*.json`, slug-kopier) er kontrollert upåvirket. `tests/guard.test.mjs` er skrevet om der den fastslo MOTSATT invariant («media/ er skrivbart uansett»), med nye positive/negative caser.
- BLOKK-LENKER GJENNOM DELT VOKTER (CodeQL-mønsteret, duplisert fire steder): brukerstyrt `href` i knapp-, bilde-, samling- og galleri-blokken ble satt rått, så en `javascript:`/`data:`-href ble levende lenke hos besøkende. Alle fire ruter nå gjennom ny `isSafeHref` i `nav-model.js`: `isSafeUrl`-skjemaene (https?/mailto/tel) PLUSS site-interne stier (`/...`, aldri protokoll-relativ `//...` og heller ikke backslash-varianten `/\...`, som nettlesere normaliserer til `//`) og ankere (`#...`), begge med ankret regex - samlings-skjemaet dokumenterer entry.href som «intern sti eller ekstern URL», og malens eget innhold bruker `#`, så et rent `isSafeUrl`-vern ville knust publiserte sider (review-funn, se 0.6.35p). Utrygg href: knappen rendres død ('#', med console.warn som ukjent-side-varselet), bildet mister `<a>`-innpakningen (lightbox virker som uten lenke), samlingstittelen rendres uten lenke, galleri-flisen blir lightbox-flis. Nav/footer beholder bevisst det strengere `isSafeUrl` (interne sider lenkes via `page`); footer-anker-arbeidet (0.6.6.5 E) kan gjenbruke `isSafeHref`. Modulepreload-lukningen er uendret (nav-model lå der fra før; anti-drift-testen bekrefter). Ny `tests/block-links.test.mjs` (isSafeHref-kontrakten + knappen via minimal document-stub) + utvidede `isSafeUrl`-caser (blandet case, ledende blanktegn, vbscript:).
- KVOTEVARSEL I UTKASTLAGRINGEN (`editor/src/lib/draftStore.js`): `save()` gjorde `setItem` uten try/catch, så base64-bilder som sprengte localStorage-kvoten (~5MB) ga stille tap av utkastet. `save()` fanger nå ETHVERT kast (QuotaExceededError-navnet varierer historisk), melder via ny valgfri `onSaveError`-callback (additiv tredje parameter) og returnerer false; data i minnet står urørt, så neste forsøk med plass lykkes. Alle elleve `createDraftStore`-kall og de fire direkte `urd-draft-*`-skrivingene i App.svelte (restore, ny side, tittel-oppdatering av en annen sides utkast, publish) går til én felles rød statusmelding («lagringsplass full ... Publiser, eller fjern store bilder»). Ny `tests/draft-store.test.mjs` med localStorage-mock fester også baseline-invarianten (nøkkel slettes ved likhet med publisert) som hittil var utestet.
- ANGRE DEKKER SAMLINGER OG PLUGINS (`editor/src/App.svelte`): `snapshot()` tok kun {pageId, page, site}, så Ctrl+Z etter en samling-/plugin-endring gjenopprettet et urelatert eldre bilde og reverterte stille innhold brukeren ikke rørte. Panelsperre ble forkastet (samlinger redigeres også klikk-og-skriv i preview); i stedet full inkludering: snapshotet bærer nå samlingsindeks, alle samlingsdata og plugin-tilstand (gatet av et `samlingerReady`-flagg satt når init har fylt ALLE stores, ikke bare indeksen; review-funn, se 0.6.35p), alle mutasjonene går gjennom `pushHistory` (`edit:samling:`-nøkler koalescerer skriveskurer; add/move/remove og plugin-toggle er egne steg), og `restore()` gjenoppretter delene FØR sidebytte-grenen (kryss-side-angring mister ellers samlingsdelen). Ny `publishedSamlinger`-cache (fylt ved init/publisering/oppretting) lar angring gjenskape en slettet samlings store mot riktig publisert baseline. Plugin-gjenoppretting er diff-vaktet: kun en reell plugin-endring koster preview-reboot (import kan ikke angres i en levende frame); vanlig angring rører aldri iframen.
- LATENT FUNN (ikke fikset her, nytt backlog-punkt under kodegjennomgangens Middels): `addSamling` gir store-baseline identisk med fresh data, så en NY, TOM samling har `hasDraft() === false` og kan publiseres som indeks-innslag uten tilhørende `content/samlinger/<id>.json`. Restore-logikken speiler bevisst dagens adferd.
- Motor-, functions-, editor-, test- og docs-filer (guard.js; blocks/button+image+samling+galleri.js; draftStore.js, App.svelte; guard-, footer-, nye block-links- og draft-store-tester; backlog/changelog/testrunder). draftStore/App.svelte buntes, så `template/admin/assets/editor.js` er gjenbygd. Verifisert: 254 kontraktstester, skjemavalidering og deterministisk bygg grønne. Underveis ble avhengighets-drift fanget og rettet (node_modules hadde Svelte 5.56.5 mot låsefilens 5.56.8, AGENTS.md-fallgruven fra 22. juli): `npm ci` + gjenbygg, så bundelen matcher låsefilen. Browser-røyken (utrygg href, Ctrl+Z samling/plugin, kvotevarsel) ligger i TESTRUNDER-batchen; maskinen mangler headless Chromium.

### 0.6.34 - KI-arbeidsflyt-runde: AGENTS.md som verktøynøytral regelkilde, uavhengig review før push, ferdig-kriterier og docs-synk-opprydding - 29. juli 2026
- BAKGRUNN: arbeidsflyten ble sammenlignet med «My AI Workflow» (christitus.com). Urd hadde det meste fra før (agent-regler, spesifikasjon/ADR-er, små nummererte pusher, tester + CI-gates + CodeQL + Dependabot, manuell push-gate og TESTRUNDER som manuell verifikasjon); tre reelle hull ble tettet i denne runden. Flat nummerering (0.6.34) fordi arbeidet ikke hører under noe 0.6.6.x-backlogpunkt.
- AGENTS.MD: hele regelverket er flyttet fra CLAUDE.md til ny verktøynøytral `AGENTS.md` i rota (åpen standard forvaltet av Agentic AI Foundation under Linux Foundation; leses automatisk av Codex, Cursor, Copilot, Jules m.fl.), så bidragsytere med andre verktøy får Urds regler automatisk. `CLAUDE.md` er nå en énlinjes `@AGENTS.md`-import (Claude Code leser ikke AGENTS.md direkte; importen er den dokumenterte broen). Levende referanser oppdatert (ADR-0011, LAERDOMMER, ELEMENTKART, backlog); personrefererende formuleringer omformulert til datofestede regler underveis i flyttingen.
- UAVHENGIG REVIEW FØR PUSH (artikkelens tydeligste poeng: aldri la samme agent som skrev endringene være den eneste som vurderte dem): push-klargjøringen har nå et eget steg der en frisk agent uten øktens kontekst gjennomgår hele diffen mot reglene og ADR-ene før commit-meldingen foreslås. Fulgt for første gang på denne pushen.
- FERDIG-KRITERIER («make it work» er ikke et kriterium): Port-linjer på v0.6, v0.7 og v0.8 i VEIKART (v0.3/v0.5 er levert og fikk ingen retroaktiv port), «Ferdig når:»-konvensjon for nye backlog-milepæler (demonstrert på 0.6.7 og 0.6.8), og regelen festet i AGENTS.md. Også festet i arbeidsflyten: et svar på et spørsmål avslutter turen (aldri valgdialog i samme tur som en forklaring, dialogen gjemmer teksten).
- DOCS-SYNK-OPPRYDDING (sammenligning/backlog/veikart, funn fra egen gjennomgang): FUNKSJONSKART status-merket der virkeligheten hadde løpt fra kartet (C1/C2/C7b levert, C15 bygget og bevisst fjernet, FAQ/footer/hero-galleri/gradient-editor/preset-miniatyrer «siden levert») og M-milepælnumrene oversatt til dagens backlog-numre (M7/M8 var byttet om mot 0.6.7/0.6.8); ELEMENTKART og LAERDOMMER fikk samme oversettelse pluss status for native/CSS-flyttene; «konkurrent»-rammingen byttet til «de andre byggerne» i tråd med lære-rammingen. VEIKART: v0.6 viser nå milepælstyringen 0.6.1-0.6.9, v0.7 fikk arkitektur-punktet (mobil-revurdering, fast innholdsbredde, klientside-navigasjon) og porten utvidet tilsvarende, og tre foreldede rader i risiko-tabellen er oppdatert. BACKLOG: v0.7-overskriften ryddet til «Finpuss + butikk», fast innholdsbredde flyttet fra Forslag og Ideer inn i v0.7, gradient-punktet flyttet inn i v0.7 og den løse «0.9»-seksjonen fjernet, Bugs-punktene fikk gyldig sjekkboks-syntaks og to av dem «mulig dekket av 0.6.6.5.3»-annotering (strykes kun ved testing), og kodegjennomgangen fra 27. juli er markert moden (Høy-punktene tas før 0.6.7).
- Kun markdown-filer endret (ingen kode, ingen skjemaendring, ingen migrering; bundelen urørt). Verifisert: 247 kontraktstester, skjemavalidering og bygg-samsvar grønne. Frisk-agent-reviewen ble forsøkt fire ganger men stoppet av forbigående API-overbelastning (529); siden diffen er ren markdown ble den denne ene gangen erstattet av mekaniske sjekker (tankestrek, sjekkboks-syntaks, stilregler, lenkemål, versjonsnummer-konsistens, ingenting fjernet fra TESTRUNDER).

### 0.6.6.5.11 - Tema uten FOUC via light-dark() + color-scheme, SVG auto-trim på alle bilde-flater, og nav-variant «Flytende (tab)» - 28. juli 2026
- TEMA UTEN FOUC → `light-dark()` + `color-scheme` (moderniserings-sporet «Moderniser til native/CSS», ADR-0011): FOUC-kilden var de hardkodede MØRKE default-tokenene i `base.css`, malt ved første paint før JS kjente sidens faktiske farger (async `site.json`-fetch), så en lys side blinket mørkt. Ny ren `buildThemeCss(theme)` (node-testet) projiserer temaet til `light-dark(lys, mørk)` bak `@supports` og gjenbruker `activeTokens`. Ved publisering materialiseres den til en render-blokkerende `content/theme.css` (lenket i index.html etter base.css), så første paint har sidens egne farger - null flimmer, null JS for besøkende uten manuelt valg. `color-scheme` følger OS; et lagret lys/mørk-valg overstyrer via `data-urd-theme`.
- CSP-KORREKSJON av backlog-punktet: punktet sa «inline `<head>`-skript», men `_headers` har `script-src 'self'` UTEN `'unsafe-inline'` (samme grunn som boot.js/speculation-rules er egne filer). Det manuelle-valg-skriptet er derfor et EKSTERNT klassisk `theme-init.js` som kun setter et attributt (ingen stilmanipulasjon, matcher rådet). `style-src 'unsafe-inline'` tillater den JS-injiserte `<style id="urd-theme">` som bærer utkastets tema i preview og er sikkerhetsnett på publisert side.
- FALLBACK, BRYTER OG SIKKERHET: `base.css`-defaultene er nå Urds STANDARDtema som trygg enkeltverdi-fallback + `@supports light-dark()` der det støttes; eldre nettlesere får `applyTheme`s uendrede `applyTokens`-vei (ADR-0011 pkt 3). `applyTheme`/`toggleThemeMode` forgrener på `CSS.supports('color','light-dark(...)')`. Lys/mørk-bryteren (markedsført fortrinn, FUNKSJONSKART §5) er bevart: manuelt valg vinner over `color-scheme`-standarden. `buildThemeCss` validerer hver verdi med ankret allowlist (CodeQL-vennlig) og lukker en CSS-injeksjonsvei siden vi skriver rå CSS-tekst i stedet for `style.setProperty`. Enkelt-tema (uten `alt`) låser `color-scheme` til sitt eget skjema, ingen light-dark/bryter. Ingen migrering (tema-kontrakten urørt).
- SVG AUTO-TRIM på alle bilde-flater: den eksisterende `svgAutoTrim` (strammer SVG-viewBox til motivet, virket kun for bakgrunnsbilder) er via en ny `compressOrTrim`-helper koblet inn i nav-logo, footer-logo, bilde-blokk (ny/bytt/galleri), samlings-innslagsbilde og bakgrunn. Favicon: SVG auto-trimmes FØR ikon-editorens rasterisering, så det kvadratiske faviconet fylles tett av merket samtidig som det forblir universelt støttet raster (web-research: Safari bruker ikke SVG-favicon og faller stille tilbake til raster). SVG-primær med raster-fallback er notert som eget oppfølgingspunkt.
- NAV-VARIANT «Flytende (tab)»: tredje flytende-variant ved siden av pille og firkant - firkant topp, kun de to nedre hjørnene avrundet, henger ned fra toppen (med «Luft over menyen» av blir det en nedhengende header). Additivt på tvers av schema-enum, `nav-model.js` (ny `isFloating`-helper + `urd-nav-tab`), `base.css`, editor-dropdown + `floatingVariant`-derived, og ny nav-test. Glød/luft-tilvalgene arves fra flytende-basen. Ingen migrering.
- Motor-, editor-, test- og docs-filer (theme.js, theme-init.js, nav-model.js, base.css, index.html, content/theme.css, App.svelte; theme.test.mjs, nav.test.mjs; SKJEMA/BACKLOG/CHANGELOG/TESTRUNDER, schema). theme.js og App.svelte buntes av editoren, så `template/admin/assets/editor.js` er gjenbygd. Verifisert: 247 kontraktstester (nye nav-tab- og `buildThemeCss`/`safeCssValue`-tester), skjemavalidering og bygg-samsvar grønne. Headless/visuell testrunde-sjekk av ingen-flimmer gjenstår (lagt i TESTRUNDER).

### 0.6.6.5.10 - Bump svelte 5.56.7 → 5.56.8 (Dependabot PR #8, utvikling-mindre-gruppa) + gjenbygd bundel - 28. juli 2026
- Svelte-runtime oppdatert 5.56.7 → 5.56.8 (patch) i `editor/package.json` (`^5.56.8`) og `editor/package-lock.json`. Siden editoren BUNTER Svelte-runtime inn i `editor.js`, er bundelen gjenbygd mot 5.56.8 og committes sammen med låsefilen (ellers feiler CI-ens bygg-samsvar-sjekk, som kjører `npm ci` + bygg + diff). Kun runtime-endringer i `editor.js` (49 linjer inn / 42 ut); `editor.css` er uendret (runtime bor i JS-en). Verifisert: `node_modules/svelte` matcher den låste 5.56.8 (ingen avhengighets-drift), deterministisk bygg (to bygg gir identisk bundel), 240 tester og skjemavalidering grønne.

### 0.6.6.5.9 - Redigerings-lerretet rendrer i full vindusbredde og skaleres til visning: previewen er nå pikselidentisk med publisert (zoom-kontroll Tilpass/100%) - 28. juli 2026
- ROTÅRSAK: previewen i admin så ut til å ligge «lavere» og annerledes enn den publiserte siden, også på en side uten parallaks. Headless-måling slo fast at motoren rendrer preview og produksjon 100 % likt ved SAMME viewport (Kaker og Hjem: header/nav/#urd-root/første seksjon `top=0`, første kort `top=140`, `#urd-root` uten BFC/fanget margin). Forskjellen lå i editor-CHROMEN: iframen var `flex:1` og fylte kun plassen som var igjen etter topplinja (høyde) og skinna+panelet (bredde), så siden rendret i en MINDRE viewport enn et fullt vindu og stod lavere. Parallaks var en blindvei (undersøkt og reversert).
- SKALERT LERRET (mønsteret fra Webflow/Framer/Wix Studio/Divi): preview-iframen rendrer nå i full vindus-BREDDE (`targetW = window.innerWidth`) og skaleres med `transform: scale()` for å passe `.frame-wrap`, i stedet for å reflowe inn i restplassen. Render-en er dermed identisk med publisert; kun visningsstørrelsen (zoom) endres. Skalaen er BREDDE-drevet (ikke min av begge akser, som ga letterboks), og iframen gjøres tilsvarende høyere (`frameH/scale`) så den skalerte høyden fyller lerretet eksakt - ingen topp/bunn-barer; siden scroller inni iframen som en ekte side.
- ZOOM-KONTROLL: ny «Tilpass»/«100%»-segmentkontroll med levende %-avlesning i topplinja (tegnet SVG-ikon, temastyrt, ADR-0009). «Ren visning» er fortsatt ekte 1:1 (fullvindu, samme kodevei: rammen ≈ vinduet gir scale 1). Åpne/lukke et panel endrer KUN skalaen, aldri render-bredden.
- Ny ren modul `editor/src/lib/preview-scale.js` (`fitScale`/`previewScale`, node-testet: 5 nye tester, 240 totalt). Den eneste koordinat-korreksjonen er `onBlockMenu`, som ganger blokk-rekta fra iframens egne (uskalerte) koordinater med skalaen. Alle andre verktøylinjer bor INNE i iframen og skalerer med den. Motoren og `base.css` er urørt; kun `App.svelte` + hjelpemodulen. Motorens mobil/desktop-beslutning i preview drives fortsatt av `urd-viewport`, ikke iframe-bredden, så en bredere/skalert iframe kan aldri vippe modus.
- editor/src-endring, så bundelen er gjenbygd. Verifisert: 240 tester, skjemavalidering og bygg-samsvar grønne, pluss headless: admin-iframen rendrer i full vindusbredde (1600 og 2342 px, ikke den krympede ~1494), første blokk står identisk med `/?page=X` (`top 256, left 310.4`), ingen topp/bunn-barer ved både normal og svært bred visning (0 px / 0 px), og «Ren visning» gir eksakt scale 1. Mønsteret er dokumentert i `docs/sammenligning/LAERDOMMER.md` (lerretet rendrer i mål-bredde og skaleres, aldri reflow inn i restplassen).

### 0.6.6.5.8 - Nettsted-panel i admin: navngi siden (fanetittel), beskrivelse og favicon flyttet hit - 27. juli 2026
- NYTT «NETTSTED»-PANEL: admin har nå et eget panel (først i site-config-gruppa, ved siden av Tema/Nav/Footer) for nettstedets identitet: Navn (`site.title`), Beskrivelse (`site.description`) og Nettstedsikon (favicon, `site.icon`). Tidligere kunne `site.title` KUN settes av oppsettsveiviseren ved førstegangs-oppsett, så et allerede oppsatt nettsted hadde ingen vei til å endre navnet - som er halve nettleser-fanetittelen (`<side> - <navn>`, satt i `engine/urd.js`) og standardteksten i menylogoen. Redigering går gjennom utkast-flyten (`setSiteName`/`setSiteDescription` via `siteMutate` med `edit:`-nøkler, så en skriveøkt blir ett angre-steg).
- FAVICON FLYTTET: nettstedsikonet er tatt ut av Tema-panelet og lagt i Nettsted-panelet der det hører hjemme. Ikon-editoren, opplasting og materialisering ved publisering er uendret.
- Følger panelreglene: ingen forklarende prosa, alt i «?»-tooltips. Feltene er additive og fantes i skjemaet fra før - ingen migrering.
- editor/src-endring, så bundelen er gjenbygd (låsefil-eksakt Svelte 5.56.7). Verifisert: 235 tester, skjemavalidering og bygg-samsvar grønne, pluss headless (admin lastet): panelet finnes og viser Navn/Beskrivelse/Nettstedsikon, Navn viser `site.title`, redigering av navn og beskrivelse persisterer i utkastet og Ctrl+Z reverserer siste endring, og favicon er borte fra Tema.

### 0.6.6.5.7 - Moderniser til native/CSS: parallaks scroll-drevet, faq som `<details name>`, lightbox som `<dialog>` (ADR-0011) - 27. juli 2026
- PARALLAKS → SCROLL-DREVET CSS: bilde-bakgrunnslagets parallaks drives nå av kompositoren via `animation-timeline: view()` (keyframe i `base.css`, gated med `@supports`), i stedet for rAF + scroll-lytter på hovedtråden. `mountParallaxCss` setter reiseveien (`--urd-px-shift`) og overskannet ved oppsett og resize (ikke scroll), med samme `parallaxPad`-formel og fortegn som før; view()-tidslinjen scrubber `translateY`. Uten støtte kobler motoren den gamle rAF-varianten (`mountParallax`) - beholdt som fallback. Stille i sluttilstand på mobil og ved redusert bevegelse. Verifisert i headless: jevn scrub -54→+61→+126 px over scroll, statisk på mobil/redusert.
- FAQ → NATIVE `<details name>`: FAQ-akkordeonen er bygget om fra egen disclosure-JS til native `<details>`/`<summary>`. `name` (bundet til blokk-id) gir eksklusiv utfolding uten JS; finn-på-siden åpner treff, og tastatur/skjermleser-semantikk følger native. Fjernet `nextOpen`/`applyOpen`/toggle-JS-en, manuell `aria-expanded` og egen åpen-tilstand; ny ren `groupName` (node-testet) fester eksklusiv-vs-multi. Myk utfolding via `::details-content` + `interpolate-size` bak `@supports` (momentant ellers). Datablokk-autoveksten (melder sammenfoldet høyde, vokser kun visuelt) beholdt. Ingen migrering (props `items`/`multi` uendret). Verifisert i headless: eksklusiv utfolding, redigering uten utilsiktet utfolding, mellomrom skrives, `urd-edit` postes.
- LIGHTBOX → NATIVE `<dialog>`: fullskjerm-bildevisningen bruker nå `<dialog>` + `showModal()`. Top-layer fjerner z-index (verifisert over et z-index:99999-element), `::backdrop` gir den mørke bakgrunnen, og fokusfelle, fokus-retur og inert bakgrunn følger native (lightboxen manglet før en fokusfelle). Esc lukker native, piltastene stepper, bakgrunnsklikk lukker, scroll-låsen gjenopprettes. Instans-vaktet opprydning så et etterslepende `close` ikke river et nyåpnet overlegg.
- MENYER (utsatt, begrunnet): nav-undermenyene forblir disclosure (ADR-0010) og flyttes IKKE til Popover/top-layer nå - det ville kreve CSS Anchor Positioning (~82 %, ikke baseline) for å holde undermenyen posisjonert under nav-punktet. Egen runde når forankring er baseline.
- ADR-0011 «Bygg komponenter native/CSS-først» + motor-lekse i CLAUDE.md: nettleser-primitiver framfor egen JS, `@supports`-gating med trygg fallback, og Observers for logikk (ikke scroll-animasjon). Fester bug-forebyggingen der arbeidet skjer.
- Motor-, test- og docs-filer (image.js, faq.js, lightbox.js, base.css, faq.test.mjs; ADR-0011, CLAUDE.md, backlog/changelog/testrunder). Editoren BUNTER motormodulene (preview), så motorendringene krevde et gjenbygg av `template/admin/assets/editor.js` (låsefil-eksakt Svelte 5.56.7, deterministisk). Verifisert: 235 kontraktstester, skjemavalidering og bygg-samsvar grønne, pluss headless-målinger over.

### 0.6.6.5.6 - docs/sammenligning/: LAERDOMMER + ELEMENTKART, innflettet i backlog/veikart, og en kodegjennomgang - 27. juli 2026
- SAMMENLIGNINGSSETTET SAMLET: de tre feltstudie-dokumentene er flyttet til `docs/sammenligning/`. FUNKSJONSKART (hva de andre byggerne har) flyttet dit, pluss to nye: LAERDOMMER (hvordan andre nettsidebyggere bygger - arkitektur og mønstre å hente) og ELEMENTKART (elementer og funksjoner: hvordan hvert tilbys brukeren OG hvordan det bygges teknisk, med en «gammel -> bug -> moderne fiks»-tabell og en støtte-tier for native/CSS-primitiver gated med `@supports`). README-doc-indeksen utvidet med alle tre, og VEIKART-lenken oppdatert til den nye stien.
- INNFLETTET I BACKLOG/VEIKART: kuratert topp-sett av grepene fra LAERDOMMER og ELEMENTKART lagt inn med presise doc-henvisninger (f.eks. «ELEMENTKART 5b.2», «LAERDOMMER 5.5»). Editor-UX-grep på 0.6.6.6/0.6.7, en egen «Moderniser til native/CSS»-seksjon (bunt B: `<dialog>`/Popover top-layer, `<details name>`, scroll-drevne animasjoner, `light-dark()` uten FOUC - med en retningslinje/ADR som er notert til et senere tidspunkt), og publiserings-/skala-grep på v0.8 og horisont. VEIKART fikk to lette linjer (v0.8 moderniser + horisont PR-per-utkast).
- KODEGJENNOMGANG (27. juli 2026): ny backlog-seksjon med fiks/endre-kandidater fra en gjennomgang av motor, editor og functions/plugins, gruppert etter alvor (Høy/Middels/Lav) med fil-referanser. Kjernen ble verifisert sunn (postMessage-snapshot overalt, ingen native select, AbortController-lyttere, eksakt-host på video/proxyer, sti-allowlist mot traversal, konfliktsjekk, skjema uten HTML-injeksjon). Kandidatene tas fatt på i egne runder.
- Rene dokument-endringer: ingen motor-, editor-, skjema- eller innholdsfiler rørt, så ingen bygd side påvirkes og den committede editor-bundelen er uendret. Verifisert: 236 kontraktstester og skjemavalidering grønne.

### 0.6.6.5.5 - Bakgrunnsbilde omdesignet til fri plassering (skala + posisjon), kraftig parallaks, Flislegg og SVG auto-trim - 27. juli 2026
- FRI PLASSERING: bilde-bakgrunnslaget er lagt om fra den for smale `cover`+fokuspunkt-modellen til fri plassering (samme idé som Figma Crop / Webflow Custom, etter research), som virker for ALLE bilder. `background-size: {size*100}%` (breddrelativ skala, ren `bgSize`) med en Størrelse-stepper (10-400 %) som ALLTID er tilgjengelig, pluss «Dekk»- og «Vis hele»-hurtigknapper som regner ut skalaen fra bilde- og seksjonsmål. Posisjon via prosent-`background-position` (`bgPosition`) med UTVIDET rekkevidde (-50 % til 150 % i editoren, 2D-punkt + sliders alltid synlige) så motivet kan legges delvis eller helt UTENFOR kanten. `fit` blir «Vanlig» (fri plassering) eller «Flislegg» (mønster); `cover`/`contain` beholdes som nøkkelord for bakoverkompat + bildegalleri-laget. Den forrige auto-skjulingen av «inerte» posisjons-akser er fjernet - aksene virker nå alltid (regelen om å skjule kontroller uten effekt er oppfylt ved design i stedet for ved skjuling).
- PARALLAKS kraftig og zoom-fri: `MAX_SHIFT` hevet 0.08 → 0.4, og den frie modellen forskyves rent med `translateY` UTEN overskann (målt ~480 px utslag over en scroll mot ~72 px før, med uendret bildehøyde = ingen zoom). Fyllmodus (`cover`/`flislegg`) beholder et strammere overskann. To fikser fra testingen: `setPointerCapture` på fokuspunkt-pad-en (draget «henger» ikke lenger når musa drar over preview-iframen), og en rAF-reapply så overskannet settes på load i stedet for å «vokse» ved første scroll.
- BLEED i alle retninger: «Flyt inn i nabo» ned/begge løftes til `z-index: 1` - seksjonen under kommer senere i DOM og malte ellers bakgrunnen sin oppå bleeden; laget maler nå over naboens bakgrunn, men fortsatt under innholdet dens (blokker på z>=1).
- SVG AUTO-TRIM: opplastede SVG-er får `viewBox` strammet til motivets faktiske omfang (målt via canvas-piksler - SVG-en rendres som bilde, ingen live-DOM-innsetting, samme rejectede sanitering; rene `tightSvgViewBox`/`svgViewBox` i `imageTools.js`), så død plass rundt en logo ikke forstyrrer skala/posisjon.
- BACKLOG/DOCS: milepælene fra 0.6.6.6 og opp ryddet (Maler skilt fra oppdagbarhet, i18n flyttet etter maler, økosystem-oppdagbarhet inn i splitten, analytics til v0.7), og alle person-formuleringer («eiers …») fjernet fra backloggen.

### 0.6.6.5.4 - Design/tema del 2: seksjonstemaer, parallax, stagger, admin-kontrastfiks - og ferdige temaer + oppusset Tema-panel - 26. juli 2026
- SEKSJONSTEMAER (rollesett): additivt `section.theme` med ferdige roller (Flate, Aksent, Invers; Standard = ingen). Rollen overstyrer seksjonens fargetokens via `--urd-base-*`-referanser (nye basis-kopier satt av `applyTheme`), så en rolle som bytter bg<->text (Invers) ikke lager en `var()`-sykel og lys/mørk følger med automatisk; blokkene arver via `resolveColor`. `SECTION_THEMES`/`sectionThemeVars`/`applySectionTheme` i `theme.js`, anvendt på seksjonens host i `render.js`, «Seksjonstema»-nedtrekk i Egenskaper. Invers-kortene fikk tydeligere separasjon (surface 78/22 mot bakgrunnen) etter testfunn.
- ADMIN-KONTRASTFIKS: editorens forhåndsvisnings-chrome (dra-verktøylinjen, roter-håndtaket, seksjonslinjen) hardkodet hvit tekst på admin-aksenten, som ga lav kontrast på lyse admin-temaer. Ny `--urd-admin-accent-text` utledes fra aksentens luminans (WCAG) og erstatter den hvite. Det planlagte kontrast-varselet i Tema-panelet ble bygget, men fjernet igjen: den levende forhåndsvisningen viser lesbarheten klart nok.
- PARALLAX: bilde-bakgrunnslaget (`version` 1→2, additiv migrering setter `parallax: 0`) fikk en `parallax`-styrke som lar laget henge etter ved scroll (ren `parallaxOffset`, ÉN passiv scroll-/rAF-lytter som luker ut frakoblede lag; AV på mobil og ved `prefers-reduced-motion`). Kontroll i bildelagets editor.
- STAGGER: ny GRUPPE-inngangsanimasjon på seksjonsnivå (`stagger` i `coreAnimations`) som slipper seksjonens kort-blokker inn forskjøvet fra ÉN felles trigger, med to mønstre - «en etter en» og «kolonnevis» (kort på samme x samtidig, ren `staggerColumnDelays`) - og justerbar trinn-tid. Blokker med egen animasjon hoppes over.
- FERDIGE TEMAER: seks ett-klikks tema-forslag (Brønn, Stein, Plomme, Rose, Hav, Natt - hver med lys+mørk) som fyller alle fargetokens og lys/mørk; alt kan finjusteres fritt etterpå.
- TEMA-PANELET PUSSET OPP (pilot for et delt panel-språk): tema-forslag-rad med synlige paletter; palett-stripa ER fargeredigereren (klikkbare `ColorPicker`-ruter med hex under, ingen egen «Farger»-gruppe); lys/mørk er samlet i Farger med en av/på-bryter og «Auto/Egne» rett under (`theme.alt.auto`, additivt: mørk-paletten avledes fra de lyse og re-avledes ved endring, eller styres selv); Lys- og Mørk-paletten vises samtidig, begge redigerbare, med «Standard»-tag for hvilken modus nye besøkende ser først; en levende forhåndsvisning (lys+mørk side ved side) viser hver farge i bruk; Typografi fikk en tekstprøve i valgt skrift og Form en levende hjørne-prøve med glidebrytere. Kontrast-varselet er fjernet.
- SKJEMA (alt additivt): `section.theme` (page), `image`-lagets `parallax` (fri-form typedProps), `theme.alt.auto` (site). Dokumentert i `docs/SKJEMA.md`.
- Verifisert: 228 kontraktstester (nye: `sectionThemeVars`/`relativeLuminance`/`contrastRatio`, `staggerColumnDelays`, image-migrering 1→2 + `parallaxOffset`), skjemavalidering og editor-bygg grønne, pluss headless Chromium: roller overstyrer fargene og blokkene arver i lys OG mørk, parallax flytter transform ved scroll og står stille på mobil, begge stagger-mønstrene gir riktige forskjøvne delays, admin-chromen er lesbar på lys aksent, og hele det oppussede Tema-panelet (forslag, palett-redigering via ColorPicker, lys/mørk + Auto/Egne, forhåndsvisning, Typografi/Form) virker i admin. Ingen bygd side påvirkes.

### 0.6.6.5.3 - SVG-logoer, nav oppå hero (overlay), tom-side-fiks og en bugsjekkrunde på footer/nav - 26. juli 2026
- SVG-LOGOER: logo-opplasting (footer-merket og nav-logoen) beholder nå vektoren i stedet for å feile eller flate den til webp. `compressToWebp` slipper SVG rett gjennom `svgToDataUrl`, som validerer og AVVISER en SVG med skript-vektorer (`<script>`/`<foreignObject>`/`on*`/`javascript:`) i stedet for å strippe (reject er robust, stripping kan omgås) - uten `DOMParser`, så teksten reinterpreteres aldri som markup. Publisering gir riktig filendelse via `mediaExtension` (SVG → `.svg`, raster → `.webp`). Rendring skjer via `<img>` (secure static mode), og avvisningen er forsvar i dybden for direkte fil-åpning.
- NAV OPPÅ HERO (overlay): nytt opt-in `nav.overlay` (kun variant `bar`, standard av) legger fullbredde-linjen oppå toppseksjonen i stedet for i eget bånd over den, så en gjennomsiktig meny avslører hero bak seg. Verten tas ut av flyten (fixed ved sticky, absolute ellers), som floating; floating/sidestilt ligger allerede utenfor flyten. Additivt - ingen bygd side endres, og toppseksjonen bør ha nok klaring øverst.
- TOM SIDE: «+ Ny seksjon» var skjult til man hovret en naboseksjon, så en side der alle seksjonene ble fjernet ble umulig å bygge videre på. Den eneste baren på en tom side vises nå alltid som en tom-tilstand.
- BUGSJEKKRUNDE (footer/nav), fire trygge fikser etter en gjennomgang: (1) alle nav-/footer-lenker (kolonner, bunnlinje, doormat, CTA-knapp) gates nå av samme trygg-URL-vokter som sosiale lenker - `isSafeUrl` er flyttet til `nav-model.js` og brukt i `resolveItem`, så utrygge skjemaer (`javascript:`/`data:`) blir `#` med missing; (2) footer-modellen herdes mot ikke-streng-felt i håndredigert data (et tall/boolean i tittel/tekst velter ikke lenger renderen - «siden dør aldri av dårlig data»); (3) det inerte «Justering»-valget skjules når Stor CTA er aktiv (den tvinger sentrering og dropper kolonner); (4) resize-grepets dra-lyttere bærer render-signalet, som resten.
- URD-LOGO (docs): README-en fikk Urd-logoen øverst (turkis lockup fra `docs/brand/`), og ASCII-skipet nederst er ryddet bort.
- Verifisert: 222 kontraktstester (nye: `resolveItem` avviser utrygg href, footer-modellen tåler ikke-streng, `hostClasses`-overlay), skjemavalidering og editor-bygg grønne, pluss headless Chromium (SVG-sanitering + riktig filendelse, nav-overlay legger hero under menyen mens av lar den stå under, tom-side-baren synlig og klikkbar uten regresjon på fler-seksjons sider, og hele footer-render-stien med utrygg lenke → `#`, utrygg sosial droppet og tallbasert tittel uten krasj).

### 0.6.6.5.2 - footer-overhaling (maler, rik bunnlinje, symmetrisk rutenett, CTA, merkevare-logo), delt bakgrunnslag for nav + Urd-logo - 26. juli 2026
- DELT BAKGRUNNSLAG (nav + footer + seksjoner): den lagbaserte bakgrunnen (`background.layers`) er faktorert ut til `renderBackgroundLayers` i `render.js` og deles nå av seksjoner, nav OG footer - samme lagmodell overalt. Nav-en får full lagbakgrunn (`nav.style.background.layers`) med egen lag-editor; uten lag faller den tilbake på den gamle flatefargen (byte-likt). Alt additivt.
- FOOTER-MALER: «Oppsett»-tekstknappene er byttet med en visuell miniatyr-velger («Startpunkt») med åtte research-baserte maler (Minimal, Sentrert, Kolonner, Sitemap, Nyhetsbrev, Stor CTA, Kontakt, Mega), tegnet av en ren `footer-thumb.js`. En mal er et startpunkt: alt kan fritt redigeres etterpå.
- RIK BUNNLINJE: copyright/tekst til venstre + valgfrie `baseline`-lenker til høyre. «Enkel tekst» (`footer.text`) er fjernet fra den rike admin-en (beholdt i data + gammel ikke-rik render for bakoverkompat). Ingen plassholdertekst fylles inn (aldri versjonsnummer/sidetittel).
- SYMMETRISK KOLONNE-RUTENETT: like brede kolonner (`grid-template-columns: 1.7fr repeat(--n, minmax(0,1fr))`), lik avstand, responsiv kollaps (2/1). En lang kolonne (> 6 lenker, eller `col.wide`) spenner over to spor og deler lenkene i to; `columnsAlign` gir venstre/sentrert overskrift for den delte kolonnen.
- HANDLINGSOPPFORDRING (CTA, `footer.cta`): `button` (knapp som lenker til side/URL/mailto - virker uten server) eller `newsletter` (e-postfelt). Nyhetsbrev sender til et konfigurerbart `endpoint` (Formspree/Mailchimp/Buttondown/ConvertKit/egen Pages-function) med `fetch` (POST JSON) + inline bekreftelse; `mailto` som fallback uten endepunkt. Honeypot mot spam, og en preview-kortslutning så innsending aldri fyrer i editoren. Ren `footer-cta.js` (kopierer hjelperne fra skjema-pluginen, importerer den ikke - kjernen skal aldri importere en plugin) med egne `node --test`-tester. Ekstern vert krever manuell `connect-src` i `_headers` (ADR-0006), som skjema.
- MERKEVARE-LOGO: footer-merket har nå tre moduser (Tekst / Logo / Begge), speiler nav-logoen: opplastet bilde (data-URL i utkast, materialiseres til `media/*.webp` ved publisering) med høydevalg, faller pent tilbake til tekst når ingen logo er lastet.
- PER-SIDE-SYNLIGHET: footeren kan skjules per side (`hideOn`); den rendres nå når side-id-en er kjent, og editoren gjenrender ved sidebytte. Det finnes ingen «Footer»-seksjonspreset - den delte footeren bor i Footer-panelet (`site.footer`), ikke som en seksjon per side.
- URD-LOGO: admin-headeren viser nå Urd-merket (uruz-runen ᚢ i én strek) med «Urd»-ordmerke, i brønn-turkis (etter Urðarbrunnr). Hele paletten er bevart som CSS-variabler (`--urd-brand` turkis, pluss bronse/indigo/mono). Favicon (offentlig side, admin og editorens fallback) er byttet fra den gamle indigo-«U»-plassholderen til runen. Frittstående, brukbare logo-filer ligger i `docs/brand/` (mark + full lockup i alle fire farger, favicon, README med palett).
- Verifisert: 219 kontraktstester (+ nye footer-cta- og footer-modelltester), skjemavalidering og editor-bygg grønne, pluss headless Chromium (merkevare-moduser tekst/logo/begge, favicon = turkis rune på både side og admin, kolonne-rutenett symmetrisk med lik avstand og bred-kolonne-deling, CTA-knapp og nyhetsbrev med inline bekreftelse/mailto-fallback, mal-velgeren tegner miniatyrer). Ingen demo-footer lagt i innholdet.

### 0.6.6.5.1 - footerfiks: tom footer forblir tom (ingen «Urd v0.5»-default) - 25. juli 2026
- BUGFIKS: en footer som nettopp ble skrudd på viste sidetittelen (f.eks. «Urd v0.5») som merkevare, fordi `footerBrand` falt tilbake til `site.site.title` når `brand` var tom. Fallbacken er fjernet: en tom merkevare gir null, og en footer uten egne felt rendres byte-likt som den gamle tekst-footeren (tom når det ikke er skrevet noe). Tittel-feltets plassholder-hjelp endret fra «Tomt = sidetittelen» til «Tomt = ingen merkevare».
- NUMMERERING (docs): backlog-notatet og CHANGELOG-ingressen presisert - hvert innslag arver nummeret til backlog-punktet arbeidet hører under, pluss ett siffer for rekkefølge (0.6.6.5 → 0.6.6.5.1). Rydder opp i tidligere misforståelse om at backlog-numrene var noe annet enn CHANGELOG-numrene; denne footerfiksen er selv det første eksemplet.
- Verifisert: 211 kontraktstester (footerBrand- og hasRichFooter-testene oppdatert til ingen-fallback), skjemavalidering og editor-bygg grønne.

### 0.6.33 - footer med kolonner, sosiale lenker og merkevare (batch E / 0.6.6.5) - 25. juli 2026
- FOOTER-LØFT: den delte footeren (`site.footer`) går fra én sentrert tekst til en fleksibel modell med merkevare (tittel + tagline), lenkekolonner, sosiale ikoner og en copyright-bunnlinje. Alt additivt: gamle footere (kun `text`/`align`) rendres byte-likt som før, ingen migrering (fravær defaultes ved lesing).
- KOLONNER: valgfrie lenkekolonner (`columns`), hver med tittel og lenker som peker på sider (via sideregisteret) eller eksterne URL-er (får `rel="noopener noreferrer"`). En kolonne uten gyldige lenker rendres ikke.
- SOSIALE LENKER: `social` = ikon-id fra ikonbiblioteket (de 12 sosiale + kommunikasjonsikonene fra batch B) + URL. URL-ene voktes av en ankret trygg-URL-regex (http/https/mailto/tel; `javascript:`/`data:` avvises), og ukjente ikon-id-er droppes.
- MERKEVARE + BUNNLINJE + BAKGRUNN: `brand` (tittel faller tilbake til sidetittelen, valgfri tagline), `copyright` (faller tilbake til de gamle `text`-linjene) og valgfri bakgrunnsfarge `bg` (theme-token eller rå verdi, standard temaets surface).
- ADMIN: Footer-panelet utvidet med grupper (Merkevare, Kolonner, Sosiale lenker, Utseende, Bunnlinje) som speiler Nav-panelets punktliste; gjenbruker `Dropdown`/`ColorPicker` og et ikon-nedtrekk med live forhåndsvisning. «?»-tooltips i stedet for panel-prosa (regel 23. juli 2026).
- REN LOGIKK + STIL: ny `footer-model.js` (gjenbruker `resolveItem` fra `nav-model.js` og `iconSvg`) med 7 kontraktstester; footer-CSS bruker flexbox-kolonner som brytes robust på mobil (en tidlig grid-variant med `1.5fr` + `repeat(auto-fit)` kollapset til én kolonne og ble forkastet).
- Verifisert: 211 kontraktstester (+7), skjemavalidering og editor-bygg grønne, pluss headless Chromium (kolonner side ved side på bred skjerm, stabling på smal, interne/eksterne lenker resolvert med riktig rel, sosiale ikoner tegnet, admin-panelet legger til kolonner uten feil). Ingen demo-footer lagt i innholdet - footeren bygges gjennom admin.

### 0.6.32 - ytelse: raskere sidelasting (modulepreload, parallell innhold-fetch, prerender, media-cache, lazy bilder) - 25. juli 2026
- BAKGRUNN: en ny side brukte ~3 sekunder å laste, mot under 1 sekund for en sammenlignbar håndbygd side (samme Cloudflare Pages, samme standard-cacheheader). Rotårsaken er arkitektonisk: `index.html` laster ÉN modul (`boot.js`), som statisk importerer motoren i flere lag, så nettleseren oppdager importene lag for lag (serielt fossefall) i stedet for i én parallell bølge. Denne runden fjerner fossefallet med additive, byggfrie grep - motoren serveres fortsatt rått, ingen bundler, og ingen bakt HTML/SEO (det forblir v0.8-arbeidet).
- MODULEPRELOAD: `index.html` forhåndslaster nå hele den STATISKE import-lukningen av `boot.js` (34 moduler) med `<link rel="modulepreload">`, så grafen hentes i én parallell bølge. Editor-laget (`preview-edit.js` m.fl.) lastes dynamisk og står bevisst utenfor - besøkende henter det aldri. Målt headless: alle 34 modulene starter i én bølge (≤20 ms) i stedet for trappetrinn.
- PARALLELL INNHOLD-FETCH (urd.js): `/urd.json` (motorversjon for plugin-sjekken) og `/content/site.json` startes nå samtidig i `boot()` i stedet for etter hverandre; bare sidefilen (`page.json`) er igjen serielt fordi den trenger sideregisteret. Målt: de to starter nå i samme tick (0 ms diff).
- SPECULATION RULES: interne lenker prerendres for nær-umiddelbar navigasjon (`/admin/*` utelatt). Regelsettet leveres som egen fil `speculation-rules.json` via en `Speculation-Rules`-header, IKKE som inline-skript - det ville krevd et `script-src 'inline-speculation-rules'`-unntak og brutt grunnen til at `boot.js` er en egen fil. Degraderer pent: ikke-Chromium ignorerer headeren.
- IMMUTABLE-CACHE PÅ MEDIA: `media/*` får `Cache-Control: public, max-age=31536000, immutable`. Trygt uten maskineri fordi filnavnene er innholdshash (`media/<slug>-<hash>.webp`): nytt innhold gir nytt navn. De tyngste bytene på en side hentes dermed kun én gang per besøker.
- LAZY BILDER: kjerne-bildeblokken får `loading="lazy"` + `decoding="async"` (satt før `src`), likt samling-/galleri-/video-blokkene, så bilder under folden ikke hentes ivrig.
- ANTI-DRIFT CI-VAKT (`tests/modulepreload.test.mjs`): regner ut import-lukningen av `boot.js` på nytt og feiler bygget om modulepreload-lista i `index.html` ikke matcher eksakt (verken manglende eller ekstra modul), og om et editor-lag sniker seg inn. Samme kultur som bygg-samsvar-sjekken.
- BEVISST UTSATT TIL M9: versjonering av motor-URL-ene (og immutable-cache på motoren + `base.css`) krever en versjonert mappe fordi relative importer ikke arver en `?v=`-query, med fil-flytting og en reell risiko for å foreldreløse publiserte `<slug>/index.html`-kopier. Det hører hjemme i M9-oppdatereren, som alt eier motorversjonering og oppfriskning av side-kopiene (se backlog + ADR-skisse). Modulepreload parallelliserer uansett revalideringen alt, så gevinsten av immutable-motor er liten nå.
- Verifisert: 204 kontraktstester (+3 nye), skjemavalidering og editor-bygg grønne, pluss headless Chromium-måling av fossefallet (modul-bølge, parallell innhold-fetch, bildeblokk med lazy/async). Immutable-cache og prerender kan kun bekreftes på en ekte Pages-preview (dev-serveren er no-store).

### 0.6.31 - teksteditor «Office-linjen» (størrelse på markert tekst), plugin-innstillinger inn i Egenskaper, og en stor bugfiks-runde - 24. juli 2026
- TEKSTEDITOR «OFFICE-LINJEN» (valgt blant tre skisser): verktøylinjen er nå TO faste rader som Word/LibreOffice. Skriftstørrelse, font og − / + gjelder MARKERT tekst (ikke lenger hele feltet), via en sentinel-markør som normaliseres til rene `<span>` (ingen `<font>`-tagger); størrelse steppes per tekst-run så blandede markeringer beholdes. Nytt: hevet/senket skrift, blokkjuster, innrykk/utrykk (marginLeft i em, aldri blockquote-søl; lister får nivåer), sitat som av/på-bryter, aktiv-markering på formatknappene, og en avstandsmeny (linjeavstand-preset + bokstavavstand). Størrelsesfeltet og font-nedtrekket speiler markeringen. Linjen forankres ved TOPPEN av feltet (følger ikke markøren). Ren `text-typo.js` med tester. Felt-typografien (font/størrelse/avstand for hele feltet) er FJERNET fra Egenskaper - den bor nå kun i linjen (marker alt for hele feltet). A-opp/A-ned og feltets innebygde spinner-piler fjernet (− / + gjør jobben).
- FARGEVELGEREN: den delte velgeren melder onpick LIVE; markeringen fornyes nå mellom kallene (saveSelection etter hver påføring), så den bevares gjennom hele dra-valget i stedet for å mistes eller legge fargen feil - den gjentakende «hver eneste fargeeditor»-koblingsbuggen. Fargemenyen lukkes ikke lenger ved valg (kun ved ny palett-trykk / annen underrad / lukket linje). «Lekker ut av boksen» fikset: `.urd-cp` er `display: grid` uten kolonnedefinisjon, så den implisitte kolonnen ble max-content-bred og RGB-feltene fløt ut; bundet med `grid-template-columns: minmax(0, 1fr)`.
- BLOKK-STYLING (kortstilen på Tekstboks/FAQ-kort): ny «Blokkfarge» (bakgrunn, med fjern-knapp), ny «Skyggefarge», Kantfarge også for «Temaets (tynn)» (velger du farge blir den en egen fargbar kant), og tykkelse som − / +-tallfelt (før slider); glass-effekten tones nå med blokkfargen; temaets tynne kant er synlig (var 12 %, nå 22 %). Additive `boxStyle`-felt `bg`/`shadowColor` (SKJEMA.md + box-style.js + tester); eldre bokser uendret.
- REDIGERINGS-CHROME FØLGER ADMIN-TEMAET: blokk-omrisset (også hover), grid-overlegget, hjelpe-/side-linjene, marquee, «+ Ny seksjon»-stripen, seksjons-dragelinjene, «?»-hjelpechippen (hint.js), preset-menyen og plugin-knappene bruker nå `--urd-admin-accent`, ikke sidens aksent. Sidens EGNE knapper/innhold beholder sidetemaet.
- BLOKK-KONTROLLENE (dra ⠿, z-orden, tannhjul) river ikke lenger Egenskaper fra blokken til seksjonen: blokk-gester poster ikke seksjonsvalg (markActiveVisual uten melding), og editoren henter palett-seksjonen fra blokkvalget i stedet.
- DOBBELTKLIKK-MENYEN («+ Ny blokk» ved pekeren): lukkes ikke lenger når musen forlater seksjonen (mouseleave-lukkingen fjernet; står til klikk utenfor eller blokkvalg), og er hevet over seksjons-chromet (z-index 100002) så seksjonslinjene og «+ Ny seksjon» ligger UNDER menyen.
- INNGANGSANIMASJONER: spiller nå når elementet blir synlig - også blokker over «folden» toner inn ved sidelasting hos besøkende (innlastings-snappen fjernet). Rotårsaken til «Ton inn fungerer ikke på blokker»: en blokk med BÅDE inngangsanimasjon OG «Løft ved peker» fikk hover-effektens `transition` til å overstyre inngangens, så opasiteten hoppet; kombinasjonen dekkes nå av en egen regel.
- PLUGIN-INNSTILLINGER INN I EGENSKAPER: de flytende «⚙ Kilder» (kalender), «⚙ Sted» (kart) og «⚙ Skjema»-pillene er fjernet fra blokkene. «Innstillinger …» i blokkens Egenskaper/blokkmeny åpner samme config-panel i forhåndsvisningen (ny melding `urd-open-block-config` klikker en skjult `urd-cfg-toggle`; plugin-koden og config-panelet er uendret, kun inngangen flyttet). Hjelpetekstene peker nå dit. «?»-chippen står igjen.
- BLOKKMENYEN (Tekst-blokk-popupen): lukkes nå ved klikk et annet sted enn menyen (på blokken, lerretet eller i admin-panelene); klikk inne i menyen holder den åpen.
- DEV-SERVEREN (dev-server.py): svarer aldri «304 Not Modified» lenger (betingede forespørsler strippes) og sender sterke no-cache-headere (no-store, no-cache, must-revalidate, Pragma, Expires), så motorendringer ikke henger igjen i nettleseren etter en reload.
- Verifisert: 201 kontraktstester, editor-bygg og skjemavalidering grønne, pluss omfattende headless Chromium-sjekkpunkter for hver delrunde (Office-linjen 23/23, blokk-styling, fargevelger, admin-tema på chrome, animasjon fade+hover, plugin-config-flytting, dobbeltklikk-meny, blokkmeny 5/5).

### 0.6.30 - bugfikser fra testrunde: blokkmeny, seksjon-chip, dra-cursor, typografifelt, animasjon/peker-splitt og cache-fri testserver - 24. juli 2026
- BLOKKMENYEN VED DOBBELTKLIKK (eiers funn): «+ Ny blokk»-chippen skjules når menyen åpnes ved pekeren (den sto over menyen), klikk hvor som helst utenfor lukker menyen (én document-lytter; før lukket den kun når pekeren forlot seksjonen), og nær venstre kant åpnes menyen mot HØYRE for pekeren i stedet for å gå ut av skjermen. Menyvalg og lukking nullstiller alltid posisjon og chip-tilstand.
- EDITOR-MENYENE FØLGER URD-TEMAET, IKKE SIDEN (eiers regel): ny melding `urd-admin-theme` sender adminens fargetema (bg/flate/aksent/tekst) til previewen ved oppstart og temabytte; base.css overstyrer sidens fargevariabler LOKALT på blokkmenyen og «+ Ny seksjon»-galleriet, med grå-temaet som fallback utenfor admin. Sidens eget innhold røres aldri.
- «+ NY SEKSJON»-CHIPPEN KAN ALLTID KLIKKES: baren ligger nå over seksjonshåndtakenes dra-flater (de fanget klikket der chippen står). Å dra i selve chippen flytter fortsatt grensen; baren er 0 px høy, så kun chip-pillen og galleriet ligger over noe.
- RETNINGSVISENDE DRA-CURSOR: begge seksjonshåndtakene (bunn og topp) viser pil ned når man drar nedover og pil opp når man drar oppover (med terskel mot dirring), og rydder ved slipp.
- TYPOGRAFI SOM I VANLIGE TEKSTEDITORER (eiers krav): Størrelse (px), Linjeavstand og Bokstavavstand er nå TALLFELT (skriv eller stepp; tomt felt = arv fra tema) i både Egenskaper/blokkmenyen og teksteditor-popupens Aa-rad. S/M/L/XL-knappene, A-knappene, sliderne og forklaringsteksten er fjernet (TEXT_SIZES ute av fonts.js).
- «ANIMASJON INN» OG «VED PEKER» ER SKILT (eiers krav: «Løft ved peker» skal kunne kombineres med «Zoom inn»): nytt additivt `hover`-felt på blokker OG seksjoner, samme form som `animation`; motoren rendrer begge felt uavhengig. Eldre sider med pekereffekt i fellesfeltet virker uendret (motoren rendrer det gamle feltet likt) og normaliseres til `hover` ved neste animasjonsendring i editoren. Skjema tre steder + demo på Om oss (Les mer-knappen har begge); panel-prosaen om avspilling flyttet til title-tooltip.
- ROTÅRSAKEN TIL «LØFT VED PEKER FUNKER IKKE» (målt i headless): et nedtrekk nederst i panelet utløste en fokus-scroll i samme klikk, og Dropdownens lukk-ved-scroll-regel smalt popupen igjen før man fikk valgt (selve effekten virket hele veien). Alle nedtrekk FØLGER nå ankeret sitt ved rulling i stedet for å lukke; klikk utenfor og Escape lukker som før.
- CACHE-FRI LOKAL TESTSERVER (ny dev-server.py i repo-roten, `python3 dev-server.py 8000`): serverer template/ med «aldri cache» på hver respons, HTTP/1.1 keep-alive og samme IPv6+IPv4-binding som python-CLI-en, pluss klare meldinger ved opptatt port/feil mappe. Bakgrunn: hard reload tar ikke iframens modulkjede, så motorendringer ble hengende i nettleseren og ga eieren «ny admin + gammel motor»-feilbilder (gradienten i går, pekereffekten i dag). UTVIKLING.md oppdatert; gammel kommando virker fortsatt.
- Verifisert i headless Chromium: 19 sjekkpunkter for hele bugrunden (også kjørt gjennom den nye serveren), pluss målt rotårsaksjakt på nedtrekket (fetch-, klasse- og transformmålinger i både ren preview og admin). 190 kontraktstester, editor-bygg og skjemavalidering grønne.

### 0.6.29 - M6 batch D del 1: gradient-editor, kortstiler og FAQ-akkordeon - 24. juli 2026
- GRADIENT-EDITOREN, EIERS PLASS-MODELL (formet gjennom fem testrunder samme dag): fargene er en LISTE i rekkefølge langs gradienten, og slideren per farge styrer hvor mye PLASS fargen tar (normalisert andel vises; plass 0 gir en hard fargekant - stripe-trikset). Rekkefølgen endres KUN ved å dra i håndtaket: pekerdra med spøkelsesrad (hele raden med fargen følger pekeren), dempet originalrad og aksentfarget innsettingsstrek; virker også ett hakk opp/ned, og hvert dra er ett angre-steg (HTML5-dnd ble prøvd og forkastet, piler lagt til og fjernet igjen på eiers ordre). Kompakte fargeknapper, «+ Legg til farge» (begrepet «stopp» forkastet som uforståelig), fjern-knapp kun ved 3+. «Form» velger Lineær (Vinkel) eller Radiell (Sentrum X/Y); kun relevante innstillinger vises, og Styrke står rett under dem.
- GRADIENT-LAGET ER VERSJON 3: `stops` gikk fra rene fargestrenger (v1) via posisjoner (v2, aldri pushet) til `{color, share}`; migreringskjeden v1→v2→v3 er testet hele veien, og v2-utkast fra eiers testing løftes stille. Editoren løfter eldre lag for visning uten å røre utkastet, og løfter I utkastet ved første endring ($state.snapshot før lift - structuredClone på en $state-proxy kaster, samme felle som postMessage). Nye bakgrunnslag får definisjonens versjon (var hardkodet 1).
- BEVEGELSE PER GRADIENT (erstatter panorerings-bryteren, valgene følger formen): Lineær har Panorer frem og tilbake, Panorer én vei og Roter sakte (vinkelen spinner via registrert CSS-vinkelvariabel; uten støtte står den stille). Radiell har Pulser (styrken puster rundt eget nivå) og Sving sakte i bane (sentrum glir i sirkel med posisjonskompensert 200 %-lerret). Redusert bevegelse hos den besøkende slår alt av; formbytte nullstiller en bevegelse som ikke finnes for den nye formen.
- «PANORER ÉN VEI» BLE RIKTIG GJENNOM FIRE MÅLTE RUNDER MED EIER: (1) background-shorthanden nullstilte flisstørrelsen fra klassen, så loopen «pustet» på stedet med dobbelte farger; størrelse/gjentakelse settes nå inline. (2) Speilflisen (1 2 3 4 3 2 1) byttet ut med eiers regnbue-modell: sirkulær gradient der siste farge glir tilbake til første (1 2 3 4 1 2 3 4). (3) Skrå fliser kan ikke gjentas sømløst (ga skjøt-rutenett); løsningen ble en gjentakende gradient i px på en løper som forskyves nøyaktig én periode langs aksen per runde - loopen FØLGER dermed vinkelen eieren setter (et midlertidig Retning-valg er fjernet; det brøt også regelen om at en funksjons innstillinger står under funksjonen). (4) Perioden er linjen pluss akkurat plassen til den STØRSTE fargen (aldri dobling, som gjorde fargene dobbelt så store): ingen farge kan splittes over kantene eller synes to ganger, og med 7 like farger er perioden kun 1/6 lengre enn den statiske. Ren geometri i loopGeometry/loopGradientCss; løperne måles på nytt ved vindusendring.
- KORTSTILER (additivt boxStyle-felt, ren boxStyleCss i ny box-style.js): Skygge (Myk/Tydelig), Kantlinje (Temaets/Ingen/Egen farge med tykkelse 1-4 px) og Glass-effekt (frostet: gjennomskinnelig flate + backdrop-blur, degraderer pent uten støtte). Gjelder Tekstboksen (innstillingene rett under Tekstboks-valget, kun når den er på) og FAQ-kortene; uten valg er utseendet som før.
- FAQ-AKKORDEON (ny kjerneblokk `faq`): spørsmålskort der svaret foldes ut (disclosure-mønsteret med aria-expanded, ADR-0010; grid-rows-overgang). Besøkende åpner på hele raden; i editoren åpner pilknappen (med samme propageringsstopp som blokkmenyens tannhjul - pointerdown ville ellers utløst re-render før click), og tekstene redigeres RETT I blokken: spørsmål som ren tekst, svar med hele formateringslinjen (Aa-typografiraden skjules i andre blokkers rike felt - den skriver tekstblokk-props). Spørsmålsliste med rekkefølge, «Flere svar åpne samtidig» og kortstil i Egenskaper/blokkmenyen; hjelpechip på blokken. Blokken lagrer alltid SAMMENFOLDET høyde (autovekst som samling-blokken, kun høyden meldes); utfolding vokser kun visuelt og skitner aldri utkastet (0.6.28-regelen målt på nytt).
- SMÅTT: /api/github/latest kalles ikke lenger før innlogging (401-støy i konsollen på urdweb); FAQ-demoseksjon (glass + myk skygge) og radiell v3-hero i demoinnholdet på Om oss, mens to heroer bevisst står på v1 som levende migreringsbevis.
- SKJEMA.md dokumenterer gradient v3 (med migreringskjeden), boxStyle og faq-blokken; page.schema.json trengte ingen endring (props er typeeide). 31 nye kontraktstester i tre nye testfiler (190 totalt). Ende-til-ende verifisert i headless Chromium over åtte runder (~40 sjekkpunkter): besøkende-rendering med migrering, panel-UI, dra-omsortering med måling av ett-hakks flytt, alle fem bevegelsene, kortstil inn i preview, FAQ-redigering uten re-render, autovekst uten skittent utkast, og loopens periode målt mot formelen med eiers eksakte 7-fargers oppsett.

### 0.6.28 - utkast-fiksene: «Upubliserte endringer» av seg selv og etter angring - 23. juli 2026
- MÅLT ROTÅRSAK (eiers kritiske funn: merket og Forkast-knappen dukket opp uten at noe var gjort, og kom tilbake sekunder etter Forkast utkast): datablokkenes autovekst. Kalender-, samling- og galleriblokker melder målt høyde (urd-grow) ved HVER rendering, og målingen varierer med innhold, feed-svar og vindu (lokalt målte kalenderen feilteksten sin til 93 px mot lagrede 299). handleGrow skrev målingen rett inn i utkastet - og siden forkast utløser ny rendering, ble utkastet gjenskapt umiddelbart. Diffen utkast/publisert besto utelukkende av slike høyder.
- FIKSEN: autovekst er en MÅLING, ikke en redigering. Målinger bokføres nå i BÅDE utkastet og sammenligningsgrunnlaget (ny amendBaseline i draftStore), så de aldri alene utgjør «upubliserte endringer» - heller ikke ETTER angring (eiers andre funn: Ctrl+Z tilbake til start lot merket stå, fordi målte høyder skilte utkast fra publisert). save()-likhetssjekken («er noe faktisk annerledes») fantes allerede og blir nå ærlig.
- PILTAST-AVRUNDINGEN (fanget av verifiseringen): flytt en blokk ned og så opp igjen etterlot likevel et utkast - piltast-håndteringen avrundet x til én desimal selv ved ren vertikal flytting (4,17 % ble stille 4,2 %). Aksen som ikke flyttes røres nå aldri (både enkeltblokk og multiutvalg), så manuell tilbakestilling gir identisk tilstand og rent merke.
- FORKAST-KNAPPEN vises kun når det finnes utkast (før sto den alltid i topplinjen, deaktivert), og er FLYTTET til høyre for «Upubliserte endringer»-merket (eiers ønske); Publiser står som før, deaktivert uten utkast.
- Verifisert i headless Chromium over to runder (ren lasting uten utkastnøkler/merke/knapp, sidebytte holder seg rent, ekte endring gir alle tre, forkast fjerner dem varig, Ctrl+Z tilbake til start gir rent merke, manuell ned+opp gir rent merke, knappeplassering målt). 159 kontraktstester og skjemavalidering grønne.

### 0.6.27 - M6 batch C del 2: multimarkering - 23. juli 2026
- MULTIMARKERING (eiers ønske 19. juli 2026): marker flere blokker med MARQUEE (dra en ramme på tom seksjonsflate; delvis overlapp holder) eller SHIFT-KLIKK (legg til/fjern enkeltblokker). Medlemmene får stiplet aksentramme og en flytende verktøylinje («N valgt») over utvalget. Utvalget er avgrenset til ÉN seksjon (bevisst; shift-klikk i annen seksjon starter nytt utvalg).
- UTVALGET SOM ÉN ENHET: gruppe-dra i et hvilket som helst medlem flytter alle med samme forskyvning (klemt mot seksjonsbredden av ren groupDelta, y fri som ved enkelt-dra); piltaster flytter i grid-steg (Shift = 1 px); Delete sletter hele utvalget som ETT angre-steg (urd-delete fikk additivt blockIds-felt); klikk på et medlem kollapser ikke utvalget (tom flate/Esc gjør det), og grep i ⠿/resize/rotasjon på et medlem beholder det også.
- CTRL+C/V: kopier utvalget (eller én markert blokk) og lim inn med BEVART innbyrdes oppsett - alle blokker får samme forskyvning, hele innlimingen er ett angre-steg (gjenbruker urd-add-blocks), det innlimte blir det nye utvalget, og nytt Ctrl+V fortsetter fra det innlimte i stedet for å stable oppå. Innliming går til den aktive seksjonen; Ctrl+D med utvalg dupliserer det. Kjent avgrensning: utklippstavlen bor i previewens modultilstand og nullstilles ved sidebytte.
- JUSTER/FORDEL: verktøylinjen har venstre/midt/høyre + topp/midte/bunn (justerer innenfor utvalgets egen omsluttende boks) og «Fordel jevnt» vannrett/loddrett (første/siste står, luften mellom gjøres lik; vises kun ved 3+ blokker, panelregelen). Hver knapp er ett angre-steg. Alle knapper tegnede SVG-er.
- Geometrien er ren logikk i ny selection.js (blocksInRect, alignMoves, distributeMoves, groupDelta) med 7 nye kontraktstester (159 totalt). Ren visning og mobilvisning er uberørt (ingen marquee/verktøylinje/rammer der). Meldingskatalogen i previewBridge.js + JSDoc i preview-edit.js dokumenterer blockIds-utvidelsen.
- Ende-til-ende verifisert i headless Chromium (20 sjekkpunkter): marquee-utvalg med verktøylinje, gruppe-piltast + ett Ctrl+Z, innliming med målt identisk delta for alle blokkene, venstrejustering til lik x, fordel loddrett, samlet sletting/gjenoppretting, gruppe-dra med likt snappet delta, og chrome borte i Ren visning. Dette fullfører batch C.

### 0.6.26 - M6 batch C del 1: sticky blokker, klikkplassering, hjelpelinjer, toppkant-høyde - 23. juli 2026
- STICKY BLOKKER (eiers valg: begge modellene i én innstilling): nytt additivt blokk-felt `sticky: { offset, until }` med «Fest ved scrolling» i Egenskaper/blokkmenyen, «Avstand fra toppen» og «Slipp taket» (standard: når egen seksjon er forbi; eller velg en SENERE seksjon i nedtrekket, som kun viser seksjoner lenger ned). JS-styrt festing (position:fixed ved festeøyeblikket, frameToCss-verdiene tilbake ved slipp) fordi CSS sticky aldri kan holde forbi egen container; ren tilstandslogikk i ny sticky-model.js (static/fixed/parked, ugyldig grense degraderer til stillstand), DOM-del i ny sticky.js med ÉN rAF-throttlet scroll/resize-lytter. I editoren kun aktiv i Ren visning (fixed ville sloss med dra-redigeringen); hos besøkende alltid; mobil-flyten ignorerer feltet. Transformert seksjonsforfar, slettet until-id og roterte blokker håndteres. Demoblokk med festing på Om oss-siden.
- «+ NY BLOKK» DER MAN KLIKKER: dobbeltklikk på tom seksjonsflate åpner blokkmenyen ved pekeren, og blokken lander sentrert på klikkpunktet - klemt innenfor seksjonen og snappet til gridet (ren frameAtPoint i ny place.js, delt av kjerneblokk- og plugin-veien; urd-request-block fikk additivt at-felt). Hjørneknappen sentrerer som før. E2E-testen avdekket at blokker fra denne menyen aldri ble markert (samme UX-hull som paletten hadde i 0.6.25, men denne veien bygges blokken i editoren): ny urd-select-melding editor→preview fikser det, så nye blokker er markert med en gang uansett vei.
- HJELPELINJER FOR HELE SIDEN: ny knapp ved visningsvalgene i topplinjen viser stiplete aksentlinjer i alle seksjoner (sidens vertikale senter, seksjonens horisontale midte, innholdsbredde-linjer på 4 %/96 % - palettens standardmarg). Samme gjenpåførings-mønster som grid-overlegget (overlever re-render), skjult i Ren visning, finnes aldri hos besøkende; valget huskes i localStorage over økter og gjenetableres ved iframe-reload. Ny urd-show-guides-melding.
- SEKSJONSHØYDE OVENFRA (designet bekreftet av eier før bygging): nytt dra-håndtak i seksjonens TOPPKANT, bunnkant-håndtaket urørt. Minstehøyden og alle blokkenes y endres i samme grep og scrollposisjonen kompenseres, så innholdet står målt bom stille (±0 px i headless-testen); naboseksjoner røres aldri. Klemmer i ren topDrag (ny section-size.js): ingen blokk over toppen (blokker med negativ y stopper krymping helt), aldri under minstehøyden, Shift = piksel-presist. urd-section-size utvidet med additivt moves-felt; editoren bokfører høyde + alle frames atomisk, så ETT Ctrl+Z ruller alt tilbake, og mobil-tilsynet flagges.
- Meldingskatalogen i previewBridge.js og JSDoc-toppen i preview-edit.js dokumenterer alle kontraktendringene (at, moves, urd-show-guides, urd-select). SKJEMA.md + page.schema.json + demodata oppdatert i samme commit (sticky-feltet). 20 nye kontraktstester i tre nye testfiler (152 totalt). Ende-til-ende verifisert i headless Chromium: 20 sjekkpunkter grønne (fest/slipp hos besøkende, editor-gating med umiddelbar slipp ved visningsbytte, klikkplassering 0,8 px fra punktet, hjelpelinjenes livssyklus, toppkant-draget med innhold i ro og full angring). Multimarkering tas som egen påfølgende push (batch C del 2, eiers valg).

### 0.6.25 - blokkmeny for alle blokker og testrunde-fikser på batch B - 23. juli 2026
- BLOKKMENY (eiers ønske: alle blokk-innstillinger tilgjengelige ved blokken, som kalender-pluginens kildepanel): tannhjul-knapp på hver blokks verktøylinje åpner en flytende meny ved blokken med ALT fra Egenskaper-panelet. Menyen rendrer samme snippet som panelet (én kilde, kan aldri divergere), plasseres til høyre for blokken (venstre når det er trangt) og lukkes med X, Esc eller avmarkering. Ny melding urd-block-menu i preview-kontrakten. Generell fallgruve funnet og fikset: pointerdown på en verktøylinje-knapp boblet til dokumentets markeringslytter og kunne utløse en re-render som byttet ut knappen FØR click fyrte; tannhjulet fikk samme propageringsstopp som dra-håndtakene.
- CTRL+D, MÅLT ROTÅRSAK (eiers testfunn «funker ikke lenger»): en blokk lagt til via paletten ble aldri markert, verken i preview eller admin, så Ctrl+D hadde ingenting å duplisere før et ekstra klikk. placeBlock markerer nå den nye blokken umiddelbart (samme mønster som dupliser). I tillegg beholder kun ekte skrivefelt nettleserens Ctrl+D; tall, slidere og brytere (der fokus blir stående etter et panelvalg) sluker ikke lenger snarveien.
- TEKSTEDITOR-POPUPEN EIER TYPOGRAFIEN (eiers krav: ALT i popup-menyen): ny Aa-knapp i formateringslinjen åpner en typografirad med Font, Størrelse (A/S/M/L/XL + fritt px-tall), Linjeavstand og Bokstavavstand for hele feltet. FONT_STACKS/TEXT_SIZES er flyttet til delt motormodul (fonts.js). To motorfikser: raden ber eksplisitt om re-render (tekst-ekko er ellers avslått for å verne skrivemarkøren), og linjen gjenoppkobler seg til det nye tekstelementet via blokk-id når re-renderingen bytter det ut (focusout-vakten skiller «feltet byttet ut» fra «brukeren forlot feltet»).
- IKON-FIKSER (eiers testfunn): (1) tegnede ikoner er nå SENTRERT i rammen - rotårsak: SVG-elementet overstyrte .urd-icon-sentreringen med fast størrelse; ytterelementet beholder sentreringen og et indre element bærer størrelsen (målt i headless: lik luft på alle sider). (2) Farge er en full fargevelger (temafarger + egen farge; color-feltet kan nå være token ELLER hex, resolveColor skiller på formen). (3) Velgerens ikoner og tegn var SVARTE på mørk meny (knappens standardfarge); nå lyse. (4) Nylige står øverst i velgeren, uten løsrevne mellomtitler. (5) Swatchen viser det som faktisk er valgt, også eget opplastet bilde; å velge tegn/ikon i menyen bytter bort fra bildet.
- «Valgområdet vokser» fra testrunden ble IKKE reprodusert: ikon-, tekst-, galleri- og kalenderblokker målt over gjentatte prop-endringer i headless Chromium, alle stabile; trolig var det sentrerings-buggen som så ut som vekst. Nytt funn punktet i TESTRUNDER.md ber om konkret oppskrift om det gjenoppstår.
- Hele runden er verifisert i headless Chromium (14 sjekkpunkter: markering etter palett, Ctrl+D i tre fokus-tilstander, sentrering, cellefarge, Nylige-plassering, blokkmenyens innhold/posisjon/lukking, typografiradens virkning og overlevelse) i tillegg til bygg/tester/validering. SKJEMA.md oppdatert (color token/hex); 132 kontraktstester.

### 0.6.24 - M6 batch B: ikonbibliotek, tegnmeny i teksteditoren, linje- og bokstavavstand - 23. juli 2026
- IKONBIBLIOTEK (ny motormodul icons.js): 51 håndtegnede SVG-ikoner i fem kategorier (Sosiale medier med Facebook, Instagram, X, LinkedIn, YouTube, TikTok, WhatsApp, Snapchat, Pinterest, Spotify, Discord og GitHub; Kommunikasjon; Sted og tid; Symboler; Piler), farget med currentColor og skarpe i alle størrelser (i motsetning til emoji). Ikon-blokken fikk det additive feltet `icon` (engelsk id, datakontrakt); prioriteten er eget bilde > tegnet ikon > glyf, og en ukjent id faller stille tilbake til glyfen, så en side laget i nyere Urd aldri knekker i eldre motor. Velgeren i Egenskaper er tegnvelgeren utvidet med ikonkategoriene øverst; med tegnet ikon valgt byttes fritekstfeltet ut med «Fjern tegnet ikon» (panelregelen: kun relevante innstillinger). Alle 51 ikonene er verifisert visuelt i headless Chromium (0.6.22-lærdommen), ikke bare resonnert frem.
- TEGNMENYEN INN I TEKSTEDITOREN (M6 batch B): tegnsettet og Nylige-logikken er flyttet til en delt motormodul (glyphs.js), så admin-velgeren og teksteditor-linjen bruker samme utvalg og deler Nylige-historikk (samme localStorage-nøkkel, samme opprinnelse). Formateringslinjen fikk en smilefjes-knapp (tegnet SVG, aldri emoji i chrome) som åpner en rullbar tegnrad i linjen med Nylige øverst; klikk setter tegnet inn ved markøren via insertText, som utløser input-hendelsen i text.js og lagrer gjennom den vanlige utkastflyten. Raden bygges først når den åpnes (flere hundre knapper).
- LINJE- OG BOKSTAVAVSTAND per tekstfelt (funksjonskartet, eiers valg): additive felt lineHeight (enhetsløs, 1-2,5, skalerer med skriftstørrelsen) og letterSpacing (px, kan være negativ for tettere tekst) med sliders og «A = arv»-knapp i Egenskaper, samme mønster som Størrelse. 0/utelatt = arv, så eksisterende sider rendres uendret. Blir teksten høyere enn rammen, vokser rammen ved neste skriving i feltet (samme adferd som font-størrelse).
- SKJEMA.md dokumenterer de nye feltene (schema/ er urørt: blokk-props er frie per page-skjemaet). 7 nye kontraktstester (132 totalt) i ny ikoner.test.mjs: kategori/bibliotek-samsvar, id-format med ankret regex, norsk etikett + SVG-kropp, trygg SVG-bygging (element-hviteliste, ingen hendelsesattributter/lenker) og Nylige-logikken. Testrunde-batch 0.6.24 i TESTRUNDER.md.

### 0.6.23 - sikkerhetsoppdatering i editorens låsefil - 23. juli 2026
- fast-uri løftet 3.1.3 til 3.1.4 i editor/package-lock.json (GHSA-v2hh-gcrm-f6hx, vertsforvirring via omvendt skråstrek). Kun utviklingsavhengighet via ajv (skjemavalideringen); påvirker verken den bygde editorbundelen eller publiserte sider. (Innslaget ført inn i etterkant sammen med 0.6.24: hvert push-nummer skal ha sitt eget innslag.)

### 0.6.22 - sidestilt-oppfølging, målt fargevelger-fiks og panelregler - 23. juli 2026
- SIDESTILT-FIKSER (eiers testfunn): (1) menypunktene havnet NEDERST i kolonnen som standard - rotårsak: Menyplassering gjenbrukte topplinje-feltet nav.layout (left = nederst), og siden sto med layout: left; kolonneplasseringen er nå et eget additivt felt nav.style.sidePlacement (top/middle/bottom, øverst som standard) helt frikoblet fra topplinjen, med kontraktstest som låser at layout aldri mer påvirker kolonnen. (2) Hover i kolonnen: trekkspillene åpner igjen på hover, men lukkes ALDRI per punkt (lukking kortet ned kolonnen under pekeren); alle lukkes samlet når pekeren forlater menyen, klikk virker som før. (3) Undermeny-design filtreres til de som gir mening i kolonnen (Standard/Pille-punkter/Understrek-liste; kort-ramme, ren flate og utfall er meningsløse i et trekkspill), og pille-/linje-designene fikk kolonneregler så de faktisk slår gjennom der.
- FARGEVELGEREN, MÅLT ROTÅRSAK: etter to feilslåtte fikser ble geometrien målt i headless Chromium: selve boksen sto riktig (innenfor panelet), men INNHOLDET fløt 59px ut av den - .cp-pop er display: grid uten definert kolonne, og den implisitte kolonnen max-content-dimensjoneres av hex-feltets naturlige bredde (284px i en 236px-boks). Fiks: grid-template-columns: minmax(0, 1fr) (panel-body-mønsteret) + klemmen mot panelets høyrekant uten bredde-vakten som slo den av. Etter-måling: null elementer utenfor boksen.
- UNDERMENY-SKYGGEN: kortets (og pille-punktenes) skygge blødde oppover og la et mørkt bånd på baren (samme fargebug som utfall hadde); skyggene faller nå kun nedover (negativ spredning).
- PILLE-PUNKTER: nytt «Punktfarge»-valg (nav.style.subPillColor, additivt; standard er undermeny-flaten), vises kun når Design = Pille-punkter.
- BURGER I PREVIEW: hos besøkende følger burgeren vindusbredden, men i editorens desktop-preview eier viewport-togglen body.urd-mobile (som også skjuler strukturverktøyene) - menyen fikk derfor en egen mobilklasse (urd-nav-mobile på nav-elementet, fra faktisk bredde via matchMedia) med tvilling-selektorer i samme regelblokker, så nav-en blir burger på smale vinduer også i desktop-preview mens strukturverktøyene består.
- NY PANELREGEL (eiers regel, nå i CLAUDE.md): innstillinger vises KUN når de er relevante for gjeldende valg, og etiketten sier hva innstillingen faktisk styrer. Første anvendelse: hover-effektfargen heter nå Strekfarge/Pillefarge/Glødfarge etter valgt Lenke-hover og skjules for Standard og Løft (uten glød).
- PANEL-OMLEGGING (eiers ønsker): «Variant» heter nå «Navigasjonsmeny»; Gjennomsiktighet og Uskarphet står rett under den (etter variantvalgene); Menyplassering og Lenke-hover (med Glødstyrke og hover-fargene) rett under Størrelse; Tekstfarge sammen med fargevalgene rett etter Bakgrunnsfarge.
- Nye felt er additive (schema + SKJEMA.md i samme commit); 1 ny kontraktstest (125 totalt). Lærdom festet: UI-bugs som overlever et fikseforsøk skal måles i headless-nettleser, ikke resonneres frem.

### 0.6.21 - nav-testrunden: seks fikser og nav-utseende-pakken - 23. juli 2026
- FARGEVELGER-FIKSER (eiers testfunn): (1) velgeren lukket seg ved klikk inne i popoveren - rotårsak: velgerne ligger i `<label>`, og nettleseren videresender klikk på ikke-interaktive flater (selve fargeflaten) som klikk på fargeruten, som toggler velgeren lukket; popoveren stopper nå label-videresendingen. (2) Klikk i forhåndsvisnings-iframen lukket den IKKE (når aldri editorens document) - window-blur lukker nå. (3) Popoveren hang ut over panelet og boksen omsluttet ikke innholdet - border-box på boksen (plasseringsmatematikken regnet med 236px mens boksen reelt var 256px) og plasseringen klemmes nå innenfor panelet. Gjelder alle paneler som bruker komponenten.
- LOGO-FIKS (regresjon fra eiers testrunde): stort logobilde vokste nav-baren igjen. Kalibreringen (negativ blokkmarg) var en fast verdi for 2rem-logo; nå skalerer den med bildehøyden (via --urd-logo-size), så bidraget til barhøyden er konstant og baren aldri vokser, i alle varianter og størrelser.
- UNDERMENY-FIKSER: (1) posisjonen ankres nå under selve baren (top: 100 % + padding) i stedet for under det midtstilte menypunktet, så den aldri legger seg over nav-teksten; (2) bakgrunnsbildet arves ikke lenger inn: undermenyen og mobilpanelet får en egen flate-var (--urd-nav-sub-bg, kun fargesløret via ny ren navSubSurface), og nytt felt nav.style.subImage skrur bildet på (av som standard).
- SIDESTILT-FIKSER: på trange vinduer (under 900px) rendres kolonnen nå som en VANLIG topplinje med horisontale menypunkter (effektiv variant bar i nav.js, re-render ved kryssing), og burgeren kommer først ved mobil-breakpointet - uavhengig av editorens viewport-valg, så det virker også i preview; undermenyene i kolonnen er klikk-styrte (hover-lukking kortet ned kolonnen under pekeren og ga feilklikk); Klistrete meny-valget er skjult for sidestilt (kolonnen står alltid fast); bildeutsnitt-slideren som ikke monnet i kolonnen fikk følge av «Bildeutsnitt (bredde)» (nytt felt imageX; høy smal kolonne panorerer i bredden, lav bred stripe i høyden).
- SIDESTILT-UTVIDELSER (eiers valg): Menyplassering betyr nå vertikal plassering i kolonnen (right = øverst/standard, center = midt på, left = nederst; etikettene i panelet følger varianten), ny «Tekstjustering» (nav.style.sideAlign) justerer punktene, undermeny-pilen følger justeringen (høyrestilt tekst = pil på venstre side; midtstilt = pilen står absolutt og skyver ikke teksten), og kolonnebredden justeres ved å DRA i kolonnekanten i preview (nav.style.width 180-400px, ny urd-nav-width-melding, klemmes av ren clampSideWidth).
- NAV-UTSEENDE-PAKKEN (eiers ønsker): fire størrelser (nav.style.size sm/md/lg/xl; md = dagens, padding + skrift skalerer og logokalibrering/undermeny-anker følger med), hover-farger (nav.style.hoverColor/hoverTextColor med fulle fargevelgere; understrek bruker den ene på streken og den andre på teksten), ny hover-stil «Løft» uten glød (lift-plain) og «Glødstyrke»-slider for «Løft med glød» (nav.style.hoverGlow; gløden ligger bak teksten), ny variant «Flytende (firkant)» (floating-square: pillen uten avrunding, samme glød/luft-valg), og undermeny-design (nav.style.subStyle: kort/ren flate/pille-punkter/understrek-liste/utfall i full menybredde) med kolonner (nav.style.subColumns 1-4: punktene i rutenett, 2x2, 2x3 osv.).
- PANEL-OPPRYDDING (eiers regel, nå i CLAUDE.md): Variant og Størrelse står øverst i Utseende med variantspesifikke valg RETT UNDER Variant, undermeny-innstillingene fikk egen «Undermeny»-gruppe mellom Utseende og Menypunkter, «Bakgrunn» ble «Bakgrunnsfarge», «Dekkevne» ble «Gjennomsiktighet» (0 % = tett flate; lagret verdi er fortsatt bgOpacity), og forklarende prosa i panelene er fjernet til fordel for tooltips.
- Alle nye felt er additive (schema + SKJEMA.md oppdatert); 7 nye kontraktstester (124 totalt), inkl. hviteliste-vern for klassenavn fra frie strenger.

### 0.6.20 - galleri-blokk, lightbox, hero-galleri og preset-miniatyrer (M6 batch A) - 23. juli 2026
- GALLERI-BLOKK (funksjonskartets C2, eiers valg): ny kjerneblokk `galleri` med tre visninger etter Squarespace-modellen (visningen er en prop, ikke egne blokktyper): rutenett (kolonner 1-6 + luft, autovekst som samling-blokken), karusell (sidescroll med snap og pilknapper) og lysbilde (automatisk fremrykk med piler og prikker). Bildene deler bildeblokkens ikke-destruktive stilvokabular (fit/fokus/zoom/filtre) og redigeres per flis i den felles bildeeditoren; flervalgs-opplasting i Egenskaper og «Galleri med bilder» i paletten (hele bunken er ETT angre-steg). Ny «Galleri»-seksjonsmal under Grunnleggende.
- LIGHTBOX: fullskjermvisning ved klikk hos besøkende (galleri-blokken har den på som standard; bildeblokken fikk tilvalget «Fullskjerm ved klikk»). Forrige/neste, piltaster, Esc, bakgrunnsklikk, scroll-lås, fokus-retur og forhåndslasting av nabobilder; lastes først ved klikk (dynamic import), så besøkende som aldri klikker laster ingenting. Eiers valg: kan prøves i editorens Ren visning - avgjørelsen tas ved klikk (chrome på gir bildeeditoren, Ren visning gir lightboxen), så ingen re-render trengtes. Lenke på et bilde vinner alltid over lightboxen.
- HERO-GALLERI: nytt bakgrunnslag `bildegalleri` som blar gjennom flere bilder med myk krysstoning; neste bilde forhåndslastes FØR toningen, ødelagte bilder hoppes over, og laget degraderer til statisk bilde ved ett bilde eller redusert bevegelse. Fokuspunkt per bilde pluss sekunder/overgang/uskarphet/styrke i bakgrunnseditoren, med flervalgs-opplasting.
- PRESET-MINIATYRER (eiers valg: auto-genererte skisser): «+ Ny seksjon»-velgeren viser en skjematisk SVG-skisse per mal, generert fra malens faktiske blokker og bakgrunn i sidens temafarger (preset-thumb.js). Ren strengbygging uten DOM; kun validerte farger slipper inn i SVG-en (ankrede regexer), og en feilende plugin-mal faller stille tilbake til tekstvalg.
- Motorens FØRSTE timere (lysbilde + bildegalleri) fikk et felles vern: selvrydding når verten forsvinner fra DOM (re-render-churn i preview), pause i skjulte faner, stillstand ved redusert bevegelse, og i preview rykker lysbildet kun frem i Ren visning.
- Ren logikk i ny galleri-model.js (indeks-stepping med rundgang, autoplay-vilkår, intervall- og kolonneklem) og preset-thumb.js; 11 nye kontraktstester (117 totalt). SKJEMA.md dokumenterer galleri-blokken, bildeblokkens lightbox-felt og bildegalleri-laget; demosiden (hjem) fikk en galleri-seksjon med tre bilder. Testrunde-batch 0.6.20 i TESTRUNDER.md.

### 0.6.19 - funksjonskartet: helhetlig sammenligning mot andre byggere - 23. juli 2026
- Nytt dokument docs/FUNKSJONSKART.md (eiers bestilling): helhetlig funksjonssammenligning mot Squarespace, Wix/Wix Studio, Webflow, Framer, Carrd, Google Sites, Shopify, WordPress/Gutenberg (+Elementor), Publii, Ghost, Blogger og GrapesJS/Silex, over 12 kategorier (teksteditor, elementer, layoutverktøy, media, nav, SEO, skjema, CMS, butikk, animasjoner, maler, diverse). Konkurrentfakta fra kildebelagt web-research (24 kryssverifiserte funn + 97 kildefestede påstander; ærlig kildemerking der dekningen er tynn), diffet mot Urds fulle inventar og klassifisert A (har) / B (planlagt) / C (mangler, anbefalt) / D (bevisst utenfor scope), med 17 prioriterte anbefalinger.
- Eiers valg ført inn i backlogg og veikart: SEO-GRUNNPAKKEN til v0.7 (per-side meta/og-felter, sitemap, robots, 404, canonical - Urds største hull mot alle konkurrentene), galleri-blokk/linjeavstand/nav-scroll/parallax+stagger/kontrast-varsel til M6, RSS/skjemafelt/video-bakgrunn/CSV+tabell til v0.7, innslagssider og mediebibliotek til v0.8, analytics-plugin til M8, redirects til M9-avklaringene, småblokker til Forslag og Ideer.
- Kartet bekreftet også styrker: mobil-tilsynet matcher Wix Studios responsive-skanner, lys/mørk-bryteren er foran Wix Studio (åpent ønske der), personvern-som-standard fjerner cookie-banner-behovet, git-eide data uten tak står seg mot Webflows 10 000-innslagsgrense, og migreringskontrakten har ingen motsvarighet.
- README-tabellen fikk FUNKSJONSKART-raden; VEIKART v0.7/v0.8 synket med backloggen.

### 0.6.18 - sidestilt meny, bildekontroller i nav, pille-luft og «Tekst på aksent» - 22. juli 2026
- SIDESTILT MENY (eiers ønske): to nye varianter i Nav → Utseende - «Sidestilt venstre» og «Sidestilt høyre». Fast kolonne (250px) langs kanten med logo øverst, menypunktene som kolonne, undermenyer som trekkspill og lys/mørk-bryteren nederst; innholdet flyttes med body-padding på samme side. På mobil faller sidestilt tilbake til topplinje med burger. Variantbytte rydder alltid vert- og body-klassene (ny ren hostClasses() i nav-model.js).
- Bildekontroller for nav-bakgrunnsbildet (eiers ønske om enkel bildeeditor): «Bildestyrke» (toner bildet mot bakgrunnsfargen med eget lag under fargesløret) og «Bildeutsnitt (høyde)» (hvilken del av bildet stripen viser, 0 = toppen, 100 = bunnen). Additive felt nav.style.imageOpacity/imageY; ren strengmatte i navSurface med tester.
- «Luft over pillen»: ny bryter for den flytende varianten (på som standard); av legger pillen helt i toppen (nav.style.topGap: false).
- Ny temafarge «Tekst på aksent» (accent-text, eiers valg etter fargegjennomgang): styrer teksten oppå aksentflater (primærknapper, uthevings-chipen). Uten tokenet brukes bakgrunnsfargen som før, så eksisterende paletter rendres uendret; auto-inverteren i alt-temaet tar den med. Dempet tekst og kantlinjer avledes fortsatt automatisk (opacity/color-mix) - settet holdes bevisst lite.
- 106 kontraktstester totalt.

### 0.6.17 - lys/mørk-bryter, flytende over hero, testrunder til egen fil - 22. juli 2026
- LYS/MØRK-BRYTER (eiers ønske fra testrunden): siden kan nå ha to paletter. Tema-panelet fikk en «Lys/mørk-bryter»-seksjon: «+ Lag alternativt tema» oppretter motstykket ferdig utfylt med inverterte utgaver av dagens farger (lyshet snus, fargetone består), som eieren justerer med de vanlige fargevelgerne; «Foreslå på nytt (inverter)» regenererer, og «Hovedtemaet er: Lyst/Mørkt» settes med theme.scheme. Finnes alt-temaet, viser menyen en sol/måne-knapp: første besøk følger prefers-color-scheme, aktivt valg huskes i localStorage. Additive felt theme.scheme + theme.alt.tokens (utelatte verdier arver hovedtemaet gruppevis); ren modus-/fletlogikk i theme.js med 7 nye kontraktstester.
- Flytende meny svever nå OVER innholdet (eiers testfunn: pillen reserverte en stripe i full bredde med tomrom rundt): verten tas ut av flyten, hero-en starter øverst bak pillen (fixed ved sticky, absolute ellers), og stripene ved siden av pillen slipper klikk gjennom til innholdet bak.
- Gløden rundt pillen er fjernet som standard (eiers testfunn: den gamle skyggen ble en hvitaktig glød på mørkt tema) og erstattet av tilvalget «Glød rundt pillen» i Nav → Utseende (nav.style.glow, aksentfarget).
- Nav-en fikk en verktøy-klynge ytterst (bryter + burger) med auto-marg-basert plassering, så menylisten står riktig i alle tre oppsettene med og uten bryter.
- Testrundene bor nå i egen fil: docs/TESTRUNDER.md (eiers ønske) - batchene flyttet ut av BACKLOG.md med pekere oppdatert i CLAUDE.md; README-en fikk de nye dokumentene i tabellen og et avsnitt om plugin-økosystemet.
- CI-byggfiks: bygg-samsvar-sjekken feilet fordi den committede bundelen var bygget med svelte 5.56.5 mens låsefilen pinner 5.56.7 (avhengighets-drift i lokal npm-cache). node_modules reinstallert med npm ci og bundelen rebygd; lærdommen er festet i CLAUDE.md (diff i rammeverkskode = sjekk svelte-versjonen mot låsefilen).
- 102 kontraktstester totalt.

### 0.6.16 - M5 fullført: flytende pille, hover-stiler og bakgrunnsbilde i menyen - 22. juli 2026
- FLYTENDE meny (nav.variant): ny «Flytende (pille)»-variant i Nav → Utseende - menyen blir en løsrevet, avrundet pille med luft mot kantene, skygge og maks-bredde. Klistret-innstillingen virker som før (pillen fester med toppluft); mobilpanelet legger seg som eget lag under pillen i stedet for å arve fasongen.
- HOVER-STILER for menylenkene (nav.style.hover): «Understrek» (glidende strek under lenken), «Pille» (avrundet aksentbakgrunn) og «Løft med glød» (svakt løft med glød i aksentfargen), i tillegg til standarden. Alle gjelder også tastaturfokus (:focus-visible) og respekterer prefers-reduced-motion.
- BAKGRUNNSBILDE i menyen (nav.style.image): last opp i Nav → Utseende (samme webp-flyt som logoen, materialiseres til media/ ved publisering). Bakgrunnsfargen og dekkevnen legger seg som slør over bildet, så menyteksten alltid har flate å stå på; undermenyene og mobilpanelet deler samme flate via --urd-nav-bg.
- Alle tre feltene er additive med standarder (bar/standard/uten bilde lagres ikke i fila), så eksisterende sider rendres uendret. 4 nye kontraktstester (93 totalt). Dette fullfører M5 Nav-omlegging.
- Endringsloggen bor nå i docs/ sammen med resten av dokumentasjonen (eiers ønske); referansene i CLAUDE.md, BACKLOG.md og UTVIKLING.md er oppdatert. En eldre løsrevet kopi som lå i docs/ er erstattet av den ekte loggen.
- Ny ADR-0010: nav-undermenyer følger WAI-ARIA-mønsteret disclosure navigation, aldri role="menu" (fester M5-valgene: lenke + pilknapp for punkter med eget mål, interaksjonskontrakten, ett nivå). CLAUDE.md oppdatert med ADR-en og to nye motor-lekser fra M5 (pointerType-vakt for hover på hybride enheter, AbortController-opprydding for re-rendrede motorkomponenter), pluss presiseringer i skrivestil- og push-reglene.
- Buggjakt før push (6 funn fikset): (1) urd-site-handleren fikk samme amputert-vern som boot(), så et utkast uten nav/site aldri fryser previewen; (2) nav.style.image valideres nå med samme ankrede regex som favicon-flyten før den limes inn i CSS-url() (ugyldig bilde forkastes, sløret består); (3) undermenyens backdrop-filter: inherit virket aldri (arver fra li-en, ikke nav-en) - blur går nå via arvbar custom property --urd-nav-blur; (4) flytende pille setter --urd-nav-pad i stedet for padding-block, så logobildets negative margin-triks holder seg kalibrert og store logoer ikke stikker ut; (5) nav-en fikk egen stacking context (z-index), så nedtrekk/mobilpanel ligger over etterfølgende innhold også med sticky av; (6) sletting av en side som et menypunkt MED undermeny pekte på, sletter ikke lenger hele undermenyen - punktet blir en ren åpner og barna består. Pluss: hover-pillens 999px-radius ligger ikke lenger på fullbredde mobilrader. Ny kontraktstest for bildevernet (94 totalt).

### 0.6.15 - M5: dropdown-menyer, tastatur/ARIA og mobilmeny - 22. juli 2026
- DROPDOWN-MENYER i nav-en: menypunkter kan nå ha undermeny (ett nivå, additivt `children`-felt i site.json). Et punkt med både egen side og undermeny rendres som lenke + egen pilknapp (siden er alltid nåbar, eiers valg); et punkt uten eget mål blir en ren åpner der hele punktet åpner undermenyen. På enheter med ekte peker åpner hover (lukker med ~250 ms forsinkelse), klikk på pilen toggler, og kun én undermeny er åpen om gangen; touch får kun klikk.
- Tastatur/ARIA etter WAI-ARIA-mønsteret «disclosure navigation» (ikke menubar): ekte knapper med aria-expanded/aria-controls, naturlig Tab-rekkefølge, Escape lukker og refokuserer knappen som åpnet, fokus ut av punktet lukker undermenyen, klikk utenfor lukker alt, og synlig :focus-visible-ring på alt interaktivt i menyen.
- MOBILMENY (helt ny - listen bare brøt om før): burger-knapp som åpner menylisten som kolonnepanel under nav-en, med undermenyene som innrykkede trekkspill. Breakpointet følger site.json (`breakpoints.mobile` via body.urd-mobile hos besøkende), så editorens mobilpreview viser burgeren automatisk. Ikke-modal disclosure: ingen fokusfelle eller scroll-lås, panelet scroller selv.
- Editor: Menypunkter-seksjonen i Nav-panelet har fått «+»-knapp per punkt for undermenypunkter, innrykkede barnerader (mål/ekstern lenke/rekkefølge/fjern som før), og målvalget «Ingen lenke (kun åpner undermenyen)» for punkter med undermeny. Sletting av en side rydder nå også undermenypunkter som pekte på den.
- Teknisk: nav-ens rene logikk (oppslag, kind-klassifisering link/split/toggle, utseende-beregning) er trukket ut i ny nav-model.js med 14 kontraktstester (89 totalt). Nav-bakgrunnen ble CSS-varen --urd-nav-bg, så undermenyer og mobilpanelet arver eierens farge/dekkevne uten ekstra oppsett. renderNav kobler fra forrige renderings lyttere med AbortController, så gjentatte utkast-oppdateringer fra editoren aldri stabler lyttere.

### 0.6.14 - kart: adressesøk + virker ut av boksen; Egenskaper-foldemeny ryddet - 20. juli 2026
- Kart adressesøk: «⚙ Sted» godtar nå en vanlig adresse («Storgata 1, Oslo»), koordinater ELLER en OSM-lenke. En adresse slås opp via ny samme-origin-proxy (functions/api/geocode → OpenStreetMaps Nominatim, med identifiserende User-Agent og cache); koordinatene lagres i blokken, så besøkende laster kartet direkte uten oppslag.
- Kartet virker ut av boksen: det brukne bildet var CSP som blokkerte OSM-iframen. https://www.openstreetmap.org er lagt i Urds standard _headers frame-src (personvennlig, shippet reference-plugin), så kartet vises etter publisering uten manuell CSP-jobb. Manifestets csp-erklæring og degraderingen består for andre hoster.
- Kart-degradering + bugfiks: hvis kartet likevel blokkeres får besøkende en «Åpne kartet på OpenStreetMap»-lenke i stedet for et brukket bilde (editoren får instruksen). Rettet samtidig en variabel-skygging innført i 0.6.12 (det indre `host` overskrev DOM-elementet, så CSP-vokteren ville kastet feil ved et brudd).
- Egenskaper: «Plassering, lag og rotasjon»-foldemenyen er nå en enkel seksjonsoverskrift i stedet for en chip/pille (eiers ønske).
- Ny CLAUDE.md i repo-roten: fester de varige reglene (invarianten, verifisering, arbeidsflyt, skrivestil, motor-lekser) og peker til ADR-ene, så en Claude Code-økt på en hvilken som helst maskin følger dem automatisk.

### 0.6.13 - CI-fiks: plugin-seksjonspresets manglet id - 20. juli 2026
- KRITISK CI-/datafiks: plugin-seksjonspresetene (Hva skjer / Kontaktskjema / Finn oss) returnerte seksjonen UTEN id (kjernepresetene setter den i section()-factoryen, plugin-presetene glemte den). En publisert «Hva skjer»-seksjon havnet dermed uten id i hjem.json, som er ugyldig mot page-skjemaet og felte CI-valideringen. Fikset i tre lag: de tre plugin-presetene setter nå id selv; handleAddSection tildeler defensivt en id om en seksjon mangler den (verner ALLE plugins, også fremtidige); og den allerede publiserte id-løse seksjonen i hjem.json fikk en id.

### 0.6.12 - CodeQL #11: eksakt vert-sjekk i kartets CSP-vokter - 20. juli 2026
- CodeQL #11 (Incomplete URL substring sanitization) lukket: kart-pluginens CSP-brudd-vokter sammenlignet den blokkerte URL-en med en delstreng («openstreetmap.org»), som også ville slått til på f.eks. openstreetmap.org.example.com. Den parser nå URL-en og sammenligner verten eksakt.

### 0.6.11 - editor-UI-sveip: bildeeditor, ikon-editor, Egenskaper-rydding, dra-teleport-fiks - 20. juli 2026
- Dra-teleport-fiks (KRITISK): å dra en datablokk (Kalender/Kart/Skjema/Samling) teleporterte den tilbake til opprettelsesposisjonen. Autoveksten sendte hele framen fra et ctx.section-SNAPSHOT (med gammel x/y) som urd-move, og handleMove erstatter hele framen. Ny høyde-kun-melding (urd-grow) oppdaterer bare h, aldri x/y. Dette var etter alt å dømme også publiserings-shiften (blokkene falt tilbake til opprettelses-x=6/25, altså «inn mot midten/vekk fra kanten» ved re-render etter publisering).
- Bildeblokk: full moderne editor og avrundings-fiks. Bildet ligger nå i en ramme som KLIPPER, så avrunding vises alltid (også med «Vis hele bildet»; før lå den på img-boksen, som med contain er større enn det synlige bildet). Dobbeltklikk/blyant åpner hele den delte editoren: fokuspunkt med tredelingsgitter, ZOOM (beskjærer inn mot fokuspunktet), filtre med gråtone/nullstill, tilpasning og avrunding. Render, editor og Egenskaper deler nå én applyImageStyle, så de aldri drifter (kilden til «redigeringer går ikke til nettsiden»). Zoom lagt til i Egenskaper.
- Tema: full ikon-editor (IconEditor.svelte). Last opp et bilde, så åpnes editoren: dra for utsnitt, zoom, lysstyrke/kontrast/metning, gråtone og nullstill; «Bruk» baker et kvadratisk 128px webp-favicon via canvas (samme coverDraw til forhåndsvisning og eksport). Blyantknappen redigerer et eksisterende ikon; fjern-knapp og forhåndsvisning fantes fra før.
- Egenskaper-rydding: X/Y/Bredde/Høyde/Lag/Rotasjon + «Skjul i mobil» ligger nå i en sammenleggbar «Plassering, lag og rotasjon» nederst i panelet, for ALLE blokktyper (de kan endres direkte på blokken).
- Kart/skjema verktøy: «⚙» og «?» lå oppå hverandre og kunne ikke nås; de ligger nå side om side i én flex-rad med hover-bro (samme mønster som kalenderen).
- «+ Ny blokk»-menyen: «Månedskalender» kortet til «Måned» (og «Neste arrangement» til «Neste»), og menyknappene bryter lange ord i stedet for å flyte ut av cellen.

### 0.6.10 - M4: skjema- og kart-referanseplugins + mørk-linje-fiks - 20. juli 2026
- SKJEMA-pluginen (plugins/skjema): kontaktskjema etter ApeironLF-modellen. Ekte form med redigerbare felt (standard Navn/E-post/Melding; legg til/endre/fjern, velg type og påkrevd), e-postvalidering med inline feil, og honeypot mot bots (utfylt skjult felt lar som om det gikk bra uten å sende). To sendemåter i «⚙ Skjema»: mailto (standard, null oppsett, åpner besøkendes e-postklient med ferdig e-post) og eksternt endepunkt (eierens Apps Script/Pages Function via fetch JSON, krever connect-src-opt-in; degraderer rolig når blokkert). Besøkende-input blir aldri HTML. Ren logikk (validering, mailto, payload) i form.js med 8 kontraktstester. «Kontaktskjema»-preset.
- KART-pluginen (plugins/kart): personvennlig OpenStreetMap-innbygging (OSMs egen iframe, ingen sporing eller tredjeparts-tiles). Eier limer inn koordinater eller en OSM-lenke i «⚙ Sted», stiller zoom og høyde. Ren OSM-logikk (tolke koordinater/lenke, bygge embed-URL og «større kart»-lenke) i osm.js med 8 kontraktstester. «Finn oss»-preset. Demonstrerer CSP-opt-in-flyten (ADR-0006) ende til ende: manifestet deklarerer csp.frameSrc (openstreetmap.org), Plugins-panelet viser eieren den eksakte _headers-linjen, og blir kartet blokkert forklarer blokken selv (via securitypolicyviolation) hvilken linje som mangler.
- Begge pluginene aktivert i plugins.json, så de dukker opp i «Plugins»-seksjonene i menyene sammen med kalenderen. 75 tester totalt (16 nye).
- Mørk-linje-fiks: på skjermer med brøkskalering runder 100vh av og etterlater en sub-piksel stripe nederst der lerretsbakgrunnen (bak body) vises; siden den var mørkere enn footeren/panelene ble det en tynn mørk linje på både admin og siden. html-lerretet er nå surface-farget så stripen matcher, i den delte base.css.
- Fargevelger-fiks: admin-fargevelgeren (Tema, Nav, bakgrunn, oppsett) lukket seg ved innvendig klikk fordi en gammel rull-lukker fanget panelbakgrunnens fokus-rull; den er fjernet (lerretets velger hadde den alt fjernet), så velgeren lukkes kun ved klikk utenfor, Escape eller ved å klikke fargeruten igjen.

### 0.6.9 - M3: kalender-referansepluginen + to nye UI-regler (ADR-0008/0009) - 19. juli 2026
- KALENDEREN: ny referanseplugin (plugins/kalender, erstatter eksempel-kalenderen; samme blokktype med migrering, så gamle blokker løftes rent). Avhengighetsfri iCal-parser med gjentakelses-ekspansjon (FREQ daglig/ukentlig/månedlig/årlig, INTERVAL, COUNT, UNTIL, BYDAY inkl. «andre tirsdag», BYMONTHDAY, EXDATE, RECURRENCE-ID-overstyringer, avlysninger; tidssoner DST-korrekt via Intl) i ren modul med 12 kontraktstester (59 totalt). Fire visninger: liste med dato-badge, kort, månedskalender med navigasjon og «neste arrangement»-panel med nedtelling. Konvensjoner: «Kategori: Tittel» gir filtrerbare kategori-chips (kan skrus av), påmeldingslenke i beskrivelsen blir «Meld deg på»-knapp, «Abonner» gir webcal-lenke og «Legg til i Google» for Google-kilder, alt med presise tooltips. Kilder settes i previewen («⚙ Kilder», vises ved pek, lukkes ved klikk utenfor): iCal-URL, webcal:// eller ren Google-kalender-id; visning som temastyrte segmentknapper; maks antall kun der det gjelder (liste/kort). «Hva skjer»-seksjonsmal. Eksempeldata i preview uten kilder; rolige tomtilstander hos besøkende.
- Feed-proxy (functions/api/ics): nettlesere kan ikke hente feeder direkte (CORS + CSP), så alt går samme-origin uten CSP-unntak. Vern mot åpen proxy: kun https, vert-allowlist (calendar.google.com + eierens ICS_HOSTS-miljøvariabel), omdirigeringer valideres mot samme liste, tidsavbrudd, størrelsestak, delt cache.
- PLUGIN-TESTEN (eiers valg): kalenderen er egentlig kjernefunksjonalitet, men leveres som plugin for å teste plugin-systemet i full bredde; funnene er loggført i BACKLOG (M3) og innbaking vurderes ved fasegaten. Testen ga: «Plugins»-seksjoner i alle tre menyene (+ Ny blokk, + Ny seksjon, Blokker-panelet; registrene merker fromPlugin, previewen melder blokkene til admin over broen), og et generisk variants-felt på blokk-defs som gir foldemeny i blokkmenyene (kalenderens fire visninger).
- ADR-0008 HJELPECHIP-REGELEN: alt med spesialfunksjoner skal ha en «?»-chip som åpner et vedvarende hjelpekort (står til klikk utenfor/Escape), én linje per funksjon; felles hjelper i engine/hint.js (selvforsynt stil), etterlevd av samling- og kalenderblokken fra start; kravet står i plugin-README og brukerveiledningen.
- ADR-0009 TEMASTYRT UI-REGELEN: native select-popuper følger OS-temaet og blir uleselige i mørkt UI (kan ikke repareres med CSS; color-scheme overstyres av enkelte nettlesere). Native select er nå forbudt i alt redigerings-UI: Dropdown.svelte i admin (alle 27 select-er byttet), createDropdown i engine/dropdown.js på lerretet (nivåvelgeren i tekstlinjen byttet; stjeler ikke fokus, så markeringen står), segmentknapper for små valgsett.
- Menyer og affordanser: «+ Ny blokk» omorganisert med Former i utfoldbar undermeny (og overflow-fiks); blokkverktøylinjen står ALLTID over blokken (inni blokken-flyttingen ved seksjonstopp fjernet, seksjoner klipper aldri); «?»/«⚙ Kilder» ligger over markeringsrammen ytterst til høyre med usynlig hover-bro; «+ Ny seksjon»-baren ligger under aktiv blokk-chrome men over blokker i ro, og seksjonslinje-grepet er fortsatt alltid øverst.

### 0.6.8 - backlog-sveip: fire livskvalitetsfikser + blokk-flyt - 19. juli 2026
- Temafølgende utheving og tekstfarge: fargen TEMA-knappene i tekstlinjen skriver byttes nå til var(--urd-color-<token>) i feltet, så uthevinger og temafarget tekst skifter live når temaet endres (før frøs aksentfargen som hex i teksten). Egne farger forblir frikoblet hex, med vilje.
- Ctrl+D (dupliser) virker nå også med fokus i admin-panelene (ny urd-duplicate-melding over broen; i tekstfelt beholder nettleseren snarveien). Duplikatet blir dessuten den MARKERTE blokken, klar for piltaster eller ny Ctrl+D.
- Nye blokker legges alltid ØVERST i lagrekkefølgen: insertBlock (fellestrakten for palett, «+ Ny blokk»-menyen og dupliser) setter z til seksjonens høyeste pluss én, så en ny blokk aldri gjemmer seg bak eksisterende. «+ kort/rad»-elementene fra malene beholder sin bevisste interne lagdeling.
- Etter angre-publisering lastes den gjenopprettede versjonen automatisk: admin poller de serverte innholdsfilene til deployen er ute (opptil 3 min), forkaster utkastene (serveren er fasiten) og laster seg selv på nytt; ved tidsavbrudd består den gamle sperren og omlast-meldingen.
- Admin-venstremenyens aktiv-markering endrer ikke lenger font-vekten (teksten flyttet seg bittelitt); aktiv markeres av bakgrunn + kant, som er stabile.
- Backlog-gjennomgang med kodeverifisering: fem punkter viste seg alt levert (requirePublisher og modalene fra 0.6.2, oppsettsveiviserens site.setup-signal, logo/ikon fra v0.5, og CSP-arven på /admin/* verifisert i produksjon). BACKLOG.md er omstrukturert: forslag og funn sorteres nå rett inn i milepælen/fasen der de tas (M6 fikk hjelpelinjer, seksjonshøyde ovenfra og multimarkering med kopier/lim inn; v0.7 fikk mobil-klyngen samlet som ett arbeid), dokumenterte begrensninger står i egen seksjon, og alt levert i v0.6-syklusen er samlet for opprydding ved fase-slippet.

### 0.6.7 - CodeQL #10: kryptografisk makeId-fallback - 19. juli 2026
- CodeQL #10 (Insecure randomness) lukket: makeId-fallbacken for usikre opprinnelser bruker crypto.getRandomValues (som virker overalt, også http://0.0.0.0) i stedet for Math.random. Begge id-veiene er nå kryptografisk tilfeldige.

### 0.6.6 - M2.6: editorene på topp-nivå (eiers krav: fulle, komplette, moderne) - 19. juli 2026
- Teksteditoren: linjen forankres i selve markeringen med blokk som fallback, en tom/ugyldig rekt flytter den ALDRI (0,0-teleporten er død), og den klemmes under sidens klistrede meny med under-markeringen-fallback. Farger og utheving er samlet i en nedtrekksrad bak palettikonet (temafarger, egen farge, uthev aksent/egen, NY fjern utheving-knapp), så hovedlinjen er smal; verktøyene ligger i grupper som radbrytes samlet. Nytt: gjennomstreking, inline lenkefelt i linjen (forhåndsutfylt, Enter bekrefter, egen fjern-knapp, aktive URL-skjemaer avvises) i stedet for prompt(). Nivåvelgeren virket ikke (linjens mousedown-vern hindret select i å åpne) - unntak for select/input med eksplisitt markering-lagring/gjenoppretting. Linjen lukkes nå ved klikk hvor som helst utenfor feltet (flate-draget sluker museklikk, så feltet mistet aldri fokus selv). Titler i samlinger er rik tekst med full linje (ADR-0007 revidert); liste-ikonene tegnet i lik høyde.
- Bildeeditoren: fokuspunktet er forklart (sikte-ring + hjelpetekst) og suppleres av ZOOM (beskjærer inn mot fokuspunktet; rammen klipper) og FORM (auto/bred/1:1/høy/rund) - ekte utsnitts- og formkontroll for samlingsbilder via additive skjemafelt. Et svakt tredelingsgitter (som i kameraer) legger seg over selve bildet på lerretet mens editoren er åpen og følger rammen live; miniatyren har samme gitter. Native select-nedtrekk erstattet med stylede segmentknapper; glidere med verdivisning, Gråtone-hurtigvalg og Nullstill justeringer; «Alt-tekst» døpt om til «Beskrivelse» med forklarende plassholder (også i Egenskaper for bildeblokker).
- Fargevelgeren: ny felles velger på lerretet (HSV-flate, kulørglider, hex- OG RGB-felt, temafargeprikker, pipette via EyeDropper) med GJENNOMSIKTIGHET (sjakkbrett-glider, #rrggbbaa) og LAGREDE farger (plussknapp lagrer, × fjerner, tak på 12, delt lager med admin-velgeren i tillegg til Nylige). Admin-velgeren løftet til full paritet. Velgeren åpnes over verktøylinjen (dekker ikke teksten som redigeres) og har ingen scroll lenger.
- Strukturgrep-fiks: en blokk som henger utover seksjonskanten kunne stjele grepet på seksjonslinjen (hover-løftet la blokken over håndtakene); seksjonslinje, «+ Ny seksjon» og «+ Ny blokk» ligger nå over blokk-løftet, under klistret meny og editorpaneler.

### 0.6.5 - M2.5: felles editorer for alle flater - 19. juli 2026
- ÉN bildeeditor for alt: nytt engine/image-editor.js med adapter-mønster (bildeblokker via dobbeltklikk/blyant, samlingsbilder via klikk); fokuspunkt-drag, lysstyrke/kontrast/metning, tilpasning, avrunding og beskrivelse, alt ikke-destruktivt. Samlingsinnslag fikk de additive feltene imageAlt og imageStyle som visningene anvender.
- Teksteditoren på alle skriveflater: innslagstekst i samlinger er rik tekst med samme besøkende-vern som tekstblokkene (delt stripper i engine/sanitize.js: event-attributter, script/iframe/object/embed og javascript:-lenker fjernes ved rendering).
- imageTools flyttet fra editoren til motoren (editor importerer derfra), så komprimering og webp-flyt er felles for admin og lerret.

### 0.6.4 - M2: samlinger (datablokk-mønsteret, ADR-0007) - 19. juli 2026
- Samlinger er data i content/: én fil per samling (content/samlinger/<id>.json med schemaVersion/id/name/kind/entries) pluss indeksfil, kontrakt i schema/collection.schema.json med samme stegvise migreringsmønster som resten. Ny motor-modul samlinger.js (sortering med udaterte sist, år-gruppering, dato-badge, utkast-overstyring fra broen).
- Ny kjerneblokk samling med tre visninger: kort (responsivt grid), liste (rader med dato-badge, ApeironLF-stilen) og arkiv (år-gruppert); manglende/tom samling gir rolige tomtilstander i editoren og ingenting hos besøkende, aldri krasj. Blokken auto-vokser med innholdet så malene slipper å gjette høyder.
- Samlinger-panel i admin: opprett/rediger/slett samlinger og innslag (sammenleggbare), utkast per samling, bilder materialiseres ved publisering, forhåndsvisningen leser UTKASTET via broen. In-block-redigering på lerretet: klikk-og-skriv tittel/tekst og bilde-adder rett i visningen.
- Tre nye seksjonsmaler bygget på mønsteret: nyhetsseksjon, oppslagstavle og publikasjonsarkiv. Ny testfil for samlinger, og validate dekker samlingsfilene.

### 0.6.3 - M1-testrunde: live plugins i preview, lokal utvikling, fikser - 19. juli 2026
- Plugins virker nå i forhåndsvisningen FØR publisering: editoren sender utkastets aktive liste (`urd-plugins`), previewen laster pluginene derfra (filene ligger alt i repoet), og av/på gir fersk innlasting. Dette er også arbeidsflyten for LOKAL plugin-utvikling (dokumentert i plugins/README): mappe inn, aktiver i panelet, se blokkene live.
- Plugin-blokker dukker automatisk opp i «+ Ny blokk»-menyen (previewen leser registrene og bygger blokken med pluginens defaults), og eksempel-kalenderen rendrer nå synlige eksempelhendelser med datobrikker i stedet for et tomt skall.
- Plugin-oppdagelsen krever ikke lenger innlogging: offentlige repo leses anonymt, sist kjente funnliste bufres lokalt, og skriv-inn-navn-feltet vises kun når oppdagelsen er helt utilgjengelig.
- KRITISK lokalserver-fiks: `crypto.randomUUID` finnes ikke på usikre opprinnelser (f.eks. http://0.0.0.0), så ALT som lagde nye id-er (seksjoner, blokker, dupliser) døde stille der; samlet i én makeId med fallback. I tillegg fikset DataCloneError i plugin-meldingen ($state-proxy sendt rått) og to kappløp som kunne gi tom plugin-liste ved rask iframe-oppstart.
- Editor-UX fra testrunden: forhåndsvisningens viewport følger nå visningsvalget i topplinjen (et smalt admin-vindu vipper aldri previewen til mobil og gjemmer strukturverktøyene); klikk i admin-panelene lukker menyene i previewen; seksjonsverktøylinjen er klistret i lange seksjoner og nye elementer ruller minimalt, så pluss-knappen alltid er tilgjengelig; nye elementer markeres.
- Ny seksjonsmal: «Funksjonskort uten ikoner» (eiers ønske), med egen pluss-knapp og testdekning.

### 0.6.2 - M1: CSP-behovsmodell + teknisk opprydding - 19. juli 2026
- ADR-0006: plugins deklarerer CSP-behov i manifestet (`csp.connectSrc`/`frameSrc`, additivt skjemafelt); `_headers` forblir Urd-eid og uskrivbar, og Plugins-panelet viser eieren nøyaktig hvilke unntak en plugin trenger.
- requirePublisher-hjelper i functions/_lib: auth-prologen (konfig, CSRF-origin, cookie, brukeroppslag, ALLOWED_LOGINS) er ett sted i stedet for duplisert i commit.js og revert.js.
- Konflikt- og angre-dialogene bruker editorens eget modalsystem i stedet for nettleserens confirm().
- Oppsettsveiviseren utløses av et eksplisitt `site.setup`-felt (settes i malens standardinnhold ved M9-splitten; navnematch som fallback), og fullført veiviser fjerner feltet ved neste publisering.

### 0.6.1 - M1: plugin-lasting for alvor + Plugins-panel - 19. juli 2026
- Ny motor-modul plugins.js: manifest-validering, `requiresEngine` sjekket mot motorversjonen (avhengighetsfri semver-intervallsjekk: >=, <, ^, ~, eksakt), staging/rollback rundt register() (en plugin som feiler halvveis etterlater ingenting, og id-kollisjoner kan aldri overskrive kjernen), og provides-kontroll med logg ved kontraktsbrudd. TODO-en fra v0.1 om requiresEngine er løst.
- Nytt Plugins-panel i admin: viser pluginene med navn/versjon/status og av/på-bryter; endringer går gjennom vanlig utkast- og publiseringsflyt (plugins.json committes, historikken sier «plugins»), og panelet advarer om ugyldige manifester og motorversjonskrav.
- 7 nye kontraktstester (semver-sjekken, manifestvalidering, staging/rollback, provides).

### 0.5.13 - CodeQL-opprydding + CI-herding - 19. juli 2026
- Favicon-vokterne (editor-effekten, favicon-boot.js, motorens applyFavicon) er skrevet om fra startsWith-sjekker til én ankret regex (kun `data:image/<type>;base64,…` eller site-relativ enkelt-skråstrek-sti): innholdsmessig samme vern, men CodeQL gjenkjenner RegExp.test som barriere, så varslene #7-9 lukkes i stedet for å gjenoppstå ved hvert bygg.
- CodeQL-workflowen ekskluderer nå den genererte editor-bundelen (template/admin/assets): kilden i editor/src og hele motoren skannes fortsatt, og funn peker på lesbare kildelinjer i stedet for flyktige bundle-linjer.
- Nytt CI-steg: committet editor-bygg må matche det kilden faktisk produserer (`git diff --exit-code` etter bygg) - fanger både glemt gjenbygging og manipulerte bundler. Var planlagt til v0.6, levert tidlig.

### 0.5.12 - stor feilsveip: 8 gjennomgangsdimensjoner, ~25 fikser - 19. juli 2026
- KRITISK migreringsfiks: v1-sidefiler fikk NaN-posisjoner (innholdet forsvant) fordi grid-konteksten allerede var løftet til kvadratformat; motoren sender nå rå site.json som kontekst, migreringen tåler begge formene, og en regresjonstest låser det. Render-løkka tåler dessuten blokker uten frames og amputert site.json (tom side, aldri krasj).
- Angre-historikken bærer nå pageId og følger over sidebytter: sletting/oppretting av sider og navigering i preview er angrbare (før tømte hvert sidebytte historikken, og «Ctrl+Z angrer» ved sidesletting var falskt). Angre-nøklene er per egenskap, og «legg bakerst» er ett steg.
- Publisering: adressebytte mellom to sider sletter ikke lenger begge rutingskopiene; glapp konfliktgrunnlaget ved innlasting hentes HEAD nå og redaktøren spørres eksplisitt i stedet for stille overskriving; «Forkast utkast» rydder ALLE siders utkast (som knappen lover).
- Besøkende-vern: innlimt rik tekst strippes for event-attributter, script-elementer og javascript:-lenker ved rendering; lenkedialogen avviser aktive URL-skjemaer; origin-sjekk (CSRF-dybdeforsvar) og sha-validering på functions-endepunktene.
- Motor: inngangsanimasjoner på seksjoner høyere enn ~7x viewporten ble aldri synlige (opacity 0 for alltid); Vimeo-privatlenker beholder hashen og YouTube-id-er valideres (ny testfil); eget ikon fikk lastevern og glyf-fallback; glød/korn uten opacity-felt fikk riktige standardverdier; lytter-lekkasje per rerender tettet; død postMessage-kanal fjernet.
- Seksjonsmalene: mobil-stablingen splittet kortene i bånd (alle ikoner, så alle bokser) - nytt additivt `mobileOrder`-felt holder kortene samlet; pluss-knappene bruker ledig-rute-søk så et slettet element i midten fylles igjen i stedet for kollisjon; FAQ-spørsmål legges nå FØR avslutningslinjen (som skyves ned); trange tekstrammer i hero/statistikk utvidet. Testene dekker moves og slette-scenarioet.
- Editor-UI: Ren visning lekker ikke lenger aktiv-seksjonskant/tilsynsomriss/snappelinjer; handlingsknappene («+ Opprett side» m.fl.) er sentrert; aktiv-markørene fikk fet skrift (synlig også i grå/nordlys-temaet); fargevelgeren fikk riktig høydeberegning og begge velgerne lukker ved panelrulling; favicon-effekten nullstiller til Urd-merket når ikonet fjernes; markert blokk ryddes når seksjonen slettes.
- Kontrakter: SKJEMA.md à jour (alle additive felt, migreringene side-v3/site-v2, nav.style/sticky/logo), BRUKERVEILEDNING (nav-utseende, dekor-regelen for ikoner), ARKITEKTUR (hele skriveflaten inkl. rutingskopier); ny test vokter at urd.json-kontrakten og publiseringsvokteren aldri glir fra hverandre. Svakheter som bevisst ikke ble fikset er loggført i BACKLOG med fasekandidat. 36 tester (6 nye).

### 0.5.11 - tegnvelger med eget ikon, favicon-fikser + CodeQL - 18. juli 2026
- Ikon-blokken har fått en tegnvelger i Egenskaper: kategorisert meny med rundt 300 tegn og emojier (Symboler, Piler, Smilefjes, Gester og folk, Natur, Mat og drikke, Aktivitet, Objekter, Hjerter) med «Nylige»-rad, ved siden av fritekstfeltet. Nederst i menyen kan et EGET IKON lastes opp (webp, maks 256px): det vises i tegnstørrelsen i stedet for tegnet, kan fjernes i Egenskaper, og publiseres som media-fil (additivt `image`-felt, ingen migrering).
- Admin-fanen blinket med Urd-merket ved hver innlasting før editoren hadde lest site.json: en liten `admin/favicon-boot.js` bytter nå til nettstedsikonet straks HTML-en parses, lenge før editor-bunten er lastet.
- CodeQL-varslene #1-3 (XSS/redirect-flyt i favicon-settingen) er lukket ved at kun kjente ikonformer slippes gjennom (`data:image/…` eller site-relativ sti, aldri `//`) i alle tre favicon-flytene: editorens effekt, favicon-boot.js og motorens applyFavicon.

## [0.5.10] - 2026-07-18

«Panelene og nettstedet rundt siden»: hele editor-UI-et er lagt om til
panelvelger + sidepanel, og alt rundt selve lerretet er på plass. Levert
og testet fortløpende på urdweb.pages.dev.

Hovedtrekk:
- **Panelene**: Sider, Blokker, Egenskaper, Grid, Tema, Nav, Footer og Historikk i gruppert panelvelger; statusmeldinger som toast; admin med sju valgbare fargetemaer (Nøytral grå som standard), knapper som ser ut som knapper, og tegnede SVG-ikoner overalt (ingen emojier).
- **Redigeringsdybde**: props-editor per blokktype, flytende teksteditor (nivåer, S/M/L/XL, farger koblet til temaet, utheving, lenker), full bakgrunnseditor med lag (farge/gradient/glød/bilde/korn), animasjoner per blokk/seksjon, rotasjon, smarte hjelpelinjer, dupliser, tastatursnarveier, fokuspunkt og bildejusteringer.
- **Nettstedet rundt siden**: nav-editor (logo tekst/bilde/begge, plassering, klistret meny med stilvalg), delt footer, nettstedsikon (favicon), nye blokktyper video og ikon.
- **Seksjonsbiblioteket**: 19 presets i gruppert galleri med beskrivelser, utvidbare seksjoner («+ kort/rad/person»), alle bygget av temafarge-tokens og voktet av kontraktstest + skjemavalidering i CI.
- **Publiseringsløkka**: konfliktvarsel (`latest?base=`), Historikk-panel med angre-publisering, oppsettsveiviser, presise commit-meldinger, per-side `<slug>/index.html`-generering, tosteg-bekreftelse på «Forkast utkast».
- **Kartleggingen «Siste steg»**: ni inspirasjonssider + ApeironLF + nettbutikk-research sortert inn i backlog og veikart (feed-basert kalender-plugin i v0.6, butikk uten betalingsgateway i v0.7).

Arbeidet ble pushet nummerert (0.5.1-0.5.10); push-loggen under er
hele historikken, nyeste først.

### 0.5.10 - testrunde-fikser: symmetriske kortmarger - 18. juli 2026
- Kortpresetene lå med mer luft til venstre enn til høyre (eiers funn: kolonner på 6/39/72 ga 6 % venstremarg og 3 % høyremarg). Alle grid-presetene er rettet til symmetriske marger (tre kolonner 6/37,5/69, team 7,5/39/70,5, sponsorrad 5,5/29/52,5/76 med bredde 18,5), i både seksjonsmalene og «+ element»-fabrikkene.

### 0.5.9.3 - feilsjekk-runde av seksjonsbiblioteket - 18. juli 2026
- Ny kontraktstest (tests/presets.test.mjs): alle presets gir velformede seksjoner med ferske objekter, og hver «+ element»-fabrikk plasserer to runder nye elementer uten overlapp. `npm run validate` validerer nå også alle 19 presets (med item-runder) mot page-skjemaet.
- Tre funn fikset: pluss-knappen deaktiveres til seksjonen er rerendret (dobbeltklikk kunne lagt to element i samme rute), preset-galleriet på nederste seksjonsgrense åpner oppover (ble klippet av iframe-høyden ved sidens slutt), og på en helt tom side åpner det fortsatt nedover.

### 0.5.9.2 - utvidbare seksjoner («+ kort/rad/person») - 18. juli 2026
- Seksjoner laget fra maler med gjentakende elementer har fått en pluss-knapp i seksjonsverktøylinjen som legger til NESTE element (kort, sak, rad, person, spørsmål, steg, produkt, tall, logo, bilde) ferdig plassert i neste ledige rute. Elementet legges som én gruppe blokker i ett angre-steg, og seksjonen vokser automatisk når det trengs.
- Teknisk: preset-definisjoner kan nå ha valgfrie `item`/`itemLabel`-fabrikker (samme mønster som `create`); seksjonene forblir generiske containere, og antall leses robust fra blokkene så knappen virker også etter redigering.

### 0.5.9 - seksjonsbiblioteket + kartleggingen «Siste steg før 0.5.0» - 18. juli 2026
- Kartlegging fullført av eiers ni inspirasjonssider + full funksjonsinventering av ApeironLF (kildekoden) + nettbutikk-research (Squarespace/Wix Commerce, Snipcart, Stripe, Vipps). Funnene er sortert inn i BACKLOG (v0.5/v0.6/v0.7/horisont) og VEIKART: kalender-pluginen (v0.6) blir feed-basert (Google Calendar/iCal) med designkrav fra ApeironLF, og butikk uten betalingsgateway (produktkort med varianter, klient-side handlekurv, bestillingsskjema, Vipps-instruks) er lagt som kjernefunksjon i v0.7.
- Seksjonsbiblioteket utvidet fra 7 til 18 presets, bygget av eksisterende blokktyper med temafarge-tokens: hero sentrert (to knapper), funksjonskort (ikon + kort + hover-løft), nyheter, arrangementer (dato-badge + påmeldingsknapp), steg for steg, hovedoppslag (én stor + to små), produkter/merch (kjøp = ekstern lenke, f.eks. Vipps), CTA-banner, sitat, statistikk, sponsorrad (gråtone) og medlemskap (prisnivåer + Vipps-linje). Team fikk e-postlinje og FAQ fikk «flere spørsmål?»-lenke.
- «+ Ny seksjon» åpner nå et gruppert galleri (Grunnleggende / Kort og lister / Fremheving) med kort beskrivelse per mal og rulling, i stedet for en flat knapperad. Klikk utenfor lukker.

### 0.5.8.10 - bekreftelse på «Forkast utkast» - 18. juli 2026
- «Forkast utkast» krever nå to klikk: første klikk gjør knappen rød med teksten «Sikker?», andre klikk forkaster. Klikk hvor som helst ellers, Escape eller klikk i forhåndsvisningen tar knappen tilbake til vanlig - før slettet ett feilklikk alle utkastene uten spørsmål.

### 0.5.8.9 - skarpere radglyfer - 18. juli 2026
- Pilene og kryssene i radknappene er økt fra 11 til 12px: geometrien var alt perfekt sentrert (målt i nettleseren), men på skjermer med 125 % skalering landet strekene på brøkdels-piksler og så skjeve ut av antialiasing. 12px treffer hele fysiske piksler (12 x 1,25 = 15) og fyller knappen bedre.

### 0.5.8.8 - presise publiseringsmeldinger - 18. juli 2026
- Publiseringer av nettstedsoppsettet sier nå HVA som endret seg i historikken («Oppdater tema via Urd-admin», «Oppdater menyen, nettstedsikonet …») i stedet for det generiske «innstillinger» - endringene diffes mot publisert site.json (tema, menyen, footeren, sideregisteret, gridet, nettstedsikonet, nettstedsinfo).

### 0.5.8.7 - nettstedsikon (favicon) + grå som standardtema - 18. juli 2026
- Nettstedsikon: last opp i Tema-panelet (skaleres til 128px webp, materialiseres som media-fil ved publisering, additivt `site.icon`-felt). Vises i nettleserfaner og bokmerker for besøkende, og admin-fanen følger nettstedsikonet når det finnes. Uten eget ikon brukes et nytt Urd-merke (innebygd SVG) i både malen og admin - før var fanene ikonløse (globus).
- «Nøytral grå» er nå standardtema for admin (tidligere Lilla dybde); valget kan fortsatt endres i velgeren og huskes per nettleser.

### 0.5.8.6 - tre nye admin-temaer + SVG-radglyfer - 18. juli 2026
- Tre nye admin-temaer basert på anerkjente fargepaletter (aksentene mørknet der det trengtes for lesbar hvit knappetekst): Nordlys (Nord: arktisk, lav metning), Skumring (Tokyo Night: neon-natt, blå) og Glo (Gruvbox: varm, glødende oransje). Totalt sju temaer i velgeren.
- Pilene og kryssene i radknappene (Sider, Nav, bakgrunnslag) er nå tegnede SVG-er i stedet for tekstglyfer: tekstglyfer sitter aldri optisk midt i knappen (fontmetrikk), SVG sentrerer piksel-perfekt og matcher resten av ikonene.

### 0.5.8.5 - sidevelgeren erstattet + jevne radknapper - 18. juli 2026
- Sidevelger-nedtrekket i topplinjen er fjernet (overflødig etter Sider-panelet): i stedet viser en knapp gjeldende sidenavn, og klikk åpner Sider-panelet der bytte, omdøping og oppretting bor.
- Radknappene (piler/kryss i Sider, Nav og bakgrunnslagene) har nå fast kvadratisk bredde og sentrert glyf - de arvet ved en feil panellistens venstrestilling, og bredden fulgte tegnet.

### 0.5.8.4 - designrunde for admin - 18. juli 2026
- Temavelger i topplinjen (ved siden av Urd-logoen): fire admin-temaer - Lilla dybde (standard), Nordisk brønn (teal), Norrønt gull (rav) og Nøytral grå. Gjelder KUN editoren (forhåndsvisningen følger brukerens eget tema) og lagres per nettleser.
- Knapper ser nå ut som knapper: fylt flate, tydelig hover og et lite trykk ved klikk - mens felt (input/nedtrekk) forblir flate, så skillet er synlig. Alt knappinnhold er sentrert (listeknapper i panelene er fortsatt venstrestilte).
- Panelvelgeren er sortert etter arbeidsflyt med skillelinjer: Sider/Blokker/Egenskaper/Grid (bygge siden), Tema/Nav/Footer (nettstedet), Historikk (verktøy).
- Gjenværende hardkodede lilla-toner byttet til aksentvariabelen, så alle temaene farger hele admin konsekvent.

### 0.5.8.3 - tomrom øverst i seksjonene + «+ Ny blokk» øverst til høyre - 18. juli 2026
- Alle seksjoner fikk et bakgrunnsløst bånd øverst: «+ Legg til blokk»-pillen lå i flyten med toppmarg, og siden seksjoner ikke har toppkant kollapset margen UT av seksjonen og skjøv den ned. Pillen er nå absolutt posisjonert (deltar aldri i flyten), døpt om til «+ Ny blokk», og flyttet øverst til høyre under seksjonsverktøylinjen; menyen åpner høyrejustert under den.

### 0.5.8.2 - bugsjekk av editor-løftet - 18. juli 2026
- Gjennomgang av 0.5.8: token-fargene er verifisert hele veien (alle motor-konsumenter går via resolveColor, også gradient-stopp og color-mix i menyen), guide-matematikken bruker samme koordinatrom som blokkene, og duplisering bevarer manuell-mobil-invarianten. To småfeil fikset: død hexFor-funksjon fjernet (velgeren løser opp tokens selv), og «Nylige farger» tåler nå korrupt localStorage-innhold.
- To kjente svakheter bokført i backloggen: utheving lagrer fargen som fast verdi (følger ikke senere temabytte), og Ctrl+D krever fokus i forhåndsvisningen.

### 0.5.8 - editor-løft etter research (M8 del 1) - 18. juli 2026
Basert på nettresearch av Squarespace, Wix og Gutenberg (funn og kilder i samtalen/planen):
- Fargevelgeren kobler til temaet: å velge en temaprikk lagrer FARGENAVNET (ikke hex), så innholdet omfarges når temaet endres - mønsteret alle de tre store bruker. Koblede felt vises med ring og «koblet til tema»-tekst; flate/hex gir frikoblet farge som før. Pluss «Nylige»-rad (siste 8 frie farger).
- Tekststørrelse som presets: A (arv) / S / M / L / XL-knapper pluss fritt px-felt (Gutenberg-mønsteret). Utheving (markeringstusj) i formateringslinjen: aksentfargen med ett klikk eller egen farge.
- Smart guides (à la Wix): under dra vises snappelinjer når blokkens kant/senter er innen 5 px av naboblokkers kanter/senter eller seksjonens midtlinje, med snapping (Shift = fritt). Dupliser-knapp på blokkverktøylinjen + Ctrl+D (kopi med liten forskyvning).
- Bildeblokker fikk fokuspunkt (X/Y styrer utsnittet ved beskjæring, som bakgrunnslaget) og ikke-destruktive justeringer: lysstyrke, kontrast og metning som glidere (CSS-filter) med nullstill-knapp. Alt additivt.
- FIKS (eiers funn): «+ Legg til blokk»-pillen lå nederst i seksjonen og havnet under folden i høye seksjoner - den er nå klebrig i synsfeltet (nær toppen i små seksjoner, følger med i høye).

### 0.5.7.13 - moderne fargevelger + angre-fikser - 18. juli 2026
- Ny fargevelger i alle paneler (egen komponent, erstatter nettleserens innebygde): flate for metning/lysstyrke, kulør-glider, hex-felt, forhåndsvisning og temafargene som hurtigvalg-prikker. Brukes i Tema, Nav-utseende, bakgrunnslag (farge/gradient/glød) og oppsettsveiviseren; alle endringer går gjennom vanlig utkast- og angreflyt.
- Ctrl+Z virket ikke etter fargevalg (fokus ble stående i fargefeltet, som slukte snarveien), og panelene viste gamle verdier etter angring (speilene ble ikke resynket) - begge fikset. Ctrl+Y er nå også gjenta, i tillegg til Ctrl+Shift+Z, både i editoren og inne i forhåndsvisningen.

### 0.5.7.12 - bildelasting uten striper, nav-panel-overflyt, øverste seksjon-bar - 18. juli 2026
- «Inntoningen» ved innlasting var IKKE animasjonssystemet, men selve bildelastingen: store bilder dekodes stripevis ovenfra mens de laster, og et halvlastet bilde som henger inn i neste seksjon så «avkuttet» ut. Både bildeblokker og bakgrunnsbildelag holdes nå usynlige til bildet er FERDIG lastet, og vises komplett med en gang (cachede bilder berøres ikke). Ordentlig bildeoptimalisering (responsive størrelser, lazy loading) står fortsatt i v0.7.
- Nav-panelet: gruppene (Utseende m.fl.) nektet å krympe til panelbredden, så fargevelgere og brytere ble kuttet i kanten - gruppene har nå samme bredde-klemme som resten av panelet.
- Den øverste «+ Ny seksjon»-pillen legges under grenselinjen i stedet for oppå, så den ikke gjemmer seg bak den klistrede menyen.
- Alle bryterrader i panelene er lagt om til innstillings-stil: tekst til venstre, bryter ytterst til høyre - konsekvent med resten av radene (etikett venstre, kontroll høyre).

### 0.5.7.11 - blokker bytter seksjon ved dra + innlastningsanimasjon tettet - 18. juli 2026
- Slippes en blokk med senteret over en ANNEN seksjon, flyttes den dit i dataene: grid, tilhørighet og mobil-stabling følger seksjonen den faktisk ligger i, ikke den den kom fra (før forble blokken koblet til gamleseksjonen og hang bare visuelt over den nye). Mobil-framen nullstilles ved flytting (avledes på nytt), og Ctrl+Z angrer hele flyttingen.
- Innlastningsanimasjonen tettet for godt: FØR brukerens første scroll snappes alle inngangsanimasjoner uansett (tidsvinduet alene kunne overskrides på trege innlastinger, som gjorde at Hjem fortsatt animerte ved lasting). Animasjonene spilles kun ved scroll-inn senere. NB: urdweb viser gammel motor til dette er pushet og deployen er ferdig.

### 0.5.7.10 - tegnede SVG-ikoner i stedet for ALL emoji - 18. juli 2026
- Hele editoren er emojifri: desktop/mobil-togglene, tilsyn-chipen, Rediger (blyant), Ren visning (øye), dekor-knappen (telefon/overstrøket telefon), lag-knappene (pil mot strek opp/ned), lenke-ikonet og bildeplassholderen er alle tegnede SVG-ikoner i samme strek-stil (currentColor).
- Alle ikonknapper er loddrett sentrert: blokkverktøylinjens knapper har fått fast høyde med flex-sentrering, og topplinjens ikonknapper linjerer ikon + tekst på midten.
- Brukerveiledningen beskriver ikonene i ord i stedet for emoji.

### 0.5.7.9 - «+ Legg til blokk» i seksjonen - 18. juli 2026
- Hover over en seksjon viser «+ Legg til blokk» nederst i den; klikk åpner en meny med alle blokktypene, og valget legges vannrett sentrert i akkurat den seksjonen. Blokker-panelet består som alternativ.
- Bilde via menyen starter tomt (fildialog kan ikke åpnes derfra) med tydelig plassholder («Velg bilde i Egenskaper») - plassholderen vises nå også for tomme bilderammer fra Bilder-/Team-presetene (før var de usynlige). Besøkende ser aldri plassholderen.

### 0.5.7.8 - «+ Ny seksjon» vises ved hover - 18. juli 2026
- «+ Ny seksjon»-barene er skjult til musepekeren er over en seksjon: da vises baren både over og under den (og holder seg synlig mens du beveger deg over på selve baren). Mindre visuell støy, og den øverste baren krangler ikke lenger med menyen.

### 0.5.7.7 - meny-lag, logo-vekst og nav-utseende - 18. juli 2026
- «+ Ny seksjon»-barene glir nå UNDER den klistrede menyen i stedet for å synes gjennom den (menyens lag ligger over editorbarene).
- Store logobilder vokser ikke lenger hele menylinjen: bildet vokser ut av linjen (inn i luften rundt) mens menyhøyden står fast.
- Nytt «Utseende» i Nav-panelet: bakgrunnsfarge, dekkevne (gjennomsiktighet), uskarphet bak menyen og tekstfarge - pluss menyplassering og klistret-bryteren samlet samme sted. Alt additivt (`nav.style`); Nav-panelet er omorganisert i tre grupper (Logo, Utseende, Menypunkter).
- Bakgrunnsbilde i menyen og menypunkt-design står som egen backlogpost.

### 0.5.7.6 - klistret meny virket aldri + flate-dra på tekstblokker - 18. juli 2026
- GAMMEL FEIL avslørt av den nye toggelen: klistret meny har aldri virket, sticky-CSS-en lå på det indre nav-elementet, som er innesperret i et header-element med nøyaktig samme høyde (et sticky-element kan aldri forlate forelderen sin). Sticky ligger nå på verten, og menyen følger faktisk med når man blar.
- Toggelen heter «Klistrete meny» (flytende meny reserveres som mulig egen stil senere).
- Flate-draget virket ikke på tekstblokker (tekst fyller hele flaten, og all tekst var unntatt): nå er tekst kun unntatt når blokken ALT er valgt. Uvalgt blokk dras fritt også fra teksten; klikk uten dra velger den, klikk igjen redigerer (à la Squarespace).
- Alle avhukingsbokser i admin er nå moderne brytere (pille med knott som glir til høyre, aksentfarget når på).

### 0.5.7.5 - dra fra flaten, tekstlinje-finpuss, meny-sticky - 18. juli 2026
- Blokker kan dras direkte fra flaten (uten ⠿, som består): liten terskel gjør at klikk forblir klikk, redigerbar tekst er unntatt (tekstmarkering virker som før), og i auto-mobil kreves fortsatt et bevisst ⠿-dra for materialisering. Pekeren viser flyttekors over blokker.
- Formateringslinjen legger seg nå over blokkVERKTØYLINJEN i stedet for oppå den (overlappen fra eiers skjermbilde), og liste-/fjern formatering-knappene har fått ordentlige ikoner (tegnet punkt-/nummerliste, T×).
- REVERSERT etter eiers test: Egenskaper åpnes ikke lenger automatisk ved klikk på blokk (kun ved ny seksjon).
- Nav: av/på for flytende meny (`nav.sticky`, additivt) - av betyr at menyen kun ligger øverst og ikke følger scrollingen.

### 0.5.7.4 - Squarespace-aktig tekstlinje og kompakt logo-editor - 18. juli 2026
- Formateringslinjen for tekst er bygget om: den vises nå over blokken SÅ LENGE feltet redigeres (ikke bare ved markering), med tekstnivå-nedtrekk (Avsnitt/H1/H2/H3 som speiler markøren), fet/kursiv/understrek, temafarger + fri fargevelger, lenke (legg til/fjern), venstre/midt/høyre-justering, punkt- og nummerert liste, sitat og fjern formatering. Lister, sitater og lenker har fått innholds-CSS så de ser riktige ut også hos besøkende.
- Logo-editoren i Nav-panelet er komprimert til tekstbehandler-aktige verktøyrader: font + px + F/K-knapper på én linje, og bildevelger + høyde + avrunding på én linje.

### 0.5.7.3 - logostil og tekst per felt - 18. juli 2026
- Logo-delen i Nav-panelet er en sammenleggbar gruppe, og logoteksten har fått full stil: font (velger), tekststørrelse, fet og kursiv. Bildelogoen har fått avrunding i tillegg til høyde. Alt additivt i skjemaet.
- Tekstblokker kan nå ha egen font og grunnstørrelse per felt (Egenskaper; tomt = arv fra tema), i tillegg til den flytende formateringslinjen for markert tekst.

### 0.5.7.2 - z-orden var maskert i editoren - 18. juli 2026
- z-orden VIRKET hele tiden (i dataene og på publisert side), men editoren skjulte den: blokken man peker på eller har markert løftes visuelt øverst for at håndtakene alltid skal kunne nås, og siden blokken man nettopp z-justerte alltid er markert, så det ut som «noe alltid ligger øverst». Løftet gjaldt feilaktig også i Ren visning - nå viser Ren visning nøyaktig den ekte lagrekkefølgen (og markeringsomrisset skjules der).
- ⬆/⬇-knappene og Lag (z)-feltet forklarer nå oppførselen i tooltip.

### 0.5.7 - full nav-editor og delt footer (M7) - 18. juli 2026
- Logoen har nå tre varianter: tekst, bilde eller bilde + tekst, med bildeopplasting (webp-flyt, materialiseres som media-fil ved publisering), bildehøyde og rekkefølge (bilde/tekst først). Logoen er «Hjem»-knappen. Additive skjemafelt; eldre sider rendres uendret.
- Menyplassering: menypunktene kan stå til høyre (som før), midtstilt eller til venstre etter logoen (`nav.layout`, additivt).
- Delt footer: nytt Footer-panel redigerer `site.footer` (vis/skjul, tekstlinjer, justering) ETT sted, og motoren viser den nederst på alle sider. Additivt felt: eldre sider uten footer er uendret; footer-PRESETEN (per-side seksjon) finnes fortsatt. Design-maler kommer i v0.6.
- Ny seksjon markeres nå automatisk og åpner Egenskaper, klar til justering (samme oppførsel som blokkvalg).
- Dropdown-menyer med flere design er flyttet til v0.6 (krever omlagt nav-rendering for besøkende); full videreutvikling av seksjonspresetene ligger i v0.5-listen og venter på eiers inspirasjonsmateriale.

### 0.5.6 - innholdsbredde og finpuss (M6) - 18. juli 2026
- Video-blokk: lim inn en YouTube- eller Vimeo-lenke, så rendres personvennlig innbygging (youtube-nocookie / dnt=1). CSP-en har fått et bevisst frame-src-unntak for akkurat de to vertene. I editoren markerer klikk blokken i stedet for å starte spilleren; ugyldig/tom lenke gir rolig plassholder.
- Ikon-blokk: glyf/emoji med valgfri størrelse og temafarge (starter som dekor, lik formene).
- Fire nye seksjonspresets: Bilder (tre bilderammer), Team/styret (tre medlemmer med portrett), FAQ (spørsmål i tekstbokser) og Kontakt (kontaktkort med e-postknapp). Rene datafabrikker; plassholderbilder byttes i Egenskaper.
- Tastatur på markert blokk i forhåndsvisningen: piltaster flytter ett grid-steg (Shift = 1 px), Delete/Backspace sletter, Esc avmarkerer. En skur av piltastetrykk blir ett angre-steg.
- Rotasjonshåndtak (⟳) på blokker: dra rundt blokkens sentrum, snapper til 15°-steg (Shift = fri vinkel).
- Klikk på knapper og lenker i editoren utløser aldri lenken lenger - klikket markerer blokken (lenker testes via «Se siden»). Eksterne knappelenker åpnet tidligere ny fane midt i redigeringen.
- Innlastning: elementer med inngangsanimasjon som er synlige idet siden lastes (eller ved vindusendring) dukker nå bare opp, uten avspilling - animasjonen spilles kun når innholdet scrolles inn senere. (Eiers observasjon: «alt dukket opp som om det kom til syne» ved lasting.)
- Klikk på et objekt åpner Egenskaper-panelet automatisk.

### 0.5.5 - publiserings-modenhet (M5) - 18. juli 2026
- Konfliktvarsel: editoren husker HEAD fra innlasting, og publisering sjekker `latest?base=`. Har noen andre publisert endringer i de samme filene, må redaktøren aktivt velge «publiser likevel» (eller avbryte og laste på nytt). Endringer i ANDRE filer enn dine stopper ingenting - da flettes de naturlig i git.
- Historikk-panelet: siste 15 publiseringer (melding, hvem, når; øverste rad er dagens tilstand) og «↩ Angre siste publisering».
- `history.js` og `revert.js` er implementert server-side. Angring er en FORWARD-revert (ADR-0003): en ny commit med HEAD som forelder - historikk slettes aldri, angringen kan selv angres, og `expect`-vernet gir 409 hvis noen publiserte i mellomtiden.
- Oppsettsveiviser: første besøk på en fersk klon (malens standardnavn står fortsatt) åpner en velkomstmodal med sidenavn, aksent- og bakgrunnsfarge. Skrives som vanlig site-utkast og peker mot Publiser; «Hopp over» huskes.
- Nav-panelet: mål- og lenkefeltene ligger nå i samme kolonne som navnefeltet (slutter på samme høyrekant, i stedet for å strekke seg ut under ↑/↓/×).
- Jevn vertikal sentrering i hele admin: alle kontroller i panelene (felt, nedtrekk, knapper, grupper, veiviser) deler nå én høyde (2.2rem) og én skriftstørrelse, radknappene følger feltene, og kontrollene arver ikke lenger sidens luftige linjehøyde. Før spriket tre høydemekanismer og to arvede skriftstørrelser om hverandre.
- Fra bug-gjennomgangen før push (åtte granskningsvinkler):
  - KRITISK: historikk og angring gjelder nå NETTSIDEN, ikke repoet. Historikken viser kun commits som rører `content/` under `GITHUB_ROOT_DIR`, og angring bytter bare nettsidens undertre - i Urds monorepo ville den gamle varianten listet utviklingscommits og revertert editor-kildekode.
  - Publisering materialiserer bilder i KLONER: en konflikt-avbrytelse (eller 409) etterlater ikke lenger utkast som peker på media-filer som aldri ble committet. Minnet speiles først når commiten er trygt inne.
  - Konfliktvinduet er tettet server-side: editoren sender `expect` (HEAD-en sjekken så) til commit-endepunktet, som avviser med 409 om noen rakk å publisere i mellomtiden; det samme gjelder non-fast-forward i selve ref-oppdateringen (før: rå 502). Avkortede GitHub-differ (300+ filer) behandles som mulig overlapp i stedet for å slippe gjennom.
  - Etter angring sperres publisering til admin er lastet på nytt: editoren viser fortsatt innholdet fra før angringen, og en publisering derfra ville stille gjeninnført det angrede.
  - Tomt repo gir tom historikk i stedet for feilmelding; robust JSON-lesing i alle nye svarstier; delt `triggerDeploy`-hjelper for commit og revert; manglende konfliktgrunnlag prøves på nytt ved neste publisering.

### 0.5.4.2 - bakgrunnseditor-runde to - 18. juli 2026
- Lagtypen kan byttes etter at laget er lagt til (nedtrekk i laget; innstillingene nullstilles ved bytte).
- Styrke-glider på ALLE lagtyper: farge og gradient har fått additivt `opacity`-felt i motoren, så lag kan blandes fritt (mix and match).
- Gradientvinkelen er glider (0-360°) i stedet for tallfelt, og «Animert» heter nå «Panorer sakte (loop)» med forklaring, så den ikke forveksles med Animasjon-valget (som gjelder innholdet).
- Inngangsanimasjoner spilles nå én gang i forhåndsvisningen hver gang de endres fra panelet (demo-avspilling), i stedet for bare å vise slutt-tilstanden - hos besøkende spilles de fortsatt ved scrolling.
- Bildelaget har fått full editor: fokuspunkt X/Y, uskarphet (0-20 px), gjenta som mønster, styrke.
- Panelet kan ikke lenger få horisontal scrolling fra brede nedtrekk («Tilpasning»-boksene).
- Egen slank, mørk scrollbar i hele admin, og nettsiden selv har fått temafarget slank scrollbar (matcher, og ser bedre ut enn nettleser-standarden på mørke sider).

### 0.5.4 - bakgrunnseditor og animasjoner (M4) - 18. juli 2026
- Bakgrunnseditor i seksjonsegenskapene: lagliste med legg til/fjern/sorter, og per lagtype: farge (velger), gradient (fra/til-farge, vinkel, animert panorering), glød (farge, posisjon, størrelse, styrke), korn (styrke) og NYTT bildelag (opplasting med webp-komprimering, tilpasning, styrke; materialiseres som media-fil ved publisering, samme flyt som bildeblokken).
- Animasjoner i motoren: `Urd.animations`-registeret har fått kjernetypene `fade-in`, `slide-up`, `zoom-in` (spilles ved scroll-inn hos besøkende via IntersectionObserver) og `hover-lift`. Alle følger version+migrate-kontrakten (egen testfil). `prefers-reduced-motion` respekteres; ukjent animasjonstype viser innholdet uanimert.
- Animasjons-UI i Egenskaper-panelet for både blokker og seksjoner: type, varighet og forsinkelse. Editorens forhåndsvisning viser slutt-tilstanden (redigering skal ikke utløse evige avspillinger); selve avspillingen skjer hos besøkende.
- Seksjoner har fått valgfritt `animation`-felt i skjemaet (additivt, ingen migrering nødvendig).

### 0.5.3.3 - panel-finpuss - 18. juli 2026
- Sider-panelet (og alle andre paneler) holder seg nå innenfor panelbredden: radene klemmes i stedet for å gi horisontal scrolling.
- Radknappene i Blokker-panelet (grupper, Knapp, Bilde) deler nå samme høyde og utlegg, så listen ser jevn ut.
- Toasten («Publisert!» m.fl.) har fått luft mot høyrekanten og ligger klar av forhåndsvisningens scrollbar.
- Nav-panelet hadde samme overflyt ett nivå ned (radene er egne grids, og nedtrekk har egenbredde): også de klemmes nå til panelbredden.
- Nedtrekkslistene har fått egen pil med luft mot høyrekanten, og teksten i dem linjerer nå med tekstfeltene (nettleserens innebygde pil og innrykk er skrudd av).

### 0.5.3.2 - dra over seksjonstoppen + knappe-lenke-fiks - 18. juli 2026
- Blokker kan nå dras over toppen av sin egen seksjon (negativ y), symmetrisk med at de alltid har kunnet henge under bunnen. Før satt blokker laget i en seksjon fast ved seksjonens overkant og kunne ikke dras oppover ut av den. Skjemaets y-minimum er fjernet (ren oppmykning, knekker ingen sider).
- Knapp-egenskapene: ekstern lenke starter nå som tomt felt med plassholder i stedet for «#», og bytte side ↔ ekstern lenke mister ikke lenken man hadde skrevet.

### 0.5.3 - Egenskaper-panelet og teksteditor (M3) - 17. juli 2026
- Egenskaper-panelet for valgt blokk: posisjon/størrelse/lag/rotasjon som tallfelt (desktopvisning), 📵-avkrysning med forklaring, og per type: tekst (justering, tekstboks av/på), knapp (tekst, mål: side eller ekstern lenke, stil), bilde (bytt bilde, alt-tekst, tilpasning, avrunding, lenke), form (type, temafarge, tykkelse, fylt).
- Egenskaper-panelet for valgt seksjon: minstehøyde (px eller CSS-verdi) og eget grid (flyttet hit fra Grid-panelet, som nå kun eier sidens grid).
- Flytende formateringslinje over markert tekst i tekstblokker: fet, kursiv, H1/H2/H3/avsnitt og temafarger (tekst/aksent). Lagres gjennom den vanlige utkastflyten.
- Blokkmarkeringen overlever nå rerendringer (endringer fra panelet fjernet tidligere det lilla omrisset).
- Backlog: mobil-tilsyn-varselets tydelighet, 📵 i manuelle seksjoner og «mobil som egen versjon» er logget som egne avklaringer etter eiers testrunde.

### 0.5.2.3 - mobil-synlighetsikon og dra på seksjonsgrensen - 17. juli 2026
- Dekor-knappen viser nå tilstanden med selve ikonet: 📱 = blokken vises på mobil, 📵 = skjult i automatisk mobil-layout (pynt). Erstatter ✦, som krevde at man visste hva fargene betydde (og tooltipen beskrev en grå tilstand som ikke fantes).
- «+ Ny seksjon»-baren kan dras: den ligger på grensen mellom to seksjoner, og dra flytter grensen (høyden på seksjonen over) med samme grid-snapping og Shift-overstyring som seksjonslinjen. Klikk åpner fortsatt preset-menyen.

### 0.5.2.2 - feilretting av ny side-flyten - 17. juli 2026
- KRITISK: site-utkastet nådde aldri forhåndsvisningen etter panelomleggingen (postMessage tåler ikke Svelte-reaktive objekter og kastet DataCloneError). Dette knakk hele ny side-flyten: siden ble registrert, men utkastet ble aldri opprettet, forhåndsvisningen ble stående på forsiden, og nav/tema-endringer vistes ikke live. Alt går nå gjennom et rent snapshot.
- Forhåndsvisningen får alltid sidedata fra editoren for upubliserte sider (serveren kjenner dem ikke og falt tilbake til forsiden).
- Publisering skriver alltid sidefilen for nye sider, også uten utkast - en registrert side uten fil ville gitt besøkende en død adresse.
- Motoren viser en tom side (med varsel i konsollen) i stedet for å krasje om en sidefil mangler.
- Sider- og Nav-panelradene holder seg på én linje (verktøyknappene brøt om).

### 0.5.2 - sider, nav og tema (M2) - 17. juli 2026
- Sider-panelet: opprett nye sider (starter tomme, legges automatisk i menyen), gi sider nytt navn og ny adresse, og slett sider (forsiden er fredet; Ctrl+Z angrer alt frem til publisering).
- Nav-panelet: rediger menyen (tekst, mål: side eller ekstern lenke, rekkefølge, fjern) og logoen (tekst eller bilde-URL). Alt vises live i forhåndsvisningen.
- Tema-panelet: fargevelgere for bakgrunn/flater/tekst/aksent, fontvalg for overskrifter og brødtekst, og avrundingsverdier. Alt vises live.
- Ruting på alle statiske hoster: publisering genererer `<adresse>/index.html` for hver side utenom forsiden (kopi av rot-index.html; motoren ruter på adressen). Slettede og flyttede sider rydder sine filer i samme commit.
- Publiseringslaget kan nå slette filer (tre-innslag med `sha: null`; stier som alt er borte hoppes trygt over), og sti-vernet tillater `<slug>/index.html` for ikke-reserverte slugs (aldri rot-index.html). Nye vern-tester.
- Dekor-knappen (✦) forklarer nå fargene i tooltipen (gul = pynt som skjules i automatisk mobil-layout, grå = innhold som vises), og teksten følger tilstanden.

### 0.5.1.4 - brukerveiledning - 17. juli 2026
- Ny [docs/BRUKERVEILEDNING.md](docs/BRUKERVEILEDNING.md): veiledning for sideeiere (editoren, blokker, dekor-flagget, seksjoner, grid, mobil, publisering). Utvides løpende; full opprydding er planlagt mot v0.9.

### 0.5.1.3 - gruppene som menyknapper - 17. juli 2026
- Gruppene i Blokker-panelet ser ut som blokk-knappene (full bredde, samme ramme) med ▸/▾-pil til høyre; åpnet gruppe viser blokkene som vertikal liste under, lett innrykket.

### 0.5.1.2 - blokkgrupper og tekstboks - 17. juli 2026
- Tekstboks: tekstblokk-variant der innholdet ligger i et kort med temaets flatefarge, kantlinje og avrundede hjørner. Valgfritt additivt `box`-felt på tekstblokkens props; eldre data rendres uendret.
- Blokker-panelet er gruppert: Tekst (Tekst, Tekstboks) og Former (Strek, Pil, Sirkel, Rektangel, Trekant) som grupper, med Knapp og Bilde imellom.

### 0.5.1 - ny editor-layout (M1) - 17. juli 2026
- Smal panelvelger til venstre med tekstknapper (Sider, Blokker, Egenskaper, Tema, Nav, Grid, Historikk) som åpner et panel mellom linjen og forhåndsvisningen. Blokkpaletten bor nå i Blokker-panelet og grid-innstillingene i Grid-panelet (gridet vises så lenge panelet er åpent); Sider/Egenskaper/Tema/Nav/Historikk er skall som fylles utover v0.5. Topplinjen er slanket til sidevelger, 💻/📱, tilsyn-chip og publiseringsknappene.
- Statusmeldinger vises som toast nederst til høyre (med lukkeknapp) i stedet for chip i topplinjen.
- Forhåndsvisningen melder blokkvalg til editoren (`urd-select-block`), så Egenskaper-panelet vet hvilken blokk eller seksjon som er valgt.

## [0.4.0] - 2026-07-17

«Responsivt»: sider bygget på desktop fungerer på mobil uten manuell
inngripen, og håndjusterte mobil-layouts voktes av tilsyn-flagget fra
det opprinnelige designet. Porten bestått på urdweb.pages.dev.

### Lagt til
- **Responsivt:** besøkende på mobil (skjerm smalere enn breakpointet, standard 640px) får automatisk én-kolonne-layout: blokkene stables i leserekkefølge som ekte flyt, tekst får naturlig høyde, og dekor-blokker utelates. Motoren bytter modus live ved vindusendring.
- Mobilvisning i editoren: 💻/📱-toggle smaler forhåndsvisningen til 390px. Mobilvisningen er layoutjustering og tilsyn (palett, strukturverktøy og tekstredigering er reservert desktopvisningen).
- Manuelle mobil-layouts: første dra i mobilvisning setter seksjonen i manuell modus og gir alle blokkene konkrete mobil-frames (lest fra flyt-posisjonene), klare til fri justering. ↺ tilbakestiller til auto.
- Mobil-tilsyn-flagget ende-til-ende: desktop-endringer i en håndjustert seksjon markerer den gult og teller opp i topplinjen («N seksjoner trenger mobil-tilsyn», klikk hopper til mobilvisning); ✓ på seksjonen kvitterer ut. Lagres som data og publiseres (deles mellom redaktører); besøkende påvirkes aldri.
- Dekor-flagg (✦ på blokkverktøylinjen): blokken utelates fra automatisk mobil-layout. Nye former er dekor som standard. Valgfritt additivt skjemafelt (`decor`), ingen migrering nødvendig.
- «Til toppen»-pil på nettsiden: dukker opp nede til høyre etter scrolling (respekterer prefers-reduced-motion). Scrollbaren skjules i editorens mobil-forhåndsvisning.

## [0.3.0] - 2026-07-17

«Lerretet»: fri komposisjon på ordentlig. Alle kjerneblokkene er på plass
(tekst, knapp, bilde med opplasting, fem formtyper), seksjoner opprettes,
flyttes, skaleres og slettes in-place, alt kan angres, og grid-systemet er
lagt om til rene hjelpelinjer som aldri flytter innhold. To skjemamigreringer
(fysiske enheter og kvadratgrid) er gjennomført etter kontrakten og bevist
med tester, og repoet fikk fullt kvalitetsvern (CI, CodeQL, Dependabot).

### Lagt til
- Angre/gjenta i editoren (Ctrl+Z / Ctrl+Shift+Z) for alle handlinger: flytt, resize, blokker og seksjoner inn/ut, seksjonshøyde, grid-endringer og forkast. Fungerer også når fokus står i forhåndsvisningen.
- Bildeblokk med opplasting: «+ Bilde» komprimerer til webp i nettleseren (maks 1600px, med størrelsesvarsel), viser bildet umiddelbart i utkastet, og publiserer det som fil i media/ i samme commit (samme bilde gir alltid samme filnavn). Valgfri lenke gjør blokken brukbar som logo.
- «+ Form»-meny med strek, pil, sirkel, rektangel og trekant.
- z-orden: ⬆/⬇-knapper på blokkverktøylinjen legger blokken helt foran/bak.
- Klikk-markering av blokker: varig omriss og synlige håndtak til man klikker utenfor.
- Målseksjon: paletten legger nye blokker i sist klikkede seksjon (markert med aksentlinje), plassert midt i synsfeltet.
- «Tilpass høyden til innholdet» (⤓) på seksjonsverktøylinjen: setter seksjonshøyden til nederste blokkkant med ett klikk, lagret gjennom vanlig utkastflyt.
- Grid per seksjon («Eget grid i valgt seksjon») og Shift-for-fri-plassering under dra/resize.
- «Ren visning»-knapp som skjuler alle editeringshåndtak og hele topplinjen; flytende Rediger-knapp tar deg tilbake.
- Tekstblokker vokser automatisk med innholdet under skriving.
- Statusmeldinger som fargekodet chip: grønn kvittering «✓ Publisert! Siden bygges på nytt (~1 min)» som rydder seg selv, rød for feil.
- Strammet CSP i `_headers` (script/style/connect kun 'self'); boot-skriptet flyttet til `assets/engine/boot.js`.
- Kvalitetsvern på repoet: CI-workflow (tester, skjemavalidering, editor-bygg), CodeQL, Dependabot (npm + actions) og dependency review. Skjemavalidering er `npm run validate` i editor/.

### Endret
- **Blokkplassering lagres i fysiske enheter** (sidefiler schemaVersion 2: x/w i prosent av seksjonsbredden, y/h i px). Gridet er dermed KUN et snappeverktøy: å endre det flytter aldri innhold. v1-filer løftes automatisk ved lasting (`liftPageFile`), Urds første reelle filmigrering, testet i `tests/page-migration.test.mjs`.
- **Gridet er kvadratisk med én innstilling: rutestørrelse i px** (slider; mindre = tettere). site.json er schemaVersion 2 og sidefiler schemaVersion 3; eldre filer OG gamle localStorage-utkast løftes automatisk (kjedet migrering).
- **Blokker kan bevisst henge over seksjonslinjen:** seksjoner klipper aldri innhold, og seksjonshøyden er nøyaktig den brukeren har satt (motoren tvangs-utvider ikke lenger). Fjernet avviket mellom editor og publisert side; sidescrolling hindres med overflow-x: clip.

### Fikset
- Robusthet mot GitHub-nedetid: 5xx/429 fra GitHub-API-et prøves automatisk på nytt, «GitHub er nede» skilles nå fra «ugyldig innlogging» (en GitHub-hikke så tidligere ut som at man ble logget ut av admin), og feilmeldinger kortes ned i stedet for å vise GitHubs HTML-feilsider.
- «+ Ny seksjon»-barene tok plass i flyten og forskjøv seksjonene i editoren i forhold til hvordan besøkende ser dem; de svever nå oppå selve skillet (null høyde i layouten), så høyde-linje, bar og fargeskift ligger på samme sted.
- Seksjonsverktøylinjen (↑ ↓ ⤓ ×, der × sletter seksjonen) vises kun mens pekeren er i seksjonen, og ligger alltid øverst (kunne før dekkes av løftede blokker).
- Tekstblokker: redigeringshåndtakene lå inne i det redigerbare feltet, slik at ny tekst kunne havne i verktøylinjeraden, håndtak-HTML ble lagret i innholdet (som så «forsvant»/rotet seg ved rerendering), og resize-håndtaket lurte vekstmålingen til å utvide blokken for hvert tastetrykk. Teksten har nå sitt eget indre innholdselement, og gammelt forurenset innhold renses automatisk ved rendering.
- Publisering tar nå med utkast fra ALLE sider, ikke bare siden man står på (endringer på andre sider ble stående upublisert i stillhet). «Upubliserte endringer»-merket teller også alle sider.
- Sidevelger-nedtrekket følger mørkt tema (nettleser-standarden ga hvit liste med uleselige valg).
- Nye blokker plasseres nå midt i synsfeltet (i aktiv seksjon, ellers seksjonen nærmest skjermmidten) i stedet for nederst utenfor syne. Plasseringen beregnes i forhåndsvisningen, som vet hvor brukeren har scrollet.
- Slette-bekreftelsene er fjernet (Ctrl+Z er angrefristen), nedtrekksmenyene lukkes ved klikk utenfor, og Rediger-knappen i ren visning ligger under nettsidens topplinje, klar av scrollbaren, og mer transparent.
- Blokk-håndtakene (flytt/z/slett/resize) kunne dekkes av overlappende blokker og bli uklikkbare: hovret/markert blokk løftes nå visuelt øverst under redigering. «Frem/bak» legger dessuten blokken helt øverst/nederst i seksjonen i stedet for ett hakk (som satt fast når alt lå på samme lag).
- Utkast kunne «forsvinne» ved sidebytte i admin: editoren sendte utkastet før motoren i iframen var klar til å lytte. Motoren melder nå «klar», og editoren venter på signalet.
- Lenker i forhåndsvisningen navigerte iframen ut av redigeringsmodus: interne lenker bytter nå side via editoren (nedtrekket følger med), eksterne åpnes i ny fane.
- Ctrl+Z virket ikke fra tallfeltene i grid-menyen (grid-endringer er del av historikken, men tastetrykket ble slukt av feltet).
- Gridet vises nå i alle seksjoner så lenge grid-menyen er åpen, med forklarende tekst og live rutestørrelse i menyen.
- Grid-innstillinger så ut til å ikke virke på Om oss-siden: eksempelseksjonen hadde et eget grid-overstyr (12 kolonner) som vant over site-gridet. Overstyret er fjernet fra eksempelinnholdet; per-seksjon-grid får eget UI senere.
- Høyde-håndtaket på seksjoner viser nå en presis kantlinje og respekterer snap-innstillingen.
- Seksjonshøyde kan dras: håndtak i underkant av hver seksjon i editoren, snappet til gridets radhøyde.
- Seksjons-CRUD i editoren: «+ Ny seksjon»-barer mellom seksjonene med preset-valg (tom, hero, footer), og verktøylinje per seksjon for å flytte opp/ned og slette. Presets er datafabrikker i motorregisteret (`Urd.sections.define`), samme API som plugins får.

## [0.2.0] - 2026-07-16

«Tynn skive»: Urd fungerer ende-til-ende. Porten bestått: en overskrift redigert
i nettleseren på en deployet side (urdweb.pages.dev) ble publisert med ett klikk
og vist live, uten at en kodefil ble rørt.

### Fikset
- Publisering kan trigge hostens deploy direkte via valgfri `DEPLOY_HOOK_URL` (git-webhooken hos Cloudflare kan glippe; publiseringen er da ikke lenger avhengig av den).
- Publisering skrev til feil sti når nettsiden ligger i en undermappe av repoet (som `template/` i monorepoet): ny valgfri miljøvariabel `GITHUB_ROOT_DIR` prefikser repo-stiene, mens editor og sti-vern fortsatt jobber nettside-relativt.
- Admin krasjet ved oppstart etter grid-arbeidet (structuredClone av Svelte-proxy i utkastlageret).

### Lagt til
- Synlig grid under dra/resize, og grid-kontroller i editoren (kolonner, radhøyde, snap av/på) som redigerer site.json gjennom utkast-og-publiser-flyten. Snap av gir fri plassering i kvarte grid-enheter.
- Lerret-redigering (v0.3-start): dra, resize og slett blokker med grid-snapping direkte i forhåndsvisningen (håndtak på hover), og blokkpalett (+ Tekst / + Knapp / + Form) i editoren. Editeringslaget (`preview-edit.js`) lastes kun i preview-modus og når aldri besøkende.
- Publiseringslaget: GitHub OAuth (login/callback/me) og commit/latest via Git Data API som Cloudflare Pages Functions, med httpOnly-token, ALLOWED_LOGINS og sti-allowlist (enhetstestet). Innloggingsstatus og «Logg inn med GitHub» i editoren. Oppsettguide i `docs/OPPSETT-PUBLISERING.md`.
- Editoren (første versjon): preview-iframe med den ekte siden, klikk-og-skriv på tekstblokker, utkast i localStorage med ærlig «upubliserte endringer»-merke, forkast og publiser (publiseringslaget er fortsatt stubb). Bygges fra `editor/` til `template/admin/assets/`.
- Motoren rendrer sider: `render.js` (grid-til-CSS, migrering, plassholdere), blokkene `text`/`button`/`shape`, bakgrunnslagene `color`/`gradient`/`glow`/`grain`, navigasjon fra sideregisteret, plugin-lasting og preview-modus i `urd.js`. Tester i `tests/render.test.mjs`.
- `CONTRIBUTING.md`: bidragsguide for eksterne bidragsytere (fork/gren/PR-flyt).
- Visjons- og arkitekturdokumenter (`docs/`), datamodell-kontrakt (`docs/SKJEMA.md` + `schema/`).
- Repo-skjelett: `template/` (motor-stubber, eksempelinnhold, functions-stubber) og `editor/` (Svelte-skall).
- `template/assets/engine/migrate.js` - stegvis versjonsløfting (kjerne-invarianten), med tester i `tests/`.
- Utviklerdokumentasjon (`docs/UTVIKLING.md`) og løpende oppgaveliste (`docs/BACKLOG.md`).
