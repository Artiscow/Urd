# Backlog

Løpende oppgaveliste for Urd-utviklingen: konkrete gjøremål, feilrettinger og forslag. [VEIKART.md](VEIKART.md) eier fasene og målene; denne listen eier de konkrete oppgavene. Flytt gjerne ting mellom seksjonene, og stryk med `[x]` når noe er gjort (rydd bort fullførte ved versjonsslipp). Nye milepæls-punkter får en «Ferdig når:»-linje med observerbare betingelser.

Fullførte punkter fra v0.2-v0.6 er ryddet bort ved versjonsslippene (0.3.0 17. juli 2026, 0.5.10 18. juli 2026, 0.6.11 9. august 2026); se [CHANGELOG.md](CHANGELOG.md) for hva som ble levert. Innkomne forslag og funn sorteres rett inn i milepælen eller fasen der de best tas; dokumenterte begrensninger uten plan står i egen seksjon nederst.

Et innslag arver nummeret til backlog-punktet arbeidet hører under, pluss ett siffer for rekkefølge (0.6.6.5 → 0.6.6.5.1, 0.6.6.5.2 …). Sifferet legges kun på når milepælen får mer enn én commit: lander hele milepælen i én commit, er nummeret milepælens eget (0.7.1), og senere commits under samme milepæl fortsetter på 0.7.1.2. Fasens stående Løpende-punkt er alltid firedelt (0.7.0.x).

**Testrundene (sjekkliste over levert arbeid som venter på testing) bor i [TESTRUNDER.md](TESTRUNDER.md)**

## Til v0.7 «Finpuss + butikk» HUSK: Se /sammenligning før vi starter

Fasen er delt opp i milepæler 9. august 2026: arkitekturavklaringene (bredde, mobil) først siden butikk- og designarbeidet bygger på dem, deretter blokker/butikk/synlighet, finpuss mot slutten. Rekkefølgen er veiledende; fase-porten står i [VEIKART.md](VEIKART.md). Milepælene 0.7.11 og 0.7.12 kom til 10. august 2026 etter en gjennomgang av kartene i [sammenligning/](sammenligning/) mot dagens plattformstøtte; begge hører til finpussen mot slutten, og 0.7.12 kan skyves til v0.8 om fasen blir for tung. 0.7.13 kom til 14. august 2026 etter en skjermbildegjennomgang mot ApeironLF, og samme dag ble Forslag og Ideer-punktene som hadde en naturlig milepæl sortert inn (0.7.4, 0.7.9, 0.7.10 og 0.7.13). 0.7.15 was added on 16 September 2026 after a survey of the builders' navigation bar controls; the survey and the settings model stand in the item itself so the work can be done straight from the backlog.

- **0.7.0 Løpende** (stående punkt, viderefører 0.6.0-konvensjonen): arbeidsflyt, kodegjennomganger og vedlikehold som ikke hører under noen milepæl; innslag nummereres 0.7.0.x. Ingen «Ferdig når»-linje, punktet lukkes aldri
  - [ ] Vaktliste fra moderniseringsgjennomgangen 10. august 2026: plattformfunksjoner som er interessante men ikke klare. Hver har en eksplisitt gate, slik ADR-0011 krever; sjekkes ved fasegjennomganger, bygges ikke før gaten er passert
    - `::scroll-marker()` / `::scroll-button()` (Chrome 135+, Safari 19+, Firefox bak flagg): ville erstattet de håndrullede prikkene og pilene i `blocks/gallery.js`, og trolig motorens eneste `setInterval`. Gate: baseline i alle tre motorene
    - `appearance: base-select` (Chromium-only): den eneste veien som noen gang kan pensjonere ADR-0009s forbud mot native `<select>` og dermed BEGGE nedtrekks-implementasjonene. Gate: Firefox og Safari i stabil
    - A client-side router (0.7.8, closed 11 September 2026): captures internal link clicks, fetches page.json and swaps `#urd-root` without a reload, removing the re-boot per page switch. Gate: built only if Interop 2026 does not deliver Speculation Rules prefetch and prerender in Firefox and Safari before v1.0; until then the intent prefetch and the platform carry it
    - MCP-protokollversjonen (nyeste spesifikasjon er 2026-07-28; roots, sampling og logging er deprekert med minst tolv måneders levetid). Urd bruker ingen av dem, men versjonen sjekkes før MCP-koden bygges i v0.8 (ADR-0017)
  - [ ] Translate the remaining developer documents to English (ADR-0022), one at a time with an English file name and every link updated in the same commit: VISJON, VEIKART, UTVIKLING, BACKLOG, TESTRUNDER, sammenligning/ and CONTRIBUTING. Delivered in 0.7.0.4: the ADRs, AGENTS.md, SCHEMA.md and ARCHITECTURE.md. Once VISJON, VEIKART and UTVIKLING are translated, their en-GB files under docs/languages/ become the bokmål translations instead
  - [ ] Node-versjonen er kun festet i CI (`tests.yml` og `release.yml`, begge `node-version: 22`), og repoet har verken `.nvmrc` eller `engines`-felt. Bump til gjeldende LTS-linje og fest den ett sted så CI og lokal utvikling ikke driver fra hverandre
- [x] **0.7.1 Oppryddingsrunden** (levert 0.7.1, venter på testrunde): Lav-funnene fra kodegjennomgangen 27. juli 2026 pluss to bugs fra testingen, hvert funn re-verifisert mot dagens kilde før fiks
  - [x] Bilde-`src` uten `SAFE_IMAGE_RE`: `isSafeImage` eksportert fra nav-model.js og brukt i nav-logo, footer-logo, ikonblokk, bildelag og bildegalleri-lag; faviconet importerer den i stedet for sin duplikat-regex (favicon og nav-bakgrunn var alt vernet). Glød-laget har defaults for x/y/radius; samling-blokken nuller `editCtx` i `finally`; `urd-preview-full` normaliserer `sections`; `callback.js` krever `ok` og tåler ikke-JSON; `ics.js` håndhever grensen mot Content-Length og en byte-teller under strømlesing. CSRF-sjekken er lagt om til Sec-Fetch-Site med Origin som reserve (gjelder alle metoder, siden oppdaterings-sjekken er en GET med eierens token)
  - [x] Housekeeping: `validate.mjs` løper over sideregisteret i site.json og tar med foreldreløse sidefiler, så alle fire innholdssidene dekkes. Funnets øvrige delpunkter (SKJEMA-csp-feltet, kalender-versjonen, urd.json-motorversjonen) var verifisert løst underveis
  - [x] Bug: bekreftelsesdialogen (blant annet «Lagre som mal») lukkes nå med Escape og klikk på bakteppet
  - [x] Bug: sticky blokker festet seg bak en klistret meny; menyhøyden legges nå til avstanden. To andre årsaker dokumentert i stedet for kodet rundt: seksjonen må være høyere enn blokken (nå i hjelpeteksten), og transform-vakten slipper taket i seksjoner med løft ved peker eller inngangsanimasjon
  - [x] Oppfølging etter testing: festingen var usynlig i redigeringsvisning, flere festede blokker la seg oppå hverandre, og det fantes ingen måte å holde noe fast på skjermen. Festingen er nå aktiv i redigering (suspenderes kun mens geometri skrives), et flerutvalg kan festes som én gruppe som beholder plasseringen seg imellom, og en blokk kan dokkes til et fast punkt i vinduet for hele siden. Nye additive felt `sticky.mode`, `sticky.dock` og `sticky.group`
- [x] **0.7.2 Fast innholdsbredde på sidene (breddegrepet, egen ADR).** LEVERT 0.7.2, 0.7.2.2 og 0.7.2.5 (venter på testrunde) (reopened for 0.7.2.6 and 0.7.2.7 on 16 September 2026: the Screen device follows the own browser window width or a per-browser editing size, ADR-0018 addendum), besluttet i [ADR-0018](adr/0018-bound-content-width.md): ny `.urd-canvas`-node per seksjon binder innholdet til `site.layout.contentWidth` (standard 1440) mens seksjonen beholder full bredde og eier bakgrunnen; `frameToCss` uendret. Standarden var først satt til 1200 på feil grunnlag og ble korrigert til 1440 i 0.7.2.2 etter ny research; samme runde byttet sidemargen fra piksler til prosent av vindusbredden (schemaVersion 3), ga innstillingen en levende prøve mot tre skjermbredder med skala og Avansert-visning, og byttet lerretets visningsmodus mot fire ekte enhetsstørrelser uten letterboks (Squarespace-modellen). Konverterte alt uten grandfather (pre-v1-klausulen), og konverteringen viste seg å ikke trenge dataendringer. Container queries og subgrid er LUKKET i ADR-en: `container-type: inline-size` gir `contain: layout` og ville drept festingen fra 0.7.1; Fluid-Engine-radmodellen er skjøvet til egen milepæl med egen ADR. 0.7.2.5 samlet oppfølgingen i redigeringslaget som breddegrepet avdekket: rutenett, hjelpelinjer og håndtakstørrelser på nedskalert lerret, tre merkede verktøyklynger og en strammere sideskinne, og en topplinje som folder seg i faste trinn i stedet for å bryte til to rader. GJENSTÅR før avkryssingen er reell: den visuelle gjennomgangen av de 24 presetene ved 1440 px, som kun testrunden kan avgjøre. Opprinnelig beskrivelse: Gå fra full-bleed (blokker plassert som % av HELE vindusbredden) til en bundet innholdsbredde med sidemarger på brede skjermer (Wix-modellen: fullbredde seksjonsbakgrunn, bundet innhold). Fikser to ting med ETT grep: (1) admin-forhåndsvisningens fold-avvik (avslører seksjonen under, fordi `frameToCss` i `engine/render.js` har vannrett i % og loddrett i px -> ikke breddeinvariant -> uniform scale, jf. 0.6.6.5.9 og LAERDOMMER §2 «Forhåndsvisnings-lerretet»); (2) «blokker/elementer strekker seg evig med bredden»-flow-out-bugene (samme rot: % av ubunden bredde). Med fast bredde blir layouten breddeinvariant -> forhåndsvisningen skalerer tapsfritt og folden stemmer. MÅ avklares i egen ADR: konfigurerbar bredde vs. fast verdi (trolig per-site med fornuftig standard, jf. Wix maks-bredde), full-bleed bakgrunner + bundet innhold, og migrering (ADR-0005: eksisterende %-plasseringer mot ny bredde, eller grandfather gamle sider som full-bleed). Rører kjerne-layout, motor, editor-lerret og publisert utdata. Tillegg 9. august 2026: ADR-en skal også vurdere container queries/subgrid (ELEMENTKART 5b.9) - breddegrepet gir det konkrete bruksstedet som har manglet - og Fluid-Engine-grepene fra LAERDOMMER 5.1 (rader som vokser med innholdet via minmax, aldri krympe en blokk under innholdet), som hører til samme layoutmodell-beslutning. Ferdig når: ADR-en er besluttet, publisert side og preview rendrer med bundet innholdsbredde (fold-avviket og flow-out-bugene er borte), og migrerings-/grandfather-veien for eksisterende sider er testet.
- [ ] **0.7.3 Mobil-revurderingen** (observasjon 17. juli 2026 etter å ha håndlaget desktop+mobil: dagens kobling desktop→auto/manuell mobil er vanskelig å jobbe med). MODELLEN ER BESLUTTET 13. august 2026 i [ADR-0019](adr/0019-synced-mobile-model.md): synket mobilmodell per blokk (radnettet). Rot-årsaken var at materialiseringen er alt-eller-ingenting per seksjon; ny modell er én mobil-rendersti for alle seksjoner (CSS-grid, én kolonne, minmax(8px, auto)-rader fra LAERDOMMER 5.1), der urørte blokker auto-plasseres i stackOrder-rekkefølge og håndrørte lagrer en partiell plassering i frames.mobile ({x?, w?, row?, rows?, z?, rot?}; tilstedeværelse = overstyrt). Seksjonsmodiene auto/manual pensjoneres (manual leses av migreringen, skrives aldri), PAGE_SCHEMA_VERSION 1→2. De fire delpunktene er sortert inn under beslutningen: (a) egen mobilversjon AVVIST med kilde (m-dot dødt, Google anbefaler responsivt; står i ADR-en) og deklarativ per-brekkpunkt-variant (LAERDOMMER 5.11) AVVIST samme sted; (b) tilsynsmerket hopper til første tilsynsseksjon i mobilvisning og seksjonen viser tilsynskort med oversatt reason/since (feltene skrives i dag men leses aldri), ✓ og to-nivås tilbakestilling (per blokk og per seksjon med bekreftelse); (c) nytt additivt felt hideMobile skilles fra decor (som renskes til kun animasjonsbetydningen), og siden renderstien blir én, kan «virker ikke i manuelle seksjoner» ikke lenger oppstå; (d) mobileOrder får pil-opp/ned-UI i mobilvisningens blokkverktøylinje (kun flytblokker; ren funksjon reorderMobileKey). I samme milepæl: Delete/Backspace får mobil-vakt i preview-edit.js (eneste snarvei uten), og ny-blokk-i-rørt-seksjon-bugen (landet på desktop-frame utenfor 390 px) forsvinner strukturelt siden nye blokker flyter inn. Etappene krysses av ved testing:
  - [ ] 0.7.3.1 ADR + docs
  - [ ] 0.7.3.2 modellbyttet (atomisk: render/migrate/base.css/preview-edit/App/bridge/presets + tre-steder + tester)
  - [ ] 0.7.3.3 tilsynet
  - [ ] 0.7.3.4 skjul på mobil
  - [ ] 0.7.3.5 rekkefølge
  - [ ] 0.7.3.6 finpuss (piltast-flytting av pinnede vurderes der)

  Ferdig når: alle seks etappene er levert og testet, testene fra planen er grønne (mobilePlacementToCss, stackOrder-filterbyttet, migreringen med idempotens, reorderMobileKey), og testrunde-batchen er skrevet med publisert-mobil-lik-editor-punktet.
- [x] **0.7.4 Blokk- og feltrunden** (levert 0.7.4, venter på testrunde): flere skjema-felttyper (funksjonskartet C5): nedtrekk, avkryssing, radio, dato - butikk-kassen gjenbruker dem; tabell-blokk (funksjonskartet C8: åpningstider, prislister); delingsknapper og nedteller (C9, flyttet hit fra Forslag og Ideer 9. august 2026: statiske delingslenker uten sporing, nedteller for arrangementer); audio-blokk (C10, flyttet hit fra Forslag og Ideer 14. august 2026: HTML5, git-eid fil - billig og statisk-vennlig). Ferdig når: de nye felttypene kan settes inn og valideres i skjema-pluginen, og tabell-, delings-, nedteller- og audio-blokkene kan settes inn fra begge blokkmenyer med Innhold/Stil-panel og i18n i kjernespråkene.
- [x] **0.7.5 Butikken.** LEVERT 0.7.5.1-0.7.5.2 (venter på testrunde; Ferdig når-kriteriene om deployet testbestilling og testbetaling avgjøres av testrunden). Butikk uten betalingsgateway som KJERNEFUNKSJON (valgt 18. juli 2026, ApeironLF-modellen): produktkort-blokk med varianter (størrelse/farge, pris + evt. medlemspris, badge, bildegalleri der fargevalg bytter bilde), handlekurv-blokk (localStorage + skuff med antall-badge), kasse = bestillingsskjema (navn/e-post/telefon/kommentar + honeypot) som går til e-post (mailto, null oppsett) eller valgfritt endepunkt (Apps Script/Pages Function), betaling via Vipps-nummer-instruks. Helt avhengighetsfri, katalogen git-eid. Bygges PÅ v0.6-datablokk-mønsteret (produktkatalog = samling). Må lande før v1: v1.0-porten (gjenskape ApeironLF) forutsetter den. I samme milepæl: produktkort-preset fra v0.5 («kjøp = ekstern lenke») oppgraderes til å kunne peke på ekte produktblokker, CSV-import/-eksport for samlinger (funksjonskartet C12: senker terskelen for medlemslister/produktkatalogen), og ekte betalings-kasse (flyttet hit fra horisont-lista 9. august 2026: Snipcart-modellen med git-eid katalog + CSP-opt-in, eller Vipps Checkout via Pages Function) som VALGFRITT lag oppå bestillingsskjema-kassen - skjema-kassen forblir standard, og kjernen forblir gateway-fri. Ferdig når: en testbestilling går gjennom handlekurv, bestillingsskjema og e-post på en deployet side, produktkort-presetet kan peke på ekte produktblokker, en samling kan importeres og eksporteres som CSV, og en testbetaling går gjennom den valgfrie betalings-kassen. Tillegg 25. august 2026 (research-runde mot Wix/Squarespace/Webflow etter testfunnet «ingen måte å legge til produkter»): butikken skal være en OPPLEVELSE, ikke bare blokker - de store byggerne skaffolder egne sider (butikkside, kasseside), har produktdetalj-visning og to innganger til katalogen (panel + direkte fra butikkelementet). Etappene:
  - [x] 0.7.5.a Butikken som side (levert 0.7.5.1, venter på testrunde): kasse-seksjonspreset + startpakkene Butikk (hero/butikk/faq/cta) og Kasse (kasse/kontakt); kurv→kasse kobles med det eksisterende Kasseside-valget
  - [x] 0.7.5.b Produktdetalj som quick view (levert 0.7.5.1, venter på testrunde; Squarespace-mønsteret, statisk-vennlig): klikk på kort åpner native dialog med bildegalleri, full tekst, variantvalg og kjøp; ekte produkt-permalinker venter på v0.8-bakingen (C4b)
  - [x] 0.7.5.c Katalogflyten (levert 0.7.5.1, venter på testrunde; to innganger): «+ Produkt»-adder i blokken (ny melding urd-collection-add), «+ Nytt produkt»/«Rediger produktkatalogen»/«+ Opprett produktkatalog» i Egenskaper, kind-bevisst innslags-seed (produkter uten dato), panelpuss (pris i summary, dato/lenke skjult for produkter), prisrad kun med satt pris
  - [x] 0.7.5.d Det valgfrie betalingslaget (levert 0.7.5.2, venter på testrunde; valget falt på Vipps Checkout via Pages Function, [ADR-0020](adr/0020-optional-payment-layer-vipps-checkout.md) - Snipcart avvist der) oppå skjema-kassen, som forblir standard
  - [x] 0.7.5.e Design-runden (levert 0.7.5.2, venter på testrunde; 25. august 2026, etter testfunn og LTT/Shopify-research mot lttstore.com): tre testfunn rettes (panelets fargeprøver følger forhåndsvisningens aktive lys/mørk-modus; handlekurven kan velges og dras med totrinns-mønsteret; produktredigering hopper ikke til toppen - målrettet rerender av samlingsseksjoner), ny Butikk-preset-gruppe (butikk-hero, butikk-kategorier, butikk-tillit, butikk-utstilling med section.theme-rollene dus/dempet/dyp), redesignet Butikk-startpakke, ny Butikkforside-startpakke (den fulle butikkfronten: kampanje-hero, produktbånd, kategorifliser, statement-bånd, tillit, CTA), og hover-bildebytte på produktkortene (første fargebilde, CSS-først)
- [x] **0.7.6 SEO- og synlighetspakken** (levert 0.7.6, venter på testrunde; deploy-kriteriene - publiserte filer, feed og analytics - avgjøres der. Fra sammenligningsgjennomgangen i samme runde: X-kortet og per-side robots-kontroll levert i tillegg; JSON-LD `Event` avgrenset til senere siden arrangementsdataene er plugin-eide, feed-oppdagelseslenker venter på v0.8-innslagssidene, og 301-redirects er eget punkt C13/ADR-0015): **SEO-grunnpakke** (valgt 23. juli 2026, funksjonskartet C3 - Urds største hull mot ALLE de andre byggerne): per-side beskrivelse + og-felter (tittel/beskrivelse/bilde) i Sider-panelet, sitemap.xml + robots.txt generert ved publisering, 404-side, canonical. Feltene og filene bygges nå; full crawler-effekt kommer med v0.8-bakingen. NB: nettstedsnivå-metadataen (navn/beskrivelse/favicon) er nå redigerbar i det nye Nettsted-panelet (levert 0.6.6.5.8); dette punktet dekker per-SIDE-feltene + de genererte filene. Pluss **Personvennlig analytics** (referanse-plugin/oppskrift, funksjonskartet C14, 23. juli 2026): Cloudflare Web Analytics eller Plausible via CSP-opt-in-flyten (manifest-csp → _headers-instruks), git-eid og sporingsfri i kjernen. Paret med SEO-grunnpakken (synlighet/måling); leveres som plugin på linje med kalender/skjema/kart, og RSS-feed for samlinger (funksjonskartet C4): statisk XML generert ved publisering; nyhetssamlingene får abonnenter - samme publiserings-genererte mekanikk som sitemap. Tillegg 10. august 2026: **JSON-LD strukturerte data** manglet i pakken. For målgruppen (lokal forening, småbedrift, arrangementer) er `Organization`/`LocalBusiness` på nettstedsnivå og `Event` på arrangementsinnslag det som faktisk gir rike søketreff, og det er ren statisk markering i `<script type="application/ld+json">` uten noen avhengighet; feltene finnes stort sett alt i Nettsted-panelet og samlingene. Ferdig når: publisert side har per-side beskrivelse/og-felter, sitemap.xml, robots.txt, 404-side, canonical og JSON-LD for nettstedet; minst én samling har RSS-feed generert ved publisering; analytics-referansepluginen/oppskriften finnes med CSP-opt-in.
- [x] **0.7.7 Video-bakgrunnslag og mediegrenser** (levert 0.7.7, venter på testrunde. Fra den fulle gjennomlesningen av sammenligningskartene i samme runde: observer-styrt avspilling med preload=metadata og parallax-paritet levert i tillegg, og C7-levert-merket ført inn i FUNKSJONSKART; grenserunden avdekket og rettet at vokterens media-liste manglet lyd-/videoendelsene): video-bakgrunnslag (funksjonskartet C6): selvhostet mp4/webm-loop som bakgrunnslag, personvennlig siden filen er git-eid; størrelsesgrense må settes (ses sammen med bildestørrelse-avklaringen i samme milepæl); og avklar maks bildestørrelse og varselgrenser i editoren (hosters filgrenser) - grensene settes samlet. Ferdig når: et selvhostet videolag kan settes som seksjonsbakgrunn med størrelsesgrense og pen degradering (prefers-reduced-motion, fallback-lag), og bilde-/videogrensene er avklart med varsler i editoren.
- [x] **0.7.8 Klientside-navigasjon** CLOSED 11 September 2026 (reopened for 0.7.8.2 on 16 September 2026: recheck at prerender activation and bfcache restore, and the deploy wait after publish) with the measurement the gate asked for (delivered 0.7.8, awaiting the test round): a full page switch on urdweb.pages.dev was 13 serial round trips, 400 to 580 ms of network floor in a non-Chromium browser, against 20 to 60 ms for one page.json. Delivered instead of a router: one fetch wave for the plugins and an early page.json fetch at boot (13 to 5 round trips), intent prefetch of the next page into sessionStorage with background revalidation (4 round trips on a hit), and a `prefetch` rule in speculation-rules.json. The router itself is not built: the remaining gap is about 100 to 200 ms plus the re-boot, against durable code that must own history, scroll, title, sticky, animations, plugins and the preview boundary, while Speculation Rules are an Interop 2026 focus. It stays as a gated item in the watch list under 0.7.0. Original scope for reference: (kryssnettleser-oppfølger til 0.6.32-ytelsen): fang klikk på interne lenker (mål i sideregisteret), hent kun ny `page.json` og bytt ut `#urd-root` i stedet for full omlasting, så interne lenker føles umiddelbare i ALLE nettlesere. Speculation Rules-prerenderen fra 0.6.32 er Chromium-only (Firefox har ikke skipet API-et, Ladybird heller ikke), mens dette virker overalt og gjør Chromium raskere også (ingen re-boot per sidebytte). Kanttilfeller som må dekkes: history/back-forward (pushState + popstate), `document.title` + nav-aktiv-markering, scroll til topp, re-init av sticky/animasjoner/plugins per side, må ALDRI kollidere med totrinns-editoren i `?preview=1` (editoren driver navigasjonen selv), og degradere til full navigasjon ved fetch-feil eller ekstern/hash-lenke. Egen versjon med egen testrunde. **MÅL FØRST (port lagt til 10. august 2026):** premisset under punktet har flyttet seg. Speculation Rules er nå et fokusområde i Interop 2026 (Apple, Google, Microsoft, Mozilla, Bocoup, Igalia), og cross-document View Transitions er i Chromium og Safari 18.2+ med Firefox underveis. Vinduet der plattformen ikke gjør dette selv krymper, mens en håndrullet ruter er varig, skjør kode vi må eie. Mål derfor faktisk navigasjonskostnad på en deployet side i en ikke-Chromium-nettleser, mot dagens fulle omlasting, FØR ruteren bygges; er forskjellen liten, er riktig svar å vente på plattformen i stedet. Ferdig når: målingen er gjort og dokumentert, og enten er ruteren bygget slik at interne lenker bytter side uten full omlasting i alle nettlesere (history/tittel/scroll/re-init dekket, degraderer til full navigasjon ved feil, preview-editoren uberørt), eller milepælen er lukket med målingen som begrunnelse.
- [ ] **0.7.9 Design-strekket fra 0.6.6** (flyttet hit 9. august 2026; var STREKK-linjen i 0.6.6): duotone-bildefilter, palett-fra-bilde (foreslå sidetema fra opplastet bilde), lagpanel (blokkliste per seksjon for overlappende blokker), design-galleri med flere ferdige stiler for former/bokser, flere design for samlingsvisningene, og flere samlingsbaserte seksjonsmaler. Punktene 4-6 bør vente til etter breddegrepet (fast innholdsbredde) siden nye seksjonsdesign ellers må gjøres om; duotone/palett-fra-bilde passer sammen med SEO-/bildearbeidet. Ryddig theme.json-token-modell (LAERDOMMER 5.3) hører sammen med duotone-/palettarbeidet her. Flyttet hit fra Forslag og Ideer 14. august 2026: kuraterte font-pakker som ett valg (ELEMENTKART 5a.7), lagre eget tema, og bakgrunn over en annen bakgrunn / faste sidebarer. Ferdig når: delpunktene er levert eller eksplisitt flyttet (punktene 4-6 forutsetter 0.7.2).
  - [ ] A view page for a shop or a release list in the Internet Archive style (moved from Suggestions 22 September 2026; belongs to the collection view designs)
  - [ ] Accordion view for collections, `view: 'accordion'` with a title row and a fold-out body (the ApeironLF course cards; sorted 22 September 2026 from the element review)
  - [ ] An «auto» section tone that alternates light and dark from the previous section's resolved tone, with a clash warning when two pinned tones collide (the ApeironLF rhythm model; sorted 22 September 2026), and a per-page default colour mode (ApeironLF's print/digital toggle)
  - [ ] Badge and edge glow animation presets on the product cards (ApeironLF's aurora/ember/neon/rainbow soft and bold; sorted 22 September 2026)
- [ ] **0.7.10 Intuitivitets-gjennomgangen og editor-finpuss** (fase-portens gjennomgang: er alt intuitivt, er ting rotete, er noe lett å misforstå?). Rikere tomtilstander med preset-forslag (LAERDOMMER 5a.9, flyttet hit fra Forslag og Ideer 9. august 2026) hører til gjennomgangen. Ferdig når: hele editoren er gjennomgått for intuitivitet og rot med funnene sortert inn i backloggen, og delpunktene under er rettet eller sortert.
  - [ ] Gradient: er oppsettet for hvordan man kontrollerer farger intuitivt og godt? Eventuelt gjøre om % til en tydelig liste (flyttet fra egen 0.9-seksjon; hører til intuitivitets-gjennomgangen i fasen)
  - [ ] Editor-finpuss: blokkverktøylinja må automatisk legge seg UNDER blokken når blokken står for nær toppen av lerretet, slik at den ikke gjemmes bak topplinja (14. august 2026)
  - [ ] Editor-finpuss (flyttet fra Forslag og Ideer 14. august 2026): endre størrelse på blokker ved å dra i alle kanter av markeringen, ikke kun nedre høyre hjørne
  - [ ] Editor-finpuss (flyttet fra Forslag og Ideer 14. august 2026): rework av rutenett/hjelpelinjer
  - [ ] Leverings-grep fra sammenligningen (flyttet fra Forslag og Ideer 14. august 2026): command palette Cmd+K (ELEMENTKART 5a.8), tydeligere to-farge drop-target (5a.6), og «hvilket layout-verktøy»-hjelpechip (5a.10 / LAERDOMMER 5.10)
  - [ ] Butikk-funn fra research-runden 25. august 2026: vurder «butikk er på»-gating av handelsblokkene (ELEMENTKART Handel: dedikerte element-sett som dukker opp når butikk er på), og live kortforhåndsvisning i katalogpanelet (LAERDOMMER §2 «in-place slår løsrevne skjemaer»)
  - [ ] Editor-finpuss: lerretsflaten utenfor siden i enhetsvisningene (mobil/nettbrett/bærbar) bør ha en tydelig bakgrunn, slik at det er tydelig hva som er siden og at flaten utenfor ikke ser ut som en bug (14. august 2026)
  - [ ] Editor-finpuss: nav-labelfelt (både menypunkter og undermenypunkter) kan tømmes helt og publiseres som `label: ""`, som bryter skjemaets minLength 1 (arvet fra før 0.6.5; funnet i buggjakten 22. juli 2026) - normaliser ved blur eller publisering
  - [ ] Editor-finpuss: etter publisering speiles kun gjeldende side i minnet, så navigering til en ANNEN nettopp publisert side før deployen er ute (~1 min) viser serverens gamle innhold (aldri datatap, kun villedende visning); tekstblokkens autovekst er kun monoton (rammen krymper aldri når tekst slettes) og et ekko-rerender midt i rask skriving kan i teorien miste caret-posisjonen
  - [ ] Editor-finpuss (moved from Bugs 22 September 2026): a smaller element lying over a larger one cannot be selected, because hover selects the larger element first
  - [ ] Editor-finpuss (moved from Bugs 22 September 2026): «Tilbakestill» must be clicked twice; the animation runs on both clicks, the reset only on the second
  - [ ] Editor-finpuss (moved from Bugs 22 September 2026): the block toolbar's click boxes are imprecise; settings, up/down, drag, copy and hide-on-mobile register only on the upper half of the icons
  - [ ] Editor-finpuss (moved from Bugs 22 September 2026): the «+ Ny seksjon» menu always opens downward and lands off-screen unless the page is scrolled; it should open upward when there is no room below (candidate for the `position-try-fallbacks` flip branch of 0.7.12's anchored popovers)
  - [ ] Editor-finpuss (moved from Suggestions 22 September 2026): assess whether «ny blokk» inside the sections is needed at all, or belongs in a panel
  - [ ] Editor-finpuss (moved from Suggestions 22 September 2026): the in-page settings menu does not follow the admin scale; everything that is not a page element should scale like the new-block menu and the admin UI
  - [ ] Editor-finpuss (moved from Suggestions 22 September 2026): move the nav settings into the Pages panel
  - [ ] Editor-finpuss (moved from Suggestions 22 September 2026): drag and drop of nav sub-items in the admin panel
  - [ ] Admin layout choices (sorted 22 September 2026 from the ApeironLF review): sidebar or tab navigation, a list-plus-detail panel layout for the collection panels, admin colour themes, and shared dashboard shortcuts stored in the site (ApeironLF's `admin-shortcuts.js`)
  - [ ] Publisering tåler ikke en forbigående GitHub-feil: et 5xx-svar (f.eks. 503 «No server is currently available» ved ref-oppdateringen, observert under en GitHub-driftsforstyrrelse 20. juli 2026) gir bare en feilmelding, og man må trykke Publiser på nytt selv. Utkastet består og repoet er urørt (committen ble ikke lagt til), så ingen datafare, kun friksjon. Ønsket: automatisk gjenforsøk med backoff ved 502/503/504 på commit-endepunktet (idempotent siden ref-PATCH-en oppgir forventet forrige-sha), eller i det minste en «Prøv igjen»-knapp i feilmeldingen
- [ ] **0.7.11 Tilgjengelighetsrunden** (ny milepæl 10. august 2026, oppgradert fra ett delpunkt under 0.7.10). Utdata-siden er alt god: nav følger disclosure-mønsteret med `aria-expanded`/`aria-controls`, Escape og fokus-retur (ADR-0010), og `prefers-reduced-motion` respekteres konsekvent i motoren. Editoren er derimot tynn, og målingen fra gjennomgangen er konkret: `editor/src/App.svelte` har 197 `<label>`-tagger og NULL `for`-attributter, `:focus-visible` finnes null ganger i `editor/src/`, `role`-attributt finnes én gang, og `tabindex` brukes ingen steder i verken editor eller motor. Verre enn devtools-varselet antydet: flere `<label>` pakker `Dropdown.svelte` og `ColorPicker.svelte`, som er `<button>`-elementer og dermed ikke labelable, så de kontrollene har ikke noe tilgjengelig navn i det hele tatt. Regelverket gjør dette til mer enn god skikk: forskrift om universell utforming av IKT krever WCAG 2.0 A og AA (unntatt 1.2.3, 1.2.4 og 1.2.5) for nettløsninger rettet mot allmennheten, også i privat sektor, WCAG 2.1 har vært krav for offentlig sektor siden 1. februar 2023, og EUs tilgjengelighetsdirektiv ble håndhevbart juni 2025 med EN 301 549 (WCAG 2.1 AA) som referanse. Målgruppen er dekket av dette og har ingen å spørre, så at sider bygget i Urd er tilgjengelige FORDI verktøyet sørger for det er en reell differensiator. To halvdeler:
  - [ ] Utdata (viktigst): publisert side revideres mot WCAG 2.1 AA blokk for blokk og seksjon for seksjon: tilgjengelige navn, fokusrekkefølge, synlig fokus, kontrast, alt-tekst-flyten i editoren (eieren må ledes til å skrive den), skjemafelt-semantikken i skjemablokken, galleriets piler og prikker, lightboxens fokusfelle
  - [ ] Editoren: eksplisitt id/for-kobling der kontrollen er labelable (flyttet hit fra 0.7.10; Braves Issues-fane 24. juli 2026: «No label associated with a form field» / «should have an id or name»), `aria-labelledby` der kontrollen er en knapp-basert komponent, `:focus-visible` i admin-CSS, og en tastaturvei gjennom panelene
  - Ferdig når: hver blokktype og seksjonsvariant er gjennomgått mot WCAG 2.1 AA med funnene enten rettet eller loggført som kjent begrensning; alle kontroller i admin har et tilgjengelig navn (ingen `<label>` rundt en `<button>` uten `aria-labelledby`); admin har synlig fokusmarkering; og en side kan bygges, redigeres og publiseres med kun tastatur.
- [x] **0.7.12 Forankringsrunden** DELIVERED 0.7.12 (awaiting the test round; five popovers on the Popover API + anchor positioning behind `nativeAnchoring()` with the measuring branch as fallback, `contrast-color()` for the text on accent with an Auto chip, ADR-0011 addendum; the local test round of 22 September 2026 also gave the submenu blur, closed groups by default, the pane clamp, the text toolbar's resolved theme colours, icon recents, `nav.style.subOpen`, fold chevrons with a fold-all toggle and a stable scrollbar gutter) (ny milepæl 10. august 2026; åpner gaten i Moderniser-seksjonen under). CSS Anchor Positioning er Baseline 2026 (Chrome 125+, Firefox 132+, Safari 18.2+), så «egen runde når forankring er baseline» kan kjøres. Det store bruksstedet er ikke nav (undermenyene posisjoneres alt i CSS; `nav.js` måler ingenting), men FIRE uavhengige håndrullede popovere som hver gjør `position: fixed`, egen måling, vending opp/ned, klemming mot viewport-kant, utenfor-klikk og Escape: `editor/src/lib/Dropdown.svelte` (163 linjer), motorens `dropdown.js` (136 linjer), `ColorPicker.svelte` og `GlyphPicker.svelte`. Popover API gir top-layer (som er hele grunnen til fixed-omveien: panelene klipper `absolute`), lysavvisning og Escape gratis; anchor positioning gir plasseringen. Runden sletter altså kode i stedet for å legge til, samme gevinst som lightbox og parallaks ga i 0.6.6.5.7. NB: `@position-try` (auto-vending mot kanten) krever Safari 26+ og Firefox 147+ og trenger derfor egen `@supports`-gren, ikke samme som forankringen.
  - [x] Popover API + anchor positioning for de fire popoverne, bak `@supports`, med dagens JS-vei som fallback-gren (five: the engine colour picker as well)
  - [x] Nav-flyout-undermenyen vurderes samtidig (ADR-0010 står fast: disclosure, aldri `role="menu"`) (assessed 22 September 2026: the submenus keep their CSS placement in the flow, nothing is measured, so no anchoring is needed; `subOpen` added instead)
  - [x] `contrast-color()` i `theme.js` etter samme `@supports`-mønster som `light-dark()` alt bruker der. Baseline Newly available april 2026 (Chrome 147, Firefox 146, Safari 26) med interoperable WPT-resultater. Løser problemet kontrast-varselet i Tema-panelet skulle løst før det ble bygget og bevisst fjernet igjen i 0.6.6.5.4 (funksjonskartet C15): nettleseren velger lesbar tekstfarge mot eierens aksentfarge, dagens verdi står som fallback
  - Ferdig når: nedtrekk, fargevelger og glyfvelger åpner i top-layer med native lysavvisning på moderne nettlesere og uendret oppførsel på gamle; ingen av dem måler viewport i JS i den moderne grenen; temaets tekstfarge velges av `contrast-color()` med dagens verdi som fallback; og ADR-0011 er oppdatert med forankringsavgjørelsen.
- [ ] **0.7.13 Elementgjennomgangen** (ny milepæl 14. august 2026): systematisk gjennomgang av ApeironLF og de andre byggernes elementer mot Urds elementliste, så det blir synlig hva som mangler før v1.0-porten (gjenskape ApeironLF). Kildene er kartene i [sammenligning/](sammenligning/) og de faktiske sidene; resultatet er en hull-liste sortert inn i backloggen. Sorted in from Bugs and Suggestions 22 September 2026 (the mapping of the milestone). Kjente hull fra skjermbildegjennomgangen 14. august 2026 (ApeironLF-mønstrene, flyttet hit fra Forslag og Ideer samme dag):
  The review of 22 September 2026 (ApeironLF read from its source, and a search on the builders' backgrounds; recorded in LAERDOMMER §2 by stage 0.7.13.2) turned the sub-items into the stages below. Decisions the same day: the ApeironLF calendar views are covered by the calendar plugin's list, cards, month and next views, so the calendar stage builds the «Akkurat nå» card instead (the next N events across all sources) and events as a COLLECTION kind goes to v0.8; the hero background takes all four ApeironLF hero-gallery styles plus the survey's cheap wins (pattern layer, shape dividers), while WebGL art, Lottie and Spline are out of scope (dependencies or GPU-only); the styled blocks are variants on existing blocks, no new pricing block. Delivered before the milestone opened: the stats block with count-up (blocks/stats.js) and the cart button (0.7.5.2, additive nav.cart).
  - [x] 0.7.13.1 The mapping: Bugs and Suggestions sorted into the milestone
  - [x] 0.7.13.2 The review written up (docs-only, 23 September 2026): LAERDOMMER §2 subsection with the ApeironLF inventory, the builders' background types and the rules; FUNKSJONSKART C19-C23 rows and the updated «Urd i dag» counts, plus the gaps from the full inventory of 22 September 2026 as C24 archiving and entry states and C25 collection filters; ELEMENTKART §3 members
  - [x] 0.7.13.3 (23 September 2026) Nav surface: `nav.style.atTop: 'clear'` (transparent, no blur, border and shadow hidden in the top zone; the surface, border and shadow appear after scrolling, via `scrolled` in `navScrollState` and a `urd-nav-scrolled` host class; the ApeironLF model, folds in «Nav-stil som innstilling»), `nav.style.radius` 0-64 for the floating variants with the three variants kept as presets («Nav rounding as a value»), and the underline hover drawn with `clip-path` instead of `scaleX` so the end frame equals the static line («Nav underline hover»)
  - [ ] 0.7.13.4 Full-screen mobile menu: `nav.style.mobileMenu: 'dropdown' | 'sheet'`; the sheet is a `<dialog>` opened with `showModal()` (top layer, focus trap, Escape and the existing `body:has(dialog:modal)` scroll lock, ADR-0011), the menu list moved into it on open and back on close, slide-in with a reduced-motion cut («Helskjerms mobilnav som valg»)
  - [ ] 0.7.13.5 Announcement bar and tool placement: `nav.announcement { text, href?, sticky? (default true), color? }` rendered inside `#urd-nav` above the nav, scrolling away through a negative sticky top when not sticky, with `--urd-nav-h` measured accordingly; `nav.style.tools { side: 'end' | 'start', inMenu }` for the tool cluster («Announcement bar», «Nav tool placement»)
  - [ ] 0.7.13.6 Link launcher: `nav.launcher { links: [{ label, href, icon? }] }` behind a drawn grid button, a popover grid placed in CSS under the cluster (the `popover` attribute where supported, the submenu disclosure pattern otherwise), rendered as a grid section inside the mobile menu; manual icons only, favicon fetching rejected (CSP and host) («Link launcher»)
  - [ ] 0.7.13.7 Ribbons: a new core block `ribbon` (text marquee with items, separator, speed, direction, pause on hover, `band` or `plain`; the track duplicated, `translateX(-50%)` keyframes, a static row under reduced motion; pure `ribbonDuration` in ribbon-model.js) and a gallery view `ribbon` for the continuous image band («animert bånd», «rullende bildegardin»)
  - [ ] 0.7.13.8 Background motion and the photos layer: `motion: 'none' | 'kenburns' | 'drift'` on the image layer (version 3) and `kenburns` on the slideshow; a new `photos` layer (ApeironLF style D: scattered photos with drift, Ken Burns or crossfade from a deterministic `photoLayout`); presets `hero-image` and `hero-photos` («Bildebakgrunn slik ApeironLF-heroen har den»)
  - [ ] 0.7.13.9 Gallery views `mosaic` (varied spans from `mosaicSpans`) and `polaroid` (framed, deterministic tilt) (ApeironLF styles B and C)
  - [ ] 0.7.13.10 Pattern layer (eight inline SVG tiles with colour, size, opacity, rotation, invert; the Divi and Wix pattern overlays) and section shape dividers (`section.divider { top?, bottom? }` with wave, tilt, curve, triangle, zigzag; the Elementor model)
  - [ ] 0.7.13.11 Styled variants and two footers: `stats.variant` plain/cards/band, `quote.card`, `faq.variant` list/cards, the line shape's `label` (the chapter divider), footer designs `chapters` and `split` («Styliserte blokker, og flere styliserte footere»)
  - [ ] 0.7.13.12 Animated images and self-hosted video as content: pure `isAnimatedImage(bytes)` (GIF, animated WebP, APNG) keeps such files as they are with warn and max caps; the video block gains `source: 'embed' | 'file'` with `<video>`, poster, loop, muted and autoplay only when muted («Animated image and self-hosted video as content»)
  - [ ] 0.7.13.13 Calendar: the `next` view extended to the «Akkurat nå» card (1-3 next events merged across sources with dedupe on start plus title, the rest under «Later») and a visible, customisable empty state (`emptyText`, `emptyIcon`) in every view («Kalendervisninger som ApeironLF», «Empty calendar state»)
  - [ ] 0.7.13.14 Back to top as an element: `site.layout.toTop { show, placement: 'corner' | 'content', shape }`, the content placement through the nav inset formula («Back to top as an element with settings»)
  - [ ] 0.7.13.15 Nav in the hero (rosenborgteater.no): decided at review with a screenshot or a description; stage 3's `atTop: 'clear'` with Overlay may already cover it; delivered or sorted to v0.8 explicitly
  - [ ] 0.7.13.16 Page index block: a small block listing the page's sections as anchor links, built at render from the section titles (ApeironLF's «På denne siden»; sorted 22 September 2026)
  - [ ] LTT-gapene (lttstore.com-gjennomgangen 25. august 2026, for full Shopify-butikkfront-identitet): horisontal produktkarusell som view-variant på produkt-blokken; klient-side søk over den git-eide katalogen (mulig statisk); kontoer/ønskeliste/regionvelger er statisk uoppnåelig og dokumentert utenfor scope; nyhetsbrevarkiv og socialgalleri er alt komponerbare med samling/galleri (oppskrifter, ikke hull); hover-bildebytte på kortene levert i 0.7.5.e
  - Sorted elsewhere 22 September 2026: the editor UX findings to 0.7.10, the narrow-width text folding to its own milestone 0.7.17, the Internet Archive style view to 0.7.9, the browser cache note to the v0.9 updater hardening item; the sticky release bug stays in Bugs

  Ferdig når: gjennomgangen er dokumentert med hull-lista sortert inn i backloggen (stage 2), og ApeironLF-mønstrene er levert gjennom etappene 3 til 16 eller eksplisitt sortert til senere milepæler.
- [x] **0.7.14 Engelsk-renskingen** LEVERT 0.7.14.1-.18 (venter på testrunde og push). (ny milepæl 30. august 2026, [ADR-0021](adr/0021-english-code-text-and-contract-identifiers.md)): all norsk tekst i koden gjøres engelsk, inkludert datakontrakt-verdiene, mens norsk består i ordbøkene, docs, nettstedsinnhold og bevisste norske testdata. Kontrakten tas først (risikoen lander mens alt annet er urørt), kommentarsveipen sist (oversettelsene skrives mot endelige engelske identifikatorer). Lagringsstiene i brukerrepoer (content/samlinger/, content/maler/, sideslugs) beholdes; se ADR-ens avgrensninger. Navnekartet står i ADR-0021.
  - [x] 0.7.14.1 ADR-0021, AGENTS.md-regelen («All tekst i kode er engelsk») og denne milepælen
  - [x] 0.7.14.2 Blokktypene + lagtypen `bildegalleri` med sidemigrering v3 (pageMigrations[2], liftContractTokens for mal-innsetting, registry-aliasmekanismen), i18n-nøklene blocks.*/bgLayer.*, eksempeldata løftet, SCHEMA.md. Utvidet under gjennomføringen: seksjonsrollene (samme sidemigrering) og prop-enumverdiene i timeline/quote (blokkenes egne v2-migreringer) var også persistert norsk kontrakt
  - [x] 0.7.14.3 Preset-idene med sidemigrering v4 (pageMigrations[3]), page-presets-startpakkene, preset.<id>.*-nøklene, eksempelsider løftet
  - [x] 0.7.14.4 Plugin-laget: mapper/ider/blokk- og preset-ider/CSS/locales for referanse-pluginene, motor-aliaser for de gamle idene, provides.maler->templates dual-read, Urd.maler-alias, alias-matrisetest
  - [x] 0.7.14.5 Motorfil-renames (git mv av ~14 moduler) med modulepreload-listene i alle skall, tests/_engine.mjs og editorens $engine-importer; forhåndssjekk av dynamiske import()-kall. Ti norske testfilnavn tatt i samme etappe
  - [x] 0.7.14.6 JS-identifikatorene, resterende i18n-nøkler (hint*/opt*/canvas*), urd-maler->urd-templates-meldingen, utkastnøkkel-migreringen (migrate-on-read i draft-storen). Også site-ordbokgruppene (shop./share./countdown.) og footer-malenes ider
  - [x] 0.7.14.7 CSS-klassene (urd-handlekurv-*->urd-cart-* osv., nytt djb2-stempel i alle 5 skall, kopiene byte-like); breaking for egendefinert CSS, bokført i CHANGELOG. Også de norske suffiksene og CSS-variablene
  - [x] 0.7.14.8 icons.js-lokaliseringen: labelKey per ikon + icon.*-nøkler i nb/en-GB/tr, engelske fallback-etiketter
  - [x] 0.7.14.9 Resterende strenger: fallback-litteraler, hardkodede titler/placeholders (med nøkler), feil-/konsollmeldinger, check-release.mjs, dev-server.py, workflow-stegnavnene, skjema-beskrivelsestekstene
  - [x] 0.7.14.10 Testene: beskrivelser, assertion-meldinger og kommentarer til engelsk (testfil-renames tatt i .5); norske fixture-data består som bevisst testinnhold, markert der det ikke er opplagt
  - [x] 0.7.14.11 Kommentarsveipen, batch 1: preview-edit.js (571 kommentarlinjer oversatt, kommentarreglene anvendt, bundelen urørt)
  - [x] 0.7.14.12 Kommentarsveipen, batch 2: App.svelte (~700 kommentarlinjer oversatt, bundel-diffen tom; «---- Foldetrinnene»-overskriften beholdt som testanker med engelsk gloss)
  - [x] 0.7.14.13 Kommentarsveipen, batch 3: motor-rot A (render, urd, nav, nav-model, sticky, sticky-model, i18n, theme, plugins, migrate, preset-thumb, imageTools; ~1 000 kommentarlinjer, bundel-diffen tom)
  - [x] 0.7.14.14 Kommentarsveipen, batch 4: motor-rot B + blocks/ + backgrounds/ + sections/ + animations/ (59 filer, ~1 300 kommentarlinjer; motoren er kommentarrenset i sin helhet, bundel-diffen tom)
  - [x] 0.7.14.15 Kommentarsveipen, batch 5: editor/src/lib + main.js (9 filer, ~225 kommentarlinjer; bundel-diffen tom)
  - [x] 0.7.14.16 Kommentarsveipen, batch 6: base.css (stempel 6b4ebcd3) + plugins + workflows + scripts + functions + ordbok-headere. Kommentarsveipen komplett
    (felles for batchene: kommentarreglene i AGENTS.md anvendes samtidig; bundel-diffen skal være tom for rene kommentarbatcher)
  - [x] 0.7.14.17 Sluttaudit i tre lag + uavhengig gjennomgang: sju oversette filområder sveipet, image.fit-migreringen, admin-tema-idene, tre funksjonsfeil fra tidligere etapper og ~60 rename-rester rettet; alle brukersynlige literaler på nøkler. Push-klargjøringen tas ved «push»
  - [x] 0.7.14.18 Siste ekstra sjekk: kontraktjakt i alle definisjoner (ren), share-blokkens nøkkellekkasje rettet med ny vakttest for literal-nøkler, tema-preset-idene, seed.phone, linjehøyde-etikettene etter admin-språket

  Ferdig når: norsk-auditen er ren utenfor locales/, docs/, content/, readme/ og bevisste fixtures; migrerings- og aliastestene dekker alle gamle tokens med idempotens og plugin-matrisen; og alle suiter, bygg og validering er grønne.
- [x] **0.7.15 Navigation bar sizing** DELIVERED 0.7.15 (awaiting the test round; the «Ferdig når» conditions on the published page are settled there; reopened for 0.7.15.2 on 18 September 2026: the Appearance group regrouped into six section folds with the mobile overrides behind a Screen | Phone switch in Size, after three drawn layouts) (new milestone 16 September 2026, after the test finding that the bar offers a very poor choice of size, and a survey of ten builders' header controls). Today the only thickness control is the four-step `nav.style.size` enum (padding and font in rem, `base.css` `urd-nav-size-*`), the bar is always full-bleed with a hard-coded side padding `clamp(1rem, 4vw, 2.5rem)` (repeated in the mobile panel and the side-to-bar fallback), the floating pill is hard-coded `min(1100px, 100% - 2rem)`, the item gap is fixed, the side column width (`nav.style.width` 180-400) is drag-only, the logo height is desktop-only, and the scroll shrink is a fixed factor 0.5 (`.urd-nav-compact`). Nothing binds the nav to the content width from [ADR-0018](adr/0018-bound-content-width.md), although `--urd-canvas-w` and `--urd-canvas-gutter-desktop` are set on `<html>` by `applySiteLayout` and readable from the nav host. **The survey** (own help pages as sources, to be recorded in LAERDOMMER §2 in stage 1): Squarespace 7.1 has width Full/Inset, a Height slider, element spacing, logo height plus a separate mobile logo height and a mobile view with its own height, fixed position Basic/Scroll back, shadow and border presets; Wix classic sets the height by a drag handle, Wix Studio by handle or Inspector height and per-side padding, stretch or the site max width, scroll effects Freeze/Disappear/Fade out with Pinned/Sticky and «Color while scrolling»; Webflow has no preset size at all (height = content plus the padding you set, width 100 % or a container); Shopify Dawn has header `padding_top`/`padding_bottom` 0-36 px, `logo_width` 50-300, `sticky_header_type` none/on-scroll-up/always/always-reduce-logo-size and a separator line; Elementor has horizontal and vertical item padding, space between, align incl. stretch and a mobile breakpoint; WordPress block themes use the Group block's content/wide/full width and padding; Framer is a layout stack with padding and scroll variants (full and compact); Duda has a separate «Shrinking header» with its own logo size, background and spacing (GoDaddy the same); Hostinger's height is the top/bottom padding; Carrd has no sticky toggle. **Five rules:** thickness is padding everywhere (Squarespace's «Height» slider and Wix's handle are the same value with a different handle; nobody fixes a bar height the content must fit into); width is a two-way choice, full-bleed or bound to the page's content width, with the background always full-bleed; the logo has its own size and often a second mobile or compact size; the compact-on-scroll state is configurable in itself; mobile gets its own thickness and logo size. **The model (ADR-0023 in stage 1):** thickness is padding, presets plus free px; width is full-bleed or bound to the content width; the mobile and compact states carry their own overrides, chosen by `nav.js` at render time. Every field is additive with today's look as the default, so no migration (pre-v1, ADR-0005); defaults are never written to the file (`setNavStyle` deletes on undefined, an emptied `mobile` object is removed). The settings, all under `nav.style` unless noted, shown only where they apply:
  - `padY` px 0-64: Thickness, the vertical padding, overrides the preset's padding and is raw in every variant (the pill's 0.67 factor applies to presets only); bar and floating. With an image logo the bar never gets thinner than 36 px (the logo's margin calibration contributes a constant 36 px), so the tooltip speaks of padding, never of bar height
  - `textSize` px 12-28: the menu text size, overrides the preset's font; the logo text follows unless `logo.textSize` is set; all variants
  - `padX` px 0-80: side padding, empty = today's fluid clamp; also drives the mobile panel and the side-to-bar fallback; bar and floating
  - `gap` px 0-64: the space between the menu items (`.urd-nav-list` only; the nav's own logo/list/tools gap and the mobile panel's 0.25rem are untouched); bar and floating
  - `inset` boolean: the bar's contents line up with the content edge (`max(padX, gutter, (100% - canvasW) / 2)`, desktop only like the canvas rule), the background stays full-bleed; bar only
  - `pillWidth` px 480-1920 or the string `content`: the floating menu's max width, empty = 1100 as today; `content` = `min(canvasW, 100% - 2 * gutter)`; suppressed at the mobile breakpoint (the pill keeps `100% - 2rem`); floating variants only
  - `width` (exists, 180-400): the side column also gets a number input in the panel next to the drag; side only
  - `shrinkTo` 0.3-0.8 (default 0.5): the padding factor in the compact state; only when `nav.scroll` is shrink
  - `shrinkLogo` boolean: the logo image shrinks with the menu in the compact state (Shopify «reduce logo», Duda); only with shrink and an image logo
  - `mobile` `{ padY?, textSize? }`: overrides at the mobile breakpoint, empty = same as desktop; all except side
  - `nav.logo.mobileSize` px 12-128: the logo image height at the mobile breakpoint (moved here from Forslag og Ideer 16 September 2026)
  - [x] 0.7.15.1 The survey and the decision: LAERDOMMER §2 gets an English subsection «The navigation bar: what the builders offer for its size (survey 16 September 2026)» with one bullet per builder and its source link, the five rules and «What Urd takes (0.7.15)», plus a «Survey 16 September 2026 (the navigation bar)» bullet under Kilder; FUNKSJONSKART §5 «Urd i dag (A)» updated with the size controls and a new gap **C18** (border and shadow presets, see 0.7.15.7) in the recommendations table; new ADR-0023 (`docs/adr/0023-navigation-bar-sizing.md`, written in this stage) with the model above, added to the ADR list in AGENTS.md
  - [x] 0.7.15.2 Engine and CSS: `nav-model.js` exports `NAV_SIZE_BOUNDS` and the pure `navSizeVars(style, logo, { mobile })` returning `{ vars, font, logoSize }` (clamping, the mobile overrides chosen here because `.urd-nav-mobile` is also set by desktop content folding and a class rule can never beat an inline value; empty style = empty vars), and `navClasses` adds `urd-nav-inset` (only `inset === true` and the effective variant is `bar`) and `urd-nav-shrink-logo` (only boolean true). `nav.js` applies the vars inline on the nav next to the other style vars, sets `nav.style.fontSize` when a text size is set, and writes the logo height as `--urd-logo-base` on the img (the CSS derives `--urd-logo-size` from it, so the compact rule can scale it). `base.css`: `.urd-nav` gets `--urd-nav-base-pad: var(--urd-nav-pad-y, var(--urd-nav-size-pad, 0.9rem))`, `--urd-nav-px: var(--urd-nav-pad-x, clamp(1rem, 4vw, 2.5rem))` and `padding: var(--urd-nav-pad) var(--urd-nav-px)`; the size classes set `--urd-nav-size-pad` instead of `--urd-nav-base-pad` and keep their font-size (no font-size rule on `.urd-nav` itself, it inherits body today); the compact rule becomes `.urd-nav-compact .urd-nav { --urd-nav-shrink: var(--urd-nav-shrink-to, 0.5) }` (the class sits on the host, the var inline on the nav); the pill overrides `--urd-nav-base-pad` (`var(--urd-nav-pad-y, calc(var(--urd-nav-size-pad, 0.9rem) * 0.67))`) instead of `--urd-nav-pad`, and its width becomes `min(var(--urd-nav-pill-w, 1100px), calc(100% - 2rem))`; `.urd-nav-list { gap: var(--urd-nav-gap, 1.25rem) }`; the mobile panel and the side fallback read `var(--urd-nav-px)`; the inset rule `body:not(.urd-mobile) .urd-nav-inset { --urd-nav-px: max(var(--urd-nav-pad-x, clamp(1rem, 4vw, 2.5rem)), var(--urd-canvas-gutter-desktop, 24px), calc((100% - var(--urd-canvas-w, 100%)) / 2)) }`; the shrink-logo rule `.urd-nav-shrink-logo .urd-nav-logo img { --urd-logo-size: calc(var(--urd-logo-base, 2rem) * var(--urd-nav-shrink, 1)) }` with a `height, margin` transition under `.urd-nav-scroll` and the reduced-motion exception; the side column's own `padding: 1.2rem 1rem` stays. `--urd-nav-h`, sticky.js and the chrome parking measure, so they need nothing. New djb2 stamp in the root, admin and every slug copy (copies byte-identical to the root). Tests in `tests/nav.test.mjs`: `navSizeVars` (empty style, clamping both ends, mobile picks `style.mobile` and `logo.mobileSize`, `pillWidth` `content`, the pill var omitted when mobile, logoSize fallback 32) and the two classes only on boolean true and the right variant
  - [x] 0.7.15.3 Schema in three places: `schema/site.schema.json` `nav.style` (`additionalProperties: false`, so every key is listed: `padY`, `textSize`, `padX`, `gap`, `inset`, `pillWidth` as `anyOf` number/`const "content"`, `shrinkTo`, `shrinkLogo`, `mobile` with `additionalProperties: false`) and `nav.logo.mobileSize`; `docs/SCHEMA.md` (the nav example block gets a `style` object with e.g. `padY` and `inset`, the `nav.logo` bullet gets `mobileSize`, the `nav.style` bullet gets the keys with bounds, the variant restrictions and «the pill factor 0.67 applies to presets only»); `template/content/site.json` gets `nav.logo.mobileSize: 32` (the demo logo is 40 on desktop; the look is otherwise unchanged) so `npm run validate` covers a new field
  - [x] 0.7.15.4 Editor: new pure `editor/src/lib/nav-size.js` mirroring `content-width.js` (the bounds tied to `NAV_SIZE_BOUNDS` by a parity test, `SIZE_PRESETS` = the rem table at a 16 px root: sm 8.8/13.6, md 14.4/16, lg 20/16.8, xl 27.2/18.4, `PILL_FACTOR` 0.67, `effectivePadY(style, variant)`, `effectiveTextSize`, `sizePresetOf` = the size id while no free value overrides it, `clampRange`; tests in `tests/nav-size.test.mjs`). In the nav panel of `App.svelte`, themed controls, tooltips only, each control directly under the choice it belongs to: under Variant, floating adds «Menu width» (Dropdown Content width/Custom plus a px field when custom), bar adds «Align to content width» next to Overlay, side adds «Column width» 180-400 (delete at 250, the drag path `onNavWidth` stays); the Size Dropdown becomes a `.seg` of four buttons (a preset click sets or deletes `size` and deletes `padY` and `textSize` in one history step) followed by the «Thickness» slider 0-64 showing the effective px and the «Text size» slider 12-28, then «Side padding» and «Item spacing» number fields (empty = automatic); for the side variant only Size, Text size and Column width; under On scroll when shrink: «Shrink to» 30-80 % step 5 (stored as n/100, delete at 0.5) and «Shrink the logo too» when the logo has an image; a new «Mobile» details group after Appearance (hidden for side) with «Thickness (mobile)» and «Text size (mobile)» via `setNavMobile(key, value)`; the Logo group gets «Logo height (mobile)» next to Logo height via `setLogo({ mobileSize })`. Rebuild and commit the bundle
  - [x] 0.7.15.5 Locales nb, en-GB and tr (no `{var}` tokens): `tip.nav.sizePreset`, `lbl.navThickness`, `tip.nav.thickness`, `lbl.navTextSize`, `tip.nav.menuTextSize`, `lbl.navPadX`, `tip.nav.padX`, `lbl.navGap`, `tip.nav.gap`, `lbl.navInset`, `tip.nav.inset`, `lbl.navPillWidth`, `tip.nav.pillWidth`, `opt.pillWidth.content`, `opt.pillWidth.custom`, `lbl.navColWidth`, `tip.nav.colWidth`, `lbl.navShrinkTo`, `tip.nav.shrinkTo`, `lbl.navShrinkLogo`, `tip.nav.shrinkLogo`, `group.mobile`, `lbl.navThicknessMobile`, `lbl.navTextSizeMobile`, `tip.nav.mobileSame`, `tip.nav.logoHeightMobile` (English Thickness / Text size / Side padding / Item spacing / Align to content width / Menu width / Column width / Shrink to / Shrink the logo too / Mobile; bokmål Tykkelse / Tekststørrelse / Sideluft / Avstand mellom punkter / Følg innholdsbredden / Menybredde / Kolonnebredde / Krymp til / Krymp logoen også / Mobil)
  - [x] 0.7.15.6 Bookkeeping (CHANGELOG and TESTRUNDER written 17 September 2026; the push preparation and the release follow at «push»): CHANGELOG entry, TESTRUNDER batch (padY 0 with an image logo and the 36 px floor, the pill at Content width in three window widths, inset against a section's content edge, shrink to 30 % with the logo, the mobile overrides in the Mobile device and on a phone, the column number field against the drag, the side-to-bar fallback below 900 px, a preset resetting the sliders, the labels in three languages, the published page equal to the preview), then push preparation and a release (0.7.3) so the admin bundle reaches urdweb.pages.dev and urd-web.pages.dev
  - [x] 0.7.15.7 Border and shadow (the remaining Squarespace controls, C18; can be split off if the milestone gets heavy): `nav.style.border` `{ width 0-8, side bottom/top/both/all, color }` and `nav.style.shadow` none/soft/strong as `--urd-nav-border`/`--urd-nav-shadow`, shown for bar and floating (the pill already has the glow); the transparent-to-solid «colour while scrolling» stays where it is, in 0.7.13 (the ApeironLF nav style item)

  Ferdig når: the eleven controls above are visible in the nav panel only where they apply, take effect in the preview and on the published page (clean view and published are equal in the same window), the example data validates with a new field, `navSizeVars` and the editor module are node-tested with bounds parity, the survey stands in LAERDOMMER with sources and ADR-0023 is decided, and the TESTRUNDER batch is written.

- [ ] **0.7.16 Arkivering og personsamlinger** (new milestone 22 September 2026, from the full ApeironLF review): a site owner must be able to ARCHIVE things with one action and keep them visible in an archive instead of deleting them: posters on the noticeboard, news, events that have passed, and boards per period. ApeironLF does it with a `done` flag plus `archivedAt` on every entry (news, posters) and with boards stored per period with highlights, filtered by period chips (all, the newest two, decades expanding to years). Urd has the `archive` view (year-grouped) and `date` on entries, but no entry state, no restore, and no people kind. This is a data-model decision (entry states cross the migration contract, ADR-0005), so the milestone starts with a survey of how the builders and CMS-es handle it (Webflow CMS archived items, Shopify draft and archived products, WordPress draft, private and trash, Squarespace and Wix hidden items) and an ADR before code.
  - [ ] 0.7.16.1 Survey and ADR-0025 «Entry states and archiving»: the states (active, archived, hidden), `archivedAt`, whether archiving is a state or a move, what the live views hide and what the archive view shows, auto-archive rules (a poster's date passed, an event ended), restore, and the section-level `hidden` flag
  - [ ] 0.7.16.2 The entry model: `archived` and `archivedAt` (additive, collection.schema.json, three places), `kicker` and `urgent` on entries (the news badge), one-action «Archive» and «Restore» in the collection panel and from the block in the preview, live views hide archived entries, the `archive` view shows them grouped by year with greyscale for cards
  - [ ] 0.7.16.3 Collection filters: chips by year, period or category, tabs, and a client-side search field on the collection views (no library), also used by the archive view
  - [ ] 0.7.16.4 The people kind: `kind: 'people'` with portrait, role, tags, email and `period`; the team preset binds to it; boards kept per period with a summary and highlights, the archive view listing periods with the chip filter (ApeironLF's styret and styret-arkiv)
  - [ ] 0.7.16.5 Section hide: `section.hidden` keeps a section in the editor (shown dimmed, listed as archived in the panel) and out of the published page; the noticeboard and the news presets get an «Earlier» section that shows the archive view

  Ferdig når: an entry can be archived and restored with one action from both the panel and the preview; archived entries are gone from the live views and appear in an archive view with a year or period filter; a board can be kept per period with its own archive page; a section can be hidden without being deleted; and ADR-0025 is decided with the migration path tested.
- [ ] **0.7.17 Breddeskaleringen** (new milestone 23 September 2026, moved out of 0.7.3 since it is not a mobile problem but a desktop one, from Bugs): between the site's content width (ADR-0018, default 1440) and the mobile breakpoint, blocks keep their percentage frames while text keeps its pixel size, so a heading or a paragraph wraps to more lines as the window narrows and runs into the blocks beneath. The builders avoid it by scaling the whole page proportionally in that band (Squarespace's Fluid Engine, Wix Studio's «scale proportionally», Framer's scale sizing) or by giving the band its own breakpoint (Webflow tablet). Urd has one desktop layout and one mobile layout, and nothing in between.
  - [x] 0.7.17.1 Survey and decision (23 September 2026): the builders surveyed in LAERDOMMER §2 (Tilda, Readymag, Wix Studio, Squarespace Fluid Engine, Duda, Webflow, Framer, Elementor, Figma Sites, Hostinger, Canva, Carrd, plus CSS `zoom` and WCAG). Decided in the ADR-0018 addendum: the canvas scales proportionally with CSS `zoom` between the binding width and a floor (`site.layout.scale = { mode, min }`, default `scale` with `min: 0.75` for new sites, omitted = today's behaviour), and below the width where the factor would drop under the floor (1227 px with the defaults) the page uses the row grid of ADR-0019. `transform` stays forbidden on the canvas; `zoom` creates no containing block for fixed descendants. The editor's Tablet and Laptop devices already render a real viewport, so they show the published page's truth
  - [x] 0.7.17.2 The scaling built (23 September 2026): pure `pageScale` and `scaleFloorWidth` in scale-model.js (tested); `--urd-scale` written on the root by a resize listener in urd.js; `zoom: var(--urd-scale, 1)` on the desktop section (the inline px `min-height` stays a plain length and scales with it, `vh` untouched, the nav clearance multiplied back by `--urd-unzoom`); `currentCSSZoom` conversions in sticky.js and the editor's geometry writes so `y`/`h` stay design-width pixels; Site panel controls under Content width («Below the content width»: Scale the page / Keep the sizes, «Smallest scale» 50-100 % with the floor width in the text); schema in three places with the example site.json set to scale; tests/canvas.test.mjs extended. A hand-off to the row grid at the floor width was built and withdrawn the same day (it stacked every tablet and small laptop window and folded the menu there)
  - [x] 0.7.17.3 The content push (23 September 2026, [ADR-0024](adr/0024-content-push-on-the-desktop-canvas.md)): the real bug was content that grows over the blocks beneath, at every width. Deep research (LAERDOMMER §2) weighed Squarespace's growing grid rows against the Wix Editor's push rules for an absolute canvas; the row grid was inventoried (about twenty files, sticky.js, negative `y`, overhang, the presets' unaligned frames, `mobileOrder` on the `y` scale) and the push model chosen. New `push-model.js` (`pushLayout`, gap 70/10 px, the middle rule for deliberate overlaps, the cascade; tested), a push pass in render.js after the blocks' own measurements with a ResizeObserver per section, shifts on the elements only, the section grows to the pushed bottom, `suspendPush`/`resumePush` beside the sticky suspension in preview-edit.js so drags happen on the design; typing growth pushes the neighbours in the data; the text block and the ten data blocks no longer write measured heights into the frames (the `urd-grow` amendment hid the growth from the push in the preview; verified in headless Brave over the DevTools protocol at 700 px, preview and page alike); the mobile breakpoint is the site's own again and the menu folds only by its fold logic or at that breakpoint. Editor: the Screen choice (own window or editing size) moved from a popover under the Screen button into the admin settings behind the gear; the Reference 1920 device removed; a Screen narrower than the site's mobile breakpoint is mobile to the engine, as the published page is. The push pass owns the box height (measured on the content with the box at the design height, so a box comes back when its content fits again), and a text block can shrink instead of wrap: `fit: 'shrink'` with `fitMin`, zoomed by the push pass only as much as the design height needs when the frame gets too narrow (the Framer Fit Text model), with «Ved smalere skjerm» and «Minste skala» in the text block's Style tab
  - [x] 0.7.17.5 The page zoom withdrawn (23 September 2026, after testing): «Skaler siden» made nothing adapt, everything only got smaller. `site.layout.scale`, `scale-model.js`, `--urd-scale`, the section `zoom` and the `currentCSSZoom` conversions in sticky.js and preview-edit.js removed; the shrink choice moved from the text block's props to block-level `fit`/`fitMin` on every block whose content can wrap (image, video, shape and icon excepted), one shared row in the Style tab; image, video, shape and icon get the same fields as a floor on the frame's width instead (`fitFloorPx`, «Følg bredden / Behold en minste størrelse»); ADR-0018 addendum marked withdrawn, ADR-0024 decisions 5 and 6, the comparison maps corrected
  - [ ] 0.7.17.4 Test-round follow-up: pinned and screen-docked blocks under zoom and push, the sticky nav offset, the lightbox, parallax and the text toolbar at 1280 and 1024 px in the three engines; findings fixed
  Ferdig når: the model is decided and documented; a page built at the content width shows no overlapping text at any width down to the mobile breakpoint in both the preview and the published page, also when a text grows at the design width itself; and the Tablet and Laptop devices in the editor show what the published page shows.

- [ ] **0.7.18 Kjerneflyttingen: map, form, calendar and analytics into the core** (new milestone 23 September 2026): the four reference plugins are what every site needs, so they move out of `template/plugins/` and into the engine and the admin, with the Swedish language pack `lang-sv` staying as the one shipped example plugin (and fixed, since it is reported not to work). Block ids (`map`, `form`, `calendar`), preset ids (`find-us`, `contact-form`, `whats-on`) and the legacy aliases keep working, so no content changes. Analytics becomes a site setting (`site.analytics { token }` in site.json, the only publishable place) edited in the admin settings pop behind the gear, with the Cloudflare hosts fixed in `_headers`. The plugin layer stays for third parties and language packs; `plugins/**` stays a user path. No ADR: the decision is visible in the code once the blocks are core, and the lasting points go where they belong (the AGENTS.md Plugins section, a dated status line in ADR-0006 and ADR-0007, the `site.analytics` field in SCHEMA.md), all in stage 0.7.18.6.
  - [x] 0.7.18.1 (23 September 2026) The plan (an ADR was written and withdrawn the same day as unnecessary); the `lang-sv` pack checked end to end: the visitor page, the preview and the Site panel picker all worked, but Swedish never appeared in the admin language picker behind the gear, since the pack translated only the visitor texts (`admin: false`, no admin dictionary). Fixed with a complete Swedish admin dictionary (1590 keys, `locales/admin/sv.js`) and `admin: true`, so the example pack now shows both registers
  - [x] 0.7.18.2 (23 September 2026) Map into the core: `blocks/map.js` with the pure `osm.js`, the CSS from the injected style tag into base.css, the dictionaries split into `locales/site/*` and `locales/admin/*` (nb, en-GB, tr with parity; nn and se carried over), `BLOCK_LABELS`/`BLOCK_DEFAULTS`/palette (Media), the `find-us` preset in sections/presets.js, `tests/map.test.mjs` repointed, the `_headers` comment, the preload link in the eight shells
  - [x] 0.7.18.3 (24 September 2026) Form into the core: `blocks/form.js` with the pure `form-model.js`, the same moves; the in-preview config panel (the gear) kept for now
  - [x] 0.7.18.4 (24 September 2026) Calendar into the core: `blocks/calendar.js` with `ics.js` loaded dynamically (outside the preload closure), the same moves; the `whats-on` preset; the in-preview sources panel kept for now
  - [x] 0.7.18.5 (24 September 2026) Analytics as a site setting: `site.analytics { token }` (schema in three places), the beacon injected by the engine at boot on the published page only, «Nettsted-analyse» in the admin settings pop writing to the site draft, the two Cloudflare hosts in `_headers`
  - [x] 0.7.18.6 (24 September 2026) The plugin folders removed and every mention corrected: `plugins.json` (`lang-sv` only), `template/plugins/README.md` (the contract with the pack as the example and the snippets as the block reference), AGENTS.md Plugins section (rule text previewed; also that the plugin layer stays with the language pack as the one shipped example), ADR-0006 and ADR-0007 dated status lines (calendar and map moved to the core 23 September 2026), ARCHITECTURE, SCHEMA.md Plugins section, the user guide, the template READMEs (four languages), VISION/VEIKART lines, FUNKSJONSKART counts (22 core blocks, 33 presets, one plugin), `tests/plugin-compat.test.mjs`
  - [x] 0.7.18.7 (24 September 2026) Properties-panel content for calendar and form (Innhold/Stil, ADR-0016) so the in-preview gears retire
  Ferdig når: the three blocks and three presets appear as core in the Blocks panel and the section gallery with no «Plugins» group left when only `lang-sv` is enabled; a page built before the move renders identically after it; the analytics token is set in admin, published in site.json and the beacon appears on the published page only; `plugins/` holds only `lang-sv` and `plugins.json`; the Swedish pack works on the published page and in the preview; and every doc that named the reference plugins is corrected.

## Til v0.8 og senere faser HUSK: Se /sammenligning før vi starter

- [ ] v0.8: contentHash for media-filnavn er 32-bit; to ulike bilder med samme navn og hash-kollisjon (~1/4 mrd) ville stille delt fil. Vurder lengre hash ved bildearbeidet
- [ ] v0.8: **Innslagssider** (funksjonskartet C4b, valgt 23. juli 2026): permalenke-side per samlingsinnslag generert ved publisering (Webflow/Wix-modellen: én mal, én URL per innslag); hører naturlig sammen med bakt HTML-arbeidet
- [ ] v0.8: **Events as a collection kind** (sorted 22 September 2026 from 0.7.13): `kind: 'events'` with date, time and place fields, an upcoming filter and a month view in the collection block, so an association can keep its programme as git-owned entries instead of an ICS feed; belongs with the entry pages since each event wants a permalink; also the programme view for multi-day events (week and day picker, accordion, overview; ApeironLF's fadderuke)
- [ ] v0.8: Mediebibliotek-panel (funksjonskartet C11): oversikt over media/-innholdet, ubrukte filer og størrelser; hører til ytelses-/bildearbeidet
- [ ] v0.8: **Image editor in admin** (sorted 22 September 2026 from the ApeironLF review; belongs with the media work): crop with aspect presets, rotate, flip, zoom and pan, brightness, contrast and saturation, and a size budget in KB per use (portrait, poster, general), all on canvas in the browser like imageTools.js
- [ ] v0.8: Finere patch-protokoll i preview (LAERDOMMER 5.2): send målrettede patcher (én blokk/attributt) i stedet for seksjons-rerender ved hver endring; er «unngå unødvendige omtegninger i preview» fra VEIKART v0.8 gjort konkret
- [ ] v0.8: Vis motorens letthet målbart (LAERDOMMER 5.6): DOM-node-tall / payload-mål i editoren eller ved «Se siden», så et usynlig fortrinn (minimal-wrapper, liten JS) blir synlig
- [ ] v0.8: Build-time data-bake for data-drevne sider (LAERDOMMER 5.8): bak samlingsdata inn i statisk HTML ved publisering; hører sammen med «bakt HTML ved publisering»-punktet
- [ ] v0.8/senere: Indeks/minne-lag for rask admin-spørring når samlinger vokser (LAERDOMMER 5.9): ikke les repoet på hvert tastetrykk; kun relevant i skala
- [ ] v0.8: `content-visibility` for seksjoner under folden (funn 10. august 2026): egenskapen brukes i dag KUN til `<details>`-overgangen i `base.css`, aldri til å hoppe over rendring av innhold besøkende ikke ser ennå. Lange sider med mange seksjoner er nettopp tilfellet den er laget for; hører til fasens ytelsesarbeid og må måles, ikke antas (feil `contain-intrinsic-size` gir hoppende rullefelt)
- [ ] v0.8: **MCP-server v1, kun LESE-verktøy + `urd_validate`** (ADR-0017): avhengighetsfri Node-prosess i `template/mcp/`, rå JSON-RPC over stdio uten SDK, kun `initialize`/`tools/list`/`tools/call`. Verktøy: `urd_site_read`, `urd_page_list`, `urd_page_read` (med `summary`-modus som utelater `frames`), `urd_block_types`, `urd_section_presets`, `urd_page_presets`, `urd_collection_list`, `urd_collection_read`, `urd_describe`, `urd_validate`. Alle lister avledes fra motorens egne registre, aldri fra kopierte lister. Lesing tas før skriving fordi den er immun mot layout-endringene i 0.7.2 og 0.7.3, og lar transport, tester og invarianter modnes uten skriverisiko. Ferdig når: en agentklient kan koble til, liste verktøyene og lese en side; testene i `tests/mcp-*.test.mjs` er grønne inkludert invariant-testen (ingen npm-import, ingen nettverk, ingenting utenfor `mcp/` importerer fra `mcp/`); og `mcp/**` er klassifisert i urd.json og guard.js med kontraktstesten grønn
- [ ] v0.8: **The updater offers refreshed shipped plugins** (added 16 September 2026, ADR-0014; rescoped 24 September 2026 after 0.7.18): `plugins/**` are user paths, so a site created from the template keeps the plugin copies it was created with (urd-web still runs the 0.6.11 copies against engine 0.7.1). Map, form and calendar are core blocks now and ride along with the engine atom group, so the offer concerns the shipped language pack `lang-sv` and any later shipped plugin under `plugins/**`; third-party plugins are never touched. The engine keeps the compatibility surface regardless (SCHEMA.md, tests/plugin-compat.test.mjs); the updater should additionally OFFER the current shipped plugins as optional files, like `functions/**`: the plan lists `plugins/<id>/**` for the shipped ids present in the repo as opt-in entries, default unticked, never written silently. Done when: the Updates panel shows the offer on a repo with 0.6.11 copies, a ticked offer replaces the folder with the release's copy, and an unticked offer leaves it byte-identical
- [ ] v0.9: Generalprøven (se VEIKART) - Urd-Design-siden + oppdatering med ekte migrering
- [ ] v0.9: **Oversettelsesrunde for nn og se** (kjernespråkene nb, en-GB og tr holdes i paritet løpende; nn og se fylles i egne runder, og paritetstesten godtar hull, jf. ADR-0012). Gjennomgå begge ordbøkene for hull OG for tekst som har blitt utdatert fordi funksjonen er endret siden oversettelsen. Kjent utdatert per 9. august 2026: `tip.stickyOffset` i se sier fortsatt at en klistret meny kan kreve større avstand, men høyden legges til automatisk fra 0.7.1 (nn er rettet i samme runde). Ferdig når: begge ordbøkene er gjennomgått mot bokmålsteksten, hull er fylt eller bevisst latt stå, og ingen tekst beskriver en adferd motoren ikke lenger har
- [ ] v0.9: **MCP-serverens SKRIVE-verktøy** (ADR-0017, bygger på v0.8-lesedelen): `urd_section_add`, `urd_block_add`, `urd_block_edit`, `urd_block_move`, `urd_section_move`, `urd_page_create`, `urd_text_write`, alle med `apply` som opt-in (uten den returneres kun et diff-sammendrag). Struktur bygges ALLTID av motorens egne fabrikker (`create()`/`item()` i sections/presets.js, blokkenes `defaults()`, `buildPagePreset`), aldri av modellen; alt annet valideres felt for felt. Skriveretten er `isAllowedPath` fra guard.js, innskrenket til `content/`. Krever at `RESERVED_SLUGS` eksporteres fra guard.js i stedet for å kopieres en tredje gang, og en tekstmodus-motpart til den DOM-baserte `stripActiveContent` med korpus-basert paritetstest. Tas etter 0.7.2, siden breddegrepet endrer `frames`-semantikken skriveverktøyene setter geometri i. Generalprøven er riktig test: en fremmed kloner malen, åpner den i en agentklient og kommer i gang uten snarveier. Ferdig når: en agent kan legge til en seksjon, redigere en blokk og opprette en side som alle passerer skjemavalideringen; ingen skriving treffer disk uten å ha passert strukturkontrollen; og stivakt-testene avviser `..`, absolutte stier, `media/`, `plugins/plugins.json` og alle Urd-eide stier
- [ ] v0.9: rydde opp og fullføre brukerveiledningen (docs/languages/user-guide/GUIDE-nb.md; startet i v0.5; skal dekke alt editoren kan, med skjermbilder?)
- [ ] v0.9: Dokumentasjonsrunden (samlet hit fra Bugs-seksjonen 9. august 2026; hører sammen med brukerveiledningspunktet over, og generalprøven følger guidene og avdekker friksjonen). Ferdig når: delpunktene er levert.
  - [ ] Legge til en bedre forklaring på hvordan man deployer siden på nytt etter førsteoppsett i Cloudflare
  - [ ] /template/plugins trenger en engelsk readme som peker til urd/docs/plugins/plugins-[språk].md
  - [ ] Flytte /readme /setup-publication og /user-guide ut fra /languages
  - [ ] Lage en Troubleshoot fil
- [ ] v0.9: Utgivelses- og oppdaterer-herding (flyttet fra v0.7-lista 9. august 2026: dekker samme kode som generalprøvens oppdaterings-slipp - check-release, oppdaterer-endepunktet og tags-listingen). Ferdig når: begge vurderingene er landet med beslutning (kode/ADR-0013-addendum eller dokumentert avvisning), og tags-pagineringen er håndtert før malrepoet nærmer seg 100 tagger.
  - [ ] Docs-only-utgivelser uten motor-bump (spørsmål reist 6. august 2026, etter 0.6.10 der ingen motorfil endret seg men mappa likevel måtte døpes om): vurder å la check-release godta at motormappa beholder forrige versjonsnavn når innholdet er byte-likt forrige utgivelse (verifiserbart mot malrepoets tagg). Sparer besøkende for en unødig re-nedlasting av hele motoren ved utgivelser som ikke rører den, mot en svakere og mer kompleks invariant enn dagens absolutte «mappenavn == engine» (ADR-0013). Krever i så fall ADR-0013-addendum og justert modulepreload-test
  - [ ] Oppdaterer-herding (lav-funn i push-gjennomgangen 5. august 2026): POST /api/github/update validerer `to` kun mot tagg-formen, så en kapret publiseringssesjon kan NEDGRADERE siden til en eldre malversjon (GET-sjekken tilbyr kun nyeste, så dette er herding, ikke et hull; en kapret sesjon kan uansett skade innholdet). Vurder å kreve at målet er nyere enn kjørende engine. Husk også at en rc-tagg med rent treparts-nummer blir synlig som «nyeste» for ALLE oppdaterere straks den pushes til malrepoet; rc-testing bør bruke numre under neste ekte versjon, eller et eget malrepo via URD_TEMPLATE_REPO. Fra buggjakten samme dag: tags-listingen leser kun første side (per_page=100 uten paginering), så med over 100 tagger i malrepoet kan nyeste versjon bli usynlig for oppdatereren; legg på paginering eller bytt til releases/latest før taggmengden vokser
  - [ ] Some Urd updates require an emptied browser cache (hard reload) before the new version shows, which reads as an Urd bug (moved from Bugs 22 September 2026); decide whether the shells need a cache-busting query or the Updates panel a note
- [ ] v0.9: **Touchvennlighet for admin og publisert side** (flyttet fra Forslag og Ideer 14. august 2026; lagt sist i fasene siden runden bør tas når hele funksjonssettet er på plass, ellers må nye funksjoner touch-tilpasses stykkevis). Admin: dra/slipp, resize-håndtak, hover-avhengig UI (verktøylinjer, hjelpelinjer) og panelene må virke med berøring; siden: hover-effekter, nav og lightbox på berøringsskjerm (hybride enheter er alt vaktet med pointerType-mønsteret, se Motor-lekser i AGENTS.md). Ferdig når: en side kan bygges, redigeres og publiseres på en berøringsskjerm, og publiserte sider er gjennomgått for berøring blokk for blokk
- [ ] v1.0: en blokk-props-migrering som eget bevis (sidefil-migreringene v1→v2→v3 ligger allerede i testsuiten)

## Moderniser til native/CSS (fra ELEMENTKART, 27. juli 2026) HUSK: Se /sammenligning før vi starter

Kuratert topp-sett av byggemåte-grep der native/CSS erstatter skjør egen-JS og forebygger bug-klassene i ELEMENTKART del 6. Alt gates med `@supports` (se støtte-tier i ELEMENTKART del 2). Seksjonen hører til v0.8 (veikartets v0.8-fase fører Moderniser-punktet); to unntak er trukket fram i v0.7: container queries/subgrid (5b.9) vurderes i bredde-ADR-en (0.7.2), der breddegrepet gir bruksstedet som har manglet, og Popover API + anchor positioning (5b.1) er milepæl 0.7.12 nå som forankringen er baseline. Støtte-tallene i ELEMENTKART er «per tidlig 2026» og er kontrollert på nytt 10. august 2026; se vaktlista under 0.7.0 for de som fortsatt ikke er klare.

- [~] lightbox/modal/menyer -> `<dialog>`/`showModal()` + Popover API (ELEMENTKART 5b.1): lightbox/modal LEVERT (0.6.6.5.7, venter på testrunde) - top-layer, `::backdrop`, native fokusfelle/-retur, Esc. MENYER: LEVERT 0.7.12 (22 September 2026, awaiting the test round). Gaten var åpen fra 10. august 2026. Utsettelsen hvilte på at CSS Anchor Positioning var «~82 %, ikke baseline»; den er nå Baseline 2026 (Chrome 125+, Firefox 132+, Safari 18.2+), og den lovede «egen runde når forankring er baseline» er milepæl **0.7.12 Forankringsrunden** over. Gjennomgangen fant også at det største bruksstedet ikke er nav (som alt posisjonerer i CSS) men fire håndrullede popovere i editor og motor
- [~] entré/parallaks -> scroll-drevne CSS-animasjoner bak `@supports` (ELEMENTKART 5b.4): PARALLAKS levert (0.6.6.5.7, venter på testrunde) via `animation-timeline: view()`, rAF-fallback beholdt. ENTRÉ/STAGGER beholdes bevisst på IntersectionObserver (ADR-0011): view() ville reversere ved scroll opp, og engangs-avdekking er riktig bruk av Observer; en scroll-scrubbet variant kan komme som eget, tydelig merket valg senere
- [~] Flere byggemåte-grep (lavere prioritet, full kontekst i ELEMENTKART 5b + del 6): VIEW TRANSITIONS (5b.8) LEVERT (0.6.0.5, venter på testrunde) - `@view-transition` krysstoner sidebytter hos besøkende, nav/footer står i ro via view-transition-name, rent klipp ved redusert bevegelse; preview berøres aldri (postMessage, ikke navigasjon). Også levert (0.6.0.5): scrollås via `body:has(dialog:modal)` (JS-bokføringen i lightbox.js slettet; første konkrete 5b.9-grep) og myk ankerscroll (`scroll-behavior: smooth` bak reduced-motion; dra-kompensasjonen i preview-edit.js gjort eksplisitt instant først). TRANSFORM/OPACITY LEVERT (0.6.0.6, venter på testrunde): `urd-bg-pan`/`urd-bg-orbit` animerer nå transform på 200 %-løpere (à la `urd-bg-loop`) i stedet for `background-position` (repaint per frame); matematisk ekvivalent, ingen props-endring. GJENSTÅR: minimal-wrapper + per-side betinget asset-lasting (5b.7) ble VURDERT ved 0.6.9-gaten og eksplisitt UTSATT (beslutning 5. august 2026, notert i ADR-0013): betinget per-side-lasting står i direkte spenning med både modulepreload-lista (anti-drift-testen krever at alle skall preloader hele lukningen) og kopi-oppfriskningsplikten (slug-kopiene må være byte-like roten); tas den opp igjen, må den løse begge. Minimal-wrapper-halvdelen er alt «har (delvis)» i ELEMENTKART, og container queries/subgrid (5b.9) trenger konkrete brukssteder. (aspect-ratio/srcset/AVIF 5b.6 dekkes av v0.8-bildearbeidet - noter fra gjennomgangen 10. august 2026 at AVIF-halvdelen ikke lenger er teoretisk: `imageTools.js` koder alt i nettleseren via canvas `toBlob`, og AVIF er nå en realistisk utdatatype der, så punktet er praktisk gjennomførbart og ikke bare ønskelig; scroll-snap-karusell 5b.3 brukes alt)

## Etter v1.0 (horisont) HUSK: Se /sammenligning før vi starter

- [ ] Import fra eksisterende side (veiviser som henter tekst/bilder)
- [ ] Butikk-utvidelser fra research-runden 25. august 2026 (Wix-paritet, ikke ApeironLF-behov): variantmatrise med pris per variant og varelager; kategorier/tags med relaterte produkter; ekte produkt-permalinker og «se produktsiden»-lenke i quick view kobles til C4b når v0.8-bakingen finnes
- [ ] Flerspråkstøtte for innhold (nb/nn/en-varianter; migreringskontrakten gjør at den kan komme når som helst)
- [ ] Samtidighetsvisning i admin («Kari redigerer Hjem nå») - krever sanntidsinfra utover statisk+functions; konfliktvarselet i v0.5 dekker det viktigste
- [ ] Galleri-nettsted over community-maler og -plugins (urd.dev; forutsetter et community)
- [ ] GitLab/Gitea-adapter for publiseringslaget (se ADR-0003)
- [ ] Ekstern medialagring (Cloudflare R2) som plugin for bildetunge sider
- [ ] PR-per-utkast + deploy-preview + open authoring (LAERDOMMER 5.5, Decap-modellen): utkast som branch, publiser som merge, preview-URL per utkast via Cloudflare Pages + commit-status, bidrag via fork+PR uten skrivetilgang. Bygger på GitHub-OAuth-publiseringen (ADR-0003) og støtter v1.0-målet «forening uten utvikler» + flere bidragsytere; kan vurderes trukket tidligere ved behov
- [ ] Utenfor scope for kjernen (fra kartleggingen 18. juli 2026): innlogging/medlemsområde, P2P-bruktmarked (ApeironLFs «Kjøp & bytte»), fulltekstsøk på siden (krever avhengighet à la minisearch - evt. community-plugin), sanntidsfeeds/personalisering. Krever server/CMS eller avhengigheter; Urd er statisk og avhengighetsfri; the LTT client-side catalogue search (0.7.13) is the nearest plan for search
- [ ] Gå gjennom kommentarer i filer og fjern unødvendige

## Bugs, (dårlige) funksjoner og ting som ellers må gjøres

- [ ] Fest ved scrolling på element- "Slipp taket ved seksjon xyz" fungerer ikke.  
- [ ] Korn i bakgrunn skal gå fra 0% til 100%, ikke 0-30% som nå
- [ ] Kan ikke gjøre strek i Shapes/former større/lengre
- [ ] Teksteditor går over elementknappene - la muligens elementknappene gå på oversiden av teksteditor

## Suggestions, Ideas and Wishes

- [ ] A per-site editing size shared between editors (added 16 September 2026): Wix Studio keeps the editing size as a site setting; Urd keeps it per browser (`urd-admin-screen`) until a second editor on the same site exists, so no schema field yet
- [ ] Påskeegg: mulighet til å legge inn hemmeligheter/easter eggs på nettsidene
- [ ] Eksporter mal(er) - eksporter som plugin, eksporter gruppe av maler
- [ ] Start-up tutorial - med ekstra wiki
- [ ] Hele nettsidemaler/eksempler/forslag lagres i Urd, ikke i /templates, for å holde brukerens side lettere og mindre i størrelse (note 22 September 2026: the start packs in page-presets.js are already code-defined in the engine and never stored in the user's repo; what this adds is the same for whole example sites, several pages plus site settings, which touches the template model and belongs with the 0.7.9 section-template work or v0.8)
- [ ] Større utvalg av fonts
- [ ] Mulighet for å legge egne fonts i en mappe i egen /template eller repo som dukker i samme liste som alle andre fonts
- [ ] Flere design på kalender.
- [ ] Kalender "Neste" arrangement instilling: mulighet til å legge til flere kalenderkoder og/eller koble den opp til kalendere allerede satt i nettsiden. Valg om hvor mange arrangement skal vises som "neste"/"oppkommende"
- [ ] Mulighet til å bestemme hvilke element som blir lagt over og under hvilke i mobilvisning, slik at i desktop kan element stå ved siden av hverandre. + ekstra instilling om å alltid legge element på høyre eller venstre side over andre siden.
- [ ] Notifikasjonsikon om Urd har ny oppdatering slik at man kan se en ny oppdatering uten å måtte åpne opp "Oppdateringer"


## Kjente begrensninger (dokumentert, ingen fiks planlagt)

- The preview hides the page's scrollbar, so browsers with a classic scrollbar lay out the published page about 15 px narrower than the preview shows (ADR-0018 addendum, 16 September 2026)
- `python3 -m http.server` ruter ikke `path`-ene fra sideregisteret (kun `/` fungerer); dokumentert begrensning (`?page=<id>` er lokal-fallback)
- Angring av en merge-commit gjenoppretter første forelders innholdstilstand (dokumentert i revert.js); sjelden via admin-flyten, men mulig når noen også jobber via git
- Tekst-selvhelbrederen fjerner alle knapper i rik tekst ved rendering, også en bevisst innlimt HTML-knapp (vernet mot foreldreløse håndtak-knapper veier tyngre); innlimte event-attributter og script strippes alltid hos besøkende
- history/latest-endepunktene krever kun innlogging, ikke ALLOWED_LOGINS (lesende; på offentlige repo er dataene uansett åpne). Dokumentert asymmetri
- revert bytter hele nettside-undertreet, men «siste publisering» finnes via content/-filteret: en commit som KUN rører media/ eller rutingskopier (mulig via git, ikke via editoren) er usynlig for angringen
- Publisering fra en ANNEN admin-fane i deploy-vinduet etter en motoroppdatering (~1 min) kan skrive slug-kopier fra den gamle serverte rot-index-en, som peker på slettet motormappe; undersidene står da døde til neste publisering (funnet i buggjakten 5. august 2026). Samme fane er vernet (publisering sperres mens oppdateringen ruller ut), og konfliktsjekken varsler overlappen i andre faner, men varselet sier ikke at det gjelder en oppdatering. Selvhelbredende ved neste publisering; flerfane-vern krever delt tilstand (BroadcastChannel el.) og tas ved behov
- Hele motormappa bytter navn ved hver utgivelse, også når ingen motorfil er endret (ADR-0013). Per-fil-navngiving med innholdshash (slik media/ gjør det) er umulig uten byggesteg, fordi motorfilene peker på hverandre: endres ÉN fil og får nytt navn, må alle filene som peker på den rettes til det nye navnet, men da har DE endret innhold og trenger også nytt navn, og alle som peker på dem igjen må rettes; én liten endring smitter oppover hele nettet. Verktøy løser det med et byggesteg som regner ut alt automatisk, men Urds løfte er null bygging, så hele nettet flyttes samlet i én versjonert mappe i stedet (én navneendring, null smitte). Prisen er at besøkende laster motoren på nytt én gang etter hver oppdatering, selv når den er byte-lik; en oppmyking for docs-only-utgivelser står som eget v0.9-punkt
- Slugen «readme» ble reservert i 0.6.9.10 etter å ha vært lovlig: en side med den slugen bygget FØR reservasjonen ville mistet ruten sin ved neste motoroppdatering (kopien friskes ikke og kan ikke re-publiseres). Akseptert uten migreringsvei fordi ingenting eksternt er bygget pre-v1; en slug-reservasjon ETTER v1 må derimot følges av en oppryddingsvei i oppdatereren (funnet i push-gjennomgangen 5. august 2026)
- Oppdatereren behandler malens EGNE eksempelside-ruter som vanlige Urd-eide filer (lav-funn i push-gjennomgangen 5. august 2026): har eieren slettet en eksempelside (f.eks. kaker), gjenoppretter neste oppdatering slug-kopien uten tilhørende sidefil (en levende URL til tom side); fjerner malen en eksempelside eieren har gjenbrukt slug-en til, slettes ruten i stillhet. Neste publisering reparerer begge (kopiene skrives fra sideregisteret); dokumentert i stedet for kodet rundt fordi kantene er selvhelbredende og sjeldne
- Angring bytter hele nettside-undertreet, så «angre siste publisering» utført ETTER en Urd-oppdatering ruller også motoren tilbake til før oppdateringen (funnet 5. august 2026 under 0.6.9.6). Tilstanden er konsistent gammel (motor, skall og urd.json følges ad), aldri blandet, og Oppdatering-panelet kan kjøres på nytt etterpå; dokumentert i stedet for vernet fordi et vern ville krevd at angringen skilte innholds- fra motorfiler, stikk i strid med dens enkle subtre-modell
- En forbigående GitHub-feil under publisering (5xx, f.eks. 503 ved ref-oppdateringen under en GitHub-driftsforstyrrelse) vises som en rå feilmelding, og man må trykke Publiser på nytt selv; utkastet består og repoet er urørt (ufarlig). Automatisk gjenforsøk/«Prøv igjen»-knapp er ønsket og loggført under v0.7 editor-finpuss
